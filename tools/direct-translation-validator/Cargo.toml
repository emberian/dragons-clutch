[package]
name = "dclutch-direct-translation-validator"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
rust-version = "1.89"
description = "Independent executable translation validator for Lean-owned Direct ABI and VM observations"

[dependencies]
dclutch-direct-aot-contract = { path = "../../crates/dclutch-direct-aot-contract" }
dclutch-direct-codec = { path = "../../crates/dclutch-direct-codec" }
dclutch-transition-vm = { path = "../../crates/dclutch-transition-vm" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[workspace]
