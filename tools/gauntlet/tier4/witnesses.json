{
  "campaign": "tier4-series-occurrence-programtest",
  "witnesses": [
    {
      "id": "every-transaction-fits-the-compute-maximum",
      "kind": "evidence-jq",
      "query": "[.transactions[].compute_units_consumed | select(. != null)] | max | (. <= 1400000) | tostring",
      "expect": "true",
      "provenance": "Solana's per-transaction MAX_COMPUTE_UNIT_LIMIT is 1,400,000 compute units. It is the chain's limit, not ours. The campaign sets ProgramTest's ceiling to exactly that value and never raises it, so a transaction that did not fit would fail rather than be re-run with headroom. Note the direction of this witness: it is a chain limit the campaign must live under, not a measurement of ours restated as a target."
    },
    {
      "id": "no-transaction-exceeds-the-runtime-cpi-depth",
      "kind": "evidence-jq",
      "query": "[.transactions[].logs[] | capture(\"invoke \\\\[(?<depth>[0-9]+)\\\\]\") | .depth | tonumber] | max | (. <= 5) | tostring",
      "expect": "true",
      "provenance": "Solana's MAX_INSTRUCTION_STACK_DEPTH is 5, counting the top-level instruction as depth 1, so the deepest admissible `invoke [n]` line any program can produce is [5]. The Series occurrence chain runs caller -> Core -> role artifact, and this witness is what would catch a composition change that quietly added a CPI level and put the route one hop from the runtime's hard stop."
    },
    {
      "id": "every-refusal-reached-a-programs-own-taxonomy",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.error != null) | select([.logs[] | select(contains(\"custom program error: 0x\"))] | length == 0)] | length | tostring",
      "expect": "0",
      "provenance": "The Solana runtime reports a privilege, frame, or account refusal it decides ITSELF as a bare InstructionError with no `custom program error:` log line; that line appears only when a program returned ProgramError::Custom. TIERS.md draws exactly this distinction and records a pre-taxonomy refusal as an unnamed one, because crediting it would overstate what the program proved. This witness asserts that NO refused transaction in the campaign was bounced by the runtime -- which is what makes naming a refusal code admissible at all. It counts the violations rather than the conformers deliberately: a witness pinned to how many hostile cases the campaign happens to have would have to be edited every time one was added, and an edited witness is one nobody reads."
    },
    {
      "id": "the-occurrence-that-founds-is-the-only-one-that-succeeds",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.error == null) | .label] | sort | join(\"|\")",
      "expect": "Series occurrence Consume founds its Market with a Core permit|Series occurrence Consume founds its Market with a Core permit (replay campaign)",
      "provenance": "A route is EXECUTED in this census only on a succeeding transaction, and a campaign that accidentally accepted one of its own hostile cases would flip a row on the strength of a bug. This witness pins which single transaction is allowed to succeed, so a future change that weakened either refusal fails the tier instead of silently improving its coverage numbers. The rule it encodes is AGENTS.md's: do not weaken a refusal to make an integration test pass."
    },
    {
      "id": "the-founding-fast-lane-is-inside-its-cu-budgets",
      "kind": "cu-budget",
      "campaign": "tier4-series-occurrence-programtest",
      "provenance": "The budgets and their tolerances live in tools/gauntlet/CU_BUDGETS.json with their measurement dates and observed noise bands; this witness carries no number of its own. The independent value is Solana's 1,400,000 per-transaction MAX_COMPUTE_UNIT_LIMIT. This tier is the PRE-CAMPAIGN check: it drives Core's found-and-permit — the same Core code that is the 433,129-CU stage 2 of the validator campaign's DCLTGMF1 — with no validator and no port, so a Core founding regression surfaces here in under a minute instead of after a six-minute campaign that needs the single global 127.0.0.1:20890 slot. The tolerances were set from six runs of this campaign on the same ELFs, not from one."
    }
  ]
}
