{
  "campaign": "dealer-family-programtest",
  "note": "The Dealer family route, driven as a real ELF under solana-program-test rather than on a validator. This is a FAST LANE: read tools/gauntlet/dealer/README.md for which of the TIERS.md fast-lane conditions it satisfies before reading any row it produces as validator evidence. The campaign lives in programs/dclutch-dealer-sbf/program-test/tests/family.rs. EVERY transaction in it is a refusal and the tier claims NO executed row: the Dealer route cannot accept a transition without an activated release set, an admitting Core Market phase, and Claims/Custody prestate, none of which this tier builds. What it does evidence is that all eight canonical actions reach the RELEASE WAIST against the real ELF, and that six of the ten DealerSbfError codes are raised by the chain rather than asserted by a unit test. The waist was a Registry Reauthenticate CPI when this campaign was written; c202ee3d retired that call as reentrancy -- Dealer is a child of a Registry continuation already at CPI depth one -- and the waist is now a read of the Registry-owned activation cache through the Registry’s own crate. The refusal code did not change, so what these rows evidence did not change either; only the mechanism behind them did.",
  "bindings": [
    {
      "label": "dealer family refuses a request that is not REQUEST_BYTES wide",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Instruction",
      "note": "The width guard. A Dealer request is exactly 144 bytes; a 143-byte one is refused before any account is read, so no shorter encoding can reach the frame checks and be interpreted as a shorter action."
    },
    {
      "label": "dealer family refuses an action tag outside the eight canonical actions",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Instruction",
      "note": "There are exactly eight actions (tags 0..7). Tag 8 is refused at decode. This is the closure property of the action space: a ninth tag cannot be smuggled past the interpreter by a client that knows the rest of the encoding."
    },
    {
      "label": "dealer family refuses a common frame narrower than 23 accounts",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountFrame",
      "note": "COMMON_ACCOUNT_COUNT_V1 is 23 and it is a floor, not a hint: a 22-account frame is refused before Policy is read, so no role can be silently absent and later re-derived from a neighbour."
    },
    {
      "label": "dealer family refuses an actor that did not sign",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountFrame",
      "note": "The actor slot must carry a signature at the outer level. Without this, every actor-bound action would be assertable by anyone who could name the actor's address."
    },
    {
      "label": "dealer family refuses a writable immutable Policy",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountFrame",
      "note": "Policy is immutable for the life of the Market. Presenting it writable is refused at the frame, before its bytes are read, so the refusal cannot depend on the Policy body being well formed."
    },
    {
      "label": "dealer family refuses a read-only State it is required to commit",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountFrame",
      "note": "The mirror of the Policy case. State is the one account this route commits; a read-only State would make a transition that appeared to succeed persist nothing."
    },
    {
      "label": "dealer family refuses a proposed Candidate outside ScheduleReplacement",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountFrame",
      "note": "The proposed-Candidate slot is admissible only under ScheduleReplacement; absence is encoded by aliasing the Trading program key. A Fill that presents a real Candidate there is a frame refusal, which is what keeps the optional-slot encoding unambiguous."
    },
    {
      "label": "dealer family refuses a Policy body owned by another program",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountIdentity",
      "note": "The same canonical Policy bytes at an account another program owns. Ownership is what makes the body a protocol fact rather than user data, and it is checked before the PDA join."
    },
    {
      "label": "dealer family refuses a Candidate body at another identity's address",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountIdentity",
      "note": "The substitution the Candidate PDA join exists to refuse: a well-formed Candidate whose own encoded identity does not derive the address it was presented at. Without this a caller could point the active-Candidate slot at any Candidate the program had ever written."
    },
    {
      "label": "dealer family refuses a State body that is not the Market's State PDA",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::AccountIdentity",
      "note": "A State-shaped, program-owned, correctly-sized account that is not the State PDA for this Policy's Market. This is the account the route commits to, so a substituted one would write a real transition into the wrong Market."
    },
    {
      "label": "dealer family refuses a scheduling request that names another actor",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Signature",
      "note": "ScheduleReplacement is actor-bound: the request must name the signer. A well-formed request naming somebody else reaches the signature stage only because instruction shape, the 23-account frame, and Policy/Candidate/State identity all passed, so the refusal is credited to the check it names."
    },
    {
      "label": "dealer family refuses a Fill bound to a slot that is not the current one",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Clock",
      "note": "Time-sensitive actions bind to the Clock the runtime serialized, not to a slot the caller asserts. A Fill priced against a stale slot would be executed against a Candidate whose validity window the caller chose."
    },
    {
      "label": "dealer family drives AddLiquidity into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "AddLiquidity, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family drives RemoveLiquidity into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "RemoveLiquidity, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family drives Fill into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "Fill, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family drives ActivateReplacement into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "ActivateReplacement, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family drives EnterTerminal into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "EnterTerminal, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family drives Unwind into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "Unwind, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family drives Retire into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "Retire, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family drives ScheduleReplacement into the release waist",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Release",
      "note": "ScheduleReplacement, having passed instruction shape, the 23-account frame, account identity, its actor rule and its Clock rule, reaches the release waist and is refused there because no release set was ever activated. The waist is a READ of the Registry-owned activation cache through the Registry's own crate, not a CPI: c202ee3d retired the Reauthenticate invocation because Dealer runs as a child of a Registry continuation already at CPI depth one, which made calling back reentrancy the runtime refuses before Dealer can do any work (programs/dclutch-dealer-sbf/src/lib.rs:1167-1175). So the evidence here is that the action reaches the waist and stops, not that a second program spoke."
    },
    {
      "label": "dealer family refuses EnterTerminal carrying a slot in its padding",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Instruction",
      "note": "EnterTerminal carries no slot in its semantics, so Action::now_discipline declares its now field canonical zero and Request::validate_shape refuses a nonzero one at decode, before any account is read. Pinning it per action matters because the discipline used to be restated on both sides of the wire and the two sides disagreed; the codec is now the single owner and this is the chain agreeing with it."
    },
    {
      "label": "dealer family refuses Unwind carrying a slot in its padding",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Instruction",
      "note": "Unwind carries no slot in its semantics, so Action::now_discipline declares its now field canonical zero and Request::validate_shape refuses a nonzero one at decode, before any account is read. Pinning it per action matters because the discipline used to be restated on both sides of the wire and the two sides disagreed; the codec is now the single owner and this is the chain agreeing with it."
    },
    {
      "label": "dealer family refuses RemoveLiquidity carrying a slot in its padding",
      "routes": [
        "dealer/process_dealer_family_instruction"
      ],
      "program": "dealer",
      "outcome": "refused",
      "refusal": "dealer/DealerSbfError::Instruction",
      "note": "RemoveLiquidity carries no slot in its semantics, so Action::now_discipline declares its now field canonical zero and Request::validate_shape refuses a nonzero one at decode, before any account is read. Pinning it per action matters because the discipline used to be restated on both sides of the wire and the two sides disagreed; the codec is now the single owner and this is the chain agreeing with it."
    }
  ]
}
