{
  "note": "SBF stack-frame-overwrite diagnostics this tier will run despite. `cargo build-sbf` exits ZERO when the backend reports that a call overwrites its own stack frame and 'may cause undefined behavior during execution', so the only thing between a potentially-undefined artifact and a campaign is a build stage that counts them. run.sh WARNS; this tier REFUSES, because the journey's whole claim is about state surviving a long chain of transactions and undefined behaviour anywhere in that chain voids the claim silently. This file is the narrow exception, and it is deliberately shaped like blocked.json: every entry names the exact mangled symbol, the measured count, why the journey does not reach it, and who owns the fix. The runner refuses any diagnostic whose line does not match an entry here, and refuses a MEASURED COUNT THAT GREW \u2014 a growing count is a new defect wearing an old exemption. A count that shrank is reported loudly as stale so the entry gets deleted rather than outliving its reason. EMPTY AS OF 2026-08-27 at 37d873f: the one entry this file ever carried -- 65 diagnostics in dclutch_resolution_proof_sbf::relay_transport_v1::process_relay_transport_v1, measured at 0ca81cc -- reports ZERO on the same function at 37d873f, so it was deleted rather than left to outlive its reason. Nothing in the Resolution program's own history obviously fixes it; the codegen moved under it, which means it can come back. If it does, this tier refuses again, which is the correct outcome and the reason the file is empty rather than holding a lapsed exemption 'just in case'.",
  "allowed": []
}
