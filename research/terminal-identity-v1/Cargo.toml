[package]
name = "clutch-terminal-identity-v1"
version = "0.1.0"
edition = "2021"
publish = false
license = "AGPL-3.0-or-later"
description = "Model-only TerminalIdentityV1 uniform rent/donation header codec and lifecycle"

[lib]
path = "src/lib.rs"

[dependencies]
clutch-liveness = { path = "../../crates/clutch-liveness" }

[features]
default = []
