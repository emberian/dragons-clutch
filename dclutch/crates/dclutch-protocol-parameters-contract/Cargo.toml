[package]
name = "dclutch-protocol-parameters-contract"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
rust-version = "1.89"
description = "The governed protocol-parameters record: fee band, protocol take, closer carve and crank reward, with a delayed change procedure"

[dependencies]
dclutch-sha256-adapter = { path = "../dclutch-sha256-adapter" }

[lints]
workspace = true
