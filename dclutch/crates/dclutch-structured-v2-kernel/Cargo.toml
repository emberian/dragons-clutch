[package]
name = "dclutch-structured-v2-kernel"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
rust-version = "1.89"
description = "Shard-backed Structured receipt kernel for dClutch"
publish = false

[dependencies]
dclutch-fractional-claim-kernel = { path = "../dclutch-fractional-claim-kernel" }

[dev-dependencies]
sha2 = { version = "=0.10.9", default-features = false }

[lints]
workspace = true
