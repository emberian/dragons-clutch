// @generated by formal/dclutch-semantics/EmitSeriesStateV3Rust.lean; do not edit.
/// Exact width of the mutable Series tail inside the composite Trading root.
pub const SERIES_STATE_BYTES_V3: usize = 64;
/// One scheduled occurrence remains to be settled.
pub const SERIES_PHASE_ACTIVE_V3: u8 = 0;
/// Every occurrence settled; only terminal ticket retirement remains.
pub const SERIES_PHASE_TERMINAL_V3: u8 = 1;
/// One past the greatest phase tag: every phase indexes its own bit of a `u8`.
pub const SERIES_PHASE_LIMIT_V3: u8 = 2;
/// Width of the canonical-zero span between the prepared flag and the occurrence cursor.
pub const SERIES_STATE_HEAD_RESERVED_BYTES_V3: usize = 2;
/// Width of the canonical-zero tail span.
pub const SERIES_STATE_TAIL_RESERVED_BYTES_V3: usize = 24;
/// Canonical Series root-tail magic.
pub const SERIES_STATE_MAGIC_OFFSET_V3: usize = 0;
/// Family-wide `SERIES_TEMPLATE_SCHEMA_V3`, at this record's coordinate.
pub const SERIES_STATE_SCHEMA_OFFSET_V3: usize = 8;
/// Family-wide `SERIES_TEMPLATE_PROFILE_V3`, at this record's coordinate.
pub const SERIES_STATE_PROFILE_OFFSET_V3: usize = 10;
/// The persisted `SeriesPhaseV3` wire tag.
pub const SERIES_STATE_PHASE_OFFSET_V3: usize = 12;
/// Whether the current occurrence already owns its prepared Ticket (0 or 1).
pub const SERIES_STATE_CURRENT_TICKET_PREPARED_OFFSET_V3: usize = 13;
/// Canonical-zero span padding the prepared flag to the occurrence cursor.
pub const SERIES_STATE_HEAD_RESERVED_OFFSET_V3: usize = 14;
/// Next occurrence that may be prepared or settled.
pub const SERIES_STATE_NEXT_OCCURRENCE_OFFSET_V3: usize = 16;
/// Number of live terminal or prepared ticket accounts.
pub const SERIES_STATE_OUTSTANDING_TICKET_ACCOUNTS_OFFSET_V3: usize = 20;
/// Current replay revision.
pub const SERIES_STATE_REVISION_OFFSET_V3: usize = 24;
/// Separately classified close-rent principal.
pub const SERIES_STATE_CLOSE_RENT_REMAINING_OFFSET_V3: usize = 32;
/// Canonical-zero tail span.
pub const SERIES_STATE_TAIL_RESERVED_OFFSET_V3: usize = 40;
