// @generated by formal/dclutch-semantics/EmitSeriesTicketStateV3Rust.lean; do not edit.
/// Exact width of one Trading-owned mutable occurrence-ticket state.
pub const SERIES_TICKET_STATE_BYTES_V3: usize = 64;
/// Exact custody is prepared and the occurrence remains retryable.
pub const SERIES_TICKET_PHASE_PREPARED_V3: u8 = 0;
/// The ticket was atomically consumed into its exact Found Market.
pub const SERIES_TICKET_PHASE_CONSUMED_V3: u8 = 1;
/// The retry window elapsed and every compartment was refunded.
pub const SERIES_TICKET_PHASE_EXPIRED_V3: u8 = 2;
/// One past the greatest phase tag: every phase indexes its own bit of a `u8`.
pub const SERIES_TICKET_PHASE_LIMIT_V3: u8 = 3;
/// Where the replay body begins: the width of the shared header in front of it.
pub const SERIES_TICKET_STATE_BODY_OFFSET_V3: usize = 16;
/// Width of the canonical-zero span between the phase byte and the revision.
pub const SERIES_TICKET_STATE_HEAD_RESERVED_BYTES_V3: usize = 3;
/// Width of the canonical-zero tail span.
pub const SERIES_TICKET_STATE_TAIL_RESERVED_BYTES_V3: usize = 8;
/// Canonical ticket-state magic, the only partition against a zeroed account.
pub const SERIES_TICKET_STATE_MAGIC_OFFSET_V3: usize = 0;
/// Family-wide `SERIES_TEMPLATE_SCHEMA_V3`, at this record's coordinate.
pub const SERIES_TICKET_STATE_SCHEMA_OFFSET_V3: usize = 8;
/// Family-wide `SERIES_TEMPLATE_PROFILE_V3`, at this record's coordinate.
pub const SERIES_TICKET_STATE_PROFILE_OFFSET_V3: usize = 10;
/// The persisted `TicketPhaseV3` wire tag.
pub const SERIES_TICKET_STATE_PHASE_OFFSET_V3: usize = 12;
/// Canonical-zero span padding the phase byte to the revision.
pub const SERIES_TICKET_STATE_HEAD_RESERVED_OFFSET_V3: usize = 13;
/// Replay revision the settlement transition checks.
pub const SERIES_TICKET_STATE_REVISION_OFFSET_V3: usize = 16;
/// Identity of the ticket record this state settles.
pub const SERIES_TICKET_STATE_RECORD_ID_OFFSET_V3: usize = 24;
/// Canonical-zero tail span.
pub const SERIES_TICKET_STATE_TAIL_RESERVED_OFFSET_V3: usize = 56;
