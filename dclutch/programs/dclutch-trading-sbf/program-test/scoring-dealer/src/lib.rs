//! Host package for the scoring Dealer's real-ELF fill campaign.
//!
//! The campaign itself is `tests/fill.rs`; this crate exists so the tests have
//! a manifest of their own, exactly as the sibling `direct-hot`, `general-hot`
//! and `user-position-admission` campaigns do.
