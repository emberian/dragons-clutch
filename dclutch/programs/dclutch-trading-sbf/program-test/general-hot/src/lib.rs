//! General through the real Trading Hot and admitted accelerator ELFs.
//!
//! This standalone ProgramTest crate deliberately depends on the generic Hot
//! bundle builder and the shared real-ELF release waist. Its tests supply only
//! semantic market corpus and current release artifacts; span widths, scratch
//! pages, accelerator requests, caller authorities, lifecycle addresses,
//! account privileges and packet topology remain derived by their owners.

#![forbid(unsafe_code)]
