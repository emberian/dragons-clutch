# Omission and succession index

Status: working challenge ledger under active architectural reconsideration.
It is not a normative rejection list, release evidence, or deployment
evidence. A row records the current hypothesis; it does not make that
hypothesis permanent.

The 2026-08-25 review found that several earlier decisions may be safe
prototype waists rather than desirable protocol limits. In particular, fixed
execution roles, interpretation-only execution, categorical-only native
liabilities, exact-denominator-only receipts, non-nested wrappers, immutable
Dealer epochs, permanent RentCredit accounts, and current Token-2022 profiles
must be challenged for principled generalizations before they can be called
final omissions.

The successor does not surpass either predecessor merely by omitting its hard
parts. Every predecessor behavior is classified here as one of:

- **hard invariant**: the narrow accounting, authority, or evidence property
  that no implementation profile may weaken;
- **current safe profile**: a conservative executable choice with an explicit
  generalization path, not permanent ontology;
- **likely scar**: a restriction inherited from implementation convenience and
  scheduled for a stronger design;
- **open research**: a useful generalization whose authority or theorem is not
  yet sound enough to accept; or
- **unfinished**: still required by the project goal and never evidence of an
  intentional omission.

A rejected or superseded row is closed only by the cited accepted decision,
an explicit strongest-alternative analysis, and the named successor evidence.
Silence, missing code, a fixture, or an unimplemented frontend path cannot
close a row. Proposed omissions must first receive an architecture decision.
An existing decision may be reopened when it encodes an implementation
limitation rather than an economic, safety, or authority invariant.

## Invariants, profiles, and scars

| ID | Predecessor behavior or tempting design | Classification | Accepted treatment | Closure or revisit condition |
| --- | --- | --- | --- | --- |
| O-001 | Eager universal Market foundation containing every venue, wrapper, resolution, recovery, and funding account | current safe profile | Keep thin Core, but allow eager atomic creation of the exact selected capability subset | [Decision 0001](decisions/0001-thin-market-core.md); mandatory universal unused state remains unjustified |
| O-002 | Cumulative historical action namespace and compatibility for experimental wire/account layouts | current safe profile | Pre-release wires have no compatibility privilege; released schemas may gain explicit version decoders and one-way migrations | A release must state its compatibility and migration policy |
| O-003 | One deployed Program per family or Market, a dynamic capability-to-Program map, or an onchain code-stamping factory | current safe profile | Five state-owning roles are V1; the same descriptor may later use a Registry-authenticated stateless interpreter or translation-validated AOT accelerator | [Decision 0003](decisions/0003-fixed-role-capability-execution.md); a sixth state owner still needs an exclusive authority boundary and new profile |
| O-004 | Closed Rust family dispatch, permanent family enum/bit positions, and `N = 2..16` control-flow monomorphization | likely scar | Descriptor semantics are canonical; interpretation and checked AOT are alternative strategies | First lift with the same Direct descriptor executed both ways and compare acceptance, refusal, CU, packet, and rent |
| O-005 | Parallel old/new decoders, authority paths, DTO truths, or fallback executors | hard invariant, narrowly stated | One live writer and one persisted truth per fact; read decoders, migrations, differential references, and non-authoritative measurement ELFs are allowed | Release must prove only one writer is authorized and identify every retained non-authoritative reference |
| O-006 | Hard-coded DREGG token behavior or mandatory house collateral | hard invariant | Immutable Realm/profile identity selects collateral; token names never select semantics | No token-name branch |
| O-007 | Mock provider authority, client/index authority, or caller-authored resolution truth | hard invariant | Mocks are test-only; release state plus provider-authenticated evidence owns truth, while clients may submit untrusted witnesses | Additional real providers remain unfinished breadth |
| O-008 | Hoard principal, future fees, or unclassified donations funding rent, work, liveness, liquidity, bounty, reserve, or treasury | hard invariant | Hoard and anticipated revenue never capitalize liveness; a donation enters only through an explicit typed classification transition | No implicit lifting path |
| O-009 | A cross-asset `total_principal`, implicit conversion, or exchange rate between lamports and Realm collateral | hard invariant | Conserve assets independently; an optional valued-risk capability must name oracle, units, timing, rounding, and failure behavior | Never infer a conversion from arithmetic convenience |
| O-010 | Undercollateralized claims, uncovered Dealer inventory, protocol credit promises, or leverage against anticipated revenue | likely scar after splitting the row | Native claims must remain solvent and anticipated revenue is not capital; Dealer may generalize from gross coverage to exact terminal-scenario coverage and separately capitalized margin | True defaulting credit remains open research with its own failure instrument |
| O-011 | Dealer sponsor RFQ compatibility, mutable administrator repricing, or configuration changes that can revalue incumbent LPs | likely scar | Reject unilateral repricing, not evolution: use consent-bound capital tranches, deterministic accepted pricing policies, or quiescent epoch transitions | Prove LP consent, value transfer, scenario solvency, and withdrawal behavior |
| O-012 | Fractional native liabilities, residual credits, remainder ledgers, or hidden redemption rounding | likely scar after splitting the row | Hidden rounding remains forbidden; exact claim shards and explicit Token-owned remainder/change instruments are valid | Implement exact denomination and conservation before expanding Structured |
| O-013 | Native polynomial, B-spline, ramp, or tent liabilities inside the elementary claim basis | likely scar | Categorical is the optimized `Q=1` basis; add certified nonnegative integer partition-of-unity bases | Slice one is a capped ramp plus exact complement. Slice two is the degree-1–3 B-spline family (`LiabilityBasisV2Spline`), with exact partition sum at every width, a per-claim one-atom bound, local support, interior knot multiplicity, and a Lean-emitted hostile corpus. Slice three is the degree ≥ 2 price gate (`LiabilityBasisV2PriceGate`, 2026-08-27): integer hull membership with `Certificate.no_arbitrage` proved, a 320-byte record, and the admission conjunct in the kernel — degree ≥ 2 requires a valid certificate, degree ≤ 1 exempt by proof. ~~**Row still open**: no consumer, and no layout by which a Market can select a spline basis at all.~~ **AMENDED 2026-08-30 (FRONTIER-2): the second half of that clause is false and it is the sentence that generated a wrong plan.** Read literally it says a Market cannot select a non-categorical basis, which is what produced the "one Market layout field unblocks 221 theorems" hypothesis that a lane was chartered against and that measurement refuted. Measured at HEAD: the selector exists and ships — `BasisKindV3` (`crates/dclutch-product-payoff-v2-codec/src/runtime_v3.rs:105`) admits `CategoricalQ1` **and `GradedExactComplement`**, and `BasisShapeV3` (`:131`) carries `Constant`, `RampUp`, `RampDown` and `Tent` over runtime-width Product-owned knots — so **degree-0 and degree-1 shaped payoffs are live on the wire today**, under a certified categorical projection with a componentwise integer error bound (`crates/dclutch-product-compiler/src/noncategorical_v3.rs`). The layout carries every field the row's sibling `U-013` demands, and it is authenticated on chain (`crates/dclutch-product-runtime-v2-svm-reader/src/lib.rs:427` refuses when `semantic_basis_id != runtime.liability_basis_id`) — **though not at the addresses `U-013`'s amendment names; see the correction appended to `U-013` below, which relocates two of the five.** **The row IS still open, for two narrower reasons that need separating from each other.** (a) *Capability*: the unreached degrees are **2 and 3 only** — curvature, not shape. (b) *Consumer*: `dclutch-liability-basis-v2-kernel` genuinely has no consumer (one dependency edge, the workspace member list), but the blocker is not wiring — the kernel decodes record magics `DCLTLBV2`/`DCLTLNK2` while the live evaluator is the handwritten `ProductBasisV3` over `DCLTPAY3`, so **there are two independent evaluators of one nominal format** and closing the row requires a ruling on which is the authority. That ruling, the wire slot for the 320-byte `DCLTPGT1` price-gate certificate, and the sequencing constraint are in [`design/BASIS_ABI_UNIFICATION_V1.md`](design/BASIS_ABI_UNIFICATION_V1.md); the full measurement is in [`design/ORPHAN_DESIGNS_TRIAGE_2026_08_30.md`](design/ORPHAN_DESIGNS_TRIAGE_2026_08_30.md) §2. See also [the eclipse scorecard](research/BSPLINE_ECLIPSE_SCORECARD_2026_08_27.md) ("the row that flipped") and `ASPIRATION_LEDGER.md` `G-1`. Closing it also needs ember's answer on whether the substitution is the requirement (`ASPIRATION_LEDGER.md` M-4) — **and that question should be put to him with the correction attached, because M-4 records his *"'5 fixed bands' is really not good enough"* as a dropped requirement when it is partly delivered under another name** |
| O-014 | Wrapper nesting, recursive payout authority, wrapper-specific collateral truth, or wrapper-owned resolution | likely scar after splitting the row | Keep one native liability/resolution truth; admit acyclic compositional recipes with canonical flattening and a bounded representation DAG | Arbitrary live wrapper-on-wrapper custody remains open research |
| O-015 | Bespoke protocol holder ledger, privileged wrapper transfer, transfer hook, permanent delegate, or silent reconciliation of token supply | likely scar after splitting the row | No shadow supply or silent reconciliation; Token behavior is selected by a versioned profile rather than one forever-fixed extension set | Lift display decimals and inert metadata first; risky extensions require separate proofs and rollback campaigns |
| O-016 | A caller or static client supplying executable Program identity, physical effect plans, semantic IDs, or trusted account projections | hard invariant | Callers may supply hints, witnesses, candidates, and physical accounts; release-selected state verifies every authoritative identity and derived effect | No caller input becomes authority by inclusion |
| O-017 | Calling the selected General result “optimal clearing” without a checked optimality certificate | hard invariant | Say “best valid submitted candidate” unless a descriptor admits and verifies a named optimality certificate | A checked certificate may lift the vocabulary restriction |
| O-018 | Instructions-sysvar adjacency as the authority joining provider publication to Source consumption | hard invariant, narrowly stated | Adjacency alone is not authority; a release-bound parser may use authenticated instruction evidence, while same-instruction CPI remains the conservative Pyth profile | [The Pyth compost decision](compost/PYTH_LOCAL_UPGRADED_2026_08_22.md) covers only its selected transport profile |

## Current physical profile restrictions

These are not mathematical or product omissions.

| ID | Current profile | Classification | Required lifting path |
| --- | --- | --- | --- |
| P-001 | Product V1 admits at most sixteen outcomes | current safe profile | First erase remaining width dispatch and measure contiguous runtime views; page only where packet/account/CU evidence requires it. Classified PURCHASABLE with its four inline authors named in [the cliff doctrine](design/CLIFF_DOCTRINE_V1.md) §1.4/§5.3 |
| P-002 | Capability manifest profile 1 admits at most sixteen entries and sixteen dependencies per entry | current safe profile | Finalized ordered paged graph with one aggregate commitment, unique kinds, and checked acyclicity. Ranked #2 on the commit-don't-inline shortlist in [the cliff doctrine](design/CLIFF_DOCTRINE_V1.md) §4 |
| P-003 | Current checked transition program has a finite instruction bound | current safe profile | Wider profiles, staged computation certificates, and AOT are valid; final economic commit remains bounded and atomic. Classified SESSION-SPLITTABLE (atomic-commit floor named) in [the cliff doctrine](design/CLIFF_DOCTRINE_V1.md) §2 |
| P-004 | Bearer/Structured V1 uses zero-decimal, narrowly admitted Token-2022 Mint and Account layouts | likely scar | Keep it as the strict default profile and add separately versioned Token behavior profiles |
| P-005 | RentCredit is Market-generation-scoped, sweeps only to its own immutable refund wallet, and closes only behind a completely retired Market | lifted 2026-08-27 | Done. The lifting path this row named — a lifecycle-scoped refund sink closing to its immutable wallet after its authenticated producer subtree closes — is `LifecycleRentCreditV2`, which tier 1 creates and sweeps. The permanent per-authority RentCredit V1 Create and Withdraw routes this row described were deleted the same day. Nothing of V1 survives: registered artifact coordinates 7 and 10 became a 128-byte `LifecycleRentCreditV2` and its Rent program on 2026-08-27, and the record type, its width and its PDA domain were deleted with that last pin (TR-A-DIR) |
| P-006 | A validated-artifact capability seal is write-once, was closed by nothing, and its rent was therefore permanently unreclaimable | CLOSED 2026-08-31 | **Accepted treatment.** [Decision 0005](decisions/0005-per-market-authentication-cache.md) deferred reclamation rather than half-designing it (`0005:303-305`: *"Because the seal is not per-Market, capability closure does not close it: a seal outlives every Market that used it and remains correct. Reclamation is therefore deliberately out of scope for this decision and is recorded as an omission rather than half-designed here."*). Keep the write-once discipline — it is what makes the seal sound — and add reclamation as a separate authenticated route rather than by weakening it. **Measured 2026-08-30 at HEAD (FRONTIER-2).** The write route is live and on chain: `programs/dclutch-trading-sbf/src/hot_v3/seal.rs:72` `process_capability_seal_v1`, which `allocate`s (`:171`) and `assign`s (`:177`). There is no close route anywhere — `CloseSeal` has zero occurrences in any `.rs` or `.lean` in the tree. Each seal is `CAPABILITY_SEAL_BYTES_V1` = 152 + 6×136 = **968 bytes** (`crates/dclutch-capability-seal-contract/src/lib.rs:69,86,89,92`), ≈ 0.00763 SOL rent-exempt, paid by whoever submits the sealing transaction and never from Hoard, fees or custody (`0005` §Rent). **The sharp edge is the release seed, not the per-Market count.** `trading_semantic_release` is the fourth PDA seed, so a Trading upgrade *"does not invalidate a seal so much as stop addressing it"* (`0005:280`) — every Trading release therefore permanently strands the rent of every seal written under its predecessor, across all descriptors × actions. The account class only grows, and it grows with the release cadence rather than the Market count. **Closure requires two things:** a `CloseSeal` route that preserves write-once (a closed seal must not be re-sealable into a different verdict at the same address), and a ruling on the beneficiary — the seal is not per-Market, so no Market's funding may receive the refund without one Market paying for every other's executions, which is the same reasoning that put the seal outside `FundingStateV1`. **CLOSED 2026-08-31 (CLOSESEAL). The ruling landed and the route is on chain.** E3 was put to ember on 2026-08-30 (`decisions/DECISION_PACKET_2026_08_30.md`) with him leaning collector-keeps -- *"garbage collect it and keep it seems somehow fine"* -- and was RULED under his 2026-08-31 full-autonomy directive (`WAVE.md`, "Rulings -- 2026-08-31"): **seal rent goes to the CLOSER, capped**, the funded-crank pattern; reward carved only from rent the close liberates; no Market's funding may receive it (SEALWIDE's constraint, and the same reasoning that put the seal outside `FundingStateV1`); burn rejected because it preserves the stranding. `hot_v3::process_capability_seal_close_v1` is the implementation: seven accounts, one signer who is also the sole beneficiary, no CPI, no creation. **Both closure requirements are met.** (1) *Write-once is preserved, and by the same seed the close turns on rather than by a second guard.* The close refuses unless the closer exhibits a Registry-owned activation cache whose Trading role still authenticates against THIS deployed program and ProgramData -- which a superseded generation cannot do, because the Loader moved its pinned slot (decision 0012) -- and whose semantic release DIFFERS from the seal's fourth seed (`0x400A CloseSealLiveRelease`). Since `process_capability_seal_v1` derives the address it writes from that same live semantic release, the only addresses the writer can reach are exactly the ones the close refuses to touch: a closed seal is an address the live executable cannot write to, not one it declines to. The residual is named rather than papered over -- `semantic_release_id` is publisher-supplied and nothing on chain checks it (`programs/dclutch-registry-sbf/src/lineage_v1.rs`), so the roles' upgrade-authority coalition could re-open a closed address under new bytes; that is the same coalition that could ship arbitrary Trading code, and therefore the same one every seal verdict already trusts. (2) *The beneficiary is the closer, capped by a GATE rather than an arithmetic.* Artifact profile 1 defines no lamport role beyond rent exemption and `SealedDescriptorClosureV1::decode` refuses every other profile, so the whole balance is liberated rent and a future class carrying a bounty or escrow is a different profile byte that refuses here until it gets its own close naming its own beneficiary. Paying `min(balance, exemption)` and leaving a residue is not available anyway -- a zero-data account below `Rent::minimum_balance(0)` is rent-paying and the runtime rejects the transaction -- and refusing an over-funded seal would hand a griefer a one-lamport permanent re-strand. **Evidence:** five real-ELF cases in `programs/dclutch-trading-sbf/program-test/tests/capability_seal_close.rs`, all green -- a stranger with no account on the chain closes a stranded seal and afterwards holds exactly `minimum_balance(CAPABILITY_SEAL_BYTES_V1)`; the loser of a close race refuses by ABSENCE (`0x4009 CloseSealAccount`) having moved nothing; a seal the on-chain seal outer wrote seconds earlier refuses `0x400A`; the real seal outer aimed at a closed address refuses `Content` and leaves it vacant, with the unsubstituted outer executing on the same substrate as its control; and a `SlotPinnedSuperseded` cache refuses `0x4007 ReleaseSuperseded` rather than witnessing a deployment that has moved. Frame hostiles (`0x400B CloseSealFrame`) cover an unsigned beneficiary and a foreign Registry. **One defect was found on the way and fixed in its own commit:** the seal WRITE outer had been DEAD since 2026-08-30 -- the Hot arm's extended-heap declaration put `require_extended_heap_admitted_v1` at the top of `reauthenticate_top_level_root_roles_v3`, which `process_capability_seal_v1` also calls, and `DCLTSEL1` was not on `declares_extended_heap_profile_v1`'s list, so every seal write refused `0x4008` unconditionally and no new `(descriptor, action, release, Registry)` tuple had a sealed execution path at all. **Still open, and it is not this row's:** no stranded seal has been closed on devnet yet -- cohort-6's stranded set is the devnet steward's queue. **The ruling gates every future seal class, and that is unchanged**: [`design/TRUST_RATCHET_V1.md`](design/TRUST_RATCHET_V1.md) holds that a product-graph seal is per `(product, basis, release)`, so it inherits the same "no Market's funding may take the refund" constraint -- and now also inherits the profile gate, which is what stops this close from paying out lamports a later class means for somebody else |
| P-007 | The capability seal's persisted byte layout is hand-authored Rust rather than Lean-emitted and byte-guarded | likely scar | **Accepted treatment.** [Decision 0005](decisions/0005-per-market-authentication-cache.md) convergence plan item 1 (`0005:506-511`) accepts the hand-authored layout as provisional and names its own lifting path: *"The byte layout is hand-authored in this crate and must migrate to a Lean-owned ABI emitter beside `EmitCapabilityProgramAbiRust.lean`; that migration is the lifting plan for its provisional status and is recorded in the omission index."* **Measured 2026-08-30 at HEAD (FRONTIER-2).** `crates/dclutch-capability-seal-contract/src/lib.rs` is 932 hand-authored lines carrying `CAPABILITY_SEAL_MAGIC_V1 = DCLTCSL1`, twelve fixed header offsets and seven row offsets, with **zero `@generated` markers** and **no `check-generated.sh`** — the crate directory contains only `Cargo.toml` and `src/`, against 14 such guards elsewhere in `crates/`. It is not a scratch layout: `programs/dclutch-trading-sbf/Cargo.toml` depends on it, `hot_v3/seal.rs` writes it on chain, and both `apps/dclutch-web/lib/directHotChain.ts` and `packages/dclutch-sdk/lib/directHotChain.ts` derive seal accounts against those offsets — so a persisted on-chain layout has **three independent authors and no byte-identity gate**. **The migration has a working model one crate over**: the emitter the decision names exists (`formal/dclutch-semantics/EmitCapabilityProgramAbiRust.lean`, a lake root at `lakefile.toml:31`), emits `crates/dclutch-capability-program-contract/src/generated.rs`, and is pinned by that crate's `check-generated.sh` (`lake build` → `lake env lean --run` → header pin → `cmp`). Closure is that same shape applied to `DCLTCSL1`, plus the TypeScript mirrors reduced to one author or pinned against the emitted artifact |

## Required work that is not an omission

The following rows remain part of the surpass-both goal. They may not be moved
to the accepted table merely because implementation is difficult.

| ID | Required successor capability | Present status | Completion evidence |
| --- | --- | --- | --- |
| U-001 | Generic Trading execution for Direct, General, Dealer, and Series | unfinished | One admitted descriptor path per family, interpreted V1 composition, and classification of standalone family artifacts as deleted authority or retained non-authoritative AOT evidence |
| U-002 | Direct inline and registered/reserved order lifecycle | unfinished successor convergence | Chain-derived operator packets, claim/custody effects, cancellation/retirement, hostile rollback, packet and CU evidence |
| U-003 | General batch collection, candidate selection, materialization, distribution, expiry, and closure | unfinished physical convergence, with two named defects | Runtime-width Trading path with best-valid-candidate semantics and full terminal campaign. Landed 2026-08-27: the ALT-backed v0 packet witness for all seven N=258 actions (`docs/evidence/GENERAL_ALT_PACKET_WITNESS_2026_08_27.md`), and the foreign-descriptor refusal. **(a) CLOSED 2026-08-27** — the root-lifecycle conjunct is two instructions in the shared transition prelude plus one `ProjectDataU8` in the canonical AccountProfile operation list, so all seven actions refuse a `Retiring`, `Retired` or unwritten lifecycle; fail-closed if an artifact omits the projection, because no request profile may project the two new scalars. Measured cost 16 CU per action, no account, packet or page moved (`docs/evidence/GENERAL_ACCELERATOR_CAMPAIGN_2026_08_27.md`). **(b) the seam is OPEN, General's release content is not.** The two blockers that made a V3 capability root unreachable are both resolved and both have real-ELF control: the activation seam wrote an all-zero family tail no family root could be decoded out of, and it authenticated the record at `selection.capability_release()` as a `CapabilityProgramV1` while `hot_v3` authenticates the same field as a `CapabilityProgramSetV2`. The tail is now the effect program's projected request buffer and a `CapabilityProgramSetV2` release activates through its selected descriptor. **General's activation artifacts EXIST (GEN-ART, 2026-08-27).** All three generations now have a public typed encoder in their owning crate — `dclutch-transition-vm::v2::encode`, `dclutch-effect-kernel::v2::encode`, `dclutch-account-profile-contract::encode_v1` — each a total function that hostile-decodes its own candidate, with the transition and profile encoders pinned byte-for-byte against Lean-emitted artifacts. `programs/dclutch-trading-sbf/program-test/tests/activation.rs` (`Campaign::General`) creates a real `GeneralRootV2` through the seam on a validator, and `activate_general_owned_v3` — run over the same manifest, config, funding prestate, custody observation and slot — agrees byte for byte on both the root tail and the FundingState poststate. **Both of the once-required halves are LANDED (WALL22, 2026-08-30).** The exactly-seven rule relaxed into four named profiles (`GeneralReleaseProfileV1`), and the eighth entry now has an artifact and a builder: `crates/dclutch-general-adapter-contract/src/activation_bundle_v1.rs` composes the triple on the family-neutral `dclutch-capability-activation-codec` template and `build_general_activation_capable_program_set_v1` writes the eight-entry set, re-authenticating its own bytes and requiring the canonical activation request to select the descriptor it was handed. `GENERAL_ACTIVATION_SELECTOR_V4 = 255` sits at the declared `selector_offset` and no controller request can produce it. The artifacts are proven against General's own author — the real effect kernel reproduces `GeneralRootV2::active` for arbitrary Market/config/generation, and the profile, transition and effect are byte-identical to the fixture triple `Campaign::General` already ran through the real Trading ELF. **The publication closure LANDED (GENPUB, 2026-08-30) and executes on chain.** `general_selected_release_v1` emits `GeneralReleaseProfileV1::SettlementWithActivation` — eight entries, because the seven-entry profile is not a smaller release but an unfoundable one: `authenticate_set_descriptor` admits only `CAPABILITY_PROGRAM_SCHEMA_RELEASE_ID_V1` and every action descriptor is stamped `v4::SCHEMA_RELEASE_ID`, so a Market founded on it could never create the root its own actions execute against. The three record labels are `activation-account-profile`, `activation-effect` and `activation-descriptor` — three and not four, because `CapabilityProgramV1` carries the activation transition INSIDE the descriptor, so a fourth record would be one the seam never reads and a second author for the transition; Direct publishes the same three. The activation descriptor's identity joined the publication as a twelfth fixed identity, and validation rebuilds the triple rather than inspecting it. The founding path publishes them: one loopback run with `DCLUTCH_MARKET_CAPABILITY=general` compiled 68 publication records where it compiled 65, and all five shared/activation records read back from the run's own ledger Registry-owned, byte-identical to the compiler's output, at the PDA derived from `(schema, sha256(body))` — the activation descriptor finalized under `7fa4d378…`, wall #22's own coordinate. `local-private-validator-general-capability-activation-v1` is the driver: two routing coordinates from the campaign report, every semantic fact re-derived from chain, the root address given a second author and the poststate two. **Still open, and it is NOT General's:** no General root has been activated, because the founding that must precede it refuses at `0x5182 ClaimsFoundingSbfErrorV5::Release` on the atomic DCLTGMF3 Open leg — and a Direct control at the same commit, on the same checked release, refuses identically. Two suspects are eliminated from chain state (the activation cache is complete and its carried bump reproduces its own address; the writability merge cannot apply to a single-protocol-instruction transaction). See `docs/evidence/GENERAL_PUBLICATION_CLOSURE_2026_08_30.md`. **(c) the zombie refusal is executed against a real root but not through the ELF.** `crates/dclutch-general-adapter-contract/tests/root_lifecycle_projection_v3.rs` runs the real `AccountProfileV2` projection over a real composite capability root and requires the root's own lifecycle byte to reach `scalar::ROOT_LIFECYCLE_OBSERVATION`, then requires every action's emitted transition to refuse the projected `Retiring`/`Retired` bank — with the discriminator that the projected banks differ at exactly one register. Not yet run through `hot_v3::process_hot_execution_v3` on a real ELF: the conjunct evaluates at phase 6 of 10, so a refusal is reachable ahead of the heap wall, but a General Hot bundle in `programs/dclutch-trading-sbf/program-test` (Hot38 frame, capability seal, ALT, five ELFs) does not exist — the Direct analogue is ~4,000 lines of fixture support in `program-test/direct-hot` |
| U-004 | Multi-LP Dealer liquidity, then scenario-solvent capital generalization | unfinished physical convergence and expansion | Real V1 custody lifecycle plus exact scenario-reserve/netting kernel, consented policy evolution, value, and rollback evidence |
| U-005 | Recurring Series founding | unfinished physical convergence | Ticket funding redistribution, Core founding, Claims initialization, selected capabilities, terminal closure, and replay evidence |
| U-006 | Source/Resolution creation, recovery, terminal admission, and funding closure | unfinished convergence | Fixed-role Resolution Core-effect campaign, real provider execution, repaired native fixtures, and exact three-ledger closure |
| U-007 | Claims initialization, native Positions, bearer representation, and terminal redemption | unfinished convergence | Real Token-2022 and Custody positive/late-refusal rollback campaigns and complete initialization/closure |
| U-008 | Structured and useful Fractional successor products | unfinished | Exact-denominator fast path plus exact claim shards/remainders, wrap/transfer/unwrap/redeem/retire, real Token-2022/Custody, and operator/UI support |
| U-009 | Providers beyond the first Pyth profile | unfinished optional breadth | One release-bound adapter at a time with real ABI/crypto and recovery evidence; no mock fallback |
| U-010 | Chain-derived creation, trading, liquidity, resolution, redemption, and retirement UI/operator workflows | unfinished | Unsigned transaction construction from hostile-decoded live chain state and user-visible refusal/status reporting |
| U-011 | Current integrated local-validator, hbox, and persvati evidence | unfinished | Exact committed-source archives, verifier-clean artifacts, rent/CU/packet/rollback evidence, and resolved cross-host reproducibility boundary |
| U-012 | Checked release, obsolete-path deletion, documentation, and devnet preparation | unfinished | Checked manifest plus deletion ledger and current documentation; devnet submission still requires explicit current authorization |
| U-013 | Certified nonnegative liability bases beyond categorical | pure theorem, kernel, and translation corpus landed; the physical Market/Claims layout slice is unfinished | Two-claim ramp/complement theorem and kernel, exact partition sum, generalized solvency, terminal payout, and translation corpus. Landed: `DClutchSemantics.LiabilityBasisV2` (evaluator totality, exact partition sum, split/merge/transfer/redemption preservation, `H >= Q*peak(T)` proved exact for both admitted families, and the sole apportionment boundary's rounding direction), `crates/dclutch-liability-basis-v2-kernel` (`plan_transition_v2`, `maximum_liability_v2`, `plan_claim_transfer_v2`), and the Lean-emitted 16 agreement / 19 refusal / 24 transition corpus. **AMENDED 2026-08-30 (FRONTIER): the layout half of this row is SATISFIED and the row's "Still required" clause was three generations stale.** Measured at HEAD: basis width is the Claims aggregate `claim_count` and payout scale `Q` is Core Market `basis_scale` (`crates/dclutch-market-core-codec/src/generated.rs:784`); the Core Market carries both `claim_basis: Identity` and `liability_basis: Identity` (`generated.rs:308-309`); the Claims aggregate and Position both carry a 32-byte semantic `BASIS` identity at fixed offsets (`crates/dclutch-claims-svm/src/liability_basis_state_v2.rs`, `MARKET_BASIS_OFFSET = 152`, `POSITION_BASIS_OFFSET = 88`); and `BasisInputV3` (`crates/dclutch-product-payoff-v2-codec/src/runtime_v3.rs`) carries `evaluator_release_id`, `knot_denominator`, Product-owned `knots`, `terms` and `failure_payouts`, with the certificate schema owned by `CategoricalApproximationCertificateV3` (`registry_v3.rs`). That layout is not a proposal: it is decoded on the live devnet path by seven `tools/local-validator/bootstrap/successor/` modules, the journey gauntlet and `dclutch-market-retirement-v1-operator`. **What is actually still required** is narrower and different in kind: the wire selector `BasisKindV3` (`runtime_v3.rs:105`) admits exactly `CategoricalQ1` and `GradedExactComplement`, so degree-0 (`Constant`) and degree-1 (`RampUp`/`RampDown`/`Tent`) shaped payoffs already ship — the unreached capability is **degree 2–3 only**, which needs a third `BasisKindV3` variant carrying degree and a knot vector, propagated through 112 `BasisKindV3::` match sites across 61 files (six of them in `programs/dclutch-claims-sbf`, where `terminal_certificate_v3.rs` matches on the `(kind, certificate kind)` pair), plus admission through the degree-≥2 price gate before any descriptor may select it. See `docs/design/ORPHAN_DESIGNS_TRIAGE_2026_08_30.md` §2 for the full measurement and the charter row. **CITATION CORRECTION 2026-08-30 (FRONTIER-2): two of the five fields above are cited to the wrong structs, and the correction matters because it relocates where a new field could go.** `crates/dclutch-market-core-codec/src/generated.rs:784` (`basis_scale`) is a field of `SeriesOpenObservation`, and `:308-309` (`claim_basis`, `liability_basis`) are fields of `Product` — both Lean *kernel* structs, neither in `CoreState`'s persisted bytes. **`CoreState` carries no basis width, no payout scale and no basis identity**; it pins `identity.product_record` (`STATE_PRODUCT_RECORD_OFFSET = 80`) and `identity.product_id` (`:112`), which are digests of a *separate* account. The row's conclusion is unchanged — the basis identity is genuinely persisted and genuinely authenticated, in the Claims aggregate at `MARKET_BASIS_OFFSET = 152` and the Position at `POSITION_BASIS_OFFSET = 88` (`crates/dclutch-claims-svm/src/liability_basis_state_v2.rs:28,37`), with scale and knots in the Registry-finalized `ProductBasisV3` record the Product digest names. What changes is the engineering consequence: a lane looking for room to add a basis field must not look at `CoreState`, which is Lean-emitted, `STATE_BYTES = 360`, gapless to exactly 360, with **zero reserved bytes and no `require_zero` anywhere in its decoder**, and 26 on-chain `data.len() != STATE_BYTES` refusal sites. The room is the 50 zero-enforced reserved bytes in `ProductBasisV3` (offsets 18 and 208), which is where `docs/design/BASIS_ABI_UNIFICATION_V1.md` §6 puts it. **And the sizing number above is wrong in the direction that would deter a lane**: *"112 `BasisKindV3::` match sites across 61 files, six of them in `programs/dclutch-claims-sbf`"* counts **references**, nearly all of them struct-literal constructions. Measured by `ast-grep` over `match` expressions: 84 files and 118 occurrences, but only **13 `match` expressions over the enum, of which 10 are exhaustive and the other 3 are fail-closed** — so a third variant cannot silently mis-evaluate anywhere in the tree, it either breaks the build or refuses. `programs/dclutch-claims-sbf` holds **one** exhaustive match (`terminal_certificate_v3.rs:86`), not six. The enum half of this row is days, not weeks; the weeks are in the evaluator. **ENUM HALF LANDED 2026-08-30 (BASIS-ENUM), and the sizing above verified.** `BasisKindV3::SplineDegree2To3 { degree }` ships, tag byte 3 is allocated in both Lean-emitted authors of the kind vocabulary, and it is refused on every route: the **13/10/3 sizing verified exactly** — all ten exhaustive matches were the ten `BASIS_ABI_UNIFICATION_V1` §1.6 names, site for site (five in `runtime_v3.rs`, one each in `terminal_certificate_v3.rs`, `product_v3.rs`, the representation operator, `wallet_terminal.rs` and `reader.rs`), with no eleventh anywhere in the seven cargo workspaces the tree spans; and all three fail-closed wildcards gained explicit named arms ahead of the wildcard. **A design document predicting a build-break set exactly is worth recording as such**, since the measurement it replaced was off by an order of magnitude in the deterring direction. Admission is `crates/dclutch-product-payoff-v2-codec/src/spline_admission_v3.rs`, a four-conjunct cascade whose certificate conjunct is discharged by `exempt_degree_below_spline_interval` in `DClutchSemantics/ProductBasisV3Abi.lean` rather than a runtime degree comparison. **The wire language is unchanged** — `BasisKindV3::decode` refuses tag 3 and the reserved spans at offsets 18 and 208 are untouched — so the `…-graded-basis-v4` schema-id bump `BASIS_ABI_UNIFICATION_V1` §1.6.2 demands is **deferred, not skipped**, and becomes mandatory in the commit that first *accepts* a kind-3 body. The named seam is `SPLINE_EVALUATOR_RELEASED_V3`. What remains is the evaluator: the de Boor port at the live wire's `i128`/`u64` widths and rounding rule (§9 commit 7), the `DCLTPGT1` record class and founding-time conjunct (commit 9), and the per-kind knot-multiplicity relaxation, which this lane deliberately did **not** grant |
| U-014 | Interpreted and stateless-AOT execution of one semantic descriptor | unfinished expansion | Exact Direct equivalence/certificate, Registry-bound artifact/toolchain, refusal equivalence, rollback, CU, packet, and rent comparison |
| U-015 | Acyclic representation composition, Token behavior profiles, lifecycle refund closure, and measured width lifting | unfinished expansion | One typed DAG, first lifted Token profile, complete producer-subtree refund closure, and contiguous/paged profile evidence |

## Maintenance rule

Every capability comparison or predecessor compost review must update this file
in the same commit that accepts a rejection, successor, or profile restriction.
Moving an item from `unfinished` requires exact evidence links. A release check
must refuse if any required row lacks evidence or if a rejected path remains an
executable fallback. The concrete expansion program is tracked in
[Expansion frontier after the omission review](research/EXPANSION_FRONTIER_2026_08_25.md).
