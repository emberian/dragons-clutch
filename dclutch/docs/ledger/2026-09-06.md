# Ledger — 2026-09-06

Dated deltas for 2026-09-06; the index is GOAL.md.

### 2026-09-06 00:10 EDT — THE ACCELERATOR'S WITNESS, THE RETIREMENT'S DEBIT
- Started: COHORT-16F (`openbatch-two-pass` on the General market with 17D's driver — the accelerator's first witness on
  any chain; close-batch and the second batch if their blockers are host inputs); PROGRAMS-17E continues (the closure
  burn as a coordinate debit in both frames; the first complete retirement on real ELFs; cohort-17 carries both links).
- Disk: the overnight churn was another project's local build (`audec`); the live tree's 108 GB debug target and the
  caches are ember's — two asks stand; the lanes build on hbox.

### 2026-09-06 00:45 EDT — COHORT-16F: both host walls green, the accelerator RUNS, and wall 3 is one field apart
- **The accelerator `6v1c2Go2…` executed for the first time on any chain** — `Program 6v1c2Go2… invoke [2]`, 30,771 CU,
  inside a Trading `OpenBatch` at **361,538 CU** (wall 1 died at 239,473, wall 2 at 319,075). Evidence level: **devnet
  RPC simulation against the deployed programs at finalized state, not a committed transaction** — devnet's own
  preflight refused the send, so nothing landed, no fee was paid, and the route is still **not corroborated** in
  `docs/evidence/witnesses/cohort-16-1-discovered.json`, third cohort running.
- **Both PROGRAMS-17D repairs hold on chain, each with a positive control.** Wall 1: the disagreeing `--rent-credit
  CE3PC9fY…` refuses `route/rent-credit-not-the-markets` and names the Market's own `6FGsfzP7…`; dropped, execution
  walks past `lifecycle.rs:1421`. Wall 2: the plan publishes
  `familyRequestDigest ff3ace4881df012e3b10e48cdb6a72ce119e184003561754d3cb9869f6c95856` — 17D's offline `ff3ace48…`,
  reproduced against the chain — and the caller-authority span moves off the probe. The digest is invariant to the
  lookup table and to the parent digest, which is what makes the two-pass loop terminate.
- **WALL 3, and the program printed both sides of it.** `TradingSbfError::Transition 0x4004` at
  `admitted_composition_v3.rs:370` (`ack.disposition() != Accepted`), POST-CPI on the accelerator's own `DCLTAAK2`
  receipt; the cause is `GeneralAcceleratorSemanticErrorV3::ConfigMarket` at `accelerator-sbf/src/general.rs:1705`,
  from `config.require_market(generation, semantic_basis_id)`. Generation agrees (2, 2); the basis does not — config
  `ed7bfc75…`, bank `48a66eee…`. **No replay was run and none was needed**: the accelerator's own `sol_log_data` prints
  both values, which is 17D's reader-instinct already shipped inside the program.
- **The two values are one portfolio field apart, on this market's own record.** `2WPUBZAH…` (`DCLTPRF2`) holds
  `48a66eee…` at `PORTFOLIO_CLAIM_BASIS_ID_OFFSET` 96 and `ed7bfc75…` at `PORTFOLIO_LIABILITY_BASIS_ID_OFFSET` 128.
  The bank register is projected from **96** by `general/account_rules_v3.rs:1508-1512`; the config field is
  `semantic_basis_identity_v3(linked_basis_hex)` from `general_market.rs:239`. **Everywhere else in the tree "semantic
  basis" IS the liability basis** — `product/payoff/registry_v3.rs:432` requires `domain.liability_basis_id() ==
  semantic_basis_id`, `market.rs:2428-2434` checks it, `market.rs:10367` fills Claims' founding request from it — so
  the account rule is the outlier, and it followed the config field's NAME (`claim_basis_id`), which is the misnomer.
  **No program change was made.** Moving the account rule to offset 128 costs a cohort-17 Trading link and NO
  re-founding (the live portfolio already holds the config's value there); moving the host derivation costs a fresh
  ~105-transaction General founding, because the config is hashed into `general_config_id` and pinned by the root.
  The owner decides. Every General fixture wrote this register by hand, so this transaction is the first time the two
  sides were ever produced by one real founding.
- **The runbook's `openbatch-two-pass` is short one act, twice.** It orders the lookup table after the second session;
  `general-successor-plan-v5` refuses `General v0 compilation: LookupTable` against the founding's `DfLj84Cz…`, and the
  four caller-authority PDAs are IN the table's canonical 53. Each digest needs its own frozen table and each table
  needs the route re-emitted onto it: `session(probe) → T1 → session(probe,T1) → plan → digest → session(digest) → T2
  → session(digest,T2) → plan → execute`. T1 `5jtjo4yb…`, T2 `73NjFXo4…`.
- `close-batch` and `second-open-batch` untouched — both need a Batch this refusal did not create; `CuW1791cAY…` is
  still ABSENT. Driver rebuilt at `4fba7a8af` in a detached worktree,
  sha256 `aa4e6de10ae85bb9042e82982c65c2d70a1e73f16bef39063cb07d030e43e977`, 3m19s and 1 GiB against the live target;
  no program rebuilt, release set `f533be49…` unchanged. Payer 1.384214644 → 1.364363844 (two permanent table rents at
  0.009550400 and ten fees); deployer unchanged. Addendum:
  `docs/evidence/COHORT161_UPGRADED_SEALED_2026_09_05.md`, dated 2026-09-06.
- COHORT-16F closed (`8ae5fb834`): the driver rebuilt at 4fba7a8af (1 GiB of local space, never near the abort); both
  host walls green with positive controls (the stale rent credit refuses by name; the plan publishes 17D's
  `ff3ace48…` digest, invariant to the table and the parent, which is what makes the two-pass loop terminate); **the
  accelerator `6v1c2Go2…` EXECUTED for the first time on any chain — 30,771 CU inside a 361,538-CU Trading OpenBatch, in
  devnet's preflight simulation at finalized state; the preflight refused the send, nothing landed, no witness yet**;
  wall 3 prints both its sides: `Transition 0x4004` on the ack, cause `ConfigMarket` — **the bank projects the portfolio's
  CLAIM basis (@96) while the config holds the LIABILITY basis (@128); everywhere else "semantic basis" is the liability
  basis; every General fixture had written that register by hand, so a real founding produced both sides for the first
  time.** RULING (orchestrator): the account rule is the outlier — it projects offset 128; no re-founding; cohort-17
  carries the Trading link. Also: the two-pass row is short one act twice (a frozen table per digest, the route
  re-emitted onto it). Payer 1.364. → PROGRAMS-17F **`(spawned)`**.
- PROGRAMS-17E closed (`7d45d6ba3`, `b1fe0193d`, `bcf346d77`, `7c1c677ab`): **decision 0025 shape A shipped — a refunding
  market has retired in the harness.** The burn is one arm on the one live writer (`signed_delta_v3.rs:1180
  burn_failure_coordinate_v1`: the coordinate derived, every other refused by name, one debit each to the aggregate and
  the escrow); the record's identity has ONE author (`semantic_basis_id_v3` — it had four hand-spellings) and the closure
  recomputes it from the bytes in frame against the aggregate's own `basis_id`; Claims closure 11 → 14 accounts, Core's
  checkpointed retirement 35 → 38 on all four packets; **the categorical retirement moved by no byte**; the burn costs
  16,160 CU (prepare 139,558 / close-vault 128,956 / close-replay 112,226 / finish 91,698); hostiles by discriminant on
  real ELFs (`0x5010`, the new `0x5506 Basis`, `0x5503` twice); the frames' account counts are hand-written Rust emitted
  by nothing (a finding); the retirement operator's `Claims` code split so a seated column is admitted; two campaigns
  red since bc9f9b4d6 repaired in passing. Digests (hbox): Claims `26809c62…`, Core `2f28309f…`. **Cohort-17 = Trading
  (17D/17F) + Claims + Core (17E): a full re-release + re-found; `7FQCfc4R…` on cohort-16.1 stays unretired as the market
  that proved the wall.** Owed → HOST-RETIRE **`(spawned)`**: the devnet path's linked-basis address (one argument the
  founding producer holds); the full refunding journey on the local validator; the checkpoint script's derived count.
- HOST-RETIRE closed (`42c3bb931`, `3302b7cf7`; its two `tools/cohort` edits rode `bded4c038`, whose whole-file
  `--only` swept them in with PROGRAMS-17F's): **the host reached decision 0025's shape A, and the tier can see it.**
  THE TAIL'S THIRD ADDRESS WAS ALREADY IN THE TREE and it has one author: `market.rs`'s `publish_market_records`
  writes it into the founding evidence as `linked_liability_basis_record` under `GRADED_BASIS_RECORD_SCHEMA_ID_V3`,
  the payout path's `terminal_routing_table_v1` has been reading it all along, and
  `aggregate_retirement_snapshot_from_chain_v1` now reads it there through `routed_record` (which recomputes the
  address from the PRODUCER's stored digest and refuses a report that does not reproduce it) while deriving the escrow
  pair off the aggregate it already reads. Seated or vacant is decided by the chain; a seated column whose evidence
  names no record is refused BY NAME. **THREE MORE WALLS were behind it, and only one was in the prompt.** (1) The
  builder took the escrow Position's OWN recorded owner as its PDA seed, so "this is the Market's escrow" was a
  property of the caller's derivation; it now calls `failure_escrow_v1` off the aggregate the way Claims calls
  `FailureEscrowIdentityV1::derive`, and a canonical Position at another owner holding exactly the residue at exactly
  the failure coordinate — which every rule stated against the Position's own bytes admits — is refused by one read
  instead of by a submission. (2) `aggregate_retirement_journal.rs` spelled the frame as `35`, `36` and `37` plus a
  fixed wire-byte table, so a loopback or devnet retirement of the very market shape the burn exists for would have
  been refused at "retirement operator changed account or data width" before it reached a chain;
  `AggregateRetirementFrameShapeV1` now names two shapes and derives all four widths from the operator's two account
  counts, the six extra wire bytes included (three table-resolved indexes in the instruction's list, three in the
  lookup's arrays — measured against the harness's own extents). (3) The Series capture required EXACTLY thirty-one
  roles while its own reader admitted three optional escrow ones: a reader and a schema with no producer, again. The
  `BeginRetiring` preflight now ADMITS a wholly seated residue — the accusation "supply is outstanding" was never true
  of that coordinate and the act that discharges it is a step of the retirement — with its other five arms untouched.
  **THE TIER DRIVES BOTH SHAPES** (`3302b7cf7`): 21 transactions where there were 12, the count derived from
  `bindings.json`'s label set rather than the literal `12`, nine bindings authored from the fold, 8 witnesses green,
  44 observations admitted, and `ClaimsSbfError::FailureEscrow`, `ClaimsMarketClosureSbfErrorV1::Basis` and
  `::Liability` go never-raised → OBSERVED. Refunding walk on hbox at `42c3bb931`, platform-tools v1.53, seven ELFs
  zero frame diagnostics: prepare 148,544 CU / close-vault 124,440 / close-replay 110,718 / finish 96,182, frames
  38 on all four, 1,089/1,145/1,145/1,025 wire bytes against the categorical 1,083/1,139/1,139/1,019. **The twelve
  categorical rows' CU figures were restated from this fold** — the accepted prepare read 225,798 in its note and
  measures 139,884 — because per-row CU is a measurement of a build, and the note now says which build.
  **STILL OWED, and it is bigger than one lane:** the full refunding journey on a local validator is UNRUN and is not
  one argument away. `tools/gauntlet/journey/` has no admission stage, no fill stage, no redemption stage and no
  retirement stage (`journey.rs:426-566` carries all four as `GapV1`), and its `Cargo.toml` does not depend on
  `dclutch-market-retirement-v1-operator` at all; the CLIs for every one of those steps exist in
  `bootstrap/successor` and none of the 25 modules the journey `#[path]`-compiles is one of them. Two of that gap
  register's stated reasons are now FALSE and should not be believed by the next lane: a Direct Hot fill landed on a
  loopback validator on 2026-08-31 (`docs/evidence/FIRST_LOCAL_DIRECT_FILL_2026_08_31.md`), and terminal settlement is
  wallet-signed and top-level Claims (`terminal_settlement_v3.rs:10-20`), not behind any Hot gate. What is genuinely
  unrun is the CHAIN of them on a refunding market, and `run-journey.sh:459-472` still cannot stand up its own
  substrate. The four checkpoint packets have executed on no chain of any kind.
- HOST-RETIRE closed (`42c3bb931`, `3302b7cf7`, `6c0fb3c1e`): the tail's one author is the founding producer's campaign
  report (`market.rs:3030 linked_liability_basis_record`) — it was never missing, the retirement path never asked;
  the devnet path recomputes the address from the producer's stored digest and refuses a report that does not
  reproduce it; three walls behind it: **the retirement builder never re-derived the escrow owner (it seeded both PDAs
  from the Position's own recorded owner — a decoy at another owner compiled a plan)**, the journal spelled the frame
  as 35/36/37 with a fixed wire table (it would have refused the burn's own shape), the Series capture wanted exactly
  31 roles while its reader admitted three optional ones; the preflight admits a wholly seated residue; **the twelve
  existing retirement bindings' CU figures were wrong** (prepare 225,798 claimed vs 139,884 measured) — restated; nine
  bindings authored; `FailureEscrow`, `Basis` and `Liability` go never-raised → OBSERVED (7 of 352 codes observed).
  **Owed: the full refunding journey on a validator is UNRUN and not one argument away — the journey harness has no
  admission, fill, redemption or retirement stage (four `GapV1`s), and two of that register's stated reasons are now
  false; the four checkpoint packets have executed on no chain of any kind.** Cohort-17 is where they will.
- PROGRAMS-17F landed (`1dd18be91`, `bded4c038`, `d9cf4200d`; report pending): the General bank's semantic basis is the
  portfolio's LIABILITY basis and the fixtures derive it from a founding; **the rule lives in the account-profile RECORD
  the founding publishes, not in any ELF — two commits differing only in that constant build byte-identical Trading
  (`65ff376e…`) and accelerator (`d2ff2b87…`) — so the General market is RE-FOUNDED, not the program redeployed** (the
  16F ruling's "no re-founding" was wrong on that point and is corrected here); the ratchet advanced with every row
  unmoved. Started 02:35 EDT: COHORT-17 **`(spawned)`** — the three moved links (Trading by 17D's lines, Claims + Core by
  17E's burn) re-released from two work roots; cohort-16.1 closed; the refunding market re-founded and walked to
  **Retired**; the General market re-founded with the corrected profile and OpenBatch landed — two firsts.
- PROGRAMS-17F closed (`1dd18be91`, `bded4c038`, `d9cf4200d`, `749be8a7d`): **the General AccountProfile projects the
  Portfolio's LIABILITY basis @128**, and the harness stopped writing both sides of `require_market` from one literal.
  The rule (`account_rules_v3.rs:1527`) read `PORTFOLIO_CLAIM_BASIS_ID_OFFSET`; it reads
  `PORTFOLIO_LIABILITY_BASIS_ID_OFFSET`, and a new guard pins the account, the destination AND the offset against the
  Lean-emitted constant — `every_action_sources_the_domain_registers_its_environment_reads` proved one writer per
  register and said nothing about which bytes, which is how one writer with the wrong field stayed green.
  `open_batch.rs` derives the config's basis from the founded Product through
  `dclutch_product::payoff::runtime_v3::semantic_basis_id_v3` (17E's one author); `market.rs` and `narrow_fixture.rs`
  stopped spelling that hash themselves; `family_hot_campaign.rs` — which reaches the real accelerator ELF and requires
  nothing of the disposition — had config `[2;32]` against bank `[5;32]`, the exact two values `require_market`
  compares, and now has one constant.
  **RED-PROVEN BOTH WAYS on real ELFs at `1dd18be91`**: @128 → 6 passed / 0 failed, the hostile refusing `0x4004` at
  349,511 CU with the accelerator's `config/bank semantic basis` log; @96 → 2 passed / 4 failed, the three committing
  campaigns failing `Custom(16388)` (devnet's own code) and the hostile OPENING A BATCH at 666,861 CU.
  **CU at `1dd18be91`**: OpenBatch 650,340 (N=2), 663,016 (N=13), 670,326 (N=258), 4 invocations / 55 accounts each;
  close-batch 642,189; second-open-batch 653,340; submit-candidate-seal 702,026. The operator's doc comment said
  "654,000 to 677,000" and named no commit.
  **THE RULING'S OPERATIONAL HALF IS REVERSED, by measurement**: the rule is in NO link. Two commits differing only in
  that constant build a byte-identical Trading `65ff376e876e3398d4e438171a955314202c9d3c7a194a9035231f203ac0c596` and
  accelerator `d2ff2b87ac79e5329b83f10e10a2f2d9b03ac16d445d070188f03946598da91a`; `programs/dclutch-trading-sbf` names
  `account_rules_v3` nowhere. The operator encodes the profile and binds its digest into the per-action descriptor a
  founding pins, so **this needs a fresh General founding, not a Trading redeploy** — which cohort-17 does anyway for
  17D's Trading, 17E's Claims and Core. Which side is wrong, and "the Portfolio needs nothing", both stand. In-place
  dated addendum on `docs/evidence/COHORT161_UPGRADED_SEALED_2026_09_05.md`.
  **Frames**: two independent captures at `1dd18be91`, both
  `ecdbac79e0b5d8ad73b4e02d7399b36813cc9cffb1b522217f21d527303c7007`, every function in all eight links identical to
  `7d45d6ba3`'s baseline — the diff is one line. `frames owed` is empty. 17D's `fc39a5bc7` was already discharged by
  17E at `b1fe0193d`; this capture separates nothing further.
  **TWO ORPHAN REDS, NEITHER THIS LANE'S, BOTH THE SAME SHAPE** (`749be8a7d`, `ca3bde280`): a producer moved to the
  domain-separated `family_request_digest_v3` at COHORT-16F's wall-2 repair and a fixture that was a second author for
  the same number did not follow. `hot_instruction_v3.rs:1233` asserted the bare `hash(request_bytes)`, failing the
  whole `general` accelerator suite; `general_successor.rs:1452` built its report the same way, so `serialize_plan_v5`
  refused its OWN test by name — and no gate watched it, because `root-targets.tsv` covers root-workspace integration
  targets and a crate's `--lib` unit tests are in no tier. Both convicted with two-revision controls (red at
  `7c1c677ab`, before this lane's first commit) rather than blamed on the account rule; both now reach the one author.
  Accelerator suite 3+3+10+13 green; `dclutch-operator --lib -- general` 58 green.
  **A FOURTH IS LATENT and left named**: `dealer_equity_hot_v3.rs:281` and `dealer_lp_hot_v4.rs:218` publish
  `hash(family_request)`. Nothing consumes either field yet, so it is a number waiting to be wrong — but it is the same
  number with the same two authors, and the General one cost cohort-16.1 a devnet transaction to find.
  **Runbook**: `openbatch-two-pass` retires at 16 and `openbatch-frozen-tables` (since 17) carries COHORT-16F's five
  acts — session, freeze a table over that route, re-emit the session onto it, plan — twice, both table acts `?` so a
  resume does not pay rent twice; `close-batch` and `second-open-batch` take the same shape with the ordering named as
  inherited. `check-steps.py` green for 14/15/16 and for a 17 selection; `--prove-frozen` reproduces both frozen tables.
  Gates on hbox: census, citations, emission, budgets PASS; `commands` NOT RUN for eleven `dclutch` CLI lines that
  predate this lane (the binary is not built).
  **OWED**: no devnet act was taken and none is claimed — `openbatch-frozen-tables` is unrun on any chain, and
  `close-batch`/`second-open-batch` have never run at all. The web and SDK were swept and mirror nothing here.
- PROGRAMS-17F closed (seven commits to `54f6d8f56`): the rule at `account_rules_v3.rs:1527` names the emitted
  liability-basis offset; a guard pins account, destination AND offset (the standing test proved one writer per register
  and said nothing about which bytes); red-proof both directions on real ELFs (@96: the hostile OPENS a batch; @128:
  6/6); two orphan reds from 16F's digest move convicted with two-revision controls and repaired (one refused its own
  test, watched by no tier — crate `--lib` tests are in no tier); `openbatch-frozen-tables` since 17. Started 02:50 EDT:
  DIGEST (every operator site publishes `family_request_digest_v3`; the Dealer's two bare hashes; a gate); JOURNEY-4 (the
  journey's four gap stages call the successor's CLIs; the full refunding journey on a live validator on hbox).
- DIGEST closed (`43665155e`): every operator site that publishes an accelerator family-request digest reaches
  `family_request_digest_v3`, and a `--lib` source census keeps it that way.
  **Census** (site: before -> after): `dealer_equity_hot_v3.rs:281` and `dealer_lp_hot_v4.rs:218`, bare
  `hash(family_request)` -> `family_request_digest_v3`, refusing through a new `ShadowDigest(ShadowDigestErrorV3)`
  variant that carries the cause instead of folding it into `Arithmetic`; `general_hot_v3.rs:854` (17D),
  `general_successor.rs:864` and `:1460` (17F), `series_current_acquisition_v5.rs:844` already reached it and did not
  move. The latent fourth this ledger named yesterday is discharged. Tree-wide the census now finds 8 authored sites
  and 0 bare across `crates/`, `programs/`, `tools/`.
  **THE DISCRIMINATOR, and it is the part worth keeping**: `family_request_digest_v3` does NOT own every request digest
  on the Hot path. The prelude computes a SECOND, deliberately bare one -- `hot_v3.rs:1117` -- carried as
  `parent.parent_request_digest` and rejoined by `hot_v3/children.rs:41,84`; the same bare form is the chain's own
  author for the Trading- and Claims-role child caller authorities (`generic_market_founding_v1.rs:1022`,
  `terminal_settlement_v3.rs:180`). `direct_inline_route_v3`, `market_founding`, `terminal_retirement_v1` and
  `wallet_terminal_payout_v3` spell those by hand and are all CORRECT; sweeping them into v3 would break every one.
  The census keys on the NAME `family_request_digest` and records why, so the next lane does not read "one author" as
  "one digest".
  **The gate is a source census, and the reason is a measurement**: no operator route report is constructible in a
  `--lib` test -- `build_general_hot_instruction_v3` is driven only from the accelerator program-test and neither
  Dealer builder has a caller or a full frame fixture anywhere -- so a report walk here would have to invent the frames
  it claims to check. `family_request_digest_one_author_v1` instead asserts that no initializer of anything named
  `family_request_digest` names a hashing primitive other than `family_request_digest_v3`, which fires on a file that
  does not exist yet. RED-PROVEN by restoring `dealer_lp_hot_v4`'s bare hash: the census names
  `dealer_lp_hot_v4.rs:233` by line. Its companion test drives the classifier over the exact convicted forms, so a
  zero count cannot be read off a disconnected instrument.
  **Control**: `cargo test -p dclutch-operator --lib` 355 passed 0 failed (353 before, +2 here). No lock row
  (`dclutch-market` was already a dependency) and no frame baseline rows (`dclutch-operator` is a DEV-dependency of
  registry/core/custody/claims-sbf and is in no SBF link). Used the shared workspace `target/`, not a scratch one: a
  cold target needed ~10-20 GiB against 19 GiB free, and AGENTS.md asks for one `target/` anyway.
  **FOUND, NOT FIXED, and it is live in the browser**: `packages/dclutch-sdk/lib/generalPlanV5.ts` holds TWO
  always-false comparisons against the operator plan's `familyRequestDigest`, which has been the v3 derivation since
  16F. Line 586 compares it to a locally recomputed BARE `sha256(request.bytes)`; line 1089 compares it to the CHAIN's
  Hot ack `requestDigest`, which `hot_v3/execute.rs:1771` fills from the bare `prepared.request_digest`. So
  `inspectGeneralPlanV5` throws on every valid plan and `decodeGeneralHotReceiptV3` on every valid receipt, on the live
  `GeneralWorkspace.tsx` path, and `generalPlanV5.test.ts:112` is green over it because the fixture is the same bare
  hash -- the producer moved and the mirror did not, for the fourth and fifth time. Not patched here: the SDK needs
  BOTH digests, the v3 one for the plan and the bare one for the receipt, and that is a decision this lane does not own.
- DIGEST closed (`43665155e`, `fd18234c6`): tree-wide census 8 authored / 0 bare — the Dealer's two sites fixed (neither
  had a consumer or a test; both now refuse through a carrying `ShadowDigest` variant); **the discriminator: the
  domain-separated digest owns the FAMILY request, while the prelude's bare digest is the chain's own author for the
  child caller authorities (`generic_market_founding_v1.rs:1022`, `terminal_settlement_v3.rs:180`) — the four sites that
  spell it bare are correct, and a sweep would have broken every one**; a source-census gate keyed on the field's name,
  red-proven by line. **Found live in the browser: the SDK's General plan inspector compares the plan's digest to a bare
  hash and the receipt's to the wrong one — every valid plan throws, green in its tests because the fixture builds the
  same bare hash (the fourth and fifth twin drift of one producer).** → WEB-DIGEST **`(spawned)`**.

### 2026-09-06 03:30 EDT — COHORT-17 IS DEPLOYED, AND IT IS FOUR LINKS
- **The candidate reproduces.** `932edc83f` built twice on hbox through `swarm-build` from two repository roots and two
  `--work` roots: release gate `a98ed988f9d266084b3835dc5f838a03c8513810669395373960d58ce7df6be3` equal, all eight ELFs
  byte-identical, `sbf_build_diagnostics_total=0`, `sbf_build_freshness=passed links=8`. Trading `65ff376e…`, Claims
  `26809c62…`, Core `2f28309f…` are PROGRAMS-17E's and 17F's own digests, reproduced.
- **Cohort-17 is FOUR links, not three.** The accelerator moved too — `587181d9…` (cohort-16.1) to `d2ff2b87…` — at or
  before `1dd18be91`, which is where 17F measured it. Not bisected: a bisect costs a build and the count is the fact.
- **The eight are on chain**, each dumped back and compared byte-for-byte before the next role spent. Registry
  `5RFL5hcw…` · rent `DQ2uHwan…` · custody `8kVd1sCp…` · resolution `gYWBUAqM…` · claims `2cCWKUtU…` · trading
  `272BExJX…` · core `2PAzXCkA…` · accelerator `WMGVfhVu…` at slot 493,942,086.
- **Ledger, to the lamport.** Closing cohort-16.1's seven roles and its accelerator returned **36.548487360 SOL**
  (28.657732010 → 65.206219370). The eight deploys cost **36.601529360**. `fund-payer` moved 2 SOL to the campaign
  payer; the ladder (36 transactions, zero errors) and the seal cost the deployer 0.032285920.
- **The release set is `d69a67c7fbc98a2c8ecfddb2d32f58a7b82fd5fc61fe501ff350f06b09dae9ea`**, final deployment-set
  `64f48d8139cf2e243ab5ad8ae573247eba91c64f50543ad7304d8ed342ed6171`, 7/7 dispositions (two carry-forward, five
  already-current), all five owned roles `equal: true` against a fresh finalized observation.
- **Four runbook defects, each found by running it.** (1) `MARKET_KIND` is keyed by row, so `openbatch-two-pass` and its
  two cohort-17 successors were sent at market 1 and the whole General family refused to emit — which is why cohort-16.1
  ran those five acts by hand. (2) `seal-general`'s VERIFIER PROSE still carried a `{stage:openbatch-refounded}` brace;
  prose is not substituted, so the tail reached the emitted script as `/session.json` and the self-contained value test
  deleted every script it had written. (3) The preamble creates `$OUT` and nothing else, so `prepare` died on the first
  of seven roles with `No such file or directory (os error 2)` and no name for what was missing; the generator now
  derives every write directory from the row's own flags and creates the PARENT, never the leaf — because
  `prepare --account-dir` creates its own and refuses one that exists, measured both ways. (4) The preflight's
  accelerator-slot arm asked only whether the source still contained the pin TEXT, so a manifest with an EMPTY
  `deployment_slot` printed a green verdict with nothing after it.

### 2026-09-06 03:45 EDT — JOURNEY-4: the tier stands up its own substrate, and the four gaps become stages
- **The journey has been unrunnable since 2026-08-31 and the reason was structural, not a missing argument.**
  `run-journey.sh` refused to start without `--market PATH`, and nothing in the tree could produce one for it: a
  Market compiles only through `DirectMarketCompilerOwnedV1::load_local`, which observes a LIVE checked deployment,
  and the runner deployed its own seven ELFs under offline-derived identities that no separately-compiled market
  could name. Its own banner said the ordering `prepare -> boot -> administer -> compile -> found -> keep going` was
  "its own unit". **That unit is landed** (`46ce130b9`, `bddfb3e5d`): the campaign links
  `relayed-vertical/src/substrate.rs` (the ladder links the same file), brings the checked-mutable substrate up from
  a `CHECKED_UPGRADE_GATE`, compiles the Market against the deployment it is standing on, founds it with
  `campaign --founding-only`, and keeps the validator for every stage after. The seven SBF builds, the offline
  program-id derivation and `frame-diagnostics.json` go with it — a gate is emitted only in strict mode and strict
  mode refuses a nonzero SBF stack-frame-overwrite count, so a gate that EXISTS is the zero-diagnostic proof
  TIERS.md asks a build stage to make.
- **THE SUBSTRATE WORKS, measured on hbox.** First live run at the COHORT-17 gate `6569364c…` (revision
  `932edc83f`, which carries 17E's burn, 17F's basis fix and HOST-RETIRE's retirement work): prepare, a fresh
  `solana-test-validator` over the prepared account directory, the administration campaign through activation, the
  two-source market compiled against the live deployment, and **189 founding transactions to an Open Market
  `CCPogLjCJGGZELrwpQ6sypyvSRR6npQZqBDGkRb8Csd3`** — including `DCLTGMF3`'s own hostile (a substituted Claims
  request rolls the whole founding back at 31,420 CU) and the frozen 60-key routing table.
- **THE FIRST LIVE RUN FOUND A CONTRADICTION 189 TRANSACTIONS DEEP, and it had been there all along.**
  `resolution::derive` locates a `recovery_policy_record` in the founding evidence and refuses "a Market whose
  record shape it does not recognise"; `LocalMarketShapeV1::default()` buys NO ladder, on the stated ground that
  "defaulting a market into buying one would be spending on the caller's behalf". Those two are unsatisfiable
  together, which is why no `--market` this runner could have been handed would have worked either. The campaign
  now founds with one rung through the SHIPPED `--recovery-rungs` parser (`2500:120`, the ladder tier's own
  string): the Resolution manifest's hard four is `1 + rungs.max(1) + 2`, four at zero rungs and four at one, so
  the market differs from the lab default in exactly the record the resolution stage needs and in nothing else.
- **The four `GapV1`s are stages**, and every act is the SHIPPED command a host runs, called in-process with the
  argument vector a host would type — the lesson `tools/gauntlet/ladder/` wrote down when it called
  `recovery_crank::run_v1` with a vector rather than building an 18-account frame. admission →
  `local-private-validator-user-position-admission-v1` (twice: the buyer with its four collateral flags, and a
  second stranger with a Position and nothing else); fill →
  `…-direct-trade-produce-v1` then `…-direct-trade-v1 --execute`; fee → `…-direct-fee-settlement-v1`;
  redemption → `…-wallet-terminal-payout-input-v1` then `…-wallet-terminal-payout-v1`; retirement →
  `…-refresh-evidence-v1`, `…-terminal-sequence-v1` (CoreBeginRetiring, DirectBeginRetiring, ResolutionCloseFund,
  DirectCloseCapability, RetirementReplayHandoff, AggregateRetirement) and `…-aggregate-retirement-v1` (the four
  checkpoint packets). Three of those drivers advance ONE durable action per invocation, so each stage is a
  bounded resumption loop with a stated ceiling; a stage that hits the ceiling reports how far it got, and a
  refused stage is a FINDING recorded with the driver's own sentence rather than a stop.
- **Every signature a driver lands is re-read from the chain** with `finalized_signed_packet`, which re-derives the
  packet and verifies it, so the census can corroborate the program that ran — a driver's own JSON carries no logs
  and could never be observed. One label per JOURNAL FILE, not per stage: a stage's journals hold transactions
  that invoke different programs (the routing-table acts are the ALT program's, the seal and the Hot execution are
  Trading's) and `census observe` refuses an observation whose logs do not show the bound program invoked.
- **TWO OF THE GAP REGISTER'S REASONS WERE FALSE and are deleted with their evidence named.** (1) "Admitting a
  second party a Claims Position is itself a Claims mutation and therefore behind the Hot gate" — it is a
  TOP-LEVEL Trading route a wallet signs for itself. The packet arithmetic that made the canonical Direct Hot
  continuation 36 bytes over the legacy limit was answered by measurement: a fill landed on a loopback validator
  2026-08-31 at 1,282,624 CU over a frozen lookup table as a v0 packet. The one wall of that entry that survives —
  the canonical artifact family is derived for ONE market geometry — is kept, as a fact about the fixture family
  and not about this tier. (2) "Redemption carries the same signing-CallerAuthority requirement as every other
  Claims mutation" — false since terminal settlement became family-neutral.
- **The `#[path]` set is now the WHOLE successor**, generated from its own `main.rs` module list rather than
  curated. The curated subset's tripwire fired four times in six days — twice silently, once for a whole day, once
  inside the hour a lane linked the previous miss — because nothing in CI builds this tier. Three names are
  excluded and the header says why: two are submodules of other successor files, and `ledger` is this tier's own
  file, which the successor links back the other way. 796 unit tests pass in the journey binary.
- **OWED, stated rather than hidden.** (a) TWO AUTHORS FOR BeginRetiring: this campaign's own hand-built stage,
  which its existing bindings witness, and the shipped terminal-sequence driver. Both run and the transcript says
  which the chain accepted; one of them is a deletion the next convergence owes. (b) `bindings.json` does not
  cover the spine's labels, deliberately — bindings are authored from what the ledger OBSERVED, which is the rule
  `tools/gauntlet/retirement-checkpoint/` wrote down after manufacturing exactly that false green; the labels
  exist now and the fold that authors them is the next act. (c) Three of the successor's own `#[cfg(test)]` tests
  in `series_lifecycle_campaign.rs` read evidence fixtures at a path relative to `CARGO_MANIFEST_DIR`, so they
  pass from the successor's manifest and fail from this one — a fixture path with two possible roots, the
  successor's to fix, and invisible until a second crate linked the file.
- WEB-DIGEST closed (`7f9d391a4`): **the browser holds both request digests, each from its own author, and a plan the chain
  actually published now decodes.** `packages/dclutch-sdk/lib/generalPlanV5.ts:586` compared the plan's
  `familyRequestDigest` to a locally recomputed BARE `sha256(request.bytes)` -> it compares it to
  `familyRequestDigestV3(request.bytes)`, a new `lib/shadowDigestV3.ts` mirroring
  `crates/dclutch-market/src/execution_strategy/shadow_digest_v3.rs:383` and reading its domain from a GENERATED
  constant (`FAMILY_REQUEST_DIGEST_DOMAIN_V3`, a new `digest` kind in the protocol-constants table, gated by the
  standing `abi:protocol-constants:verify`); `:1089` compared the Hot ack's `requestDigest` to that same plan digest ->
  it compares it to the bare hash the CHAIN fills the field from (`hot_v3.rs:1117 hash(family_request)` ->
  `hot_v3/execute.rs:1771 request_digest: prepared.request_digest`), carried on the inspection as `ackRequestDigest`
  because a receipt arrives where nothing may await. **The domain is not filed under the `seed` kind**: a seed names
  an ACCOUNT and a digest domain names a NUMBER, and the kind column exists so a survey classifies by declaration.
  **A THIRD, EARLIER DRIFT WAS IN FRONT OF BOTH**, found only by trying the live plan: the producer emits
  `[limit, heap, hot]` since `432d07339` and the browser still required exactly `[heap, hot]`, so
  `inspectGeneralPlanV5` refused every real plan at its first line, before any digest was compared. The mirror now
  destructures three, checks the limit byte-for-byte against a generated `GENERAL_HOT_COMPUTE_UNIT_LIMIT_V3`
  (`registry.rs:62`, alias pinned by an assertion), and the Hot instruction's index is a named constant because a
  second reader (`reacquireGeneralSuccessorStatusV5`) held the literal `1` and would have reported "lifecycle
  projection differs".
  **RED-PROVEN THREE WAYS, each by restoring the exact old line**: plan compared to the bare hash -> 8 of 16 failed;
  ack compared to the plan's digest -> 2 of 16; the two-instruction prologue -> the live case alone. Green: 16/16.
  The fixture at `generalPlanV5.test.ts:112` derives its digest through the SDK function instead of spelling
  `sha256(requestBytes)` (the second author that kept both wrong sides green), and the receipt test now ASSERTS the
  two digests DIFFER, so one author for both cannot pass it.
  **THE LIVE CASE, and it is why a synthetic fixture was not one**: every fixture in that file is built by the same
  function the inspector is checked against, so a producer that moves takes both sides with it.
  `fixtures/general-successor-plan-v5.devnet.json` is COHORT-16F's exact `open-batch` report against cohort-16.1
  (`familyRequestDigest ff3ace48...`, the one 17D reproduced offline and the chain published); nothing in the tree
  derives it, and `familyRequestDigestV3` over its request bytes reproduces `ff3ace48...` exactly.
  **CENSUS** (every SDK/web site that recomputes or compares a request digest, with its chain author): 21 non-test
  sites classified, and **every one is correct**. v3: `generalPlanV5.ts:626` only (author
  `shadow_digest_v3.rs:383`). bare, each against a chain author read line by line: `dealerEquityV3.ts:194`
  (`dealer/equity_request.rs:443`), `claimsCustodyReplay.ts:429` (`custody-sbf/lib.rs:275`),
  `rationalTerminalHotV3.ts:421` (`hot_v3.rs:1117`) and `:439` (`claims_composition_v3.rs:579`),
  `walletTerminalPayoutV3.ts:605` (`rational_terminal_v3.rs:484`) and `:656` (`terminal_settlement_v3.rs:131`),
  `rationalOpenChainV4.ts:643`, `rationalRetireReceiptV4.ts:666` and `:879`, `rationalOpenWasmV1.ts:101-102`,
  `generalPlanV5.ts:627`. Neither (record-content, schema or journal digests, not request digests):
  `claimsCustodyReplay.ts:215`, all of `dealerEquityChain.ts`, `directHotChain.ts`, `directInlineV3.ts`,
  `directHotBumpHintsV1.ts` (it hashes nothing; it assembles seeds), `founding/genericFoundingRequest.ts:240`,
  `directParticipant.ts:179`, and every `sha256` in `apps/dclutch-web/lib/` and `components/` -- all client journal
  or WASM-integrity digests. No site uses v3 where the author is bare or the reverse.
  **OWED, named not fixed** (mirrors a Rust derivation without a generated constant; none is a request digest):
  `dealerEquityChain.ts:69-70` hand-spells the two request-PROFILE schema preimages while four siblings in the same
  boolean come from `generated/dealerEquityV3.ts`; `walletTerminalPayoutV3.ts:217` the exposure-bundle schema
  preimage; `directHotChain.ts:130` and `rationalRetireReceiptV4.ts:126` are two independent copies of
  `dclutch/product-basis/semantic/v3` (both already carry an `abi-coverage.baseline.json` row);
  `dealerEquityV3.ts:51` re-spells `SIGNED_DELTA_REQUEST_OFFSET = 80` that `generated/walletTerminalPayoutV3.ts:95`
  exports. `walletTerminalPayoutV3.test.ts:395-404` hand-spells four domains its own module imports from `generated/`.
  Also owed to the convergence owner: `docs/reference/abi/{protocolConstantsV1,generalSuccessorV5}.md` are stale by
  one constant each -- `tools/gate reference` refuses a dirty tree and lanes do not run it.
  **GATES**: tsc 0, both lints 0 errors (19 pre-existing web warnings, none in a touched file), `generalPlanV5` 16/16,
  the SDK suite 86 files passed / 0 failed, `abi:protocol-constants:verify` + `abi:general-v5:verify` + `abi:coverage`
  + `abi:capability-surface:verify` green. **`tools/gate web` FAILED (1,665s) on two checks, NEITHER this lane's and
  both named**: (1) `apps/dclutch-web` 6 failures in `sourceProviderWasmParity` / `sourceReadinessWasmParity`, whose
  cause is printed as `extern location for sha2_const_stable / keccak does not exist: target/debug/deps/lib*.rmeta` --
  the SHARED `target/` being rewritten under them by the concurrent Rust lanes, plus one `ECONNREFUSED 127.0.0.1:3000`
  -- no Rust was built by this lane; (2) `packages/dclutch-sdk` "THE SHIPPED COHORT DID NOT ANSWER" --
  `deploymentLiveness.live.test.ts` reads the featured market `closed` where the cut says `alive`, which is cohort-16.1
  having been closed by COHORT-17. The full `npm test` also carries three `abiVerification` reds --
  `abi:route-census`, `abi:rational-open-wasm`, `abi:rational-retire-child` -- all three cargo-driven over Rust,
  none reading any path this lane touched, and all three last regenerated at the 09-04/09-05 convergence against
  which 157 files under `crates/`+`programs/` have since moved. They are the convergence owner's `--converge`, not
  a WEB-DIGEST regression.

### 2026-09-06 04:20 EDT — JOURNEY-4: the recovery arm of the founding journal's table is a stale mirror, and a live chain says so
- **The second live run walked to `DCLTGMF3` and refused by name:** `DCLTGMF3 resolved account count changed:
  expected 62, observed 60 (market carries a recovery policy)` (`market.rs:458-460`), after the substrate,
  the administration campaign, the two-source market, and the whole founding prologue including the frozen
  60-key routing table. **The compiled message's own census printed 60 twice** — "60 complete keys (3 static,
  14 writable loaded, 43 readonly loaded)", pre-mutation and pre-ALT recheck, digest
  `5b7bae09940bb9b8ce4791609831af5b73befb056d0094b4ea1765b56f3f963b` — so the program and the host agree about
  the frame and only the EXPECTATION disagrees.
- **Located, in one line.** `founding_submission_journal.rs:42` `FOUNDING_RECOVERY_POLICY_RECORD_PAIR_V1 = 2`
  and `:105-110` `Self::Dcltgmf3 => if recovery_policy { GENERIC_MARKET_FOUNDING_COMPLETE_KEYS_V3 + PAIR } else
  { … }`. `GENERIC_MARKET_FOUNDING_COMPLETE_KEYS_V3` is 60 and is the MEASURED count; the `+2` asserts that a
  recovery-bearing `DCLTGMF3` additionally carries the RecoveryPolicyV2 record's raw/staging pair. At
  `932edc83f` it does not. **A recovery-bearing market cannot be founded through DCLTGMF3 by the successor at
  this revision.**
- **This is the FOURTH time this one table has been a stale mirror of a frame that moved**, and the file says so
  itself: it "restated it as 58 and was not moved when seating the failure escrow at founding took the frame to
  60", which cohort-16 met on devnet on 2026-09-05 — the other arm of the same match, three transactions from
  Open. The repair that landed then moved the CATEGORICAL arm to read the constant and left the recovery arm
  holding `constant + 2`. A number that is measured on one arm and typed on the other is not one author.
- **Not this lane's to fix** (the successor's drivers belong to the cohort lane), and named for whoever takes
  it: the fix is the same one the categorical arm already got — read the number, do not hold one. An
  UNCHECKED inference the next lane can settle in one command: `tools/gauntlet/ladder/` founds a recovery-
  bearing market through the same route, and its last green run was at `6818c212` on 2026-09-04, before this
  revision; if the arm is wrong at `932edc83f` then the ladder tier is red at HEAD too, and one
  `run-ladder.sh --walk capture` says so.
- **What the two live runs establish.** Run 2 (`CCPogLjCJGGZELrwpQ6sypyvSRR6npQZqBDGkRb8Csd3`, 189
  transactions to Open) proves the rebuilt tier's substrate, market compilation and founding work end to end
  against a checked release gate on a loopback validator — the thing that had been impossible since
  2026-08-31. Run 3 proves the recovery-bearing founding does not, and says exactly why. **The spine's four
  stages have therefore been BUILT and COMPILED but not yet EXECUTED on any chain**: the campaign has never
  reached them, because both routes to an Open Market it can resolve are blocked — the no-ladder market by
  `resolution::derive`'s `recovery_policy_record` requirement, and the ladder-bearing market by this table.
  That is the single wall left between this tier and the first full refunding journey, and it is one constant
  wide.
- **`tools/gate journey` PASSES at 58s warm on hbox, 800 host tests, 0 failed** (`94e6d93a4`). Linking the whole
  successor made the gate run 800 tests where it ran a handful, and three of them were red for a reason worth the
  finding: `series_lifecycle_campaign.rs` resolved its evidence fixtures as `CARGO_MANIFEST_DIR/../evidence`,
  which is one root from the successor's manifest and a different, absent one from any other — a fixture path
  with two possible roots, invisible until a second crate compiled the file. They walk to the repository root
  now. The successor's own three still pass, which is the control.
- **No blocked.json row is discharged and none is claimed.** The routes the four new stages would witness —
  `trading/user_position_admission_v1::…#Admit`, `trading/hot_v3::process_hot_execution_v3`,
  `claims/terminal_settlement_v3::process`, `core/retire_v1::process#Retire`,
  `claims/market_closure_v1::process` — have executed on no chain from this tier, because no run has reached
  them. A binding authored from what a campaign OUGHT to touch is exactly the false green
  `tools/gauntlet/retirement-checkpoint/` was built to remove.
- JOURNEY-4 closed (`46ce130b9` … `b4478b895`): **the journey tier stands up its own substrate** (a checked-mutable
  deployment; the Market compiled against the deployment it stands on; 189 founding tx to Open in ~14 min) — the seven
  offline SBF builds and the offline program-id derivation gone; **the four stages exist as the shipped commands' verbs**
  (admission ×2, fill's three drivers, redemption's two, retirement's terminal sequence + the four checkpoint packets),
  one label per journal file; the register's two false reasons deleted with evidence; the `#[path]` set is the whole
  successor, generated from its own module list (the curated subset's tripwire fired four times in six days); a fixture
  root defect found by the gate (`CARGO_MANIFEST_DIR/../evidence`); `tools/gate journey` PASS 58 s / 800 tests. **The
  stages have executed on no chain: a ladder-bearing founding refuses at the founding journal's arithmetic (60 + 2
  typed vs 60 observed) — the fourth time that table mirrored a frame that moved.** Owed: `BeginRetiring` has two
  authors (the tier's stage and the shipped driver); the spine's labels unbound. → FOUNDING-JOURNAL **`(spawned)`**: the
  journal reads the compiled frame's own count; the journey to Retired on hbox; the ladder walk re-run.
- WEB-DIGEST closed (`7f9d391a4`, `c8bffefd4`, `38ac8dbf5`): the plan's digest compared to `familyRequestDigestV3` and the
  ack's to the bare hash the chain fills it from, each with its Rust author named; `shadowDigestV3.ts`; the domain a
  generated constant under a new `digest` kind (gated twice); **a third drift in front of both: the operator has emitted
  `[limit, heap, hot]` since 432d07339 and the browser required exactly `[heap, hot]`, so every real plan was refused at
  its first line** — the limit checked byte-for-byte against a generated constant; red-proof per line (8/16, 2/16, the
  live case alone); the live case is COHORT-16F's real plan report, whose `ff3ace48…` nothing in the tree derives; the
  census of 21 request-digest sites: one v3, eleven bare (each against its author), the rest neither — none wrong.
  `tools/gate web` red on the cleaned target being rebuilt under it and on cohort-16.1 closed by cohort-17 (expected).
  Owed to the converge: three abi verifies and two reference pages stale since the convergence.

### 2026-09-06 05:40 EDT — FOUNDING-JOURNAL: the frame is the one author of its width, a recovery-bearing founding reaches Open twice, and three more walls stand behind it
- **The count has one author and it is the frame** (`0f4f33c4e`). `compile_current_founding_message_v1`
  returns `CompiledFoundingFrameV1` — the compiled message together with the exact key set the bounded
  instruction list it compiled DECLARES (`market.rs:397` the struct, `:433` `declared_frame_keys_v1`) — and
  both halves come out of the same `bounded`, so no second reader can bound the instructions differently and
  then disagree. `authenticate_resolved_founding_message_v1` (`market.rs:456`) now compares SETS: a program id
  can never be moved into a lookup table and the payer is always a message key, so a correct compilation
  resolves to exactly the frame's declaration. Set equality is strictly stronger than the count it replaces
  and names the offending key. The journal carries the declared count into its plan row
  (`founding_submission_journal.rs:206`), checks it there, and re-checks the persisted count against the
  persisted message on every load.
- **Deleted with it:** `FOUNDING_RECOVERY_POLICY_RECORD_PAIR_V1`, the whole `exact_unique_accounts` table
  (all six operations, not only the two arms that were wrong), `FoundingSubmissionBindingV1::market_has_recovery_policy`,
  both campaign sites that computed it, and the live-report test helper that SEARCHED two candidate bindings
  because the journal could not say which shape it was.
- **A census pin that had never run.** `generic_founding_final_compiler_census_pins_the_60_key_shape` carried
  no `#[test]` attribute: five numbers and a digest, compiled and never evaluated. It runs now and passes —
  60 complete, 3 static, 14 writable loaded, 43 readonly loaded. Only the unused-item warning showed it, in a
  crate that emits sixty of those.
- **The LITERALS item.** `GENERIC_FOUNDING_OPEN_WINDOW_ACCOUNTS_V1` reads
  `dclutch_market::GENERIC_FOUNDING_OPEN_ACCOUNT_COUNT_V1` — Core's own declaration, through a contract crate
  this host tool already depends on, so a host utility gets the program's number without depending on an SBF
  program crate. FOUR stale prose numbers, not three: the window's "21-account" at `:9712`, `:11320`, `:17269`
  and the `DCLTGFP1` frame's "104" at `:11076`. Both widths moved when the failure escrow was seated and
  neither sentence moved with them.
- **Red-proven twice on a chain, not inferred.** hbox, loopback validators, gate `6569364c…` at `932edc83f`,
  host from HEAD (`--worktree`; the ELF/host revisions differ by one registry devnet-observation module, so
  this is local-validator evidence and not release evidence).
  - `tools/gauntlet/ladder/run-ladder.sh --walk capture`: **Open Market `9Xj77aG4y9LnnhxXzCf8ce61SsZkopdaEMVkK5yhaKMJ`,
    23 steps, 198 transactions**, `recovery_policy_hex` a real `DCLTRCV2`. Its `DCLTGMF3` census printed
    `60 complete keys (3 static, 14 writable loaded, 43 readonly loaded)`. The `expected 62, observed 60` wall
    is gone. 230 transactions, 10,954,635 CU for the whole walk.
  - `tools/gauntlet/journey/run-journey.sh`: **Open Market `89JAapNBSLeSfQWVe2QavyFkJVx7tLv5rUdimQXCzv6p`,
    23 steps, 198 transactions**, same 60/3/14/43 census. 238 transactions, 10,956,370 CU.
- **The journey's ladder-buying market was written and never committed** (`4307b5772`). JOURNEY-4 left
  `journey.rs` dirty, so `git archive HEAD` — how `run-journey.sh` builds outside `--worktree` — carried the
  no-ladder `demo_market_input`. That run founded an EMPTY `recovery_policy_hex`, walked 189 transactions to
  Open, and refused at `resolution::derive`: "the founding's evidence names no `recovery_policy_record`". The
  instrument was wrong, not the tree.
- **The ladder's capture walk threw its own run away** (`4307b5772`). `drive_crank` answers the preflight's
  too-early refusal by building NO PLAN, which makes the not-yet-due guard — it measures `due - observed` off a
  plan — unable to fire, so the walk went into the driver's bounded wait, which refuses for the same reason and
  whose refusal was propagated with `?`. The first walk today died with "30283340s away, past the stated ceiling
  of 600s" and **no transcript at all**. It is recorded now, as `not-yet-due` with the driver's own sentence,
  nothing sent. The walk completes: substrate EXECUTED, two-source market EXECUTED, founding through Open and
  funded EXECUTED, advance NOT-YET-DUE, the rung UNREACHABLE. 5 witnesses checked, 1 failed —
  `every-crank-frame-is-the-relay-contract-eighteen` reads `null` because no AdvanceRecovery frame is ever
  BUILT on this fixture, and that is unsatisfiable for the reason the tree already documents at
  `LocalMarketShapeV1::terminal_max_age_seconds`: the local Pyth fixture declares a one-year shelf life, so
  every rung a loopback market buys is anchored a year past the capture (target 1818967681, chain clock
  1788685903). The witness is left red on purpose. A rung ANSWERED on this fixture needs a publication the lab
  can refresh, which is a fixture question and stays owed.
- **A branch that had never run, and the chain named it in one line** (`18c5da93b`).
  `distribute_collateral` sends its Token-2022 `TransferChecked` only when `share > 0`, and `share` had been
  zero on every run this tier ever completed — the founding runs its projected-Custody prestate ladder twice
  and the two lanes consume the whole supply. A LADDER-BEARING founding leaves the founder holding a share, the
  branch executed for the first time, and the transfer came back `Error: owner does not match` / `0x4` at
  instruction 4. It named the campaign's `core-upgrade-authority` as the transfer authority; Token-2022
  validates against the SOURCE account's own `owner`, which the founding writes as its `campaign-payer` role
  (its suffix-resume check is literally `wallet.owner == payer.pubkey()`). `substrate::campaign_payer_keypair`
  names that role beside `authority_keypair` and says how they differ. Chain-proven after: four holder opens at
  3,656 CU each and the four-transfer ring at 2,020 CU each, all landed.
- **The next wall, located but not convicted.** `chain-derived CreateFund: Funding`. Both
  `material.recovery_policy()` and the observed policy are `Some` — the journey's create snapshot passes
  `recovery_policy` at index 12 — so this is the RECOVERY arm of `select_resolution_funding_entries`
  (`crates/dclutch-resolution-core-v3-operator/src/lib.rs:2525-2571`), which selects three manifest entries by
  the first attempt's `funding_allocation_id`, the policy digest and the material hash. Four of its returns are
  the same `Funding` code and nothing distinguishes them, which is the coarse-code case AGENTS.md names: the
  discriminant wants splitting, not guessing. The journey's and the ladder's market inputs are byte-identical
  apart from the manifest and the random fee recipient, and the ladder never runs this route.
- **The four stages have still executed on no chain.** Admission, fill, redemption and retirement are built and
  compiled; the campaign now dies five stages later than it did this morning, at the resolution funding ladder
  rather than at the founding journal's arithmetic.
- **Owed, and why each is owed rather than done.**
  1. `BeginRetiring`'s two authors are NOT converged. The tier's own criterion is "whichever of the two the
     chain accepts first, the other reports what it met, and the transcript says which — which is the shape a
     convergence needs before one of them is deleted." No chain has accepted either; deleting the hand-built
     stage now would retire the only `BeginRetiring` this campaign's bindings witness on an inference. One
     journey run reaching retirement settles it.
  2. **The journey writes no transcript when a stage's HELPER errors.** Its design claim — "the transcript and
     the complete conservation ledger are written first, the refusal is recorded" — holds for stages that
     RECORD refusals, and a hard `Err` out of `execute()` still throws the run away. Three runs today, three
     hard `Err`s, zero transcripts, and every wall cost a full 35-minute run to see once. This is the same
     defect the ladder's `drive_crank` had, and it is the highest-leverage thing left in the tier.
  3. `bindings.json` still does not cover the spine's labels, for the reason it did not this morning: bindings
     are authored from what the ledger OBSERVED, and the ledger has observed nothing past the ring transfers.
- Shas: `0f4f33c4e` (one author + the LITERALS item), `4307b5772` (the journey's ladder market, the ladder's
  recorded refusal), `18c5da93b` (the collateral owner). 49 founding host tests pass; the ladder and journey
  campaigns both build against the changed successor, which is the `#[path]` tripwire.
- FOUNDING-JOURNAL closed (`0f4f33c4e`, `4307b5772`, `18c5da93b`, `0c6d7258c`): **the frame's count has one author — the
  compiled founding message returns the exact key set its bounded instruction list declares, and the journal compares
  SETS, not counts** (a program id never moves into a lookup table; the payer is always a message key); the `+2` typed
  pair, the six-operation `exact_unique_accounts` table and two campaign sites deleted; two recovery-bearing markets
  founded to Open on hbox (journey 238 tx / 10.96 M CU; ladder 230 tx), both censuses at 60; four stale prose numbers;
  a census test that carried no `#[test]` for weeks now runs; three walls behind the founding — the journey's ladder
  market never committed (the lane's own instrument), the ladder's capture walk threw its own run away (recorded as
  `not-yet-due` now), `distribute_collateral` signed with the wrong authority on a branch that had never run (chain-proven
  after). **The journey now dies at `chain-derived CreateFund: Funding` in the recovery arm of
  `select_resolution_funding_entries`, four returns sharing one code; and the journey writes NO transcript when a
  helper errors — three 35-minute runs to see three walls once.** → JOURNEY-5 **`(spawned)`**: transcripts first, the
  refusal named, the four stages to Retired on a validator, the ladder's red turned by the shelf-life field.

### 2026-09-06 11:00 EDT — THE SEATING HOLDS, AND THE FILL IS CERTIFIED
- **`escrow-seated` is GREEN on chain, and it is the whole cohort.** Market
  `9e8fTH75s82pjcEK8pY8PaLPoZW6W1am1qQ6J4JHjagQ` (generation 2, payout scale 3, basis width 4 — REFUNDING) reads
  `L1 holds tracked 1000000002 atoms across 3 accounts == Mint supply`, `L3 holds 4 Positions sum to the aggregate
  supply vector [166666667 x4]`, with escrow `8HLLen86V9Vy7NXQYHxP1hGEm4FT99av6WWsagm2FbNA` holding
  `[0, 0, 0, 166666667]` and the founder `[166666667, 166666667, 166666667, 0]`. Cohort-16.1 read that census
  **VIOLATED** and could not certify its fill.
- **The fill is CERTIFIED**, `4cevxQwCHnTdfT5o6upCVH71ioE5Th99ew4hqgMhdDmweFsEBXnhFFbzwik9NwQwi9GgEHGgAwRqi7ajt2WCc5Zm`,
  slot 493,995,625, **1,116,618 CU** against a 1,400,000 ceiling. Cohort-16.1's Hot transaction LANDED and its driver
  refused "Direct terminal claim schedule is not the exact K+1 partition", because only nonzero balances are collected
  and the seller could not contribute a column it never held. **The escrow is the fifth row.**
- Activation 508,377 CU on 36 accounts; capture `38nrFuZGaz…` slot 493,992,422; fee settlement `2ike4FN3Mk…` 94,853 CU,
  199 atoms to the seller and 2 to the venue fee account. Post-fill census: L1, L3, L4 all hold across five token
  accounts and four Positions.
- **The General basis wall is CLEARED.** `E5jHZvCPLJ2K22dayGuJM9MzDZhKS5FGPqz45cJ3HabY`, founded on the corrected
  account profile, activated at 484,523 CU and sealed at `3AryF51KGHt6BLTxBzA58A1tCZM1w4PAmf8C2gY5dPAn` for 622,058 CU.
  `openbatch-frozen-tables` ran both passes and both frozen tables; the accelerator was INVOKED (31,797 CU) and no
  longer disputes the market — it reaches the CANDIDATE PROJECTION and refuses
  `GeneralHotCandidateErrorV3::InvalidPlan`, "a record did not decode", reported by Trading as `0x4004 Transition` at
  361,626 CU. Devnet's preflight refused the send; nothing landed. **That code is `map_err(|_| …)` at eight sites in
  `hot_candidate_v3.rs` and is NOT localized. No program was changed.**
- **Ten runbook defects, each found by running the row**, all repaired in the runbook: the General family could not be
  emitted at all; a `{stage:}` brace in verifier prose; no write directories; a write directory removed by its own
  backup; the `?` guard checking an input; `{execute:…}` never expanding `$HERE`; the fill naming a file the producer
  does not write; `seal-general` passing an empty `--routing-table`; `escrow-seated` without a producer and then
  without its token list; and `LABEL=PUBKEY` census lists no cohort had ever filled in.
- **And one outside it**: `cargo --manifest-path` does not move cargo, so a stage launched from a sibling checkout
  built this tree's driver under `nightly`. Repaired at `236245509`.

### 2026-09-06 07:30 EDT — JOURNEY-5: THE TIER STOPPED THROWING ITS RUNS AWAY, AND THE WHOLE CAMPAIGN WALKED
- **A stage whose helper hard-errors now writes the transcript.** `execute` owns the document and the campaign
  only fills it; `run` holds the session and the ledger OUTSIDE the campaign body, which is what keeps the
  validator alive while the refusal that killed the run is written down — a body that owned the guard would
  have killed the chain on its way out of the very error being diagnosed. The wall carries the stage
  (`entering`, once per stage, so it is control flow rather than a guess read off the error text), the
  helper's own sentence verbatim, the finalized slot, and every account the conservation ledger watches plus
  the Market, aggregate, mint and Hoard — present or **ABSENT**, because "the ledger this campaign derived
  does not exist" and "it exists and something else owns it" differ by one column. Red-proven on the host:
  making the write conditional on success fails the new test at `transcript.is_file()`. With it: the
  run-evidence document writes on the same paths (the runner's own comment promised both documents and then
  `die`d on the missing one), each stage row is recorded when ITS stage finishes rather than eleven fallible
  calls later, the gap register is in the document from the first line, and `run-journey.sh` prints the wall.
- **`Funding` is split for the whole CreateFund path** — `ResolutionFundingCauseV3`, sixteen named causes
  carrying what they compared: the compartment selection (both arms, and the arm where material and policy
  disagree), the manifest scan, the entry mask, the vacant Source destination, the pending subset ledger. No
  coarse `Funding` remains anywhere reachable from `build_resolution_create_fund_v3`; `VerifyFundReady`,
  `ActivateFund` and `CloseFund` still carry theirs and the type says so. Host test red-proven (collapsing two
  of four back onto `Funding` fails the pairwise-distinctness assertion), and two existing coordinate tests
  that asserted `Err(Funding)` now name their conjunct — the exact case AGENTS.md describes.
- **THE REFUSAL, CONVICTED BY NAME ON THE FIRST RUN THAT REACHED IT** (hbox `20260906T104204Z`):
  `chain-derived CreateFund: FundingConjunct(SourceDestinationNotVacant { expected == observed, owner = the
  Resolution controller, data_len: 224 })`. **It was never the recovery arm of the selector.** This morning's
  entry inferred that from the two `Some`s and said so as an inference; the split settled it and the inference
  was wrong. The Source resolution state EXISTS at exactly the address the campaign derives, and the
  activation receipt is there at its exact width: **the atomic founding creates the Source state, writes the
  activation receipt, and leaves all three selected funding rows Active** — the tier's stage had been a second
  author of an act the chain already accepted. The stage now observes what the founding left (owner, phase,
  market, generation, receipt width, row statuses) and drives only what the founding did not. Its
  double-create hostile is STRONGER than the transaction it replaces: the builder refuses to construct the
  second frame at all, and the check names the discriminant and the accounts inside it.
- **THE WHOLE CAMPAIGN WALKED, and one run named five walls** (hbox `20260906T110501Z`, 16m24s of chain after
  a 1-minute warm build): 198 founding transactions and 6,184,510 CU to an Open recovery-bearing Market, the
  distribution (4 tx, 14,624 CU), the holder ring (4 tx, 8,080 CU), the funding stage `not-driven` with what
  it found, and the rent sweep (2 tx, 7,517 CU) — **223 transactions, 6,634,011 CU, all ten conservation
  censuses evaluated, verdict `conserved`, no violated law.** The five refusals, each on its own stage row:
  `PacketTooLarge` compiling the admission message; the fill, which cannot read the report the refused
  admission never wrote; the Pyth submit's `custom program error 0x8004` = `ResolutionError::FinalizedRecord`
  ("a finalized raw-record owner, PDA, digest, rent, or vacancy proof was invalid"), on instruction 2; the
  redemption, which needs a terminal receipt the Pyth leg never minted; and the terminal sequence, which
  refuses campaign evidence carrying `direct_trading_funding_ledger` without `direct_capability_root` because
  the Direct first-use accounts are created together by the first trade. **Two are independent walls
  (admission packet width, `FinalizedRecord`); three are downstream of them.**
- **The buyer's wallet had never held a lamport.** The founding gives the participant a collateral token
  account and never gives the participant's KEY anything, and an address that has never held a lamport is not
  an account; the shipped admission driver authenticates the collateral source's OWNER rather than taking the
  token account's word for it. Named exactly by the first transcript ("snapshot missing required account
  2HazeEr9…", which resolves in the founding's own evidence to `localParticipantFixtureLiquidity.sourceOwner`)
  and fixed by funding it beside the second stranger the campaign already funded.
- **One author for the funding compartments**: `select_resolution_funding_entries_v3` is public and the
  journey's hand-written copy is deleted. **The gate runs the ladder's host tests**: `tools/gate journey`
  `cargo test`ed only the journey and `cargo check`ed the rest, which compiles a test module and never runs
  it, so the ladder's two tests were no gate's evidence; both binaries run now (801 rows and 384, the
  ladder's own two named among them as the positive control). **`tools/gate journey` PASS, 88s warm.** No
  `tools/gate ladder` was added: a tier per campaign would compile the same successor twice for one tripwire.
- **`afac67fca`'s frame-ratchet paragraph is WITHDRAWN.** It said the commit leaves the ratchet red because
  `dclutch-core-sbf` links the operator crate. It does not — `dclutch-resolution-core-v3-operator` is a
  **dev-dependency** there, so it reaches no link under the ratchet's normal-and-build closure, and
  `tools/gate frames owed` at `d5e599ae` names only COHORT-17's `108f950f`. No JOURNEY-5 commit owes rows.
- **Loopback transient, instrumented.** Two runs died inside the founding on `… did not reach finalized
  transaction history`, on different record-publication transactions, with the validator still advancing its
  processed slot in its own log. That sentence is produced by two different chains — one that confirmed and
  stopped rooting, one where the bytes never landed — so it now carries two read-only probes that say which,
  and a founding wall reports whether the validator answered a fresh reader at all.
- **Owed.** `BeginRetiring`'s two authors are unconverged for the reason they were this morning: the terminal
  sequence has still not been accepted by a chain, and the transcript is the criterion. `bindings.json` is
  unauthored past the ring transfers because bindings are folded from what the ledger OBSERVED. The recovery
  arm's three missing-compartment causes want a `RecoveryPolicyV2` fixture the operator's test module lacks.
  The ladder's one red is named exactly and **cannot** be turned by threading `terminal_max_age_seconds`:
  `max_age` governs the crank's admissibility AND the publication's freshness, so no value satisfies both
  legs against a frozen capture — a rung answered there needs a publication the lab can refresh, which is a
  fixture question, not a parameter.
- Shas: `afac67fca` (transcripts + the split), `8ba596894`, `06d5de2b7` (one selector), `a36435124` (gate),
  `3dda869a7`, `3c85dacef`, `74952f3ab`, `ba71147af`, `c2fd6b4c7`, `a68002326`, `6ec1f1812`, `0fb0cb0af`,
  `851dc44a8` (the founding is CreateFund's author; the buyer's wallet), `df6c961de`, `366971dfa` (TIERS).

### 2026-09-06 07:55 EDT — JOURNEY-5 addendum: the ladder at HEAD, and two measurements that closed guesses
- **The ladder's capture walk, at HEAD, hbox `20260906T112408Z`: 891 s, market `B3xW1XLRDi1c…`, founded to
  Open and funded through the shipped `--recovery-rungs 2500:120` parser, the crank RECORDED `not-yet-due`
  with the driver's own sentence** (the primary leg is due at unix 1,818,967,680 and the chain clock read
  1,788,694,735 — 30,272,945 s, the one-year Pyth shelf life), and the rung `unreachable`. FOUNDING-JOURNAL's
  repair holds: the walk is no longer thrown away. **4 of 5 witnesses OK; the one red is
  `every-crank-frame-is-the-relay-contract-eighteen`, "expected 18, chain says null"** — the 18-account
  AdvanceRecovery frame is never built on this fixture, which is the red left on purpose and it is named
  exactly. It **cannot** be turned by threading `terminal_max_age_seconds`: `max_age` governs the crank's
  admissibility AND the publication's freshness, so no value satisfies both legs against a frozen capture.
- **The admission's `PacketTooLarge` is not a key-count problem.** The obvious reading was that nobody
  published a routing table — the driver compiles a v0 message and takes `--routing-table`, the spine has
  carried the argument since it was written, and the tier passed the empty slice. Measured (hbox
  `20260906T112833Z`): a frozen table over the founding's WHOLE account set was published and declared, and
  the admission refused with the same `PacketTooLarge`. The table is not kept — six transactions a run for no
  change — and the measurement is, at the empty slice. **The next step is to read the compiled message's own
  key/data split, not to publish a bigger table.**
- **`afac67fca`'s frame-ratchet paragraph is withdrawn** (see the entry above): the operator crate is a
  dev-dependency of `dclutch-core-sbf`, so no JOURNEY-5 commit owes frame rows.
- Run 5 reproduces run 4 exactly: 229 transactions, 6,719,028 CU, `conserved`, the same five refusals, the
  funding stage `not-driven`. Two complete walks agreeing is the control this tier did not have this morning.
- Shas: `91f6a767f` (the table, published and measured), `2776c1138` (removed, measurement kept).
- JOURNEY-5 closed (nineteen commits to `20f980456`): **a transcript on every error** — the runner holds the session and
  the ledger outside the campaign body so the wall is written while the validator still answers (the stage, the helper's
  sentence, the finalized slot, every watched account present or ABSENT; red-proven); **the funding refusal was never the
  selector's recovery arm — the morning's inference is withdrawn: the atomic founding had already created the Source
  state and left the funding rows Active, and the tier's stage was a second author of an accepted act** (it now observes
  what the founding left; `ResolutionFundingCauseV3` splits the whole CreateFund path); **the journey walked to rent
  recovery twice on hbox — 223/229 tx, ~6.7 M CU, all ten conservation censuses conserved**; five refusals named in one
  run: the admission's `PacketTooLarge` (not a key count — measured), the fill behind it, the Pyth submit's
  `FinalizedRecord 0x8004`, the redemption behind it, the terminal sequence refusing evidence with a funding ledger and
  no capability root; the ladder's one red cannot be turned by a shelf-life field (one `max_age` governs both legs
  against a frozen capture — a refreshable publication is a fixture); `tools/gate journey` PASS 87 s with the ladder's
  host tests. → JOURNEY-6 **`(spawned)`**: the three independent refusals convicted at their authors; the journey to
  Retired.

### 2026-09-06 12:05 EDT — BEGINRETIRING EXECUTED, AND THE LAST WALL IS ONE CONJUNCT
- **Four walls cohort-16.1 named are behind us.** The stranger was paid 600 atoms
  (`5GLKEVCi5pg…`, 215,396 CU); the founder's three payouts drained the Hoard to **0**; **core BeginRetiring**
  (`1jKfxPjvY6J…`) and **direct BeginRetiring** (`54JL4eQLcLP…`) both finalized; **ResolutionCloseFund**
  (`3L6ULNQN4S4…`) finalized. Cohort-16.1 could not reach `BeginRetiring` at all — its failure column was held by
  nobody and no payout could lower it.
- The census at the retiring boundary: `L1 holds`, `L3 holds 4 Positions sum to [0, 0, 0, 166666667]`, Hoard 0,
  `L4 inapplicable` because settlement discharged the liability. **What remains is exactly the column decision 0025
  says the closure burns.**
- **THE LAST WALL IS `Successor(MakerRootCountInvariant)` AND IT IS LOCATED.** The Direct capability root
  `3dgyRvrebvaXKL8Z1hqgYMWRewXXsgxBjbseERjep3xK` reads `open_maker_root_count = 2` in its own bytes; the fill opened
  two maker replay roots and both are live. The producer that closes them EXISTS — `devnet-direct-close-maker-v1` —
  and **the runbook has no row that calls it**. Called by hand it refuses "embedded Direct Hot journal lookup closure
  was not exact, ordered, and distinct", one sentence over three conjuncts at `direct_trade.rs:5757-5765`, and the
  failing conjunct is `any(|key| *key == Pubkey::default())`. **`Pubkey::default()` is the System program**, and the
  Hot frame's frozen table `H2QMjhby88h4mGAovFX8rwS3BJcgUXTMEnEfMAVLexox` holds it at index 36 — read off the chain,
  57 addresses, all distinct, index 36 identical to the journal's word, because Trading CPIs System to create
  accounts. **No change was made.** Whether that conjunct is redundant against the `pubkey_list_sha256` check beside
  it is a ruling, not a lane's call.
- **No market has been retired on any chain, and this cohort says so.** What moved is which wall it is: no longer the
  founding's seating, no longer the manifest's dependency edges, no longer `BeginRetiring`, no longer
  `ResolutionCloseFund`.
- The site's featured market was **not** updated, for cohort-16.1's reason: there is no retired market to feature.
- Ledger: deployer `26.572399090` unchanged since the seal; payer `3.364363844 -> 2.004939706` across the whole Direct
  spine, the General founding and seal, both OpenBatch tables, four payouts and the terminal sequence. One recorded
  top-up, 2 SOL.
- COHORT-17 closed (docs/evidence/COHORT17_SEATED_FILLED_RETIRING_2026_09_06.md): **deployed from two repository roots
  and two work roots** (gate `a98ed988…` equal, eight ELFs byte-identical, zero diagnostics) — **FOUR links moved, not
  three: the accelerator too** (`587181d9…` → `d2ff2b87…`); closing 16.1 returned 36.548, the eight deploys cost 36.602;
  seal 7/7; **the refunding market `9e8fTH75…`: L3 HOLDS with the escrow seated (cohort-16.1 read it violated), the fill
  CERTIFIED at 1,116,618 CU with the escrow as the fifth partition row, the stranger paid 600, Hoard 0, core and direct
  BeginRetiring FINALIZED, ResolutionCloseFund FINALIZED — in RETIRING**; the wall is one host line: the close driver
  refuses any key equal to `Pubkey::default()`, which IS the System program at index 36, beside a digest check that
  already covers an unset slot; the General market founded, activated and sealed — wall 3 cleared, OpenBatch now refuses
  `InvalidPlan` at the candidate projection (eight sites under one `map_err`, simulation only); twelve runbook defects
  repaired by running; `cargo --manifest-path` does not move cargo (a sibling checkout built this tree's driver under
  nightly). Deployer 26.57, payer 2.00. RULING: the zero-key conjunct is deleted (the digest is the invariant). →
  COHORT-17B **`(spawned)`**: the first market Retired on any chain; the General `InvalidPlan` replayed.

### 2026-09-06 08:55 EDT — JOURNEY-6: the admission is keys after all, and the Direct root exists
- **(a) THE ADMISSION IS A KEY-COUNT PROBLEM, and the negative that said otherwise was a WRONG-TABLE result.**
  Measured on the host with no chain, from the frame's own constants
  (`dclutch_operator::user_position_admission_v1::the_admission_packet_is_keys_and_a_routing_table_makes_it_fit`):
  the admission compiles to **1,477 wire bytes over 28 static keys, 366 bytes of instruction data and 2 signatures**
  against a 1,232 packet limit, and over the route's own canonical lookup addresses to **798 bytes, 5 static keys, 23
  loaded**. Keys are ~897 of the message; the outer request is a fixed 328 bytes and is not the overrun. JOURNEY-5's
  table was published over the founding's EVIDENCE ACCOUNT MAP — about a dozen market coordinates — which carries
  almost none of this route's addresses, so the compiler moved a handful of keys against an overrun of roughly two
  hundred bytes and refused identically. **The measurement that reading needed was structural and cost no chain time.**
- **The journey routes through the founding's own frozen DCLTGMF3 table**, which is what every devnet cohort has done
  since cohort-16.1 stopped dead without one (`tools/cohort/build-sim-config.py`,
  `simlife_drivers.frozen_routing_table_for`), authenticated against the same address the devnet producer uses
  (`founding_market`). It publishes nothing: the founding already paid for that table. **And the label is the
  discriminator, not the market**: run 6's validator carried FIVE frozen tables and THREE of them route the founding
  market (`aat7JJTz` 56 addresses, `2DQVa5N2` 45, `GFpvDDra` 66), so "frozen and routes this market" identifies
  nothing on its own.
- **`PacketTooLarge` now carries the two numbers it could not.** The tree's one "by how much" diagnostic,
  `measure_v0_wire_bytes`, REQUIRED a lookup — so it answered `NoLookupUsed` on exactly the tableless route that needs
  measuring, and nothing in the admission driver called it. It is a diagnostic that never yields a sendable message; a
  supplied table that contributes nothing still refuses.
- **(e) THE JOURNEY NEVER RAN THE DIRECT CAPABILITY ACTIVATION, and two walls were that absence.** Nothing else in
  this tree creates a Direct execution root — Core's `ActivateCapability` CPIs Trading's `process_activation` and only
  `direct_capability_activation` reaches it. So the fill's root check had nothing to find, and `evidence_refresh`
  emits `direct_capability_root` only where the account EXISTS while the founding emits
  `direct_trading_funding_ledger` unconditionally (`market.rs:8387-8410`, the line pair that manufactures the half
  set): `require_direct_first_use_evidence_v1` then refused the whole retirement, and its sentence was true.
  **LANDED, hbox `20260906T120811Z`: `activate Direct capability (first capability root)`, 494,881 CU, slot 8095** —
  the first Direct execution root this tier has created on any validator. The stage is the spine's first act, in
  devnet's own order (`steps.tsv`: found-direct, activate-direct, arm-relay) and with devnet's own key, and it is
  idempotent, so a resumed run converges.
- **(c) THE SUBMIT'S `0x8004` NAMES ITS RECORD NOW, before it costs a transaction.**
  `ResolutionError::FinalizedRecord` is one code over eleven disjuncts and six record pairs, and the operator's own
  client-side `authenticate_raw` tests four of the eleven — so a submit that BUILDS and then refuses says nothing.
  The probe takes no account indices and no record widths, because the program's criterion needs neither: a finalized
  raw record lives at the hash of its own body, so an account whose bytes reproduce its own address under one of the
  six schemas the frame carries is self-consistent, and the cursor paired with that (schema, digest) must be
  System-owned, zero-lamport and zero-byte. **Two of the six pairs are already cleared by hand against run 6's live
  validator**: the registry `ArtifactReleaseV1` (`2fYRncpn…`, 216 bytes, Registry-owned, cursor ABSENT) and
  `PythReleaseV1` (`2yNAT2km…`, 440 bytes, cursor ABSENT) — and the plan's `registry_artifact_release` content digest
  `616c2f91…` is the same one both infrastructure profiles record, so the artifact DISAGREEMENT hypothesis is dead and
  the four founding-published source records are what remain.
- **A recovery that fails now costs one stage, never the walk.** The first version of the table recovery propagated
  its refusal and ended hbox `20260906T120811Z` with a hard error AFTER the founding, the distribution, the ring and
  that first activation, and BEFORE the four stages the run existed to reach. It also reported "absent from finalized
  history" for a transaction the founding's own evidence records at slot 7338 with a null error — a claim about the
  chain it had not measured, because it read `getTransaction`'s JSON view instead of the base64 packet `confirm` has
  always read. Both are fixed: the create is selected from the DECODED message by its program id (never
  `instructions[0]`, which is a ComputeBudget declaration `bounded_instructions` prepends), a shape the reader cannot
  use is reported with the result's own keys, and the journey catches the refusal and compiles the admission bare.
- **The ladder's red is named where a reader of the red will be.** The shelf-life finding was in the tier's register
  and the witness was not; both halves now carry it, including the provenance text a failing row prints.
  `every-crank-frame-is-the-relay-contract-eighteen` reads *expected 18, chain says null* because no AdvanceRecovery
  frame is ever built: hbox `20260906T112408Z`, the primary leg due at unix 1,818,967,680 against a chain clock of
  1,788,694,735 — 30,272,945 seconds, the fixture's one-year shelf life to the second. A field cannot fix a fixture.
- **Run 6, hbox `20260906T120811Z`**: substrate EXECUTED, founding through Open **198 tx / 6,115,480 CU**,
  collateral distribution **4 tx / 14,624 CU**, holder ring **4 tx / 8,080 CU**, conservation verdict `conserved`,
  then the hard error above. `tools/gate journey` **PASS 98 s** at `c5ddf118c`.
- **Owed.** The journey has still not reached Retired: the redemption needs the terminal receipt the Pyth leg has not
  minted, and `BeginRetiring` admits only `Phase::Terminal`, so `BeginRetiring`'s two authors stay unconverged and
  `bindings.json` stays unauthored past the ring for the reason it always has — bindings are folded from what the
  ledger OBSERVED. The Pyth leg is the whole critical path now, and the next run reads the probe's verdict.
- Shas: `dca6eb7b1` (the measurement, the table, the activation stage), `c9cd82b2e` (the record probe), `745e7e773`
  (the ladder register), `0985e0f05`, `59964961e`, `c5ddf118c` (the packet read, non-fatal).
- COHORT-17B (addendum to docs/evidence/COHORT17_SEATED_FILLED_RETIRING_2026_09_06.md): **the ruling executed and the
  wall moved one stage.** The zero-key conjunct is deleted (`40f1cb703`), red-proven both ways over four builds -- a
  frame naming the System Program at any of 57 indices passes, a coordinate zeroed after the digest still refuses
  through it. **A second host defect stood behind it** (`d7d09500e`): the freeze-to-seal interval was read off
  `evidence.mutations` with ACTION ordinals, one row short of the two setup mutations, so cohort-17's activation at slot
  493,995,464 was compared to a `lookup-extend` and the freeze instead of the freeze and the seal. Host only; the
  candidate commit and the release gate did not move. The runbook gains `close-maker` (since 17, no `blocks` edge: it
  runs BETWEEN two passes of `retire`, because a close runs inside Retiring). **BOTH MAKER REPLAY ROOTS ARE CLOSED** --
  `771oHXjD…` and `5NJTCZ3z…`, 98,248 CU each, 1,463,040 lamports each to the beneficiary the replays name, and the
  Direct root reads `open_maker_root_count = 0`. **The new wall is stage four again**: `Direct close caller preflight:
  Capability(InvalidLength)`, because stage THREE (`ResolutionCloseFund`, `3L6ULNQN…`, finalized) CLOSED the Resolution
  dependency funding ledger `GqyjjFmG…` that stage four decodes and preserves -- the second fact `tools/cohort/README.md`
  had already named as a question, now measured with both stages executed. Not repairable on this market (the ledger is
  gone) and not by this lane (every candidate repair moves `dclutch-operator`, which is in four SBF links, or an order
  the market already ran). **No market has been retired on any chain**; the featured market is unchanged. General:
  `InvalidPlan`'s eight sites are **two conjuncts** by reading alone -- five belong to other actions, the block between
  them returns `InvalidCoordinate` so its passage is evidence, and site 755 is excluded by the composite root's own
  bytes (`DCGROT02` at offset 232, exactly 128 bytes, revision 1, sequence 0). The frames debt `108f950f` is over a
  `#[cfg(test)]` module: the shipped link cannot have moved, the ratchet is file-granular and stays red. Deployer
  26.572399090 unchanged; payer 2.004939706 -> 2.004789706, two 75,000-lamport fees and nothing else.
- COHORT-17B closed (`40f1cb703`, `d7d09500e`, `b7e069828`): the zero-key conjunct deleted, red-proven both ways over four
  builds; a second host defect behind it (mutation indices offset by the two setup mutations); the `close-maker` row (no
  `blocks` edge — it runs between two passes of `retire`); **both maker roots closed on devnet permissionlessly (98,248 CU
  each; `open_maker_root_count` 0)**; **then stage four refused `Capability(InvalidLength)`: stage three `ResolutionCloseFund`
  had CLOSED the Resolution dependency funding ledger that stage four `DirectCloseCapability` decodes to preserve — the
  open question the `retire` row named, now measured with both stages executed; not repairable on this market (the ledger
  is gone; recreating it would fabricate protocol state)**; `9e8fTH75…` stands at Retiring, `outstanding_capabilities` 1,
  Hoard 0. The General `InvalidPlan`: eight sites are two conjuncts by reading (`expected_revision` vs two different
  scalars, or the batch identity) — the replay names it. The frames debt `108f950f` is a `#[cfg(test)]` module (bytes
  cannot move; the ratchet is file-granular). RULING (orchestrator): **`DirectCloseCapability` runs BEFORE
  `ResolutionCloseFund` — the close preserves the dependency it names, its owner closes it after; one author for the
  order.** → PROGRAMS-18A **`(spawned)`**; cohort-18 founds the market that retires.

### 2026-09-06 09:00 EDT — JOURNEY-6 addendum: the activation lands, the half-pair wall is gone, and the packet is measured on chain
- **THE PACKET, ON CHAIN.** hbox `20260906T122838Z`, from the admission driver's own sized refusal:
  `PacketTooLarge -- 1443 wire bytes over the 1232 packet limit (211 too many) with 0 routing table(s) declared;
  24 of this route's addresses can be carried by a table, at about 31 bytes each`. The host measurement predicted
  1,477 with the two rent transfers the `--execute` path pays separately, so **the two readings agree to within ten
  bytes** and the question "is this keys" is closed. 24 carriable at ~31 bytes each is ~744 bytes of headroom against
  a 211-byte overrun.
- **THE DIRECT CAPABILITY ROOT EXISTS.** `trading: the Direct capability root is activated` EXECUTED: verdict
  **ACTIVATED**, slot 8081, **505,381 CU**, a **256-byte DCLTCRT1 root holding 2,672,640 lamports** where the
  coordinate had been `AccountNotFound` — the shape cohort-17's `activate-direct` row states, on a validator, for the
  first time.
- **AND THE TERMINAL SEQUENCE'S HALF-PAIR WALL IS GONE.** `retirement: the founding's evidence is refreshed against
  the live chain` EXECUTED and the terminal sequence no longer says "it carries `direct_trading_funding_ledger` and
  omits `direct_capability_root`". It refuses one wall further on, honestly: *terminal ALT projection requires the
  exact terminal/retiring founding Market generation* — the Market is not Terminal, because the Pyth leg has not run.
  Wall (e) is retired and what stands behind it is wall (c).
- **THE FULL WALK, hbox `20260906T122838Z`**: substrate, founding through Open **198 tx / 6,201,106 CU**, collateral
  distribution **4 tx / 14,624 CU**, holder ring **4 tx / 8,080 CU**, the Direct activation, rent recovery
  **2 tx / 7,517 CU**, ten conservation censuses, verdict `conserved`, every remaining stage named. Four refusals
  stand where five did.
- **(c) NARROWED TO THE CHAIN'S OWN WORD.** The record probe ran to the end of the frame without finding a live
  staging cursor, so **all six pairs the submit names are self-consistent finalized records with vacant cursors**.
  With the widths read by hand out of the founding's own evidence (`source_material` 240, `source_spec` 192,
  `provider_release` 176, `window_spec` 112) and the two infrastructure records checked against run 6's live chain
  (`ArtifactReleaseV1` 216, `PythReleaseV1` 440, both Registry-owned, both cursors absent), and the artifact digest
  `616c2f91…` agreeing with BOTH on-chain infrastructure profiles, every one of `authenticate_record`'s eleven
  disjuncts is accounted for. The probe also read the activation cache's 1,288 registry-owned bytes as "reproduces no
  address under any schema this frame carries" — true, and not a finding: the frame carries Registry-owned accounts
  that are not records. It reports now instead of refusing, so the chain's own code is read beside it.
- **THE READ-BACK CANNOT WORK ON THIS SUBSTRATE, and that is a fact about the loopback validator.** `getTransaction`
  answered **a null result** for the founding's own `CreateLookupTable` at a slot its evidence records with a null
  error; it does not serve a signature ~750 slots old. The table's address is a FUNCTION of the founding's payer and
  the recent slot it was given, so it is derived over a 192-slot window anchored on the create's own recorded slot and
  then authenticated. Exactly one candidate may answer, and the recovery says so if two do: this founding creates five
  frozen tables and **three of them route its market** (56, 45 and 66 addresses, measured on run 6), so "frozen and
  routes this market" identifies nothing on its own.
- **A HARVEST THAT UNDERSTATED BY FIFTEEN TIMES, silently.** The activation stage reported 33,015 CU — the four
  routing-table transactions — and omitted its own 505,381 CU act, because `signatures_in` enumerated three key names
  and the driver reports under `activationSignature`. The rule is now `signature` or any `*Signature`. The test's
  negative case was `notASignature`, chosen against the exact-match rule; it is replaced by a key that names no
  signature, and the missed case is pinned as a positive.
- **Still owed, and unchanged in kind**: the journey has not reached Retired. `BeginRetiring` admits only
  `Phase::Terminal`, so its two authors stay unconverged and `bindings.json` stays unauthored past the ring —
  bindings are folded from what the ledger OBSERVED. **The Pyth submit is the whole critical path.** And the terminal
  sequence's stage-four `Capability(InvalidLength)` is convicted ahead of the journey by cohort-17: `ORDERED` closes
  at stage three the Resolution dependency ledger `DirectCloseCapability` decodes at stage four, PROGRAMS-18A owns the
  order, and this tier now prints that conviction beside the driver's own sentence rather than reading it as new.
- Shas: `4a2a12813` (the derivation), `97ed9cbff` (the probe reports), `a930bb4a5` (the harvest), `c856d0e72`
  (the stage-four conviction). `tools/gate journey` PASS 98 s.

### 2026-09-06 09:10 EDT — PROGRAMS-18A: the preserver runs first, the order gets one author, and no ELF moves
- **The ruling is executed.** `TerminalStageV1::ORDERED` is now CoreBeginRetiring, DirectBeginRetiring,
  **DirectCloseCapability, ResolutionCloseFund**, RetirementReplayHandoff, AggregateRetirement. Core `CloseCapability`
  decodes BOTH physical funding ledgers and PRESERVES the Resolution dependency one; `ResolutionCloseFund` closes it.
  The stage that preserves a dependency runs before the stage that owns it, and a `const _: () = assert!(..)` holds
  that with its reason — writing the shipped order back does not compile, measured.
- **The "two authors" were one author and three silent copies, all in one file.** The declaration lived at
  `terminal_sequence.rs:407` beside every consumer: the `#[cfg(test)]` router, the completion verifier's
  `protocol_order` array and that verifier's fixture. The `dclutch-svm-harness` retirement campaign is not a second
  author at all — it drives `AggregateRetirement` ONLY, against a prestate seeded with `outstanding_capabilities = 0`
  and a `SourceClosureReceiptV3` written into the account rather than produced by `ResolutionCloseFund`. It agreed
  with the wrong order because it could not see it. The order now lives in
  `crates/dclutch-market-retirement-v1-operator/src/terminal_stage_order_v1.rs`, the operator crate the driver and the
  campaign BOTH link, and the three copies are derived from it.
- **NO PROGRAM ENFORCES THE ORDER, and the frames answer is NO.** Core admits `CloseCapability` in Open, Terminal and
  Retiring alike; Resolution admits `CloseFund` in exactly `(Retiring, Consumed)`; the only ordering fact either can
  see is `retire`'s `outstanding_capabilities == 0`, which orders the Direct close before the aggregate and says
  nothing about `CloseFund`. **COHORT-17B's "compiled into the Claims, Core, Custody and Registry links" is WRONG**:
  `dclutch-operator` is a `[dev-dependencies]` entry of those four and appears in NO link's path-dependency closure
  (`tools/gates/frames.py:309` drops dev edges; the union of all eight closures is twelve crates and no operator).
  The repair is host-only: no ELF, no frame baseline row, no release digest.
- **Three hostiles, each red-proven, each naming the destroyed input.** A journal directory carrying
  `ResolutionCloseFund` with no Direct close in front of it (cohort-17's own) refuses at the driver's prefix check by
  name, not as a generic hole. The router meeting a Retiring Direct root over a closed Resolution fund refuses the
  same way, with the fund-still-open control routing forward. And a Direct close whose frame carries a VACATED
  funding ledger refuses `TerminalRetirementErrorV1::ClosedFundingLedger` on both entry points and at both ledger
  positions — where cohort-17 got `Capability(InvalidLength)` and had to convict it from a journal read and two chain
  reads. Controls: a live pair cannot reach the accusation, and a short-but-funded ledger is still a record defect.
- **The retirement on real ELFs, hbox under `swarm-build`, seven ELFs at ZERO stack-frame-overwrite diagnostics
  each**; 3 campaign tests, 8 witnesses, 0 failed. Categorical → **Retired**: prepare 139,884, close-vault 126,725,
  close-replay 117,503, finish 102,967 CU. Refunding → **Retired**: prepare 148,544, close-vault 124,440,
  close-replay 110,718, finish 96,182 CU. Five of seven ELFs are byte-identical to PROGRAMS-17E's build on the same
  host across several intervening commits; the two that differ are those commits and not this one.
- **Four sibling tests in that binary fail, and they are NOT this lane's.** The Pyth-provider walks die at
  `solana-program-runtime-4.3.0-beta.2/src/loaded_programs.rs:455` "Unexpected replacement of an entry" — the exact
  panic `crates/dclutch-svm-harness/Cargo.toml` documents. The positive control is PROGRAMS-17E's tree on the same
  host, at an earlier commit with none of this work: same test, same panic site, same 10.8 s.
- **Owed.** The ruled pair is still uncovered off devnet — a fixture with a live Direct capability child and both
  physical funding ledgers has never existed; the campaign now asserts that boundary in its own words rather than
  reading as coverage it does not have. `tools/gauntlet/journey/src/spine.rs` still prints the old order in two prose
  strings and is JOURNEY-6's file this cycle. **Cohort-17's `9e8fTH75…` stays unretirable**: its `GqyjjFmG…` is gone
  and recreating it would fabricate protocol state. **Cohort-18 carries the reorder as a HOST change** — same ELFs,
  same release gate, a driver rebuild.
- Sha: `ea9174135`. Suites: `dclutch-market-retirement-v1-operator --lib` 6/6, `dclutch-operator --lib` 358/358,
  `dclutch-local-successor-bootstrap` 820/820 (1 ignored), retirement-checkpoint 3/3 + 8 witnesses. No devnet act.
- PROGRAMS-18A closed (`ea9174135`, `bb0578e31`): **the order has ONE declaration** — `terminal_stage_order_v1.rs` in the
  retirement operator crate (linked by the driver and the harness, by no program): DirectCloseCapability BEFORE
  ResolutionCloseFund, held by a compile-time assertion whose panic text is the invariant; the driver's three silent
  copies (router, completion verifier, its fixture) derive from it; **the second author never existed — the harness's
  retirement drove only the four checkpoints against a seeded prestate with `outstanding_capabilities = 0`, so it agreed
  with the wrong order because it could not see it** (the campaign now asserts that boundary itself); no program
  enforces an order (Core admits the close in Open|Terminal|Retiring; Resolution admits CloseFund in Retiring/Consumed);
  the operator is a dev-dependency of four programs and in no link's closure (COHORT-17B's claim settled against it);
  hostiles: a journal with CloseFund and no Direct close names the destroyed input; a vacated ledger refuses
  `ClosedFundingLedger` where it was `InvalidLength`; the walk on real ELFs: categorical and refunding → Retired (prepare
  139,884 / 148,544 CU …). **The reorder is host-only: same ELFs, same gate, a driver rebuild.** Resolution owns those
  lamports and takes them last (the README's open question closed). → COHORT-17C **`(spawned)`**: a new refunding market
  on cohort-17's deploy, retired in the ruled order — the first on any chain.
