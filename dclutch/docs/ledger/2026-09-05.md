# Ledger — 2026-09-05

Dated deltas for 2026-09-05; the index is GOAL.md.

### 2026-09-05 11:00 EDT — THE CONVERGENCE CLOSED; THE REPAIR WAVE
- CONVERGE-2 closed: **final cut dragons-clutch `4606168b0` = dClutch `f6b127fdc`**; evidence
  docs/evidence/SIMPLIFICATION_CONVERGENCE_2026_09_04.md. Counts by the map's recipe, 330bbfaba → f6b127fdc: programs
  308,362 → 277,403; crates 534,000 → 515,896; formal 73,280 → 66,967; tools 352,059 → 315,551; apps 105,671 → 59,114;
  packages 75,958 → 74,287; docs 115,341 → 133,580 (the ledger absorbed the narrative); **the seven together 1,579,029 →
  1,445,994 (−133,035, −8.4%)**; programs 12 → 8, crates 94 → 33, GOAL.md 4,992 → 106. **Every one of the eight ELFs moved**
  (renames reach `.strtab`; the macro, the split, the fold and the layout reach every `.text`) — cohort-16 carries every
  redeploy; frames function by function: 1,322 unchanged, 312 renamed same frame, ONE grew (Claims' `process_generic_plan`
  1,344 → 1,728, taking the deleted Core-effect dispatch), five shrank, 209 deleted whole. Gates green: cheap 8/9,
  reference, sbom, guards, abi, frames, clippy (four never-reached crates reached and fixed), root-targets (three repairs),
  tier 1 and General hot on hbox, hot-cu 20/20 (floor 91,593 → 91,039, zero jitter). Red by column: `commands`
  (dclutch-terminal); web 4 tests; journey (the ladder vs the journey target; fractional-exterior); suites 11/15 — core's
  five Series tests (red at the base), dealer (the fold), general (the merged Product reader), claims-lifecycle (no
  control). The base ELF digests were lost with /private/tmp in the fault (rebuildable at 330bbfaba). The `twins` tier
  deleted with its instrument. The wrapper's workflows had called `tools/gate` since c21451344.
- What the map priced and nobody executed: the 55 workspaces / 71 lockfiles (272k lines) — the one-workspace merge.
- Started 11:05 EDT: SUITES-REPAIR (dealer, general, claims-lifecycle, core's Series tests, journey's two workspaces),
  WEB-REDS (the four web tests and the `commands` row), WORKSPACE-ONE (one Cargo workspace, one lock).

### 2026-09-05 12:05 EDT — WEB-REDS closed: four reds, each at its author (a29496337, 1761b7c26)
- **The selected gate was never missing.** The census enumerates both rows at HEAD and always did (inventory at
  23fda4edf: `direct-root: Open` and `series-ticket: Prepared` on `trading/hot_v3::process_hot_execution_v3`). It writes
  `selected_by` as it walks the AST, so the trading split moved both names one module segment — `hot_v3::direct::` and
  `hot_v3::series_expiry::` — while the SDK's hand-pinned `HOT_FAMILY_CLASSIFIERS_V1` still typed the pre-split path. An
  unbound classifier is an error nowhere in the model: the gate is answered for nobody, so `direct.inline` went back to
  reporting no gate at all. Names follow the census; the pin that should have caught it read one fixed file (`hot_v3.rs`)
  and now resolves the classifier's own module path.
- **`DCLTRIX1` is an instruction magic, and the survey had only the declaration to classify by.** The constants table
  carries a kind column now (`record` | `instruction`, per row, emitted rather than inferred from the export name); both
  generators publish `INSTRUCTION_MAGIC_EXPORTS_V1`; `explorer-coverage.mjs` reads it and holds every excluded constant to
  a census instruction magic, so a record marked as an instruction is visible rather than dropped from both surveys.
  Its magic pattern also demanded a single quote, which hid `relayTransportV1`'s magic from it — closed.
- **`tradeFlowRefusals` typed a sentence of the SDK's wall** ("public page" for "public client"): the web's twin of
  `directTradeSpine.ts` said it that way. Derived from the wall now — the tail past the fragment the table matched.
- **`dclutch-terminal --help` named no verb in a fresh checkout**, so eighteen published commands were rejected by their
  own program. The command list and flag table move to `packages/dclutch-cli/src/usage.ts` — imports nothing, erasable
  syntax — and the launcher loads it directly when `dist/` is absent, printing the page the bundle would. Controlled by
  running the launcher in a scratch tree with no `dist/`, against the client's own `USAGE`.
- Two the tier surfaced: `tools/gate commands` held `run-ladder.sh --help` to the `--checked-release-gate` its usage line
  names bare (a command that asks for usage cannot be incomplete — exempted, with a self-test control), and
  `run-ladder.sh` never named its own `-h|--help`. `tools/gate web` ran `deploymentLiveness.live.test.ts` against both
  client trees and read the web's "no test files found" as a dead cohort; the SDK owns that question since `dcaba4770`,
  and the tier now asks the carriers, names the tree that carries none, and FAILS if no tree carries it.
- Gates at 1761b7c26: `web` PASS, `commands` PASS (0 defects in a fresh worktree too), `selftest` PASS, `emission` PASS,
  `reference --check --converge` at its fixpoint. web vitest 1,057/29/0, SDK 892/20/0, CLI 131/1/0; web+SDK tsc clean,
  lint 0 errors, a11y 0.
- **Not this lane's, named**: `guards` is FAILED at HEAD (71 ran, 1 failed) — `tools/direct-translation-validator/check.sh`
  passes its translation validation (91 Lean ABI values, 7,280 mutations, …) and then fails clippy over `dclutch-trading`:
  66 errors, never-used constants in `src/general_config/generated.rs` and `indexing_slicing` sites, all arriving with
  `d0219d449` (the crates merge). The crates column. And `packages/dclutch-cli/src/commands/walk.ts` does not typecheck at
  HEAD: its `BOOK_FIELDS` say `failureFunding` where the SDK's `FailureWalkBookV1` says `resolutionFunding`. The rename is
  the Resolution role's (`crates/dclutch-source/src/relay/frame.rs` names the old spelling) and
  `tools/local-validator/bootstrap/successor/README.md` still publishes `"failureFunding"` as the book key, so which name
  the operator's book carries is that column's call.
- WEB-REDS closed (`a29496337`, reference `1761b7c26`, ledger `92090237d`): **the census had never lost the selected gates —
  the trading split moved two classifier paths one module segment and the SDK's hand-pinned `HOT_FAMILY_CLASSIFIERS_V1`
  still typed the old names; an unbound classifier was an error nowhere** (the pin now resolves the classifier's own module
  path); the protocol-constants table gains a `record | instruction` kind column the explorer survey reads (a double-quoted
  magic had been invisible to the survey); `tradeFlowRefusals` derives its wall from the SDK's registry; the CLI launcher
  answers `--help` from source when `dist/` is absent (0 defects in a fresh worktree); `tools/gate web` PASS, commands PASS,
  emission 91/91. Standing reds routed to MERGE-REDS: `tools/gate guards` fails clippy over the merged `dclutch-trading`
  (66 errors from d0219d449); `walk.ts` does not typecheck on a book-field name with three spellings.

- WORKSPACE-ONE closed (`01db9fdf1` the fold, `ce50bbaa4` the ratchet): **55 Cargo workspaces and 61 lockfiles become 1
  and 1 — 246,594 lines of `Cargo.lock` become 13,165 — and all 8 SBF links are byte-identical by `sha256` to a clean
  export of the parent commit.** The map (§1.5/§4 item 7) priced this as the biggest single deletion in the tree and
  left the reason to whoever executed it.
  - **The census answer is that there was no reason.** No `[patch]` section exists anywhere in the tree. 53 of the 54
    nested `[workspace]` tables were an empty marker, and each manifest wrote the same sentence: *"adding this crate to
    the root `members` list is an integration decision this crate does not get to make for everyone else."* Fifty-four
    deferrals of one decision, not one dependency conflict. **One conflict class, not two**: the host/SBF split the
    brief allowed for does not exist — `solana-program-test` and the eight SBF program crates unify.
  - **What the deferral cost.** Independent resolution let the exact pins drift, and the drift then read as the conflict
    that justified the separation. Eleven requirements lagged the root's *on the same major*: `solana-program-test`
    `=4.2.1` vs `=4.3.0-beta.2` (8 manifests), `solana-account` `=4.3.2` vs `=4.6.0` (7), `solana-transaction` (9),
    `solana-message` (7), `solana-address-lookup-table-interface` (6), `serde` `=1.0.228` vs `=1.0.229` (6), plus four
    more. `crates/dclutch-direct-ticket/Cargo.toml` documented that drift as a permanent fact of the tree and shaped a
    dependency requirement around it.
  - **Why no byte moved.** The authority for every rewrite was the ROOT lock: a nested pin was raised to the version the
    root already resolved in the same semver-compat class, never lowered. The one lock is 855 packages where the root's
    was 713 — **zero removed, zero whose old version disappeared**, every addition an older major joining beside the one
    the programs compile against. Positive control: the same build of a probe tree with one `usize` changed in rent-sbf
    moved that link's sha. Frame diagnostics zero.
  - **Six orphaned lockfiles (19,800 lines)** belonged to packages already members of a parent workspace, so cargo never
    read them — and `tools/gate locks` was re-resolving their parent while appearing to check them.
  - **The fold reveals the `#[path]` tripwire and breaks nothing.** `cargo check --workspace --all-targets` now reaches
    the three gauntlet campaigns and reds: `successor/src/local_mutable.rs` calls a 9-argument function with 8, and
    journey's `provider.rs` misses a field. **Control run, not assumed**: the identical errors reproduce at HEAD's
    topology inside each campaign's own workspace, and the root's own two reds (`wallet-terminal-{input,payout}-wasm`
    lib tests) reproduce there too. Zero new errors; five commands became one. **Owner: the successor's column.**
  - **Three gates needed the fold to keep measuring.** `journey`'s tripwire found tool workspaces *by their `[workspace]`
    table*; with none left it would have matched nothing and passed on an empty set — it now enumerates `tools/`
    PACKAGES from `cargo metadata` and refuses if there are none. `fmt` reached only the root's 41 members, so it had
    never looked at a program-test suite, a gauntlet campaign or the successor: **126 unformatted files**, every one a
    dated baseline row (a handoff — SUITES-REPAIR and WEB-REDS are live there). `fmt` also had to learn that a `#[path]`
    link makes rustfmt name one file under four spellings; paths are normalised before comparison.
  - **Swept in the same commit**: one workspace is one `target/`, so the TS CLI's successor-binary search,
    `run-lineage-loopback.sh`, `run-local.sh`, `stage-devnet-sponsored-market-open.sh`, the load-simulator template, the
    fractional-atomic hint and the Rust CLI README all resolve `${CARGO_TARGET_DIR:-<root>/target}` and build by package
    name. AGENTS.md's nested-`target/` rule is replaced by: a new `[workspace]` table is a claim that some dependency
    cannot be unified, and must name it.
  - **The ratchet was green for twenty-six commits and the ledger said otherwise.** `frames owed` named five debtors
    (four CONVERGE-2 commits and the fold). None had moved a frame: two independent captures of `01db9fdf1` on one host
    are identical to each other *and* to the rows admitted at `018ea525f`, so the baseline's commit field advances and
    nobody's unmeasured movement is admitted under anyone's name. `owed` is empty. The accusation was structural —
    `owed` reads which FILES a commit touched against a link's closure, the right cheap proxy for who should measure and
    a wrong answer about who moved anything.
  - **The license closure lost 144 unique package versions** (`19578626e`): the SBOM goes from 58 manifests and 1,193
    cargo rows to 4 and 1,049. Those 144 are what the 55 workspaces cost in substance, not in a report — a nested lock
    pinning `solana-program-test` `=4.2.1` against a root at `=4.3.0-beta.2` dragged its own copy of the agave stack
    into the tree under a second set of versions, and the tree compiled both. `sbom_check.py` still DISCOVERS workspace
    roots rather than assuming one, so a re-introduced workspace is covered without an edit.
  - Gates at `01db9fdf1`: `cheap` all nine PASS (`locks` reports **1** lockfile where it reported 61), `frames` PASS.
  - **Owed, named**: `tools/fractional-exterior` still pins `solana-sdk`/`solana-client` `=2.1.0` and is the sole reason
    ~90 packages of an obsolete Solana major sit in the one lock — a source change, not a manifest one. The 126 `fmt`
    rows. The three campaign reds. `bundle-builder` and `direct-hot` can finally inherit `[workspace.lints]`: the
    structural reason is gone and only the `clippy-debt.tsv` ordering remains. `tools/gate workspaces` (the cut tier)
    now checks one archived workspace; its docstring says so, but whether an archive-and-recheck still earns its keep
    over `cargo check --workspace --locked` is a question for the gate column.

### 2026-09-05 11:30 EDT — SUITES-REPAIR: four of the five reds convicted at their authors; the fifth is a coin flip
- **`dealer`** green, 2 of 2 on five draws (was 1 of 2). The fold (`5b4fe2313`, merged `3bee5f3f1`) made the accelerator's
  arm a function of the top-level Hot envelope's family magic; `geometry_complete_fixture` still carried a 4-byte
  placeholder, so the frame was General's and refused `HeapFrameNotRequested` 0xC006 before any Dealer conjunct. The
  frame now names `DEALER_EQUITY_REQUEST_MAGIC_V3` and the assertion is unchanged — 0xC101 at CPI depth two. The same
  fold deleted `tests/accepted.rs` (the 31-test accepted campaign, with the checkpoint routes it drove) and its
  `dealer_chain`/`custody_delivery` drivers; the runner had gone on building **four SBF links nothing reads** and now
  builds the two `physical.rs` loads. `fb36992d7`.
- **`general`** green, 29 of 29 on five draws. The convergence evidence attributes this to the crates merge and its
  merged Product `svm_reader`; **both are wrong** — that reader is not on this path and the test still passes at
  `73499583b`. The author is `0c9811bc6` (the OPERATORS branch, reaching HEAD through `bee9db34c`): `map_err(|_| Product)`
  became `map_err(ProductGraphObservation)`, and the accelerator program-test's own `[workspace]` kept any root check
  from compiling it. `fb36992d7`.
- **`core`** green, 20 passed / 0 failed / 2 ignored on five draws (was 15/5). All five Series rows died at ONE conjunct:
  `2cf96117a` (SERIES-4) ruled the root's `selection().config()` is the Registry RECORD DIGEST `hash(t)`, not the
  domain-separated `template_content_id(t)` that names a coordinate where no record can exist. It swept the pre-market
  expiry test and never reached `found_program_test.rs`, whose root header had carried `template_id` since `b561326b9`.
  Not the Custody coordinate, and no program change owed. `fb36992d7`.
- **`journey`** — the campaign's target compiles and its host tests are 354 passed / 0 failed / 1 ignored (was: does not
  compile). That build is the **only** one in the tree that compiles nineteen successor modules with `cfg(test)`, so four
  things had gone stale unseen: RECOVERY-3's `recovery_rungs` (`6a3079454`) broke the successor's own two callers,
  `beca9243e`'s `recovery_ladder` never reached the journey's `provider.rs`, `ef7e1c868` moved the readiness planner and
  left a string literal pointing at a deleted directory, and the fold made `SHIPPED_LINKS` eight against two literal
  `12`s. `657420d69`. `tools/fractional-exterior` is **stated, not fixed** (manifests are WORKSPACE-ONE's): verified
  green in scratch on the 3.0 line — `solana-client "=3.1.12"` plus `solana-commitment-config`, `solana-message`,
  `solana-compute-budget-interface` and `solana-address-lookup-table-interface` at the versions `dclutch-operator`
  already pins, with three import moves in `validator.rs`/`claim_check.rs`; the exact diff is in `657420d69`'s message.
- **`claims-lifecycle` is NONDETERMINISTIC** — the same binary against the same ELF passes **4 of 8** draws at HEAD and
  **3 of 8** at `330bbfaba`. It is not the convergence's (the control the convergence could not run now runs, eight
  times), and `d6c4dea63` landed it flaky on 2026-08-27 without stating the suite's verdict. When it fails the refusal is
  stable: `RationalLifecycleSbfErrorV2::Token` 0x5216 at 79,632 CU, no Token-2022 invocation. **`fefef8149` concluded from
  single runs that the refusal was ELF-layout-dependent; `b60a24548` withdraws that** — every one-toss row is void. The
  first suspect is ProgramTest's per-run random payer keypair. Evidence
  `docs/evidence/CLAIMS_LIFECYCLE_LAYOUT_WALL_2026_09_05.md` **and its addendum**.
- **The consequence nobody had priced**: `tools/gate suites` runs each row ONCE, so the `claims-lifecycle` tier verdict —
  and the convergence's "11 of 15" — is a coin flip. Owed to the gate column.
- Measured on hbox under `swarm-build` from clean `git archive` exports (Linux x86_64, cargo-build-sbf 4.0.0,
  platform-tools v1.53, rustc 1.89.0), every row run alone with `--test-threads=1`. No crate compiled into an SBF link
  moves in any of these commits; `tools/gate frames owed` names none of them.
- SUITES-REPAIR closed (`fb36992d7`, `657420d69`, `fefef8149`, `b60a24548`, `44af48336`; hbox, clean exports, five draws
  each): dealer green (the fixture carried a four-byte placeholder from when the ELF WAS the Dealer accelerator; the
  `accepted.rs` campaign the brief named no longer exists — the fold deleted it with its routes, and the runner had gone
  on building four links nothing reads); **general green — the convergence evidence was wrong on both counts** (the merged
  Product reader is not on the path; the author was an operators `map_err`); core 20/0/2 — all five Series tests died at
  ONE conjunct, SERIES-4's record-digest ruling sweeping the pre-market expiry test (**not the Custody coordinate; no
  program change owed**); journey 354/0/1 (the only build compiling nineteen successor modules under cfg(test) — four
  things had gone stale unseen); **claims-lifecycle NONDETERMINISTIC since d6c4dea63 (08-27): 4 of 8 draws at HEAD, 3 of 8
  at the base, `Token 0x5216` with no Token-2022 invocation** — a single-run inference withdrawn by a dated addendum; and
  **`tools/gate suites` runs each row once, so a flaky suite's verdict is a coin flip** → SUITES-2 (the payer keypair first;
  draws in the tier; the two coarse codes split; fractional-exterior to the 3.0 line).

### 2026-09-05 11:50 EDT — MERGE-REDS closed: WEB-REDS' two standing reds, each at its author (9fe4506f2, 311797cc3, 16e79dfaf)
- **`guards` green** (71 ran, 0 failed), the 66 clippy errors in `dclutch-trading` gone. Two commits had to meet for
  `tools/direct-translation-validator/check.sh` to see them: `d0219d449` absorbed the Dealer codec — which had held itself
  to `clippy::all` alone, its own edition and its own lint table — into a crate under the workspace deny table, and
  `01db9fdf1` dissolved the validator's own `[workspace]`, so `dclutch-trading` became a member of the workspace clippy
  wraps and check.sh's `-D warnings` finally reached it. Two authors, two fixes.
- **The 21 never-used constants were the General V2 activation block, and the fix is at the emitter.** Its reader was
  `crates/dclutch-general-config-contract/src/activation.rs`, which `aef29f74c` deleted as superseded by
  general-adapter-contract's `activation_bundle_v1` (`DCGNACT1`); the emitted mirror outlived it, and so did the
  `#[cfg(test)]` example nothing had ever read. `EmitGeneralConfigAbiRust.lean` stops emitting the width, action, magic,
  the eighteen offsets and the example; `general_config/generated.rs` is that emitter's re-emitted output and its
  byte-identity guard compares it. No `#[allow]`. `GeneralConfigAbi.lean` keeps the activation layout and its four
  theorems — the Lean spec is not what lost a reader, and a spec with no Rust mirror is not a defect.
- **The 45 `indexing_slicing` sites were the Dealer's per-outcome arrays**, and the crate already owned the checked family
  (`checked_slice`, `byte_at`, `u64_at`, `u64_array_at`, `put`). Added `outcome_slot` / `outcome_slot_mut` for the
  request-driven reads and writes; the rest became zipped iterator walks, total by construction rather than by an
  unreachable refusal. The accessors refuse with `Error::InvalidCurve`, the same accusation the adjacent
  `outcome >= policy.outcome_count` conjunct already makes, since Policy validation bounds `outcome_count` by
  `MAX_OUTCOMES`. **Two latent panics went with them**: `activate` and `root_tail::project` both looped
  `0..outcome_count` over `[u64; MAX_OUTCOMES]`, so a policy that escaped validation would have panicked, not refused.
- **`web` green and the CLI typechecks; the book's one author is the Rust frame, and the name is `resolutionFunding`.**
  The brief had it backwards: `crates/dclutch-source/src/relay/frame.rs` names the slot
  `RelayAccountNameV1::ResolutionFunding`, with the rename argued in its own doc comment (the account has always held all
  three Resolution compartments, so `FailureFunding` named a debit rather than an account class). That name is **emitted,
  not mirrored by hand** — `packages/dclutch-sdk/lib/generated/relayTransportV1.ts` carries it from
  `npm run abi:relay-transport`, verified by `abi:relay-transport:verify` in `tools/gate abi`. The SDK type and
  `tools/relayer/src/keeper.rs`'s book writer had both followed; only the CLI's hand-kept `BOOK_FIELDS` and its test
  literal had not.
- **The successor README is a DIFFERENT book, and it was right.** `tools/local-validator/bootstrap/successor/README.md`
  publishes `"failureFunding"` for the sponsored-push address book, whose author is `sponsored_push.rs`'s
  `failure_funding` serde field (with its own `lookupTable` and oracle keys). Untouched. That two books name one account
  class two ways is real and is **owed to whoever owns the bootstrap book**; renaming it breaks operator books on disk.
- **The drift had been caught by a typecheck, which is luck, not a gate.** `failureWalk.test.ts` now asserts the book's
  fields are exactly the frame slots the caller must supply, in frame order, lowerCamel of the emitted name — proved red
  first by renaming the fixture's field back.
- **The frame ratchet: `9fe4506f2` said it left the ratchet red and the measurement withdraws that.** Two independent
  `tools/gate frames --at 311797cc3 --capture` runs in separate build roots are byte-identical to each other **and to the
  `01db9fdf` baseline in every one of the eight links' function tables** — the `commit` field is the whole delta of
  `frames-baseline.json`. The iterator rewrites and the two accessors cost nothing in stack. Advancing the base is
  measured, not absolving: the window's frames are proven identical.
- Gates at `16e79dfaf`: `guards` PASS (225s, 71/0), `web` PASS, `emission` PASS, `fmt` PASS, `frames owed` names none,
  and the whole `cheap` tier PASS (selftest, census, emission, budgets, fmt, locks, seam, commands, release).
  `cargo test -p dclutch-trading --lib` 593/0, both generator-fresh guards, CLI 131/1 with `typecheck` and `lint` clean,
  SDK `failureWalk` 5/0.
- **Not this lane's, named**: (1) `dclutch-trading` stays RED for `tools/gate clippy` and its `debt` row stays honest —
  but the row's `type_complexity 1` is a pre-merge census; what is red today is the **lib-TEST** target
  (28 `unwrap_used`, `panic`, `too_many_arguments`, `indexing_slicing`), plus one lib item that only the test build calls
  dead: `GENERAL_SETTLEMENT_ITEM_IDENTITY_STRIDE_V2` (`general/runtime_candidate.rs:24`), a public constant with **zero
  readers anywhere in the tree** — a stride declared zero to document an absence. (2)
  `tools/direct-translation-validator/check.sh` still prints `validator_cargo_lock_sha256=` **empty**: the one-workspace
  merge deleted that package's `Cargo.lock` and the evidence line now digests a file that does not exist, silently. Its
  own column's, and it is a "distrust silent success" shape — the line reads as evidence and asserts nothing.
