# Ledger — 2026-09-05

Dated deltas for 2026-09-05; the index is GOAL.md.

### 2026-09-05 11:00 EDT — THE CONVERGENCE CLOSED; THE REPAIR WAVE
- CONVERGE-2 closed: **final cut dragons-clutch `4606168b0` = dClutch `f6b127fdc`**; evidence
  docs/evidence/SIMPLIFICATION_CONVERGENCE_2026_09_04.md. Counts by the map's recipe, 330bbfaba → f6b127fdc: programs
  308,362 → 277,403; crates 534,000 → 515,896; formal 73,280 → 66,967; tools 352,059 → 315,551; apps 105,671 → 59,114;
  packages 75,958 → 74,287; docs 115,341 → 133,580 (the ledger absorbed the narrative); **the seven together 1,579,029 →
  1,445,994 (−133,035, −8.4%)**; programs 12 → 8, crates 94 → 33, GOAL.md 4,992 → 106. **Every one of the eight ELFs moved**
  (renames reach `.strtab`; the macro, the split, the fold and the layout reach every `.text`) — cohort-16 carries every
  redeploy; frames function by function: 1,322 unchanged, 312 renamed same frame, ONE grew (Claims' `process_generic_plan`
  1,344 → 1,728, taking the deleted Core-effect dispatch), five shrank, 209 deleted whole. Gates green: cheap 8/9,
  reference, sbom, guards, abi, frames, clippy (four never-reached crates reached and fixed), root-targets (three repairs),
  tier 1 and General hot on hbox, hot-cu 20/20 (floor 91,593 → 91,039, zero jitter). Red by column: `commands`
  (dclutch-terminal); web 4 tests; journey (the ladder vs the journey target; fractional-exterior); suites 11/15 — core's
  five Series tests (red at the base), dealer (the fold), general (the merged Product reader), claims-lifecycle (no
  control). The base ELF digests were lost with /private/tmp in the fault (rebuildable at 330bbfaba). The `twins` tier
  deleted with its instrument. The wrapper's workflows had called `tools/gate` since c21451344.
- What the map priced and nobody executed: the 55 workspaces / 71 lockfiles (272k lines) — the one-workspace merge.
- Started 11:05 EDT: SUITES-REPAIR (dealer, general, claims-lifecycle, core's Series tests, journey's two workspaces),
  WEB-REDS (the four web tests and the `commands` row), WORKSPACE-ONE (one Cargo workspace, one lock).

### 2026-09-05 12:05 EDT — WEB-REDS closed: four reds, each at its author (a29496337, 1761b7c26)
- **The selected gate was never missing.** The census enumerates both rows at HEAD and always did (inventory at
  23fda4edf: `direct-root: Open` and `series-ticket: Prepared` on `trading/hot_v3::process_hot_execution_v3`). It writes
  `selected_by` as it walks the AST, so the trading split moved both names one module segment — `hot_v3::direct::` and
  `hot_v3::series_expiry::` — while the SDK's hand-pinned `HOT_FAMILY_CLASSIFIERS_V1` still typed the pre-split path. An
  unbound classifier is an error nowhere in the model: the gate is answered for nobody, so `direct.inline` went back to
  reporting no gate at all. Names follow the census; the pin that should have caught it read one fixed file (`hot_v3.rs`)
  and now resolves the classifier's own module path.
- **`DCLTRIX1` is an instruction magic, and the survey had only the declaration to classify by.** The constants table
  carries a kind column now (`record` | `instruction`, per row, emitted rather than inferred from the export name); both
  generators publish `INSTRUCTION_MAGIC_EXPORTS_V1`; `explorer-coverage.mjs` reads it and holds every excluded constant to
  a census instruction magic, so a record marked as an instruction is visible rather than dropped from both surveys.
  Its magic pattern also demanded a single quote, which hid `relayTransportV1`'s magic from it — closed.
- **`tradeFlowRefusals` typed a sentence of the SDK's wall** ("public page" for "public client"): the web's twin of
  `directTradeSpine.ts` said it that way. Derived from the wall now — the tail past the fragment the table matched.
- **`dclutch-terminal --help` named no verb in a fresh checkout**, so eighteen published commands were rejected by their
  own program. The command list and flag table move to `packages/dclutch-cli/src/usage.ts` — imports nothing, erasable
  syntax — and the launcher loads it directly when `dist/` is absent, printing the page the bundle would. Controlled by
  running the launcher in a scratch tree with no `dist/`, against the client's own `USAGE`.
- Two the tier surfaced: `tools/gate commands` held `run-ladder.sh --help` to the `--checked-release-gate` its usage line
  names bare (a command that asks for usage cannot be incomplete — exempted, with a self-test control), and
  `run-ladder.sh` never named its own `-h|--help`. `tools/gate web` ran `deploymentLiveness.live.test.ts` against both
  client trees and read the web's "no test files found" as a dead cohort; the SDK owns that question since `dcaba4770`,
  and the tier now asks the carriers, names the tree that carries none, and FAILS if no tree carries it.
- Gates at 1761b7c26: `web` PASS, `commands` PASS (0 defects in a fresh worktree too), `selftest` PASS, `emission` PASS,
  `reference --check --converge` at its fixpoint. web vitest 1,057/29/0, SDK 892/20/0, CLI 131/1/0; web+SDK tsc clean,
  lint 0 errors, a11y 0.
- **Not this lane's, named**: `guards` is FAILED at HEAD (71 ran, 1 failed) — `tools/direct-translation-validator/check.sh`
  passes its translation validation (91 Lean ABI values, 7,280 mutations, …) and then fails clippy over `dclutch-trading`:
  66 errors, never-used constants in `src/general_config/generated.rs` and `indexing_slicing` sites, all arriving with
  `d0219d449` (the crates merge). The crates column. And `packages/dclutch-cli/src/commands/walk.ts` does not typecheck at
  HEAD: its `BOOK_FIELDS` say `failureFunding` where the SDK's `FailureWalkBookV1` says `resolutionFunding`. The rename is
  the Resolution role's (`crates/dclutch-source/src/relay/frame.rs` names the old spelling) and
  `tools/local-validator/bootstrap/successor/README.md` still publishes `"failureFunding"` as the book key, so which name
  the operator's book carries is that column's call.
- WEB-REDS closed (`a29496337`, reference `1761b7c26`, ledger `92090237d`): **the census had never lost the selected gates —
  the trading split moved two classifier paths one module segment and the SDK's hand-pinned `HOT_FAMILY_CLASSIFIERS_V1`
  still typed the old names; an unbound classifier was an error nowhere** (the pin now resolves the classifier's own module
  path); the protocol-constants table gains a `record | instruction` kind column the explorer survey reads (a double-quoted
  magic had been invisible to the survey); `tradeFlowRefusals` derives its wall from the SDK's registry; the CLI launcher
  answers `--help` from source when `dist/` is absent (0 defects in a fresh worktree); `tools/gate web` PASS, commands PASS,
  emission 91/91. Standing reds routed to MERGE-REDS: `tools/gate guards` fails clippy over the merged `dclutch-trading`
  (66 errors from d0219d449); `walk.ts` does not typecheck on a book-field name with three spellings.

- WORKSPACE-ONE closed (`01db9fdf1` the fold, `ce50bbaa4` the ratchet): **55 Cargo workspaces and 61 lockfiles become 1
  and 1 — 246,594 lines of `Cargo.lock` become 13,165 — and all 8 SBF links are byte-identical by `sha256` to a clean
  export of the parent commit.** The map (§1.5/§4 item 7) priced this as the biggest single deletion in the tree and
  left the reason to whoever executed it.
  - **The census answer is that there was no reason.** No `[patch]` section exists anywhere in the tree. 53 of the 54
    nested `[workspace]` tables were an empty marker, and each manifest wrote the same sentence: *"adding this crate to
    the root `members` list is an integration decision this crate does not get to make for everyone else."* Fifty-four
    deferrals of one decision, not one dependency conflict. **One conflict class, not two**: the host/SBF split the
    brief allowed for does not exist — `solana-program-test` and the eight SBF program crates unify.
  - **What the deferral cost.** Independent resolution let the exact pins drift, and the drift then read as the conflict
    that justified the separation. Eleven requirements lagged the root's *on the same major*: `solana-program-test`
    `=4.2.1` vs `=4.3.0-beta.2` (8 manifests), `solana-account` `=4.3.2` vs `=4.6.0` (7), `solana-transaction` (9),
    `solana-message` (7), `solana-address-lookup-table-interface` (6), `serde` `=1.0.228` vs `=1.0.229` (6), plus four
    more. `crates/dclutch-direct-ticket/Cargo.toml` documented that drift as a permanent fact of the tree and shaped a
    dependency requirement around it.
  - **Why no byte moved.** The authority for every rewrite was the ROOT lock: a nested pin was raised to the version the
    root already resolved in the same semver-compat class, never lowered. The one lock is 855 packages where the root's
    was 713 — **zero removed, zero whose old version disappeared**, every addition an older major joining beside the one
    the programs compile against. Positive control: the same build of a probe tree with one `usize` changed in rent-sbf
    moved that link's sha. Frame diagnostics zero.
  - **Six orphaned lockfiles (19,800 lines)** belonged to packages already members of a parent workspace, so cargo never
    read them — and `tools/gate locks` was re-resolving their parent while appearing to check them.
  - **The fold reveals the `#[path]` tripwire and breaks nothing.** `cargo check --workspace --all-targets` now reaches
    the three gauntlet campaigns and reds: `successor/src/local_mutable.rs` calls a 9-argument function with 8, and
    journey's `provider.rs` misses a field. **Control run, not assumed**: the identical errors reproduce at HEAD's
    topology inside each campaign's own workspace, and the root's own two reds (`wallet-terminal-{input,payout}-wasm`
    lib tests) reproduce there too. Zero new errors; five commands became one. **Owner: the successor's column.**
  - **Three gates needed the fold to keep measuring.** `journey`'s tripwire found tool workspaces *by their `[workspace]`
    table*; with none left it would have matched nothing and passed on an empty set — it now enumerates `tools/`
    PACKAGES from `cargo metadata` and refuses if there are none. `fmt` reached only the root's 41 members, so it had
    never looked at a program-test suite, a gauntlet campaign or the successor: **126 unformatted files**, every one a
    dated baseline row (a handoff — SUITES-REPAIR and WEB-REDS are live there). `fmt` also had to learn that a `#[path]`
    link makes rustfmt name one file under four spellings; paths are normalised before comparison.
  - **Swept in the same commit**: one workspace is one `target/`, so the TS CLI's successor-binary search,
    `run-lineage-loopback.sh`, `run-local.sh`, `stage-devnet-sponsored-market-open.sh`, the load-simulator template, the
    fractional-atomic hint and the Rust CLI README all resolve `${CARGO_TARGET_DIR:-<root>/target}` and build by package
    name. AGENTS.md's nested-`target/` rule is replaced by: a new `[workspace]` table is a claim that some dependency
    cannot be unified, and must name it.
  - **The ratchet was green for twenty-six commits and the ledger said otherwise.** `frames owed` named five debtors
    (four CONVERGE-2 commits and the fold). None had moved a frame: two independent captures of `01db9fdf1` on one host
    are identical to each other *and* to the rows admitted at `018ea525f`, so the baseline's commit field advances and
    nobody's unmeasured movement is admitted under anyone's name. `owed` is empty. The accusation was structural —
    `owed` reads which FILES a commit touched against a link's closure, the right cheap proxy for who should measure and
    a wrong answer about who moved anything.
  - **The license closure lost 144 unique package versions** (`19578626e`): the SBOM goes from 58 manifests and 1,193
    cargo rows to 4 and 1,049. Those 144 are what the 55 workspaces cost in substance, not in a report — a nested lock
    pinning `solana-program-test` `=4.2.1` against a root at `=4.3.0-beta.2` dragged its own copy of the agave stack
    into the tree under a second set of versions, and the tree compiled both. `sbom_check.py` still DISCOVERS workspace
    roots rather than assuming one, so a re-introduced workspace is covered without an edit.
  - Gates at `01db9fdf1`: `cheap` all nine PASS (`locks` reports **1** lockfile where it reported 61), `frames` PASS.
  - **Owed, named**: `tools/fractional-exterior` still pins `solana-sdk`/`solana-client` `=2.1.0` and is the sole reason
    ~90 packages of an obsolete Solana major sit in the one lock — a source change, not a manifest one. The 126 `fmt`
    rows. The three campaign reds. `bundle-builder` and `direct-hot` can finally inherit `[workspace.lints]`: the
    structural reason is gone and only the `clippy-debt.tsv` ordering remains. `tools/gate workspaces` (the cut tier)
    now checks one archived workspace; its docstring says so, but whether an archive-and-recheck still earns its keep
    over `cargo check --workspace --locked` is a question for the gate column.

### 2026-09-05 11:30 EDT — SUITES-REPAIR: four of the five reds convicted at their authors; the fifth is a coin flip
- **`dealer`** green, 2 of 2 on five draws (was 1 of 2). The fold (`5b4fe2313`, merged `3bee5f3f1`) made the accelerator's
  arm a function of the top-level Hot envelope's family magic; `geometry_complete_fixture` still carried a 4-byte
  placeholder, so the frame was General's and refused `HeapFrameNotRequested` 0xC006 before any Dealer conjunct. The
  frame now names `DEALER_EQUITY_REQUEST_MAGIC_V3` and the assertion is unchanged — 0xC101 at CPI depth two. The same
  fold deleted `tests/accepted.rs` (the 31-test accepted campaign, with the checkpoint routes it drove) and its
  `dealer_chain`/`custody_delivery` drivers; the runner had gone on building **four SBF links nothing reads** and now
  builds the two `physical.rs` loads. `fb36992d7`.
- **`general`** green, 29 of 29 on five draws. The convergence evidence attributes this to the crates merge and its
  merged Product `svm_reader`; **both are wrong** — that reader is not on this path and the test still passes at
  `73499583b`. The author is `0c9811bc6` (the OPERATORS branch, reaching HEAD through `bee9db34c`): `map_err(|_| Product)`
  became `map_err(ProductGraphObservation)`, and the accelerator program-test's own `[workspace]` kept any root check
  from compiling it. `fb36992d7`.
- **`core`** green, 20 passed / 0 failed / 2 ignored on five draws (was 15/5). All five Series rows died at ONE conjunct:
  `2cf96117a` (SERIES-4) ruled the root's `selection().config()` is the Registry RECORD DIGEST `hash(t)`, not the
  domain-separated `template_content_id(t)` that names a coordinate where no record can exist. It swept the pre-market
  expiry test and never reached `found_program_test.rs`, whose root header had carried `template_id` since `b561326b9`.
  Not the Custody coordinate, and no program change owed. `fb36992d7`.
- **`journey`** — the campaign's target compiles and its host tests are 354 passed / 0 failed / 1 ignored (was: does not
  compile). That build is the **only** one in the tree that compiles nineteen successor modules with `cfg(test)`, so four
  things had gone stale unseen: RECOVERY-3's `recovery_rungs` (`6a3079454`) broke the successor's own two callers,
  `beca9243e`'s `recovery_ladder` never reached the journey's `provider.rs`, `ef7e1c868` moved the readiness planner and
  left a string literal pointing at a deleted directory, and the fold made `SHIPPED_LINKS` eight against two literal
  `12`s. `657420d69`. `tools/fractional-exterior` is **stated, not fixed** (manifests are WORKSPACE-ONE's): verified
  green in scratch on the 3.0 line — `solana-client "=3.1.12"` plus `solana-commitment-config`, `solana-message`,
  `solana-compute-budget-interface` and `solana-address-lookup-table-interface` at the versions `dclutch-operator`
  already pins, with three import moves in `validator.rs`/`claim_check.rs`; the exact diff is in `657420d69`'s message.
- **`claims-lifecycle` is NONDETERMINISTIC** — the same binary against the same ELF passes **4 of 8** draws at HEAD and
  **3 of 8** at `330bbfaba`. It is not the convergence's (the control the convergence could not run now runs, eight
  times), and `d6c4dea63` landed it flaky on 2026-08-27 without stating the suite's verdict. When it fails the refusal is
  stable: `RationalLifecycleSbfErrorV2::Token` 0x5216 at 79,632 CU, no Token-2022 invocation. **`fefef8149` concluded from
  single runs that the refusal was ELF-layout-dependent; `b60a24548` withdraws that** — every one-toss row is void. The
  first suspect is ProgramTest's per-run random payer keypair. Evidence
  `docs/evidence/CLAIMS_LIFECYCLE_LAYOUT_WALL_2026_09_05.md` **and its addendum**.
- **The consequence nobody had priced**: `tools/gate suites` runs each row ONCE, so the `claims-lifecycle` tier verdict —
  and the convergence's "11 of 15" — is a coin flip. Owed to the gate column.
- Measured on hbox under `swarm-build` from clean `git archive` exports (Linux x86_64, cargo-build-sbf 4.0.0,
  platform-tools v1.53, rustc 1.89.0), every row run alone with `--test-threads=1`. No crate compiled into an SBF link
  moves in any of these commits; `tools/gate frames owed` names none of them.
- SUITES-REPAIR closed (`fb36992d7`, `657420d69`, `fefef8149`, `b60a24548`, `44af48336`; hbox, clean exports, five draws
  each): dealer green (the fixture carried a four-byte placeholder from when the ELF WAS the Dealer accelerator; the
  `accepted.rs` campaign the brief named no longer exists — the fold deleted it with its routes, and the runner had gone
  on building four links nothing reads); **general green — the convergence evidence was wrong on both counts** (the merged
  Product reader is not on the path; the author was an operators `map_err`); core 20/0/2 — all five Series tests died at
  ONE conjunct, SERIES-4's record-digest ruling sweeping the pre-market expiry test (**not the Custody coordinate; no
  program change owed**); journey 354/0/1 (the only build compiling nineteen successor modules under cfg(test) — four
  things had gone stale unseen); **claims-lifecycle NONDETERMINISTIC since d6c4dea63 (08-27): 4 of 8 draws at HEAD, 3 of 8
  at the base, `Token 0x5216` with no Token-2022 invocation** — a single-run inference withdrawn by a dated addendum; and
  **`tools/gate suites` runs each row once, so a flaky suite's verdict is a coin flip** → SUITES-2 (the payer keypair first;
  draws in the tier; the two coarse codes split; fractional-exterior to the 3.0 line).

### 2026-09-05 11:50 EDT — MERGE-REDS closed: WEB-REDS' two standing reds, each at its author (9fe4506f2, 311797cc3, 16e79dfaf)
- **`guards` green** (71 ran, 0 failed), the 66 clippy errors in `dclutch-trading` gone. Two commits had to meet for
  `tools/direct-translation-validator/check.sh` to see them: `d0219d449` absorbed the Dealer codec — which had held itself
  to `clippy::all` alone, its own edition and its own lint table — into a crate under the workspace deny table, and
  `01db9fdf1` dissolved the validator's own `[workspace]`, so `dclutch-trading` became a member of the workspace clippy
  wraps and check.sh's `-D warnings` finally reached it. Two authors, two fixes.
- **The 21 never-used constants were the General V2 activation block, and the fix is at the emitter.** Its reader was
  `crates/dclutch-general-config-contract/src/activation.rs`, which `aef29f74c` deleted as superseded by
  general-adapter-contract's `activation_bundle_v1` (`DCGNACT1`); the emitted mirror outlived it, and so did the
  `#[cfg(test)]` example nothing had ever read. `EmitGeneralConfigAbiRust.lean` stops emitting the width, action, magic,
  the eighteen offsets and the example; `general_config/generated.rs` is that emitter's re-emitted output and its
  byte-identity guard compares it. No `#[allow]`. `GeneralConfigAbi.lean` keeps the activation layout and its four
  theorems — the Lean spec is not what lost a reader, and a spec with no Rust mirror is not a defect.
- **The 45 `indexing_slicing` sites were the Dealer's per-outcome arrays**, and the crate already owned the checked family
  (`checked_slice`, `byte_at`, `u64_at`, `u64_array_at`, `put`). Added `outcome_slot` / `outcome_slot_mut` for the
  request-driven reads and writes; the rest became zipped iterator walks, total by construction rather than by an
  unreachable refusal. The accessors refuse with `Error::InvalidCurve`, the same accusation the adjacent
  `outcome >= policy.outcome_count` conjunct already makes, since Policy validation bounds `outcome_count` by
  `MAX_OUTCOMES`. **Two latent panics went with them**: `activate` and `root_tail::project` both looped
  `0..outcome_count` over `[u64; MAX_OUTCOMES]`, so a policy that escaped validation would have panicked, not refused.
- **`web` green and the CLI typechecks; the book's one author is the Rust frame, and the name is `resolutionFunding`.**
  The brief had it backwards: `crates/dclutch-source/src/relay/frame.rs` names the slot
  `RelayAccountNameV1::ResolutionFunding`, with the rename argued in its own doc comment (the account has always held all
  three Resolution compartments, so `FailureFunding` named a debit rather than an account class). That name is **emitted,
  not mirrored by hand** — `packages/dclutch-sdk/lib/generated/relayTransportV1.ts` carries it from
  `npm run abi:relay-transport`, verified by `abi:relay-transport:verify` in `tools/gate abi`. The SDK type and
  `tools/relayer/src/keeper.rs`'s book writer had both followed; only the CLI's hand-kept `BOOK_FIELDS` and its test
  literal had not.
- **The successor README is a DIFFERENT book, and it was right.** `tools/local-validator/bootstrap/successor/README.md`
  publishes `"failureFunding"` for the sponsored-push address book, whose author is `sponsored_push.rs`'s
  `failure_funding` serde field (with its own `lookupTable` and oracle keys). Untouched. That two books name one account
  class two ways is real and is **owed to whoever owns the bootstrap book**; renaming it breaks operator books on disk.
- **The drift had been caught by a typecheck, which is luck, not a gate.** `failureWalk.test.ts` now asserts the book's
  fields are exactly the frame slots the caller must supply, in frame order, lowerCamel of the emitted name — proved red
  first by renaming the fixture's field back.
- **The frame ratchet: `9fe4506f2` said it left the ratchet red and the measurement withdraws that.** Two independent
  `tools/gate frames --at 311797cc3 --capture` runs in separate build roots are byte-identical to each other **and to the
  `01db9fdf` baseline in every one of the eight links' function tables** — the `commit` field is the whole delta of
  `frames-baseline.json`. The iterator rewrites and the two accessors cost nothing in stack. Advancing the base is
  measured, not absolving: the window's frames are proven identical.
- Gates at `16e79dfaf`: `guards` PASS (225s, 71/0), `web` PASS, `emission` PASS, `fmt` PASS, `frames owed` names none,
  and the whole `cheap` tier PASS (selftest, census, emission, budgets, fmt, locks, seam, commands, release).
  `cargo test -p dclutch-trading --lib` 593/0, both generator-fresh guards, CLI 131/1 with `typecheck` and `lint` clean,
  SDK `failureWalk` 5/0.
- **Not this lane's, named**: (1) `dclutch-trading` stays RED for `tools/gate clippy` and its `debt` row stays honest —
  but the row's `type_complexity 1` is a pre-merge census; what is red today is the **lib-TEST** target
  (28 `unwrap_used`, `panic`, `too_many_arguments`, `indexing_slicing`), plus one lib item that only the test build calls
  dead: `GENERAL_SETTLEMENT_ITEM_IDENTITY_STRIDE_V2` (`general/runtime_candidate.rs:24`), a public constant with **zero
  readers anywhere in the tree** — a stride declared zero to document an absence. (2)
  `tools/direct-translation-validator/check.sh` still prints `validator_cargo_lock_sha256=` **empty**: the one-workspace
  merge deleted that package's `Cargo.lock` and the evidence line now digests a file that does not exist, silently. Its
  own column's, and it is a "distrust silent success" shape — the line reads as evidence and asserts nothing.

### 2026-09-05 12:00 EDT — C16-REHEARSAL-3: the third hostile walk of the completion matrix
- Evidence `docs/evidence/C16_REHEARSAL_2026_09_05.md` (`b4dfb28b5`), pinned at `873a43914`, delta against
  `C16_REHEARSAL_2026_09_04.md` over `08fe86470..873a43914`. Six categories, second walk → today: never-executed **40 of
  163 → 32 of 148**; user-inaccessible **73 of 85 → 68 of 81**; stale claims **58 → 16**; unowned economic flow **12 of 80
  → 11 of 80**; unexplained authority **121 unadjudicated, gate red → 121, gate green**; material gap **16 of 17**.
  **Three of those falls are deletion, not evidence**: 163 → 148 is sixteen routes deleted with their code and zero
  driven, ten of them witnessed and eight losing their only campaign in `3ed848701`. **Every ELF moved, so the register's
  42 devnet witnesses corroborate binaries built at `1cae26fd6`, before the swarm base, and cohort-16 has no manifest;
  the folded accelerator three families now run on has never been deployed.** Verified off devnet at slot 493,609,778:
  all seven cohort-15 ProgramData accounts LIVE at the documented slots, market 3 `C9dLhWj7…` at phase 2 **Terminal**,
  market 1 `3QytL1bB…` at phase 3 with `outstanding_capabilities` 1, seven markets, none at phase 4. Gates run here:
  `witness` PASS (4 documents, 0 problems, 61 s), `seam` PASS (`AUTHORITY 0`, no drift), `root-targets` **FAIL** (six
  rows name packages the fold removed). Closed since the second walk: D1/D4/D5 ruled and confirmed (0024/0026/0027), the
  seam gate, the two register pages reconciled, the `DCLTCRQ2` blindness, the client's cohort pin, `HoardPrincipal →
  FeeVault` pinned at the Custody contract (`8ed7f242f`), `supported_builders`. Nineteen new gaps, the sharpest being
  **three false sentences hardcoded inside `tools/genref/generate.mjs` (`:668`, `:1253`, `:1647`), so `reference --check
  --converge` reaches a fixpoint on them forever**; **all twenty-three gate tiers are single draws and `Verdict.record`
  cannot express "passed 5 of 5"**; decision-record citation damage at **18.5 %** with three cited commits that do not
  exist; and the fifteen `U-` rows retired rather than moved, with `U-014`'s subject owned by no C-row. C-16 **not met**;
  the shortest honest path is still six items, three of the second walk's six having closed, and **two of today's six are
  downstream of deploying cohort-16**. Three corrections owed to the second walk, all false at its own commit (the CLI's
  "cannot submit", `DCLRFCQ1`'s "zero builders", the detector's "four tests"). Owed and not this lane's:
  `docs/MASTER_COMPLETION_CONTRACT.md:75-77` still names only the 09-03 and 09-04 walks.
- C16-REHEARSAL-3 closed (`b4dfb28b5`; docs/evidence/C16_REHEARSAL_2026_09_05.md): second walk → today: never-executed
  40/163 → **32/148**; inaccessible 73/85 → 68/81; stale claims 58 → **16**; unowned flows 12 → 11 of 80; authority 121
  unadjudicated with the gate GREEN; material gaps 16/17. **Three of the six falls are deletion, not evidence** (sixteen
  routes deleted with their code, zero driven; eight witnessed routes lost their only campaign with the checkpoint chain).
  **Every ELF moved; cohort-15's 42 devnet witnesses corroborate binaries the tree can no longer produce; the folded
  accelerator has never been deployed.** New gaps: three false sentences hardcoded in genref so the reference fixpoints on
  them forever; all twenty-three gate tiers are single draws; decision-record citation damage 18.5% with three cited
  commits that do not exist; U-014's subject owned by no C-row; `root-targets` red at HEAD. The path is still six items,
  two downstream of the first: **deploy cohort-16** → one market Open→Retired → a cohort-landing tripwire → a citation
  tripwire → the gate's draw count → build C-11. → COHORT-16 **`(spawned)`** on the spine; WALK-3-FIXES **`(spawned)`** on the
  docs/tools gaps; SUITES-2 already on the draws.
- WALK-3-FIXES closed (`08a0b1157`, `e2b1eec3b`, `532307d26`): the third walk's five docs/tools gaps.
  **(a) The three false sentences hardcoded in `tools/genref/generate.mjs` are derived.** `:668`'s "the seven dClutch
  programs are deployed on Solana devnet, where one market is now open" -- five lines under a derived "8 programs" --
  is now "7 of the 8 programs have at least one route corroborated by a finalized transaction on Solana devnet;
  `accelerator` has no devnet witness at all", read off the witness channel the register already loads, plus the
  sentence the walk's N-1 wanted: a witness says a transaction ran, not that the program that ran it is the one this
  tree builds. `:1253`'s "partition the 163" is `rows.length`. `:1647`'s "the five pre-docket rulings (0019-0023) ...
  stay PROVISIONAL", two lines above a derived "0 PROVISIONAL", is now "16 records name that one reading as their
  confirming act -- 0019-0034", derived from the records and range-compressed only where contiguous. A `wrap()` helper
  carries the reflow, which is what lets a sentence hold a derived count instead of being hand-fitted to the column.
  `tools/gate reference --check --converge` is at its fixpoint at `08a0b1157` and again at `532307d26`.
  **(b) `tools/gate citations` is the citation tripwire, in the `cheap` tier at 0.22 s.** 861 backticked
  `[0-9a-f]{8,40}` tokens under `docs/decisions` and `docs/evidence` through one `git cat-file --batch-check`; a token
  that names no commit must be adjudicated BY NAME in `tools/gates/citations-register.json`, and the register is gated
  in both directions -- a row whose token starts resolving, a row nothing cites, or a row naming the wrong documents is
  a refusal too. Red-proven live (a synthetic `4b1d0d0d0` takes it to exit 1 naming token and file) and by
  `tools/gates/tests/test_citations.py`, 11 tests, 8 of them refusal proofs; the whole gate suite is 43 OK.
  **The damage was not what the walk read.** Its 253 sha-citation denominator for `docs/decisions` reproduces exactly;
  dangling sites there go **20 → 16**, and dangling COMMIT citations **4 → 0**. Both damaged shas are real commits of
  `~/dev/dragons-clutch`: they are PUBLICATION CUT shas taken off the `Cuts:` line of the same ledger bullet as the lane
  commits beside them. `0030:194`'s `ace5d24e9` cut RELEASE-PREFLIGHT's rent work and now names the live-tree commits it
  published (`e73aca142`, `1973f4bd1`, `61105d8c2`); `0031:87,134,255`'s `b31b35a21` cut `7f382cdd5`, "the root module
  imports the two mechanism modules" -- exactly what those three sentences claim. The walk's third, `01a00a3d` at
  `0014:59`, **is not a commit**: it is the founding Claude Code session id, which `ASPIRATION_LEDGER:55` uses in that
  sense fourteen more times. So docs/decisions carried TWO damaged citations, not three, and neither was a wrong sha --
  both were right shas read against the wrong repository. The other 16 remaining dangling sites are decision 0010's
  schema digests and an activation deadline, all adjudicated. Across both directories 87 non-commit tokens are named:
  36 artifact digests, 18 compost-repository commits, 14 schema digests, 11 magnitudes, 5 session ids, 2 truncated
  release-set ids, 1 wire byte string. What it cannot see is in its own docstring: eight hex characters can collide with
  this tree by accident, so it proves a cited token names A commit, never the right one.
  **(c) `U-014` has the C-row it never had.** The retirement's ground -- "their subjects are the contract's rows" --
  held for fourteen of fifteen; `grep -i 'aot|interpret'` over the contract returned zero, so for four days that
  ambition was in the third state the scope section forbids, reached by retiring the register rather than the row.
  **C-17, alternative execution strategies for one semantic descriptor**, written from what U-014 asked for and what is
  left at HEAD: `crates/dclutch-direct-aot-contract` and `tools/direct-translation-validator` survive;
  `dclutch-direct-aot-sbf` was deleted 2026-09-04, is `false` in SHIPPED_LINKS, in no cohort, band 10 RETIRED -- no
  interpreted or AOT descriptor has ever run on any chain. It sits with the capabilities and before the audit row; the
  matrix preamble now states the ordering rule so an out-of-numeric-order id does not read as an error, and
  `OMISSION_INDEX.md` says which fifteenth row it got wrong and where the subject went.
  **(d)** `MASTER_COMPLETION_CONTRACT.md:79-81` names all three walks -- the 09-05 file's own §7 closed on this as the
  one thing it knew would be stale on landing, and said it was not its edit to make.
  **(e)** `tools/direct-translation-validator/check.sh` printed `validator_cargo_lock_sha256=` EMPTY because
  `sha256_file` on a path the one-workspace merge deleted fails inside a command substitution, which `set -e` does not
  catch: an evidence line published nothing and read as published. It digests the ROOT lock now, as does the `cp`
  beside it, which would have failed outright under an evidence directory. The pack FIELD name stays
  `validator_cargo_lock` -- it is input 20 of `crates/dclutch-release-tool/src/translation.rs` -- and that struct's doc
  comment now says which lock it is and why the name did not follow the path.
- WALK-3-FIXES correction, same day: **`e2b1eec3b` swept SUITES-2's uncommitted `tools/gates/tiers.py` hunk** -- the
  per-row draw count (`SUITE_DRAWS_DEFAULT = 3`, `_suite_draws`, the draw loop and the NONDETERMINISTIC verdict class,
  the third walk's item 5). `lane.sh commit --only` takes the whole working-tree file, and the ` M tiers.py` I checked
  before committing was two lanes' edits, not one; `commit-patch` is the verb for that and I did not use it. Nothing was
  lost and no history was rewritten: the hunk is complete and live in HEAD (`tools/gate suites --dry-run` reports "3
  draws per row"), but it is attributed to WALK-3-FIXES rather than SUITES-2, and `git diff` on that file is now empty
  for its author. Named on the wave board so its owner is not left guessing. **The general shape**: reading `git status`
  for your own paths does not tell you which HUNKS in them are yours, and a file another lane is live in needs
  `commit-patch` whether or not you knew it was live when you started.

### 2026-09-05 12:45 EDT — SUITES-2 closed: the coin was a duplicate signature, and the tier is 15 of 15
- **The nondeterminism is convicted, and it is not the payer.** `b60a24548` named `ProgramTest`'s per-run random payer
  keypair as the first suspect. Refuted by draw, not by argument: payer random, **3 of 8** draws pass; payer PINNED to
  one fixed keypair — printed each draw, funded by `set_account` before anything else runs — **3 of 8 again**, the same
  rate, one binary, one ELF (`e20643ec7245a2aa…`), sixteen draws. Nothing downstream of `context.payer.pubkey()` decides
  the row.
- **What decides it is the recent blockhash, and the mechanism is a duplicate transaction signature.** The honest
  `RetireCoordinate` and the nonzero-supply hostile immediately before it are **the same instruction** — the hostile
  submits `retire_coordinate.clone()` against a Mint whose supply the test wrote, the honest one submits
  `retire_coordinate` against the Mint restored — same payer, same lookup table. On the same blockhash they compile to
  the same v0 message and sign to the same signature, and the bank refuses the second as **`AlreadyProcessed`**: no
  invocation, no compute units, no refusal code, `log_messages` empty. `ProgramTest` registers blockhashes from a
  background task on a **wall-clock timer** (`target_tick_duration` 100 µs × 64 ticks = 6.4 ms,
  `solana-program-test-4.3.0-beta.2/src/lib.rs:1066`); the two submissions land ~4 ms apart. Instrumented with every
  submission's blockhash, signature and error, 8 draws: the **5 that failed carried the hostile's exact signature**; the
  3 that passed had a later blockhash.
- **So `fefef8149`'s `0x5216` at 79,632 CU "with no Token-2022 invocation" is real and is the HOSTILE's own correct
  refusal** — Claims observing a Mint whose supply is not the zero the request declared — read off the interleaved tail
  of the log and attributed to a transaction that never ran. The wall document gets a dated second addendum closing both
  of its withdrawn verdicts.
- **Fixed at the author** (`5c534b016`): `submit` takes a blockhash strictly newer than the one visible on entry, so
  every submission is its own transaction. Two of this suite's requests are identical by construction —
  `replay_receipt` IS `activate_receipt.clone()` — so the replay row is now refused by Claims's own replay guard rather
  than possibly by the ledger's signature dedup. **8 draws of 8 green**, the five-row suite 5 of 5 on three draws. Cost:
  `get_new_latest_blockhash`'s 200 ms poll — the row 0.45 s → **2.7 s**, the suite → **6.0 s**.
- **`RationalLifecycleSbfErrorV2::Token` split**, which is what would have caught this on the first reading: eleven
  sites, one symbol, so the hostile's refusal and a failure of the retirement it precedes were indistinguishable. Four
  causes appended in band under 0007 — **`MintProfile` 0x5219** (the Mint is not the closeable profile the family
  requires, SUPPLY included — the hostile's site), **`CustodyLayout` 0x521A**, **`CustodyState` 0x521B**, **`Allocation`
  0x521C** (two sites that were the SYSTEM program's business, never Token-2022's). The six keeping `Token` are the
  CPI-effect sites, where Token-2022 writes its own cause on its own `Program … failed` line in the same log: already
  localizable, and one accusation.
- **`ProductGraphObservationErrorV3::InvalidRecord` split** (`b03b1c26e`): eight disjuncts of one conjunction plus four
  derivation sites, now `RawRecordAddress` / `RawRecordOwner` / `RawRecordExecutable` / `RawRecordEmpty` /
  `StagingCursorAddress` / `StagingCursorOwner` / `StagingCursorExecutable` / `StagingCursorNotVacant` /
  `ContentIdentity`. Host-side only — no `#[repr]`, no band, no link. Both consumers name their exact site:
  `a_nonvacant_product_staging_cursor_refuses` → `StagingCursorNotVacant`; `direct_inline_v3`'s two `hot38_state`
  exteriors → `RawRecordAddress`, **measured, not assumed**.
- **The Series rent floor gets its own code** (`17f1b6dec`), the second half of what `fb36992d7` left owed.
  `a_market_short_of_its_occurrences_budgeted_rent_still_refuses` -- whose own docstring says "if it ever passes, the
  floor became no check at all" -- expected `CoreSbfError::Reference`, returned from an **eighteen-disjunct**
  conjunction in `series_consume`, fifteen of whose disjuncts the fixture never touches. **`SeriesMarketRent` 0x301E**
  (the slot holds less than the occurrence budgeted -- a balance, not an identity: `request.market()` is pinned to
  `occurrence.market()` a dozen lines above, so nothing here can be mislinked) and **`SeriesMarketVacancy` 0x301F**
  (the slot is already occupied) are split out, in the shape `FundedRent` was split from `Funding` on 2026-09-04.
  **This refutes `fb36992d7`'s recorded suspicion** that the test "receives a bare 0x3003 from an earlier site than the
  floor it means to test": with the floor carrying its own discriminant the test refuses exactly `SeriesMarketRent` and
  passes, so the fixture always did reach the floor and only `Reference` could not say so. `core` 3 of 3 draws,
  `found_program_test` 20/0/2.
- **`tools/fractional-exterior` to the 3.x line** (`cda85ef47`), the diff `657420d69` verified and could not land.
  **The one lock: 1,049 package versions → 960, net −89** (152 removed, 63 added — the 3.1.12 RPC/TPU client stack
  replacing the 2.1.0 one). **75 `solana-*` packages were pinned at 2.1.0; none is.** No package version is added that
  `dclutch-operator` did not already pin. `tools/gate locks` green.
- **The tier draws** (`580f162a4`, `55c19f5fa`, over the hunk `e2b1eec3b` swept in): a missing prerequisite is no longer
  drawn three times, and the cost is measured rather than inherited. Twice: 16m59s for thirteen rows plus 72 s for
  `claims-position`/`claims-fractional` once `TOKEN_2022_SO` was supplied, and then, at the tree carrying this lane's
  Core split, the whole tier in one run — **15 rows, 3 draws each, 45 runs, every one a pass, exit 0 in 14m40s** warm.
  `claims-lifecycle` is green on every draw for the first time since 2026-08-27, and nothing is NONDETERMINISTIC.
- **The convergence's "11 of 15" gets a dated in-place addendum** (`49c554afe`): it is one draw of each of fifteen rows,
  and stands for nothing more. Three of §8's four reds survive because `fb36992d7` convicted them at their authors with
  controls at two commits; the `claims-lifecycle` cell does not.
- Measured on hbox under `swarm-build` (Linux x86_64, cargo-build-sbf 4.0.0, platform-tools v1.53), draws run one thread
  at a time with a name filter. The suites tree is an export of `9fe4506f2` carrying only this lane's changes; no file
  another lane committed that afternoon is on any of the fifteen runners' paths (checked with `git log --name-only`).
- **The frames ratchet is GREEN, and `5c534b016`'s message was wrong to say otherwise** (`e136a6e3e` corrects it). Two
  independent captures at `17f1b6dec` are identical and the complete per-function manifest matches the ratchet admitted
  at `311797cc3` **byte for byte**: six new refusal discriminants across two links, and one conjunction split into three
  guards, move **no function's exact frame in any of the eight links**. Only the manifest's `commit` field advances.
  Worth measuring rather than assuming — `map_err(|_| A)` becoming `map_err(|_| B)` at eleven sites is exactly the edit
  that could have moved a spill. `tools/gate frames owed` names nobody.
- **OWED, and not this lane's to run**: the generated reference and its client mirror are stale by exactly the six new
  codes (four claims, two core). `docs/reference/abi/refusalRegistryV1.md` carries 344 where `tools/gate census` now
  enumerates **350**; `refusals.md` and `abi/routeCensus.md` mirror the same table, and
  `packages/dclutch-sdk/lib/generated/refusalRegistryV1.ts` is generated FROM `docs/reference/refusals.md` and carries
  the same 344 behind `abi:refusal-registry:verify` — the cycle AGENTS gives to `tools/gate reference --converge`, the
  convergence owner's to run and not a lane's. Named here rather than hand-edited. Nothing else is owed: `census`
  (350 codes, 0 unclassified), `locks` and `fmt` are green.

## COHORT-16 — the eight are on devnet, the eighth for the first time

- **Cohort-16 deployed.** Eight programs from `f2ae6bf75`, built by the NAMED RELEASE BUILDER ARTIFACT
  (platform-tools v1.53 on Linux/x86_64, hbox through `swarm-build`), zero SBF frame diagnostics on every link,
  each live image dumped back and compared to its candidate ELF over the ELF's whole length before the next role
  spent. `registry 6gRRiB9B…`, `rent 42xN9ULo…`, `custody 8UkoNCPD…`, `resolution jrjXw2Rp…`, `claims 8JfHfBBG…`,
  `trading ESQhDyV7…`, `core 4wv7JxoA…`, **`accelerator 6v1c2Go2…`**. Evidence:
  `docs/evidence/COHORT16_DEPLOYED_SEALED_2026_09_05.md`. **The eighth link had never been deployed to any chain**:
  the folded accelerator carries General, Dealer and series-shadow, and C-05, C-06 and C-07 all depended on a program
  nothing had put on a chain. The register's 42 devnet witnesses attesting to pre-swarm binaries now have a cohort to
  be re-anchored against; `tools/gate witness --discover` against this cohort is the next lane's and is NOT done here.
- **The ledger, to the lamport.** Closing cohort-15's seven ProgramData accounts returned **42.505848399 SOL** and the
  superseded accelerator **1.915282857**, taking the deployer 23.890559434 → 68.311650690 net of eight 5,000-lamport
  fees. The eight deploys cost **36.496508160** of rent and 0.042329960 of fees, leaving 31.772812570; funding the
  campaign payer left 29.772807570. **Every one of the eight ProgramData balances equals `(128 + 45 + elf_bytes) ×
  5,080` exactly** and every `Data Length` equals its candidate ELF's byte length exactly. The rate is 5,080 and not
  cohort-15's 6,333 — devnet moved it at the epoch-1141 boundary — which is why `steps.tsv`'s `-42.26` is cohort-15's
  price and not today's.
- **Five defects that only a RUN could produce, all repaired as commits.** (a) **No row deployed the accelerator**:
  `prepare` had always merely OBSERVED one, a separate job had always deployed it, and that was invisible for three
  cohorts because the artifact did not move — `deploy-accelerator` and `close-prior-accelerator` are new rows.
  (b) **`SHIPPED_LINK_COUNT=12` in `checked-release-candidate.sh` refused every candidate at HEAD**, the second time
  a literal count there has done so; the file's own comment predicted it after the POPULATION lane hit 13→12 on
  2026-09-02. The script now READS `SHIPPED_LINKS` from the archived source and compares label/package pairs by NAME —
  which also caught (c) **a positional-order defect the count was hiding**: the fold's rename of
  `dclutch-general-accelerator-sbf` to `dclutch-accelerator-sbf` moved the package to the front of the `programs/*`
  enumeration while its `SHIPPED_LINKS` entry stayed where the deleted dealer accelerator had been.
  (d) **The campaign release pack named the `Cargo.lock` the one-workspace fold deleted** — three call sites, now the
  root lock; the sweep half of the third walk's N-9 for the entries that were release EVIDENCE rather than prose.
  (e) **The `seal` row named `devnet-deployment-set-journal-v2 --init`, a mode no driver implements** — the
  producer-missing pattern inside the runbook itself, because what actually ran for cohort-14 and cohort-15 was a
  hand-written script inside each job directory. It is `tools/cohort/deployment-set-journal.py` now. Two emitted-stage
  defects besides: `programdata` needed a default signer to perform a READ and refused inside a command substitution
  so the next command reported a missing ARGUMENT, and `prepare` omitted `--{role}-semantic-release-id` entirely.
- **`refund-scale` landed, and it is a founded record rather than a founded program.** `compile_linked_basis_v3`
  hard-wired `payout_scale: 1` — the shape that paid cohort-13's founder the whole failure column while the two
  strangers who traded got nothing. It DERIVES the scale from the width now (`basis_width - 1`, decision 0025), and
  the byte-identity test that asserted the literal `1` was a second author for a rule
  `categorical_refunds_on_failure_v3` owns.
- **CORRECTION to `docs/HANDOFF_2026_09_05.md:114`.** It says the capability manifest's dependency edges were *"fixed
  for cohort-16"*. **They are not, at HEAD.** The founding compiler builds every manifest entry with
  `dependency_count` 0 and an all-zero array (`.../successor/src/market.rs:14678-14680`), so
  `capability_dependency_closure_mask_v1` still returns a singleton for the Direct entry and stage four of retirement
  still refuses `physical_count 2 > logical_count 1` at `terminal_retirement_v1.rs:699`. **This lane did not guess the
  edge set**: the manifest entry is a seed of the Market PDA, so a wrong array founds a DIFFERENT market rather than
  the same one misconfigured. The wall is bounded and named and it is the next lane's, with a hostile test attached.
- **The preflight is RED before the deploy for a cohort that deploys its own accelerator, and that is correct.** The
  slot, the ELF digest and the liveness are outputs of `deploy-accelerator`. The verdict is not softened — founding
  against an unpinned accelerator founds a different market — but it now names the row that produces the fact and
  says which acts the RED admits. The README says such a cohort runs the preflight twice.
- **The first founding on cohort-16 refused, and the refusal is decision 0025's own rounding boundary.**
  `found-direct` drove 140 founding transactions -- the realm, the Product graph, the capability manifest, Found37's
  hostile controls, the Found31 market coordinate `4Pm8Mfg2…` -- and stopped at *"founding collateral reserve is not
  exactly divisible by basis scale"* (`.../successor/src/market.rs:6137-6152`). At the legacy scale `1` every budget
  divides; at the refunding scale `3` the fixture's collateral atom count does not, and the guard's own comment says
  why it does not learn a floor: *"accepting another floor here would create a second rounding boundary and an
  unclassified remainder"*, which is 0025 §6 in as many words. **The refusal is correct.** The repair is a founding
  INPUT -- pick the reserve as a multiple of the scale the width derives -- with a hostile test that a reserve one
  atom off still refuses. **`refund-scale` is therefore half a row**: the record side is authored, the ELF side needs
  nothing, and no market of cohort-16's reached `Open`. The General and two-source markets were not attempted.
  Cohort-16 stands DEPLOYED and SEALED with a funded campaign payer; `docs/evidence/COHORT16_DEPLOYED_SEALED_2026_09_05.md`
  §8 names the three blockers in the order they block each other.
- COHORT-16 closed (`e311d2936` … `887e00630`; docs/evidence/COHORT16_DEPLOYED_SEALED_2026_09_05.md): **cohort-16 deployed
  and sealed on devnet** — eight programs from `f2ae6bf75` by the named builder (hbox, platform-tools 1.53), zero diagnostics,
  every live image `cmp`'d against its candidate before the next role spent; **the folded accelerator on a chain for the
  first time**; the ladder 36 tx, the seal 0 SOL; closes returned 44.421 SOL, deploys cost 36.497 — every ProgramData
  equals `(128 + 45 + elf) × 5,080` to the lamport; deployer 23.89 → 29.74. **The founding refused correctly on decision
  0025's rounding boundary: the reserve must be an exact multiple of the derived payout scale (3 for four outcomes)** — a
  founding input; and **the manifest's dependency edges are NOT fixed** (the compiler writes `dependency_count 0`; the
  handoff's line corrected). Five runbook defects found by running it (no accelerator row; `SHIPPED_LINK_COUNT=12` refusing
  every candidate; a positional-order defect; the deleted lock named; a seal mode no driver implements — the producer-missing
  pattern inside the runbook). → COHORT-16B **`(spawned)`**: the input, the edges, three markets, one Open → Retired.
- COHORT-16B (`fe86bac5b` … `d76b13158`; addendum in `docs/evidence/COHORT16_DEPLOYED_SEALED_2026_09_05.md`):
  **the first cohort-16 market is Open** at `GyD95eyERwRfwj8fSFNhWjKF2eaDg5XcREidPKex65zY`, founded with a reserve
  DERIVED against the payout scale (1,000,000,002 atoms, budget 500,000,001, complete sets 166,666,667) and with the
  **first capability-manifest dependency edges any chain has carried**: entry 0, the selected Direct entry,
  `dependency_count 3`, dependencies `[1,2,3]`, read back off the manifest record. Its `ProductBasisV3` carries
  `basis_width 4` and `payout_scale 3`, so **`refund-scale` is green on a founded market** and an outage refunds the
  holders. Blocker 1 is closed; the guard was never weakened and the founding now prints
  `founding-reserve-terms-v1` rendered through the guard itself, which the staging surface quotes and refuses to omit.
  **Blocker 2 is reshaped, not closed.** The edges are right — the close frame's two ledger masks can only partition
  the closure if the selected entry names the other three — but the selected entry sits at index **0** on a real
  market, not the fixture's 3, and every route that consults the closure then needs a two-ledger frame. Activation
  refused in the deployed Trading program at `TradingSbfError::Content` (`0x4003`, 108,180 CU): the Direct activation
  bundle declares `ACTIVATION_ACCOUNT_COUNT = 2`, *"the root being created and the sole selected funding ledger"*, and
  `require_activation_local_effects` widens its forbidden window by `funding_count`. Those indices live in published
  records the manifest entry names, and the manifest digest seeds the Market PDA, so **retirement needs the edges and
  activation at this release forbids them** — a Direct release change inside the Trading SBF link, not a founding
  input. Also found by running it: the `DCLTGMF3` key pin was a **stale second author** of a frame the failure-escrow
  seating had already moved (58 vs the measured 60), green in every offline suite and refused three transactions from
  Open, with a test asserting the same two literals as a third author. The pin now reads the frame.
  → NEXT: the Direct activation bundle carries the dependency ledger (program authority), then re-found and retire.
- COHORT-16B closed (at `6c44b6da7`): **the founding input closed on chain** — `categorical_founding_payout_scale_v3` the one
  author of `basis_width − 1`, the reserve raised to the smallest multiple the scale divides (a stated reserve is never
  rounded, only held to the guard), printed through the guard on the terms surface; **market `GyD95eyE…` is Open on
  cohort-16 with the escrow seated and `refund-scale` green on a founded market for the first time**; the local lab's
  default shape had been unfoundable since 0025 (fixed); **the dependency edges derived from the close frame** (the two
  ledger masks partition the header's required union; the selected Direct entry is index 0 with ledgers 0x0001/0x000e —
  the retirement fixture had assumed index 3) **and activation at the deployed release refuses them: `0x4003 Content`,
  `ACTIVATION_ACCOUNT_COUNT = 2` — a Trading release change**; the DCLTGMF3 key pin was a stale second author of a frame
  the escrow seating moved (58 vs 60, red-proven); a refused founding consumes the mint (keys rotated). Witness: 125
  signatures, 18 routes corroborated, **no accelerator route driven yet**. Payer 1.877 → 1.522; deployer untouched.
  → PROGRAMS-17A **`(spawned)`**: activation admits the entry's ledger set; the retirement walk on real ELFs; the candidate;
  then COHORT-16C redeploys Trading and retires the first market.
- WEB-COHORT16 closed (at `e4f58d453`): **the site moves to cohort-16 and the deployment manifest carries EIGHT programs**
  — the accelerator is in `programs` and deliberately not in `PROTOCOL_ROLES_V1` (it owns no account, is nobody's owner,
  and the gauntlet's local layout deploys seven), with `deployedProgramRolesV1` the one place a surface asks which. Every
  row derived: the eight ids off the sealed plan (the eighth from its optional `general_accelerator`), the ProgramData and
  slot read off chain by `derive-deployment-manifest.mjs`, the activation-cache hint `2xVxMvfy…` by the call the client
  makes, the checked-release row ingested from the seal's fragment under a `--release-set` read separately off the Market
  at byte 208, the share-card facts staged off devnet. **Cohort-15 retired BY REFUSAL**: the same derivation, run against
  cohort-15's sealed plan, names all eight ProgramData accounts vacant and emits nothing — and the OG staging came back
  with exactly one market for the same reason. The liveness gate asks about the eighth, which no owner check could;
  `stage-checked-release.mjs` had been unrunnable since the twins merged (second time it shipped never having been run).
  Release table gains cohort-16's row, observed twice over (the cache's five pinned slots equal the manifest's AND its five
  pinned ELF digests equal the release gate's). Five live cases stop naming cohort-15 literals — two were ABSENT, two were
  still passing against a closed program's accounts, which is worse — and two lost their subject out loud: the
  certificate-to-partition join (no settled market; decided offline from the cut's own `resolve` signature) and the Direct
  root tail (no activation root; the absence asserted positively instead). Two controls rebuilt rather than dropped:
  close-fund's admit branch moved offline, and the Source-derivation control moved onto the generation seed.
  **/pulse draws cohort-16's founding census with L1–L8 all HOLDING and none inapplicable** — and the first run went L3
  VIOLATED naming the exact missing column until the derived failure-escrow Position was named, which is decision 0025's
  seating proven by the law that needed it; `simulator-series.mjs --no-status` is the producer half, and the halted
  simulator's status left with the closed market it linked to. **One derived sentence was stating the opposite of the
  truth**: `outageDisclosureV1`'s headline had two arms where its `payee` has three, so an unread payout scale fell to the
  legacy answer and told a reader of a REFUNDING market that an outage pays the failure claim everything. Three arms now,
  pinned. **Owed**: nothing supplies `refundsOnFailure` on the market page, so the site says it has not read the scale
  rather than saying, derived, that this market refunds — the record's Registry address is a PDA of its own content digest,
  the Claims aggregate carries only the semantic id, and the one path that reads it needs the Direct capability root
  activation refused. Controls: featured page 1,139/1,127 words at 1280×900 and 390×844, zero page errors, zero bare hex;
  43 live cases green; web 1,047 and SDK 864 offline; tsc 0, lint 0, a11y 0; `tools/gate web` green but for
  `sourceReadinessWasmParity`, red at this lane's baseline for the same 30s-timeout-on-a-137s-cargo-build reason.
- WEB-COHORT16 closed (`9582ae51f`, `95259fb3d`, `7c093a4e5`, `df7b1b0f4`, `e4f58d453`): the manifest carries eight
  programs from cohort-16's sealed plan (the accelerator in `programs`, not in `PROTOCOL_ROLES_V1` — it owns no account;
  `deployedProgramRolesV1` is the one place any surface asks seven or eight); cohort-15 retired by refusal (all eight
  ProgramData vacant); the release table's cohort-16 row observed twice over; the featured page `GyD95eyE…` with the
  checked-release row from the seal's fragment under a byte-208 read; **`stage-checked-release.mjs` had been unrunnable
  since the twins merged and had shipped twice never having run**; two live cases were still PASSING against a closed
  program's accounts (rebuilt); **/pulse: L1–L8 all HOLD on the founded market — the first run went L3 VIOLATED until the
  derived failure-escrow Position was counted (0025's seating proven by the law that needed it)**; **a reader-facing wrong
  fixed: the outage headline had two arms over a three-arm payee, so an unread scale told a refunding market's reader that
  an outage pays the failure claim everything**; the explorer renders the accelerator. 43 live cases; captures 1,139 /
  1,127 words, zero errors. Owed: the page cannot yet SAY it refunds (the basis record's address is a content-digest PDA;
  the one reader needs the activation the deployed Trading refuses — after PROGRAMS-17A/COHORT-16C).
- PROGRAMS-17A: **the activation conflict was not real, and the release identity does not move.** Cohort-16B read
  `0x4003 Content` at 108,180 CU as "the Direct activation bundle must declare three accounts". It cannot: measured
  against `encode_account_profile_v1_atomic` with exactly those three rules, the encoder returns
  **`Err(UnanchoredAccount)`** — `AccountProfileV1` requires every self-representative rule to be named by a REQUIREMENT
  operation, and the requirement operations an activation artifact can write compare against the seam-seeded identity
  bank, which publishes Trading, Core, Registry and the root and **no foreign controller**. The defect was in
  `outer.rs::RuntimeFrameV2::new`, which composed the interpreted frame out of the root plus EVERY physical ledger while
  the native-close route beside it had already answered the same question — `new_close` projected through root, selected
  ledger and RentCredit only, *"a foreign dependency can never acquire a descriptor-owned runtime permission"*. **The two
  constructors are now one**; dependency ledgers keep every authentication they had (manifest binding, PDA derivation,
  funded-rent custody, per-row Active status, the canonical mask partition) outside the interpreted frame, and their
  lamports are checked against their own balances instead of a projection that can no longer name them. On real ELFs
  built on hbox under `swarm-build`, `capability_close_alias_program_test` 7/7: the two-ledger market — devnet
  `GyD95eyE…`'s shape, four entries, selected Direct entry at index 0, ledgers `0x0001`/`0x000e` — **activates**, at
  **521,780 CU** against the zero-edge control's **298,598**, with the Resolution ledger byte-identical and its lamports
  unmoved; `DirectCloseCapability` and the 20-seed begin-retiring campaign are unchanged. **No artifact byte moves** —
  the sealed Direct activation tests are green untouched — so the release id, the manifest entry and the Market address
  are what they were and `GyD95eyE…` activates at the new Trading link **without re-founding**. `terminal_retirement_v1`
  stopped being a four-entry fixture: `entry_count == 4 && entry_index == 3 && required_union == 0b1111` is gone, only
  the `F=2` frame width is fixed, and the close frame's writable meta follows `selected_funding_position` — discovered by
  reading `selected_mask` — instead of two fields named after controllers. New code `TradingSbfError::ActivationLedgerCount`
  (`0x402B`) names the ledger-set accusation on Trading's side of the CPI; **it has never fired and cannot from a
  Core-routed frame** — both hostiles refuse `CoreSbfError::Funding` (`0x3008`) before the CPI and the test names Core's
  code, not Trading's. → OWED: the complete retirement walk still runs in no harness (its stages have real-ELF coverage
  one at a time, in five binaries with five fixtures), so per-stage CU exists only where each harness prints it.
- PROGRAMS-17A closed (at `87eec1c3a`; code `b653fecc9`, frames + stage-four `c38b26054`, sequencer `87eec1c3a`): the release the retirement wall needed is a **Trading link
  and nothing else**. Real ELFs on hbox under `swarm-build`, `capability_close_alias_program_test` **8/8**: activation
  zero-edge **298,598 CU** (the byte-identity control), activation two-ledger **521,780 CU**, `DirectCloseCapability`
  on the fixture's index-3 market unchanged and on a real market's **index-0** market **528,792 CU** with
  `outstanding_capabilities` 0 and the preserved Resolution ledger byte-identical; both ledger-set hostiles refuse
  `CoreSbfError::Funding` (`0x3008`) before the CPI, which is where that partition is owned. The family-neutral
  activation suite is **14/14** and begin-retiring/close-maker are unchanged. Frames: two identical captures of
  `b653fecc9`, 8 links, **only Trading moved** — `RuntimeFrameV2::new` 64 → 192 bytes having absorbed `new_close`, which
  is gone — and `tools/gate frames owed` is clean. Checked release candidate on hbox, genesis, builder hbox:
  **8 links, freshness PASS, Trading `e7f8e476006ce1248994ae065bffd7ea0039c8681f85fed141368790e021931b`** — the same
  digest the program-test binary reported, which is an incidental control that the tree tested and the sources archived
  are one thing. **Exactly one of the eight links moves.** Built on hbox from a `git archive` of `57e4b9b27`, the commit
  before this lane: core `29200c855bfe…` and trading `69292c3391924d…`; the candidate at `87eec1c3a` reproduces
  `29200c855bfe…` for core **byte for byte** and only trading differs — and `69292c3391924d…` is cohort-16's own
  DEPLOYED Trading digest, so the pre-lane Trading is what is on devnet now. Accelerator, claims, custody, registry,
  rent and resolution are byte-identical to cohort-16's deployed set as well; core's move away from cohort-16's
  `f637e5df9ef9…` happened between `f2ae6bf75` and `57e4b9b27` and belongs to COHORT-16B, not here. **COHORT-16C needs
  the Trading link and nothing else from this lane.** **That candidate then REFUSED at its product handoff**, and the refusal is the
  lane's own miss: `terminal_sequence.rs` still built the close coordinate out of two fields named after controllers,
  and `cargo check --workspace --all-targets` never compiled it — cargo stopped scheduling after two unrelated wasm
  crates failed on a pre-existing cross-crate `cfg(test)` reference, reported those seven errors, and looked green
  enough. `--keep-going` is what this lane should have run; the candidate is what caught it, two stages and twenty
  minutes later. Repaired at `87eec1c3a` (`terminal_sequence` 47/47), and the candidate at that
  revision is clean: exit 0 in 433s, `release_builder=true`, `sbf_build_diagnostics_total` **0**, lock immutability and
  spline handoff passed, `reproducible_release_gate_sha256`
  `ba0205ccd3537c1f9c67b0ffcb431defe96f70a6f209fa2738157e96abe5ce40`. ONE candidate on ONE host — cohort-16's
  two-`--work`-root build-path control has not been run here. **The count has one author**:
  `crates/dclutch-market/src/capability_program/mod.rs:164,178` publish
  `ACTIVATION_RUNTIME_FUNDING_ACCOUNTS_V2` and `activation_account_count_v2`, and the two template literals
  (`capability_activation:149`, Direct `activation_bundle_v1:127`) and the seam's own `first_nonfunding`
  (`outer.rs:2125`) now all read them. → FOUND, NOT MINE: `cargo test -p dclutch-trading-sbf --lib` is red 2/396 on
  `stage2_frame_width_is_the_raw_prefix_plus_the_codec_open_count` (25 vs a typed `2 + 21`) and
  `frame_width_is_runtime_funding_polymorphic` (132 vs a typed 128) — stale literal mirrors of a General founding frame
  that decision 0025 item 2's failure-escrow seating moved, the third instance of that pattern in one week and owned by
  the lane that seated the escrow. → OWED: the complete retirement walk still runs in no harness; and the generated refusal
  mirrors (`docs/reference/abi/refusalRegistryV1.md`,
  `packages/dclutch-sdk/lib/generated/refusalRegistryV1.ts`) end at `0x402A` and do not carry `0x402B`, so
  `tools/gate reference --check --converge` is red until the convergence owner regenerates — a lane does not run that
  generator and this one did not.
- PROGRAMS-17A closed (`b653fecc9`, `c38b26054`, … tip `75bfe6fc4`): **the premise was wrong — `ACTIVATION_ACCOUNT_COUNT`
  cannot widen: a rule for a Resolution-owned ledger is unencodable in the activation artifact at any width
  (`validate_structure` requires a requirement operation, and the seam-seeded identity bank names no foreign controller);
  the defect was the outer's `RuntimeFrameV2::new` building the interpreted frame from every physical ledger while
  `new_close` on the same page already had it right — the two constructors are one** (root, selected ledger, effect
  accounts; dependency ledgers keep every authentication outside the frame); three literals became one author
  (`activation_account_count_v2`); real ELFs: activation zero-edge 298,598 CU (byte-identity control), two-ledger 521,780,
  `DirectCloseCapability` 528,792 with `outstanding_capabilities` 0; hostiles refuse `CoreSbfError::Funding 0x3008`
  before the CPI (Core owns the partition); frames: only Trading moved (one frame 64 → 192); **no artifact byte moves —
  `GyD95eyE…` activates at the new Trading link without re-founding; exactly one of eight links moves** (Trading
  `e7f8e476…`, candidate at 87eec1c3a). The lane's own miss: `cargo check` without `--keep-going` never compiled the
  successor, and the release candidate caught it twenty minutes later. RULING (orchestrator): **cohort-16.1 — Trading
  upgraded in place, the generation re-released, the market kept** → COHORT-16C **`(spawned)`**: the two-root candidate,
  the upgrade, the re-release, the first market ever retired, then the General and two-source markets.
- LITERALS closed: **`cargo test -p dclutch-trading-sbf --lib` is 396/396**, and the two reds PROGRAMS-17A found are
  convicted rather than renumbered. Both were mirrors of the composed General founding frame that decision 0025 item 2's
  failure-escrow seating moved, and both now read the one author of the number they restated:
  `GENERIC_FOUNDING_OPEN_ACCOUNT_COUNT_V1 = 23` (`crates/dclutch-market/src/generic_founding_v1.rs:46`, "twenty-three
  since the failure escrow was seated") for `stage2_frame_width_is_the_raw_prefix_plus_the_codec_open_count`'s `2 + 21`,
  and `CLAIMS_FOUNDING_ACCOUNT_COUNT_V6 = 33` with its companion
  `CLAIMS_FOUNDING_ESCROW_ACCOUNT_COUNT_V6 = 2` (`crates/dclutch-claims/src/founding_v5.rs:27,39`) for
  `frame_width_is_runtime_funding_polymorphic`'s `128`/`141` — the escrow rides twice, allocated in the Claims founding
  window and read back in Core's Open window, which is the whole of the 132 − 128. Each assertion became the invariant it
  was groping for: the Open window's escrow pair sits immediately after the founder's admission and is exactly as wide as
  Claims declares it; the Claims layout's escrow indices are the declared count's last two; and the polymorphism claim
  now drives `select_generic_found_count_v4` at every admitted funding count (0, 3, `GENERIC_FOUNDING_MAX_FUNDING_STATES_V1`)
  instead of two spelled totals, refusing each partial price-gate extension and each frame sized for a different count.
  Proved red before trusting green: perturbing `OPEN_ESCROW_POSITION` 21 → 19 and `CLAIMS_ESCROW_POSITION` 31 → 29 fails
  exactly these two, reverted. Sweep of the crate's test modules for other bare frame-count literals restating a
  declared constant: **22 sites read across 9 files -> 3 derived, 19 left.** The third derivation is `&[0; 13]` ->
  `GENERIC_MARKET_FOUNDING_INSTRUCTION_BYTES_V3` (`generic_market_founding_v1.rs:1458`), so the all-zero buffer stays the
  wrong-magic case rather than silently becoming a wrong-width refusal the day the bump count moves. The 19 are
  deliberate value pins already bound to an author beside them (`lib.rs:1058-1059` with the admits/refuses property under
  it, `admitted_composition_v3.rs:1244-1248` already tied to `HOT_FIXED_ACCOUNT_COUNT_V3` by an earlier lane, the seven
  `projected_custody_bootstrap_v1.rs` layout pins with their derived cross-checks, `series/artifacts_v3.rs:61`'s
  compile-time wire pin), offsets already read off a start constant (`hot_v3/series_expiry.rs:58,61`), or fixture-local
  lengths no constant declares (`dynamic_accounts_v4.rs:228,232`; `series/effect_v4.rs:760`'s `+ 7 +` is that test's own
  funding count). **`#[cfg(test)]` only — nothing in an SBF link moved, so no frame row is owed.**
  Left standing, not mine: `tools/local-validator/bootstrap/successor/src/market.rs:9683` still spells the Open window's
  twenty-three (`GENERIC_FOUNDING_OPEN_WINDOW_ACCOUNTS_V1`) and its neighbour's prose still says "Core's exact
  21-account Open window" — a host-side mirror of the same author, correct today and stale in the same way tomorrow.

- **COHORT-16C: the release generation moved, and an open market cannot follow it.** Devnet cohort-16.1 upgrades cohort-16's
  Trading and Core **in place** and re-releases the generation; six of the eight links never move. The ruling asked to keep
  the open market `GyD95eyE…`. **No path keeps it**, and the argument is layout rather than judgement: a market pins the
  EXECUTION RELEASE SET (`Market.release_set_id`, offset 208), which is `sha256` over five `(program_id,
  artifact_release_id)` bindings, and an artifact release id hashes its role's ELF digest **and its deployment slot**
  (`artifact.rs`, `DEPLOYMENT_SLOT_OFFSET` 176). The field is written only by `initialize_market` into an all-zero account;
  the activation cache is derived from it; Loader V3 writes the current slot on every `Upgrade` and refuses one in the
  deployment's own slot, so redeploying the *identical old bytes* gives a different set id too (`56eb1897…`, computed); and
  `lineage_walk` — the one forward mechanism — **has no consumer in any capability program**, only its own tests, under a
  doc comment describing a Core route that does not exist. `GyD95eyE…` lost nothing: it could not be activated at
  cohort-16's release either (`0x4003`, 108,180 CU), and a market that cannot activate cannot be retired. This reverses one
  sentence of the PROGRAMS-17A addendum and the `manifest-edges` verifier, both corrected in place.
- **COHORT-16C: the first funding frame on any chain that carries a dependency ledger.** Market
  `BMK3BY415TicG5nTf43ii7YncgMyVcGGySoKWeGXKLKG`, founded on release set `f533be49…` with the selected entry at index 0 and
  edges `[1,2,3]`, **ACTIVATED at 521,895 CU on 36 accounts** (`553E4rJH…`, slot 493,763,946) — 35 was the one-ledger shape.
  PROGRAMS-17A's offline real-ELF harness predicted **521,780** for this market's shape against real Core, Trading and
  Registry. **115 CU apart, 0.02%**, across a boundary neither side had run. The whole cohort-16 wall — retirement needs the
  edges, activation forbade them — is executed, not argued.
- **COHORT-16C: three defects a run had to find.** (a) **No producer binds a completed Upgrade row into the deployment-set
  journal**: the only writer of a journal is `already-current` (`upgrade.rs:2370`), the *other* disposition, so the audit
  refuses "dump exists but the set journal pins no digest" and the set is stuck — invisible for three cohorts because a
  genesis cohort's owned roles are all AlreadyCurrent. (b) **The checked-upgrade phase loop cannot beat a fast cluster**:
  it re-audits all seven roles per phase, ~44 s from `getLatestBlockhash` to `sendTransaction`, against devnet at a measured
  **6.13 blocks/s** — a 24.5 s window. Three attempts died `BlockhashNotFound` at simulation having spent nothing; the
  Upgrade's designed escape (`--adopt-existing-buffer` + `--adopt-finalized-cli-upgrade-signature`) landed both, the
  extension has none. (c) **The Loader refuses any `ExtendProgram` below 10,240 bytes**, so the driver's exact-top-up
  arithmetic is unreachable for Core's 48-byte shortfall; the extension went at the minimum and Core's ProgramData is now
  10,192 bytes wider than its ELF. Which is why **a deployed link's digest is not its candidate's**: Loader V3 zero-fills
  past the payload, so cohort-16.1's releases bind `d7430226…` (trading, 776 pad) and `691cbc69…` (core, 10,192 pad), not
  `e7f8e476…` and `29200c855…`. Whole upgrade: **0.082784640 SOL**, of which 0.052019200 is permanent rent.
- COHORT-16C closed (five commits; the evidence addendum): **the ruling's "keep the market" clause was FALSE, established
  before spending** — a market pins its execution release set (offset 208, written by exactly one function), an artifact
  release id hashes the ELF digest AND its deployment slot, so any upgrade moves the set (even redeploying identical
  bytes), and `lineage_walk`, the one forward mechanism, has no consumer in any program; six of eight links were
  byte-identical, not seven (Core moved at 17f1b6dec), and a checked set admits no mixed generation. **Cohort-16.1:
  the candidate reproduced from a second root (gate `ba0205cc…` equal); Trading and Core upgraded in place, the set
  closed 7/7, the generation re-released (14 tx, 0 errors) for 0.0828 SOL; market `BMK3BY41…` re-founded and ACTIVATED
  at 521,895 CU on 36 accounts — the first funding frame with a dependency ledger on any chain, 115 CU (0.02%) from the
  offline harness's 521,780.** Stopped at admissions: `PacketTooLarge` — the routing lookup table only the simulator's
  config supplies (a missing input; the six addresses recovered). Three defects only a run produces: no producer binds a
  completed Upgrade row into the deployment-set journal (invisible for three genesis cohorts); the checked-upgrade loop
  cannot beat a 6.13-block/s cluster (three attempts died BlockhashNotFound spending nothing); the Loader refuses
  `ExtendProgram` below 10,240 bytes. → COHORT-16D **`(spawned)`**: the table as a runbook fact, a re-found market, the
  spine to the first Retired, then the General and two-source markets.
- COHORT-16D closed (four commits; the addendum §§1-11): **the capture window was OPEN, not "long past"** — COHORT-16C's
  prose estimate was wrong and `market.json`'s own `DCLTWIN1` record was right, so nothing was re-founded. `PacketTooLarge`
  was never a defect: **no runbook row produced `sim-config.json`**, the file that carries the routing lookup table, and
  every cohort since 12 hand-wrote one. `tools/cohort/build-sim-config.py` + row `sim-config` now DERIVE that table from
  the founding's own create transaction and authenticate it (frozen, routing this market) — turning COHORT-16C's six
  recovered addresses from an input into a check — and derive the buyer's 201-atom allowance from the manifest's economics.
  **The spine then ran end to end for the first time**: capture 119,272 CU, two admissions, the fill's Hot at **1,111,824 CU**
  (ceiling 1,400,000; cohort-15's 1,137,522), fee settlement, settle (certificate kind 1 not 4), phase 1→2 with
  `terminal_receipt` carrying the certificate, and **the winning stranger paid — 0 → 600 atoms, the Hoard down exactly 600**,
  the first stranger payout on this protocol; the founder was then paid at indices 1, 0, 2 and **the Hoard reached 0**.
  **Retirement is still unreachable, and the wall MOVED.** `BeginRetiring` blocks on Claims supply at index 3 — the explicit
  failure outcome — where the aggregate minted a complete set and no Position was ever seated: every holder answers
  `payout quantity must be within 1..=0 atoms`, and no other founding identity has a Position at all. That single founding
  defect is also `VIOLATED L3` at the first census and the K+1 refusal that left a **landed** fill uncertified. The README's
  named wall (`DirectCloseCapability` behind a manifest with no dependency edges) was neither refuted nor reached —
  cohort-16.1 founded such a manifest and stopped one stage earlier. **The General market is founded and ACTIVATED**
  (`65Yq3q6t…`, root `4NcnH7pt…` Active, 520,541 CU, its tail digest equal to the one named in advance), after re-minting
  the translation-validation manifest `84ddf259…` — cohort-14's `aa97c0c1…`, reused by cohort-15, no longer hashes this
  tree (Cargo.lock, dclutch-trading and dclutch-vm all moved) — and the deployed accelerator is byte-identical to this
  candidate's ELF, so cohorts 14 and 15's source drift is gone. **A founding frame has no suffix resume**: a `timeout` at
  446 transactions left every ladder stage complete and the founding half-started, burning one market's identities.
  24 routes corroborated, 4 new; `witness --check` 6 documents, 120 routes, 0 problems. **Still no accelerator route,
  third cohort running** — only OpenBatch N=2 would give it one. → owed: the failure-outcome seating (everything else is
  downstream), the journal's Upgrade-row producer, the blockhash race, the Loader's 10,240-byte minimum, a General
  translation-validation corpus.
- COHORT-16D closed (`7d54c89b3`, `b05efae9a`, `ce1bde719`, `92dd1ab3e`, `c5fb2035b`, `cc6990fa4`): the resume note's
  "window long past" was wrong (decoded from the market's own `DCLTWIN1`: open 19:02–19:32; capture at 19:08 — nothing
  re-founded); **`PacketTooLarge` was never a defect — no runbook row produced `sim-config.json`, every cohort since 12
  hand-wrote it; the routing table now derives from the founding's own create transaction** (equal to the hand-recovered
  address, turning the input into a check); **the spine end to end on cohort-16.1: capture 119,272 → two admissions →
  fill 1,111,824 CU → fee → settle (kind 1) → phase 1→2 → the winning stranger paid 0 → 600 atoms, Hoard down exactly
  600 — the first stranger payout on this protocol — founder paid → Hoard 0.** **Retirement unreachable, upstream of its
  machinery: `BeginRetiring` blocks on Claims supply at index 3 (the failure outcome) where the aggregate minted a
  complete set and no Position was seated — the same founding defect as the first census's L3 VIOLATED and the K+1
  partition refusal that left a landed fill uncertified.** The General market `65Yq3q6t…` founded and activated (520,541
  CU; the translation-validation manifest re-minted from a real run — cohorts 14/15's no longer hashes this tree); the
  accelerator still has no witness (only OpenBatch gives it one). A founding frame has no suffix resume: a misread-clock
  timeout burned one identity set (§6 keeps the reasoning). Payer 1.417, deployer 28.658.
- Started 21:45 EDT: PROGRAMS-17B (the index-3 founding defect convicted and fixed at the founding; BeginRetiring reaches
  the checkpoint chain on real ELFs); COHORT-16E (OpenBatch N=2 on the General market — the accelerator's first witness).
- PROGRAMS-17B: **§4 and §9 REVERSED — the founding seats four of four, and the wall is retirement's, not founding's.**
  Cohort-16.1 is REFUNDING (`CategoricalQ1`, width 4, `payout_scale` 3 = `basis_width - 1`), the deployed candidate
  `87eec1c3a` contains `ebbccbd4e`, and the escrow the founding seated is on chain and always was: Position
  `7FQCfc4RrrsATEe969eNVYoLjDukmBVKMAxM1yg7AzcQ` under capability owner `Hq6sF5pv3i8C…` at `(3xoSXBVs…, 3)`, 160 bytes
  `DCLLBP02`, **`[0, 0, 0, 166666667]` at revision 1 — the founding write, never moved** (admission
  `4WUZ2qZKz7nk…`, 512 bytes). At the first census `founder [q,q,q,0] + escrow [0,0,0,q] = [q x4] = the aggregate`:
  **L3 held.** `addFrom_addBelow_eq_addEvery` in bytes. The census reported VIOLATED because L3 sums
  `self.positions`, a map an operator types, and nobody types a PDA — an aperture, not a founding.
  **The real wall, and it is unconditional: a refunding market can never retire.** The failure column is unpayable
  under every certificate (`runtime_v3.rs:972` refuses `FailureCoordinateNotPayable`; `:990` pays "nothing to the
  failure coordinate"), and its holder cannot sign — `terminal_settlement_v3.rs:635` binds coordinate 0's signature to
  the Position owner and `:620` says a Claims capability owner "has no key". `MergeRefundingCompleteSet` is the one
  route that moves the escrow and it needs a Hoard Terminal has drained. On chain the conjunct is closure's
  (`market_closure_v1.rs:656`, `Liability` = `Custom(0x5503)`); the operator hoists it to the BeginRetiring preflight,
  which is why no transaction was ever built. **Every market founded under decision 0025 is unretirable, on any chain,
  whatever the founding does** — recorded as a dated addendum on 0025 with two shapes and a PROVISIONAL ruling for A
  (closure admits the seated escrow as the terminal residue; B, a keyless-signer exemption on the terminal route, is
  reversible-to at the cost stated). Landed: the ledger DERIVES the failure escrow from the aggregate's own owner,
  logical market and width (`ledger::failure_escrow_v1`, one host author — `market.rs`'s inline spelling deleted and
  routed through it) and joins it to L3 under the reserved label `failure-escrow`;
  `the_derived_escrow_is_the_account_cohort_16_1_founded` pins it to this market's real addresses; 19/19 ledger tests,
  successor `cargo check --tests` clean. Also landed: the Direct terminal certification's `K+1 partition` rule —
  which demanded a nonzero seller row at EVERY outcome and so refused every refunding fill, cohort-16.1's landed one
  included — is replaced by a join against the two Positions' own nonzero coordinates, the invariant the count stood
  in for and strictly stronger than it (a row at the right index carrying a quantity the Position does not hold used
  to pass); 22/22 `direct_trade::tests`, the fixture's placeholder Position blobs replaced with canonical
  `encode_liability_basis_position_into_v2` bodies so the join has something to join against. Reads only, no devnet
  act; no SBF crate touched, so no frame rows are owed. → owed: the closure conjunct itself
  (`market_closure_v1.rs:656`) — a Claims ELF change, so cohort-17 carries it as re-release + re-found under 0012 —
  and the operator's BeginRetiring message, which instructs an act no party can perform.
- PROGRAMS-17B closed (`c991f8944`, `fd589ff4a`, `cb7d76c5c`, `1adda2fbf`, `a55e41d39`): **the conviction reversed on the
  evidence — the founding was correct**: cohort-16.1's market is refunding (payout_scale 3), the deployed ELF is the
  escrow-seating one, the escrow Position `7FQCfc4R…` holds `[0,0,0,q]` at revision 1, founder + escrow = aggregate, L3
  HELD; **the census was blind** (its Position map is operator-filled; nobody types a PDA) — it now derives the escrow
  from the aggregate's header (`ledger::failure_escrow_v1`, pinned to the devnet addresses); the `K+1 partition` rule
  refused every refunding fill (it demanded a nonzero seller row at every outcome) — joined against the Positions'
  nonzero coordinates. **The real wall: a refunding market's failure column is unpayable under every certificate and its
  holder has no key, so closure's `Liability 0x5503` refuses — every market founded under 0025 is unretirable on any
  chain.** Decision 0025 addendum: shape A ruled provisionally — closure BURNS the column and closes both escrow accounts
  (a tolerating repair is a rent leak) — CONFIRMED by the orchestrator. Reads only; no walk (it would have measured the
  wrong thing). → PROGRAMS-17C **`(spawned)`**: the closure burn, Lean-first; the first complete retirement on real ELFs;
  cohort-17 carries the Claims ELF as re-release + re-found.
- COHORT-16E closed (`432d07339`, `bb720fb6e`, `dd852cb49`): **the General capability seal is on chain and the
  accelerator still is not**. Landed: the first `DCLTSEL1` on any chain — seal `HH7c3zEH…` for descriptor
  `47a5303d…` action 7 (`OpenBatch`), `5tu23XBM…` slot 493,837,946, **625,058 CU**, 968 bytes, Trading-owned, at the
  address the frame report stated at fixed coordinate 38; the register now corroborates
  `trading/hot_v3::process_capability_seal_v1` (25 routes, none lost, `--check` 6 documents / 121 routes / 0
  problems). **The defect this run found is ours: `compile_general_hot_v0` declared its heap frame and never a
  compute limit**, so every General successor transaction this tree ever compiled carried the runtime's ~203,000-unit
  default against a 654,000–677,000 CU action — invisible to the harness, which pushes its own
  `set_compute_unit_limit` and so was a second author for one frame; `GENERAL_HOT_COMPUTE_UNIT_LIMIT_V3` repairs it
  (+8 wire bytes on all seven N=258 rows, no account count moves, widest 940/1,232; no SBF-linked crate touched, no
  frame rows owed). **OpenBatch then refuses `TradingSbfError::Content` `0x4003` at 239,473 of 1,399,700 units with
  no CPI in the log, and could not be localized**: 1,686 sites, and the tree's `hot_cu_checkpoint!` instrument is
  feature-gated while a release pins the ELF digest, so it cannot run on a release-pinned chain. Ruled out by
  measurement: the ceiling; the caller-authority probe span (all four addresses replaced via
  `--parent-request-digest`, CU identical to the unit); the seal's absence; the sealed-alias shape. Two runbook
  defects named: `openbatch-refounded` passes no `--parent-request-digest`, so its route always names four
  caller-authority PDAs derived from
  `sha256(b"dclutch:devnet-general-session:caller-authority-probe:v1")` — the composition is a two-pass loop and
  nothing says so; and **`CloseBatch` has no producer at all** (`SESSION_ACTION_V1` is hardcoded to `OpenBatch`, no
  `--action`, no other route emitter), so two of this unit's three acts cannot be composed whatever the chain
  answers. Payer 1.417282684 → 1.384214644 (0.005567680 permanent seal rent, three frozen routing tables, fourteen
  fees); deployer 28.657732010 unchanged. → owed: the `Content` phase (needs an instrumented replay against a
  release the chain pins, or a checkpoint instrument that survives release pinning); a `CloseBatch` producer; the
  runbook's two-pass digest; and `docs/reference/route-witnesses.md`, which the convergence owner regenerates and
  which was already owed cohort-16.1 before this lane.
- COHORT-16E closed (`432d07339`, `bb720fb6e`, `dd852cb49`, `8636f1287`): the General routing table derived from the
  founding's create transaction (`DfLj84Cz…`); **the capability seal landed — the first `DCLTSEL1` on any chain**
  (625,058 CU; the verifier's "Trading and ComputeBudget only" was wrong — System creates the seal); **a wall of ours:
  `compile_general_hot_v0` pushed a heap frame and no compute limit, so every General transaction this tree ever
  compiled carried the runtime default (202,850) against a 654–677k action — the harness set its own limit, two authors
  for one frame** (fixed; +8 wire bytes, no account count moves); **then the deployed Trading refused OpenBatch
  `Content 0x4003` at 239,473 CU before invoking the accelerator** — 1,686 sites, the ceiling/probe span/seal/alias
  ruled out by measurement, unlocalizable on a release-pinned chain (the phase instrument is a test feature); two runbook
  defects: no `--parent-request-digest` (a probe digest names four caller-authority PDAs no execution derives) and the
  session's action hardcoded to OpenBatch (CloseBatch in no driver). 25 routes corroborated; the accelerator still has
  no witness after three cohorts. Payer 1.384. → PROGRAMS-17D **`(spawned)`**: the instrumented replay of the devnet
  frame; the fix at the author; the driver composes every General action.

- **PROGRAMS-17C** — 2026-09-05 late night. Decision 0025's shape A, from the law down. **The Lean**
  (`EconomicKernel.lean`, eleven declarations, zero `sorry`, axioms `propext`/`Quot.sound`): the WALL first, as
  `a_seated_failure_column_forecloses_retirement` — `retireTerminal` wants `allZero supply` and the residue is not zero,
  so `accepts` is false and `execute?` refuses `notAdmissible`, instantiated against nothing but the residue itself;
  then why it is stuck, as `the_failure_column_is_owed_nothing_under_every_certificate` — `escrowedRedemptionPayout` is
  zero at the failure selector under EVERY terminal phase, which the file previously had only for the outage; then the
  burn, `closureBurnPost`, which does not touch the Hoard and IS the kernel's own `redeemPost` off the failure walk
  (`the_closure_burn_is_the_kernels_zero_payout_redemption`), with `the_closure_burn_conserves_L1_and_L3`,
  `the_closure_burn_touches_no_ordinary_coordinate`, `the_closure_burn_empties_the_escrow` (the conjunct
  `protocol_position_v2.rs:608` refuses on) and `the_closure_burn_admits_the_retirement_it_foreclosed` — the same
  predicate, nothing relaxed. `lake build` green across 137 jobs. **The operator**: the `BeginRetiring` preflight said
  "produce and execute wallet terminal payouts first" for every nonzero supply, which on a refunding market's failure
  column is an instruction no party can follow. It now separates five facts that share one number — escrow unobserved,
  escrow absent, escrow partial, observation substituted, escrow seated — and only the seated arm loses the payout
  sentence, replaced by the escrow, its keyless owner, and the burn that is owed. That arm was RED before the commit and
  its failure printed the old sentence verbatim. The escrow derivation moved to `dclutch-operator` as the one host
  author (the census was the first consumer, the preflight the second) and still reproduces cohort-16.1's real
  `7FQCfc4R…`. 2/2 + 2/2 + 19/19. **What it did NOT do, named**: the closure route itself. `execute_basket` turns out to
  be reachable from no live path — the live aggregate is `DCLLBM02` with no phase and no Hoard, and the writer is
  `signed_delta_v3::apply_coordinate` — so shape A is a coordinate debit needing the linked basis record and the escrow
  pair in BOTH the Claims closure frame and Core's four-packet retirement frame, plus frame rows for two links. Decision
  0025's fourth section states the whole route, the forced ordering and the hostiles; `tools/cohort/README.md`'s
  `retire` row now says a refunding market cannot reach it. No devnet act; no SBF crate touched.
  **And then the witness.** Every reading since 17B named the closure's `Liability` conjunct from source or from a host
  message; cohort-16.1's refusal arrived as a preflight sentence and no transaction was ever built, so the ON-CHAIN
  refusal had never been seen refuse. `a_seated_failure_column_refuses_the_claims_handoff_by_name`
  (`market_retirement_v1_lifecycle.rs`) is its first, on real ELFs through the real Core-into-Claims CPI: plant the
  residue terminal settlement leaves, submit the retirement's prepare packet, and the transaction reports **`0x5503`,
  the Claims closure's own `Liability`** — NOT Core's `ChildCpi`, which `retire_v1.rs:585`'s `map_err` would have
  suggested; the runtime surfaces the inner code, so an operator reading a failed retirement reads the Claims band and
  goes to the right program. The residue is planted after the plan is built on purpose: three authors carry that
  conjunct — the retirement builder (refusing its coarse `Claims`, the code ten identity mismatches carry), the
  preflight, and the program — and only the third is the chain. Positive control: the suite's own
  `checkpointed_retirement_is_packet_bounded_resumable_and_conserving` drives the same fixture through the same packet
  with a zero aggregate and passes, so the refusal is the column and not the setup. Seven ELFs built locally, zero frame
  diagnostics, `cargo-build-sbf 4.0.0 / platform-tools v1.53 / rustc 1.89.0`; macOS-host artifacts, not the canonical
  Linux release digests — claims `cf11356f…12e6`. No SBF crate touched (every program's `dclutch-operator` edge is a
  dev-dependency and `tools/gates/frames.py:273` walks normal and build edges only), so this lane owes no frame rows.
- PROGRAMS-17C closed (`f4a951b78`, `bc9f9b4d6`, `b2d774311`, `1475f21e7`, `b3d34814d`): **the wall written down in Lean for
  the first time** (`a_seated_failure_column_forecloses_retirement`); the failure column owed nothing under EVERY
  certificate (the file had only the outage case); **the closure burn IS the kernel's own zero-payout redemption off the
  failure walk** — conserves L1/L3, touches no ordinary coordinate, empties the escrow, and admits the retirement it
  foreclosed under the same predicate (zero sorry, 137 jobs); the operator's preflight stops instructing an act no party
  can perform (five arms, red-proven printing the old sentence); **`0x5503 Liability` seen refusing on real ELFs for the
  first time** (the runtime surfaces the child's code, not Core's `ChildCpi`). **The route itself not shipped, on a
  finding: `execute_basket` is reachable from no live path (it decodes `DCLTEMK2`; a founding writes `DCLLBM02`) — shape
  A is a coordinate debit through the one live writer, needing the linked basis record and the escrow pair in both the
  Claims closure frame and Core's four-packet retirement frame; two ELFs move.** Third-party finding: the retirement
  operator's `Claims` code is one over ~11 conjuncts. → PROGRAMS-17E **`(spawned)`**: the debit in both frames; the first
  complete retirement on real ELFs; cohort-17 carries both links.

### 2026-09-06 00:20 EDT — PROGRAMS-17D: the coarse refusal is convicted, and both OpenBatch walls are the host's
- **"Could not be localized" is reversed.** A hot route never hashes an ELF — `slot_pinned_release_elf_digest_v1`
  compares the ProgramData header's slot and authority and returns the release's own digest — so both release pins live
  in 45 bytes and the profiled build can run behind them. `programs/dclutch-trading-sbf/program-test/devnet-replay`
  loads cohort-16.1's own `transactionBase64`, its 55 accounts at finalized and its lookup table into ProgramTest and
  reproduces `InstructionError [2, Custom(16387)]` offline. Recipe: `docs/design/DEVNET_FRAME_REPLAY_V1.md`; addendum on
  `docs/evidence/COHORT161_UPGRADED_SEALED_2026_09_05.md`.
- **WALL 1, the cohort's 0x4003**: `hot_v3/lifecycle.rs:1421`, the first conjunct of
  `authenticate_lifecycle_credit_v3` — `account.key != market.rent_beneficiary` — at plan 0, invocation 0,
  RentCredit coordinate 7. The route named `CE3PC9fY…`, the PREVIOUS General market's credit (market `8MSesLUs…`,
  generation 1); `65Yq3q6t…`'s own `rent_beneficiary` at offset 296 is `6FGsfzP7…`, generation 2, live. The author is
  `--rent-credit`, a required address the session never joined to the market, filled by the runbook from a cohort
  variable. Fixed: the Market is the author, the flag is an optional cross-check that refuses when it disagrees.
- **WALL 2, revealed by fixing wall 1**: swap that one lookup-table entry and the same frame walks 65,000 units further
  and refuses `0x4001` at `admitted_composition_v3.rs:749`, 319,075 CU, still with no accelerator invoke. The chain
  seeds every caller-authority PDA with `family_request_digest_v3` (`sha256(domain ‖ 0 ‖ len ‖ request)` = `ff3ace48…`);
  the operator published the bare `sha256` (`6ac82555…`) in two places. **That reverses the addendum's §3**: the "real"
  digest was never real, which is why all four addresses moved between passes and the refusal did not.
- Also convicted and NOT repaired: `shadow_accelerator_auth/deployment.rs:103-108` folds `DeploymentSlotMismatch` into
  `Content` through a bare `_`, though both enums carry the discriminant and Trading publishes `0x400a` — decision 0007's
  `map_err(|_| Coarse)` with the cause already computed. It cost this lane a false conviction before the instrument
  caught it.
- `--action` is a flag on `devnet-general-session` (all fifteen spellings; the thirteen it cannot frame refuse by name);
  `openbatch-refounded` retires at 15 and `openbatch-two-pass` replaces it; `seal-general` emits its own frame report
  instead of reading the stage it blocks; `close-batch` and `second-open-batch` exist at since-17 with their blocker
  named. check-steps green for 14/15/16 and `--prove-frozen` still byte-exact.
- **COHORT-16F needs no redeploy.** Both walls are the host's; the deployed Trading `e7f8e476…` at `87eec1c3a` is
  correct. Owed: wall 2 has never been shown green on any chain, nothing past `cx-cpi-buffers` is measured, and
  PROGRAMS-17D owes `tools/gate frames --at HEAD --capture` for the diagnostics commit.
- PROGRAMS-17D closed (`fc39a5bc7`, `2a8784685`, `4fba7a8af`; hbox only): **the evidence's "could not be localized" reversed
  — a hot route never hashes an ELF (the registry compares the ProgramData's slot and authority), so the profiled build
  runs behind the release pins; cohort-16.1's own transaction, its 55 accounts and its lookup table replay offline and
  reproduce `0x4003`.** Wall 1: `lifecycle.rs:1421`'s first conjunct — the route named the PREVIOUS General market's rent
  credit (`CE3PC9fY…`, generation 1) while the market's own beneficiary is generation 2 (the session copied `--rent-credit`
  into the route joined to nothing; the Market is the author now). Wall 2, one swap later at 319,075 CU: `0x4001` at
  `admitted_composition_v3.rs:749` — the chain seeds caller authorities with `family_request_digest_v3` (domain-separated)
  and the operator published a bare sha256, so "the real digest" was never real. Both host; **the deployed Trading is
  unchanged and correct — no cohort-17 for OpenBatch**; `--action` real (fifteen spellings; thirteen refuse by name);
  `openbatch-two-pass` replaces `openbatch-refounded`; the devnet-replay instrument at
  `programs/dclutch-trading-sbf/program-test/devnet-replay` with `docs/design/DEVNET_FRAME_REPLAY_V1.md`. Owed: wall 2
  never shown green on chain; a `map_err(|_|)` folding `DeploymentSlotMismatch` into `Content`; frames red (inserted
  lines move the ELF) — recaptured with PROGRAMS-17E's build.

### 2026-09-06 01:05 EDT — PROGRAMS-17E: the closure burns the column, and a refunding market retires
- **THE WALL IS DOWN, on real ELFs.** `a_refunding_market_retires_once_the_closure_burns_its_failure_column`
  (`crates/dclutch-svm-harness/tests/market_retirement_v1_lifecycle.rs`) drives the four checkpoint packets from a
  prestate where every ordinary coordinate is paid and the failure column stands in the escrow the founding seated:
  `prepare` burns 166,666,667 units, closes the escrow's Position AND its admission, hands their rent to the
  checkpoint, and the market reaches `Retired` with the refund wallet paid to the lamport. It is the first complete
  retirement of a refunding market anywhere. 17C's `a_seated_failure_column_refuses_the_claims_handoff_by_name` is
  unchanged and is now its negative control: the same state, the thirty-five-account frame, `0x5503`.
- **ONE AUTHOR FOR THE DEBIT.** `signed_delta_v3::apply_coordinate` stays private; `burn_failure_coordinate_v1` sits
  beside it, derives the coordinate from `refunding_failure_index`, refuses every other by name, and writes no account.
  The credits-only gate is untouched and now has the named debit beneficiary its asymmetry always implied.
- **ONE ACCOUNT PROVES THE RECORD.** `semantic_basis_id_v3` becomes the sole author of a record's semantic identity (it
  had four hand-spellings) and the closure compares it to the aggregate's own `basis_id`. The preimage carries kind,
  width and payout scale — exactly what `categorical_refunds_on_failure_v3` reads — so a record that reproduces a
  Market's `basis_id` cannot disagree with it about whether that Market refunds. No Product-graph walk, no staging.
- **FRAMES.** Claims closure 11 → 14 (12 → 15 with the continuation); Core's checkpointed retirement 35 → 38 on ALL
  FOUR packets, because the journal requires one frame per retirement. Trailing and optional: a categorical retirement
  is byte-for-byte the thirty-five that shipped and its four extents did not move. No request byte changed.
- **CU, categorical in parentheses**: prepare 139,558 (123,398), close-vault 128,956 (128,241), close-replay 112,226
  (111,511), finish 91,698 (90,983). The burn costs **16,160 CU**; each extra account ~715. The Claims closure's stack
  frame goes 2,176 → 2,752 of the 4,096 bound, which is the row to watch next.
- **FOUR HOSTILES BY DISCRIMINANT**, honest packet last from the same fixture as the control: not the derived escrow →
  `0x5010` (the code three other routes already raise); substituted or non-refunding basis record → `0x5506` (`Basis`,
  split from `Identity` because it sends its reader to another program's record); a stranger holding one unit of the
  column → `0x5503`; an escrow holding a tradeable claim beside the residue → `0x5503`. A debit of an ordinary
  coordinate through the new arm refuses at the arm.
- **NO LEAN CHANGE WAS FORCED, and that is a finding.** `EmitClaimsMarketClosureV1Rust.lean` and
  `EmitMarketRetirementV1Rust.lean` emit request/receipt widths, magics and field offsets — the frames' account counts
  are hand-written Rust and are emitted by nothing. 17C's `closureBurnPost` law stands as the semantics; there was no
  generated layout to go Lean-first through.
- **REPAIRED IN PASSING**: `market.rs` reached the escrow derivation through `crate::ledger`, a module one of the three
  campaigns compiling that file has, so the ladder and relayed-vertical campaigns had been red since `bc9f9b4d6`. The
  derivation moved once more, into `dclutch_claims::protocol_position_v2`, because the retirement builder is its third
  host consumer; `dclutch-operator`'s module is the host name for it and keeps the cohort-16.1 pin.
- **Evidence and controls**: 7 ELFs, Linux, `cargo-build-sbf` under platform-tools v1.53, **zero frame diagnostics**.
  16/16 retirement tests attempted, 12 pass, and the same 4 fail identically **at HEAD on the same box** with
  `RpcError(DeadlineExceeded)` in the Pyth provider-update step — environmental, and the control was a pristine
  `git archive HEAD` tree with its own ELFs. 93 claims-sbf lib, 319 dclutch-claims, 70 dclutch-product, 812 bootstrap,
  364/384/367 journey/ladder/relayed-vertical, `tools/gate census` PASS at 352 codes.
- **Frame rows carried** (`b1fe0193d`, two identical captures of `7d45d6ba3`). Trading's `prepare_lifecycle_v4` moves
  2,176 → 2,240 and this capture cannot say whose it is: 17D's `hot_v3.rs` edits and this lane's `semantic_basis_id_v3`
  are both in that link's closure. Named rather than assigned.
- **OWED**: the devnet retirement path passes `None` for the tail because the linked basis record's address is carried
  by the founding producer and not by the terminal evidence — one argument to thread, and until then a devnet
  refunding retirement builds the 35-account plan and is refused `0x5503` by name. The retirement builder proves the
  escrow pair's canonical shape but does not re-derive the capability owner (the chain does). This is ProgramTest, not
  validator evidence. **Cohort-16.1 cannot be repaired in place**: decision 0012 forbids changing a founded release
  set, so cohort-17 is a re-release of every program plus a re-found, and `7FQCfc4Rrrs…` stays where it is.
- No devnet act. No cluster contacted. `MarketRetirementOperatorErrorV1::Claims`'s conjuncts are named for 17C's
  third-party finding: eleven in one disjunction at `crates/dclutch-market-retirement-v1-operator/src/lib.rs:1140-1151`
  (owner, executable, zero lamports, the derived aggregate PDA, `claim_count < 2`, logical market, release set,
  registry program, product instance, realm, generation) plus the zero-supply sweep, which this lane split out into
  `authenticate_failure_escrow` so a seated column is admitted rather than refused.
- **The two ELF digests, canonical Linux**, built from a detached worktree at `7d45d6ba3` on hbox with
  `cargo-build-sbf 4.0.0 / platform-tools v1.53 / rustc 1.89.0`, zero frame diagnostics:
  Claims `26809c62bf1ffa675ec73a746f4c9956e8d734612c59b04f9b827b2aa1a2ecdc`,
  Core `2f28309fee8c8d2dde6c141ae65c16ad1a24a2bcdb5284b441984d9def01e0f0`. These are the two links cohort-17 re-releases;
  every other role's ELF is unchanged by this lane and its digest is the cohort's existing one. `tools/gate frames owed`
  is clean over `7d45d6ba..bcf346d7`.
- **Also owed, and small**: `tools/gauntlet/retirement-checkpoint/run-retirement-checkpoint.sh` filters to the
  categorical test and asserts exactly 12 recorded transactions, so the refunding walk's four packets and its four
  hostiles reach no census binding. Folding them in is a count, a filter and eight bindings — not a route.
- **The founding half of the loop is green on the SAME Claims ELF.** The audited Token-2022 v11 fixture
  (`e2acdfb750881462ad613a15cc9c54ae17ce066580e867e1e635fbdfe01f5697`, matching
  `programs/dclutch-claims-sbf/fixtures/token-2022-v11.provenance`) was already on hbox, so the Claims fractional-atomic
  program-tests ran against `26809c62…`: **39/39** — `claims_founding` 5 (including
  `a_refunding_founding_seats_the_escrow_on_the_real_elf`, the write this lane's closure burns),
  `claims_conservation` 5, `escrow_pda_handover` 2, `fractional_atomic` 13, `permissioned_burn_wall` 2,
  `fractional_compaction` 12. Zero frame diagnostics on the three test callers plus `founding-caller`.
  The first attempt reported 0/5 with a missing-ELF assertion at `claims_founding.rs:213` — an INSTRUMENT gap
  (`dclutch_claims_founding_test_caller_sbf.so` was not staged), not a refusal, and the distinction is the whole reason
  that assertion names the file it could not read.
