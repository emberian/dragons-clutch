# Ledger — 2026-09-07

Dated deltas for 2026-09-07; the index is GOAL.md.

### 2026-09-07 03:30 EDT — TWO FUNCTIONS WALLED THE FRAME RATCHET, AND ONE OF THEM COULD NOT EXECUTE AT ALL
- **`process_dealer_fill_v1` was never a ratchet problem, and the proof is a runtime one.** The frame is **4,288 →
  4,160 → 3,456** bytes and the diagnostic count **21 → 0** on both links that carry the route. The lift is this
  tree's own idiom, a stage per frame: `admit_fill_v1` holds the inventory, `AdmittedFill` and the two decoded Claims
  views — none of them read once the rule admits — and hands back seven values; `settle_cash_legs_v1` and a stage per
  leg hold the three `CustodyLegV1`s that were live in the route's own frame at once. **Passing the leg by reference
  was measured first and moved the frame by exactly zero bytes**, because `invoke_custody_transfer_v1` takes it by
  value and the temporary stays where it was built: a call boundary moves a local, an `&` does not. **The conviction
  is the control.** The new campaign was run against the PRE-FIX ELF and the fill dies — `failed: Access violation
  reading 8 bytes at address 0xd3 (in unallocated region)`, after 78,718 CU — while all fourteen hostiles still refuse
  by their correct names against that same ELF. **That is why a refusal-only campaign would have stayed green**: only
  driving the route to its receipt reaches the depth that overruns. Since the dealer landing, `main` has carried a
  route that could not complete a single fill.
- **The dealer family's first real-ELF test, and what it does not cover.**
  `programs/dclutch-trading-sbf/program-test/scoring-dealer` drives one `DealerFill` over the exact 19-account prefix
  and 64 window accounts against the real ELF: fourteen hostiles each refused by its exact `ScoringDealerErrorV1` code
  read out of the runtime's own logs — Request, Frame (twice, from two different substitutions), Fund, FundStale,
  Width, RuleSeal, Phase, Release, Custody, Position, Claims, OffSchedule, NotNormalized — and one accepted fill at
  **82,844 CU** that returns the receipt and advances the fund by one revision. **Said out loud: the accepted case is
  the NULL fill** (`mint = 0`, `receive = deliver = 0`), which is the only shape that reaches the receipt without a
  CPI, because every nonzero coordinate produces either a Claims signed delta or a Custody transfer leg. A fill that
  moves a claim or an atom needs the founded aggregate, the admitted Positions, the opened vaults and the SPL mint
  that only a full Claims + Custody fixture supplies. **That fixture is owed and this campaign does not pretend to
  it.**
- **The wall was two functions, not one, and the second is on a link the census had measured at zero.**
  `resolution-proof::relay_transport_v1::plan_and_encode_ensemble_fold` measured **exactly 4,096** against SBPF v0's
  4,096, four diagnostics. It held `EnsembleFoldPlanV1` by value — a Source state, a certificate, a receipt, a
  complete three-row ledger poststate, the per-member bounties and captors — and beside it the `Box::new` literal of
  the full wire image of all four. Two stages later, `plan_ensemble_fold_boxed_v1` (1,344) and
  `encode_ensemble_fold_v1` (1,920), the function is at **64 bytes** and the link is at zero. **Attribution, stated
  exactly**: the link was at zero at FRAMES-CENSUS's control `1775f134d` and at four diagnostics at `c15e02b59`; the
  function was introduced by `e100878f4` (CONVERGE-recovery-capture), but **that commit does not compile** — an
  unclosed delimiter in `resolution-proof/src/lib.rs` — so no measurement can place the wall at it rather than at
  `8e22424f1`, the rebase that first carried a buildable version of it. `dcbd96cea`'s message says "the wall arrived
  with `e100878f4`" and that is one step past what was measured. **The ensemble fold has no real-ELF test either**,
  which is how a frame at the wall sat on `main`; that one is the ensemble family's to write.
- **The frame baseline is accepted and the dam is empty.** Two captures of `dcbd96cea` from two independent clones on
  hbox under platform-tools v1.53 are **byte-identical** (`2c2d7d8c…`), all eight links freshly compiled, **zero
  `overwrites values in the frame` diagnostics on every one**. `tools/gates/frames-baseline.json` now pins **1,849
  frames across eight links**; the deepest frame in the tree is **3,968 bytes**, tied across `trading::outer::
  process_close`, `core::generic_founding_v1::authenticate_claims_and_custody`, `custody::projected::
  realize_and_close` and `resolution-proof::pre_market_funding_abort_v1::authenticate_release_and_caller` — 128 bytes
  of headroom, and the ratchet is now exact in both directions over all of it. **`tools/gate frames owed` goes from 85
  commits across 24 lanes to zero**: every debtor since `1dd18be9` is discharged, including all 74 the merge queue
  counted (CONVERGE-DEALER 12, BUILD-general-lifecycle 10, CONVERGE-general-lifecycle 7, CONVERGE-recovery-capture 6,
  CONVERGE-ECONOMICS 6, CLOSEOUT 6, and eighteen more).

### 2026-09-07 04:10 EDT — THE BUILD WAVE LANDS: TEN FAMILIES, ONE SKIPPED
- MERGE-QUEUE-1 closed. **Ten of the eleven READY families are on `main`**, each a `--no-ff` merge, each green before the
  next started, each cut to the public repo separately: deploy-lifecycle (`31de5e2bd`), failure-arm (`74a2d00c8`),
  general-lifecycle (`4bfe6c6b2`), claims-split-merge (`367749979`), economics (`5042f9a08`), founder-bond (`98eb0deb8`),
  dealer (`1ec574ce5`), series (`ff082ad91`), product-shapes (`d5768f5d1`), batch-spine (`b5f277e63`). The order was the
  wave's own advice and it paid twice: `deploy-lifecycle` first meant ONE branch moved TEN refusal codes at landing 9
  instead of ten branches moving one, and `failure-arm` second meant `founder-bond`'s band macro refused the merge until
  `0x5013` was written — fail-closed, one literal. **`joint-clearing` is SKIPPED and its merge aborted**: the family
  declares itself NOT READY, its two on-chain writes have no frame to happen in, and it hands the merge lane an
  unfinished V2 migration across four program-test files it never compiled — authoring what those walks assert is the
  family's work, not a conflict resolution. The interesting conflict there was resolved before the abort and is recorded:
  general-lifecycle had split `InvalidPlan` into nine clause enums while joint-clearing kept the coarse code, so the
  answer is main's refusal clauses with joint-clearing's types, and taking either side whole re-coarsens nine refusals.
  **The wave's own instruments found two reds nobody had run**: `lake build` was failing on
  `RelayedVenueDecodingRulesV1` (an `Int.ofNat` coercion `simp` left standing) in a file `product-shapes` authored 254
  lines of and never elaborated — which that family's notes asked the merge to check — and
  `generate-stage-scripts.py` refused BOTH cohort-17 and cohort-18 after claims-split-merge's rows landed, a deployed
  cohort's runbook stopping emitting, with `check-steps.py` green on the same rows. Regenerated once at the end:
  `tools/gate reference --converge` to its fixpoint (**149 → 160 routes, 370 → 448 refusal codes**), emission coverage
  91 → 100 generated with unguarded 0 throughout, and the whole 146-module Lean library. `tools/gate cheap` is nine of
  ten; `seam` is left red on purpose (21 findings, and `--write` would adopt other lanes' hazards as accepted). The frame
  ratchet is left RED across seven families and needs two independent SBF captures on the canonical builder.
  **`batch-spine` is a re-release obligation under decision 0012**: it moves every artifact identity a live Direct market
  pins, so the next cohort redeploys the Direct set and re-founds. Evidence:
  `docs/evidence/BUILD_WAVE_MERGE_2026_09_07.md`.
- **The merge queue closed: ten of eleven families are on main**, each merge green and each cut separately
  (`31de5e2bd` … `b5f277e63`; cuts `9284f45c6` … `5ad100d1e`; final `01273cadc` published as `5fe23b653`). **The order
  paid twice**: landing deploy-lifecycle first meant ONE branch moved TEN refusal codes instead of ten branches moving
  one; landing failure-arm second meant the band macro's contiguity assertion REFUSED founder-bond's merge until
  `0x5013` was written — fail-closed, one literal, exactly as predicted. **Joint-clearing skipped, and the reason is
  worth more than the merge**: both conflicts were resolved (main's nine refusal-clause enums with joint-clearing's V2
  types — the right answer), but the family hands over an unfinished V2 migration across four program-test files it
  never compiled, and its joint arm makes the row shape single-outcome where every main fixture is all-outcomes, so
  completing it means authoring what those walks assert. **Two things the wave's own instruments caught that nobody had
  run**: `lake build` was FAILING on a file one family authored 254 lines of and never elaborated; and
  `generate-stage-scripts.py` refused cohort 17 AND 18 after a family's rows landed — a DEPLOYED cohort's runbook
  stopped emitting while `check-steps.py` stayed green on the same rows (cohort-17's 69 scripts proved byte-identical
  before and after the whole queue). Reference regenerated at its fixpoint: **routes 149 → 160, refusal codes 370 →
  448**; emission coverage 91 → 100 generated, unguarded 0; the fmt baseline SHRANK 120 → 106. Owed: the frame ratchet
  (red across seven families), the seam register, the ELF campaigns nobody has run, and **batch-spine's re-release
  obligation** — it moves every artifact identity a live Direct market pins. One finding of the lane's own: two live
  Custody routes are **not enumerated by the route census at all**, because it reads an inline dispatch shape and those
  two go through a helper — found by planting blocking entries and watching the census call them stale.
- CONVERGE-RECOVERY-CAPTURE closed, **READY** (branch at `957fd0b35`, rebased three times as the queue advanced; the big
  rebase resolved eleven landed families by hand): **`process_ensemble_fold` is written**, and so is a second
  undispatched action — a planner, two seed types, a frame validator and two instructions that had no caller anywhere
  now have one. **Two frames had to GROW because they could not serve their own routes**: the fold's prefix carried no
  primary source spec and no statistic spec, so member zero's route and the median's scale would have come from the
  caller; and the reclaim frame could not know the quorum, so its stated conjunct was unenforceable. The ensemble's six
  laws run under their Lean names, each **broken first as a positive control**. Both ladder walks are the default now,
  with arm-scoped witnesses — the old shape was wrong in both directions: capture witnesses went green on the exhaust
  walk, and three others declared themselves red on the capture walk on purpose. **A correction to my merge plan:
  "renumber around another branch" is impossible for any branch in isolation** — the band macro asserts a contiguous run
  from the base, so a hole is a compile error and land-then-renumber is the only order there is. Findings about main:
  two workspaces outside the root one were red and only one gate compiles them; a test named for refusing an unknown
  action was red and unrun because it named a byte a new action had taken; `seam` and `clippy` are red on main itself,
  measured against a clean worktree. **The ensemble is unreachable on any chain and now says so**: no founding writes an
  ensemble material and no route writes a fragment, so its observed count is exact rather than optimistic.
- MERGE-QUEUE-2 closed. **The eleventh family is on `main`**: `recovery-capture` merged at `ab29bbe97` with **no
  conflicts** — the lane had rebased itself through all ten landings — and the reference regenerated separately to its
  fixpoint at `15934ad5a`; cuts `d63e4696a` and `e9773f660`. **The codes stand as taken: `EnsembleMember 0x8029`,
  `EnsembleQuorum 0x802A`, `EnsembleQuorumMet 0x802B`**, and the family's correction to the wave's plan is the
  generalisable part — `pin_refusal_band!` asserts a contiguous run from the band base, so a hole is a compile error and
  **"renumber around an unlanded branch" is impossible for any branch in isolation**; land-then-renumber is the only
  order there is. **Routes 149 → 160 → 163 → 165, refusal codes 370 → 448 → 451**: the 163 is no new code at all, it is
  `673220e24`'s census recogniser reading a guard's BODY instead of its name and finding three routes that were always
  there — including the two live Custody wires the first queue could only find by planting blocking entries. All three
  reach the published surface here. `cargo check --workspace --tests --offline` is **exit 0 with ZERO errors**, which
  **corrects §6 of the wave's evidence**: the two wallet-terminal wasm crates it carried forward as red through four
  landings had been repaired at landing 6 by `founder-bond`'s own `3f9e3c810`, whose message says so — a control
  re-asserted at each step stops being a measurement. Gates at `15934ad5a`: census PASS (165/451, 0 stale), emission
  PASS (101/101 guarded, 0 unguarded), `reference --check --converge` is the fixpoint, cheap eight PASS / `seam` FAILED
  / `commands` NOT RUN in an isolated worktree. `seam` is **28** and `clippy` is **five red packages hiding 37**, both
  measured rather than asserted — and clippy is five rather than six because this family repaired one line of main's
  that made `dclutch-source` red, and **a red package hides every package above it**. **An incident, found because a cut
  was rejected non-fast-forward: the public repo's `dclutch/` tree was `build/joint-clearing`'s** — `tools/cut.sh` cuts
  from HEAD and the live tree was parked on that lane's branch, so a correct script published a correct tree of an
  unmerged, self-declared NOT-READY branch; `d63e4696a` restores `main`, and a branch guard in `cut.sh` is owed.
  **`joint-clearing` is still not landed and was not forced**: its lane was still committing at 02:32 (12 ahead,
  `5e76533f3`) with no merge notes, so §3's reasons stand. Owed, unchanged and now one family larger: the frame ratchet
  (eight of eleven, two SBF captures), batch-spine's re-release obligation, the seam register, the ELF campaigns, and
  the producer-missing work — to which the ensemble adds two, since no founding writes an ensemble material and no route
  writes a fragment. Evidence: `docs/evidence/BUILD_WAVE_MERGE_2026_09_07.md` §8–§10.
- MERGE-QUEUE-2: **`recovery-capture` LANDED** (merge `ab29bbe97`, zero conflicts, 61 files +7,806/−680; cuts
  `d63e4696a`, `e9773f660`, `555e4b6d5`); `joint-clearing` NOT landed and not forced — its lane is still live. **The
  workspace is now fully green: `cargo check --workspace --tests`, 98 members, ZERO exceptions** — and the two wasm
  crates the first queue carried as "red on main" had been fixed four landings earlier by the family that broke them,
  which said so in its commit message: **a control re-asserted at every step had stopped being a measurement.** Census
  165 routes / 451 codes (149/370 before the wave); emission 101/101; the reference at its fixpoint. **Two things the
  queue found about the machinery rather than the code**: the publication host had been carrying an unmerged branch —
  the live tree is a shared checkout, a lane had parked it on `build/joint-clearing`, and `cut.sh` cuts from HEAD, so a
  cut published that branch's tree and the tree-identity gate could not tell (restored, and **a branch guard now
  refuses it, proved both ways from a lane's branch before landing** — `1ac22f45e`); and the twelfth family's ledger
  entry lives on its own branch on purpose, because a duplicate would make its merge conflict on a paragraph that
  agrees with itself. Owed: the frame ratchet across eight of eleven families, the seam register's 28, clippy's five
  (five not six — one family repaired the package that was hiding every package above it), the ELF campaigns,
  batch-spine's re-release obligation, and the twelfth family.

### 2026-09-07 02:30 EDT — THE FRAME RATCHET IS WALLED BY ONE FUNCTION, AND THE CENSUS ENUMERATES ITS OWN BLIND SPOT
- **No frame baseline was accepted, and the reason is a refusal rather than a lane's choice.** Eight links were built on
  hbox under `swarm-build` on the canonical builder (Linux/x86_64, platform-tools v1.53) from **two independent clones**
  at `1775f134d`, and both roots refused identically at the first link: **21 `overwrites values in the frame`
  diagnostics, every one of them on ONE symbol** —
  `dclutch_trading_sbf::scoring_dealer_v1::fill::process_dealer_fill_v1`, whose frame is **4288 bytes, 192 over the
  4096-byte SBPF v0 wall**. Two independent instruments convict it: the backend's diagnostic, and
  `tools/sbf-frame-sizes.py`'s at-or-over-the-wall exit, which `tools/gate frames` treats as a refusal in its own right.
  A per-link sweep says the damage is exactly two links and one function — `dclutch-accelerator-sbf` and
  `dclutch-trading-sbf` at 21 each (the accelerator links Trading under `dealer-family`), and **claims, core, custody,
  registry, rent and resolution-proof at zero**. **The control is decisive**: the same sweep at `67ecd841b`, main before
  the wave, reports **zero on all eight**. The wall is the wave's, and it is the `dealer` family's (`1ec574ce5`). **The
  obvious suspect was measured and refuted**: passing `CustodyLegV1` by reference at all five call sites — the three-way
  by-value temporary was the visible candidate — left the frame at **exactly 4288**, unmoved. Lifting it is a split of
  the route, and that is the dealer family's work, not an instrument lane's: by this queue's own evidence document that
  family has **no real-ELF program test and no CU measurement**, so nothing in the tree can show a re-shaped
  `process_dealer_fill_v1` still executes. `tools/gate frames owed` names the size of what is dammed behind it: **74
  commits from 21 lanes** since the baseline's commit `1dd18be9` changed sources compiled into a link and carried no
  rows — `CONVERGE-DEALER` 12, `BUILD-general-lifecycle` 10, `CONVERGE-general-lifecycle` 7, `CONVERGE-ECONOMICS` 6.
  Every one of them stays owed until that one function is under the wall. **A route that cannot call without clobbering
  its own frame is not a ratchet problem; it is a live program that cannot execute correctly**, and it has been on
  `main` since the dealer landing.
- **The route census could not see three live wires, not two, and now it says so itself.** The recogniser rule was the
  guard call's NAME (`is_*`), so `custody/upkeep_vault_v1::process` and `custody/protocol_parameters_v1::process`, which
  dispatch through `<module>::selects(instruction_data)`, yielded no discriminant, `push_route` declined the forward,
  and the register's denominator simply did not move. The rule is the guard's **content** now — a call in guard position
  is a wire recogniser when its body states a magic or a width, whatever it is called. **160 → 163 routes; 448 refusal
  codes unchanged; no route removed and not one selector changed on the other 160**, measured at `09068cfa9` against the
  pre-change binary. The third route is one nobody had found: **`accelerator/dealer::process`**, whose
  `dealer_family_selected(accounts)` selects on the Instructions sysvar rather than on the instruction data. **The
  silence is what actually closed.** An entry guard that forwards to a named handler under a condition the walk read no
  discriminant out of is now REPORTED as an unclassified position, and `inventory --check-unique` refuses on any; the
  count was already printed and gated nothing, and it is a ratchet at zero. Running the detector with the recogniser
  rule still off enumerates 160 routes and names **exactly those three guards with their file:line** — the census
  answering by construction the question the merge queue had to answer by hand, by planting a blocking entry for a route
  id it had guessed and watching the report call it stale. Three tests pin it, each proved red with its half of the
  change reverted, and the end-to-end control is a planted tree with the magic renamed out of `selects`'s body: 163 →
  162 routes and `--check-unique` exit 1 naming `custody/lib.rs:242`. **Owed**: `docs/reference/` is generated from this
  inventory and reads 160 where the tree has 163, and that regeneration is the convergence owner's, not a lane's.

### 2026-09-07 03:35 EDT — THE TWELFTH FAMILY LANDS: THE JOINT CLEARING, ITS FIXTURES AUTHORED, AND TWO WRITES THAT NAME THEIR OWN MISSING FRAME
- CONVERGE-JOINT-CLEARING-2 closed. **`build/joint-clearing` is on `main`** (merge `24a3f595c`), the one family the build wave's queue
  aborted. The queue's own resolution was reused and is right: main's nine refusal-clause enums with joint-clearing's V2 types.
  **The unfinished V2 migration was SIX program-test files, not four**, and finishing it meant AUTHORING what each walk asserts:
  the joint arm makes an order a single-outcome INTERVAL and every fixture's orders moved claims at all outcomes at once. The rule
  the fixtures now follow, one decision in five files: one-outcome orders; every maker exactly on their own limit AND filled to
  their own maximum, so both quote conjuncts and the marginal conjunct are checked at their boundaries; and the price vector the
  box FORCES rather than one picked to pass. **The books balance, and that is Wall A speaking**: a clearing that mints complete
  sets strands a residual at every outcome with no taker, the strand's Claims burn has no child frame in `Action::Close`, so a
  fixture with fewer orders than outcomes cannot mint and still reach a terminal close — and two of these walk to a terminal close
  at width 258. **Three real defects the compile found**, all invisible while the files did not build: the bundle-builder's
  eighteen span tests were RED (nine of them) because the Batch envelope went Product-width and the fixture still wrote the
  224-byte V1 prefix into it; the batch account's **five physical sites** declared `224 + 0N` against an envelope encoding
  `296 + 16N`, and their own tests passed because nothing joined a rule to an envelope (there is a test now, proved red first);
  and **`tools/gate census` REFUSED the branch** because the V1→V2 rename walked the batch and order record magics onto `DCGBAT02`
  and `DCGORD02`, both already live under other names (`general/lifecycle.rs`, `runtime_manifest.rs`). Decision 0007's rule one
  wire object over: the side that has never reached a chain moves, so the records are `DCGBTCH2` and `DCGSORD2`, chosen for Hamming
  distance from the squatters and moved in Lean first, the two transition-program magic WORDS with them. **The two on-chain writes
  are named, reasoned and tested rather than deferred**: `SettlementClauseV3::PositionCloseStrandUnseated` (was
  `PositionCloseGeometry`, which described the shape and not the cause) with a positive control that a close stranding NOTHING
  still projects; `ClaimsSbfError::StrandUnseated = 0x5014` with a producer; and for the publication, whose refusal cannot exist
  because no plan tries, the fail-closed fact is asserted instead — the Product-width batch account the chain writes carries a
  VACANT clearing tail at every outcome, so a batch claiming a clearing did not come from this protocol. Green at the merge:
  `cargo check --workspace --tests` (both wasm crates included — the `test-fixtures` feature landed with an earlier family),
  273 General unit tests, the four Lean byte-gates, `lake build` 149 modules with only the two pre-existing `ScoringRuleV1`
  `sorry`s, `tools/gate census` PASS (165 routes, 452 refusal codes) and `tools/gate emission` PASS (103 generated, 103 guarded,
  0 unguarded). **Owed, and the largest is the browser**: `abi:general-v5:verify` is RED because `generalSuccessorV5.ts` mirrors
  the General batch and order records at V1 and those records no longer exist — the browser is FAIL-CLOSED on them (its decoders
  compare the magic and now refuse) but it can no longer read them at all until swept. Also owed: the frame ratchet, still red and
  now moved by the batch geometry too; `crates/dclutch-trading/src/general/lifecycle.rs` is fully ORPHANED, every public type it
  declares with zero consumers tree-wide, and it is what holds `DCGBAT02`; and the SDK's ABI-coverage baseline GREW by three
  deliberate rows for a hand-mirror whose exit is seam S12.
- **A defect of MAIN's that this lane found by running a suite main does not**: `packages/dclutch-sdk/lib/marketDetail.ts`
  imported two names TWICE from one module — a PARSE error, from the build wave's own landing-6 union resolution, which is right
  for exports and duplicates an import. Every module reaching it failed to LOAD: **twenty `apps/dclutch-web` test files reported
  a failure and ran zero tests between them**. Fixed here, because the silence was hiding a regression of this lane's own — the
  family's `ClearingPriceHistory.test.tsx` still spelled the old batch magic and four of its five cases were red underneath the
  parse error. What the fix REVEALED belongs to others and is now visible: seven stale `abi:*:verify` mirrors, two
  `capabilityMachineDerivation` cases, two `explorerCoverage` cases naming nine `DCLS*` dealer/scoring magics the explorer does
  not render, and the stale SBOM. The browser suite goes 20 failed FILES → 4, 1,052 passing.
- DEALER-FRAME closed: **`process_dealer_fill_v1` 4,288 → 3,456 bytes, 21 diagnostics → 0** on both links, lifted by two
  `#[inline(never)]` stages measured step by step (an admission stage folding the inventory read, so the inventory and
  both decoded Claims views die before the rule admits; then one stage per cash leg, because all three legs were live in
  the route's frame at once). The refuted repair is documented in the source: passing a leg by reference moves nothing,
  since the callee takes it by value and the temporary stays where it was built. **The route now has the test its family
  never had** — a real-ELF campaign with an exact 19-account prefix over an ALT-compiled message: the accepted fill at
  82,844 CU and fourteen hostiles each asserted against its exact code from the runtime's logs. **The positive control is
  the finding**: against the pre-fix ELF the fill dies on an access violation in an unallocated region after 78,718 CU
  while all fourteen hostiles still refuse correctly — **a refusal-only campaign would have been green, and main had
  carried a Dealer fill that could not complete since that family landed.** A second wall nobody had briefed: the
  ensemble fold's planner sat at exactly 4,096 with four diagnostics, holding a plan by value and the boxed wire image
  of all four of its records; split, it is now 64 bytes — and the lane refused to attribute it to the commit that
  introduced it, because that commit does not compile, so no measurement can place the wall there rather than at the
  rebase. **The ratchet is accepted from two byte-identical captures on two independent clones: 1,849 frames across
  eight links, deepest 3,968, and `frames owed` goes 85 commits across 24 lanes → 0.** Owed: the fixture for a fill that
  moves a claim or an atom (the accepted case is the null fill, stated in the campaign's own header), a real-ELF test for
  the ensemble fold, and a runner that still names two program paths the accelerator fold merged away.
- **JOINT-CLEARING LANDED — all twelve families of the build wave are on `main`** (merge `24a3f595c`). The unfinished V2
  migration was SIX program-test files, not four, and each had to be AUTHORED rather than fixed, because the joint arm
  makes an order a single-outcome interval where every fixture moved claims at all outcomes at once. One decision
  carried through five files: every order a one-outcome interval with its per-lot vectors DERIVED, every maker sitting
  exactly on their own limit and filled to their own maximum so three limit conjuncts are each checked at their
  boundary, the price vector the one the box FORCES rather than one picked to pass, and the live-order count read off
  the batch. **The books balancing is the wall speaking**: a minting clearing strands a residual at every outcome with
  no taker, so a fixture with fewer orders than outcomes cannot both mint and close. The two writes with no frame got a
  name, a reason and a test each. Three defects the compile and the gates found: nine of eighteen span tests were red
  and unrun; five sites declared a batch width the envelope contradicted; and **the census refused the branch because
  the rename walked two record magics onto magics already live** — moved in Lean first, a sixteen-byte re-emission. Two
  things that were not its own and bit: a duplicate import was a PARSE error hiding twenty web test files, and one of my
  own ledger commits was clobbered by a commit race in the shared tree and cherry-picked back, after which `cut.sh`
  exited 1 in silence on a stale ref — the refspec is forced now. Census 165 routes / 452 codes, emission 103/103, the
  Lean at 149 modules, cohorts 15–18 green. Owed: the browser still mirrors the General batch and order records at V1
  and its gate is RED and fail-closed; the frame ratchet moved again with the batch geometry.
