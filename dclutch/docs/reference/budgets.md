<!--
  @generated by tools/genref/generate.mjs -- DO NOT EDIT.
  Regenerate: tools/genref/generate.sh
  Verify:     tools/genref/generate.sh --check
  Sources: tools/gauntlet/CU_BUDGETS.json
-->
# Compute budgets for the golden transactions

What the protocol's key transactions cost, in compute units, measured from
real runs. Solana allows at most
**1,400,000 CU** per transaction; a budget
above that ceiling fails this file's own checks, so a transaction that
stops fitting gets caught here, loudly.

Each budget is measured cost plus a tolerance
(`tolerance = roundup(observed_band, 10000) + 10000, with a floor of 15000; budget = measured + tolerance`), where the measured value is
the highest cost seen across runs, never a single run. Changing a number?
Read `tools/gauntlet/CU_BUDGETS.md` first -- costs move with the accounts
in play, and it explains how much of that is noise.

## Enforced budgets

| id | campaign | scope | what | measured | tolerance | budget |
| --- | --- | --- | --- | ---: | ---: | ---: |
| `dcltgmf1-whole` | tier1 | transaction | found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF1) | 1,278,747 | 70,000 | 1,348,747 |
| `dcltgmf1-stage-1-custody-lock` | tier1 | stage | stage 1: Custody LockHoardAndCloseSource (custody) | 144,840 | 40,000 | 184,840 |
| `dcltgmf1-stage-2-core-found-and-permit` | tier1 | stage | stage 2: Core Found and one-shot permit (core) | 433,129 | 30,000 | 463,129 |
| `dcltgmf1-stage-3-custody-realize` | tier1 | stage | stage 3: Custody RealizeAndClose (custody) | 103,858 | 20,000 | 123,858 |
| `dcltgmf1-stage-4-claims-foundingv5` | tier1 | stage | stage 4: Claims FoundingV5 (claims) | 267,951 | 20,000 | 287,951 |
| `dcltgmf1-hostile-rollback` | tier1 | transaction | DCLTGMF1 refuses a substituted Claims request and rolls the whole founding back | 32,686 | 20,000 | 52,686 |
| `dcltpcb1-whole` | tier1 | transaction | create the projected-Custody founding prestate (DCLTPCB1) | 845,307 | 90,000 | 935,307 |
| `dcltpcb1-stage-1-custody-initialize` | tier1 | stage | stage 1: Custody Initialize, including Core ProjectFound (custody) | 354,337 | 30,000 | 384,337 |
| `dcltpcb1-stage-2-custody-openhoard` | tier1 | stage | stage 2: Custody OpenHoard, including Token-2022 InitializeAccount3 (custody) | 115,594 | 20,000 | 135,594 |
| `dcltpcb1-stage-3-custody-opensourcecompartment` | tier1 | stage | stage 3: Custody OpenSourceCompartment (custody) | 159,108 | 50,000 | 209,108 |
| `dcltpcb1-second-prestate-whole` | tier1 | transaction | stage a second projected-Custody prestate for the expiry abort (DCLTPCB1) | 792,807 | 90,000 | 882,807 |
| `dcltpcb1-reordered-tail-refusal` | tier1 | transaction | DCLTPCB1 refuses a reordered FundingState tail and rolls the transaction back | 781,102 | 70,000 | 851,102 |
| `dcltpcb1-non-terminal-refusal` | tier1 | transaction | DCLTPCB1 refuses a non-terminal projected-Custody request | 22,176 | 15,000 | 37,176 |
| `found31-whole` | tier1 | transaction | create canonical Found31 Market | 237,041 | 15,000 | 252,041 |
| `found31-substituted-market-rollback` | tier1 | transaction | Found31 refuses a substituted Market coordinate and rolls the transaction back | 143,399 | 15,000 | 158,399 |
| `core-infrastructure-profile-init` | tier1 | transaction | initialize Core infrastructure profile | 229,835 | 15,000 | 244,835 |
| `activation-role-core` | tier1 | transaction | activate immutable release-set role: Core | 546,984 | 20,000 | 566,984 |
| `activation-role-claims` | tier1 | transaction | activate immutable release-set role: Claims | 573,441 | 20,000 | 593,441 |
| `activation-role-trading` | tier1 | transaction | activate immutable release-set role: Trading | 721,945 | 20,000 | 741,945 |
| `activation-role-resolution` | tier1 | transaction | activate immutable release-set role: Resolution | 330,385 | 20,000 | 350,385 |
| `activation-role-custody` | tier1 | transaction | activate immutable release-set role: Custody | 235,103 | 20,000 | 255,103 |
| `activation-refuses-pre-revocation-core` | tier1 | transaction | immutable release activation refuses pre-revocation Core | 535,927 | 20,000 | 555,927 |
| `dcltpca1-unwind` | tier1 | transaction | unwind an expired founding's funded source compartment (DCLTPCA1) | 159,496 | 30,000 | 189,496 |
| `dcltpca1-pre-expiry-refusal` | tier1 | transaction | DCLTPCA1 refuses to abort a funded source before expiry | 142,166 | 20,000 | 162,166 |
| `series-consume-founds-with-permit` | tier4-series-occurrence-programtest | transaction | Series occurrence Consume founds its Market with a Core permit | 722,142 | 15,000 | 737,142 |
| `series-consume-founds-with-permit-replay-campaign` | tier4-series-occurrence-programtest | transaction | Series occurrence Consume founds its Market with a Core permit (replay campaign) | 722,142 | 15,000 | 737,142 |
| `series-consume-late-hoard-refusal` | tier4-series-occurrence-programtest | transaction | Series Consume refuses a late Hoard postcondition | 682,391 | 15,000 | 697,391 |
| `series-consume-replayed-ticket-refusal` | tier4-series-occurrence-programtest | transaction | Series Consume refuses a replayed ticket | 366,167 | 15,000 | 381,167 |
| `series-consume-substituted-programdata-refusal` | tier4-series-occurrence-programtest | transaction | Series Consume refuses a substituted Claims ProgramData | 183,196 | 15,000 | 198,196 |
| `claims-sparse-admit-transfer-close` | claims-family-programtest | transaction | claims sparse: admit, transfer and close | 561,881 | 15,000 | 576,881 |
| `claims-sparse-stage-1-admit` | claims-family-programtest | stage | stage 1: Claims Admit (claims) | 207,719 | 15,000 | 222,719 |
| `claims-sparse-stage-2-sparse-native-transfer` | claims-family-programtest | stage | stage 2: Claims SparseNativeTransfer (claims) | 178,046 | 15,000 | 193,046 |
| `claims-sparse-stage-3-close` | claims-family-programtest | stage | stage 3: Claims Close (claims) | 117,315 | 15,000 | 132,315 |
| `claims-position-admit` | claims-family-programtest | transaction | claims position: admit | 236,191 | 15,000 | 251,191 |
| `claims-sparse-substituted-admission-receipt-refusal` | claims-family-programtest | transaction | claims sparse: transfer under a substituted admission receipt | 362,405 | 15,000 | 377,405 |
| `custody-legacy-open-vault` | custody-family-programtest | transaction | custody legacy: open vault | 145,500 | 15,000 | 160,500 |
| `custody-token-2022-open-vault` | custody-family-programtest | transaction | custody token-2022: open vault | 145,567 | 15,000 | 160,567 |
| `custody-legacy-delegated-external-transfer` | custody-family-programtest | transaction | custody legacy: delegated external transfer | 148,297 | 15,000 | 163,297 |
| `custody-token-2022-delegated-external-transfer` | custody-family-programtest | transaction | custody token-2022: delegated external transfer | 139,746 | 15,000 | 154,746 |
| `custody-legacy-close-vault` | custody-family-programtest | transaction | custody legacy: close vault | 144,553 | 15,000 | 159,553 |
| `custody-token-2022-close-vault` | custody-family-programtest | transaction | custody token-2022: close vault | 158,020 | 15,000 | 173,020 |
| `structured-cross-schema-exposure-as-descriptor` | structured-v2-programtest | transaction | claims rational-representation-v2: cross-schema substitution: exposure as descriptor | 153,291 | 15,000 | 168,291 |
| `structured-cross-schema-descriptor-as-exposure` | structured-v2-programtest | transaction | claims rational-representation-v2: cross-schema substitution: descriptor as exposure | 153,290 | 15,000 | 168,290 |
| `structured-recipe-substitution-permuted-coefficients` | structured-v2-programtest | transaction | claims rational-representation-v2: recipe substitution: permuted coefficients | 197,124 | 15,000 | 212,124 |
| `structured-receipt-backed-by-receipt` | structured-v2-programtest | transaction | claims rational-representation-v2: receipt backed by receipt | 230,625 | 15,000 | 245,625 |
| `structured-one-atom-skew-coordinate-0` | structured-v2-programtest | transaction | claims rational-representation-v2: one-atom K_i skew at coordinate 0 | 264,338 | 15,000 | 279,338 |
| `structured-one-atom-skew-coordinate-1` | structured-v2-programtest | transaction | claims rational-representation-v2: one-atom K_i skew at coordinate 1 | 269,296 | 15,000 | 284,296 |
| `structured-one-atom-skew-coordinate-2` | structured-v2-programtest | transaction | claims rational-representation-v2: one-atom K_i skew at coordinate 2 | 265,254 | 15,000 | 280,254 |
| `structured-under-configured-receipt-mint-issue` | structured-v2-programtest | transaction | claims rational-representation-v2: issue against a receipt Mint with only the Mint role | 229,834 | 15,000 | 244,834 |
| `structured-under-configured-receipt-mint-unwrap` | structured-v2-programtest | transaction | claims rational-representation-v2: unwrap against a receipt Mint with only the Mint role | 216,007 | 15,000 | 231,007 |
| `claims-wallet-position-payout` | claims-rational-representation-v2-programtest | transaction | claims rational-representation-v2: a wallet-held Position is paid | 354,370 | 15,000 | 369,370 |
| `claims-wallet-position-payout-owner-paid` | claims-rational-representation-v2-programtest | transaction | claims rational-representation-v2: the Position owner pays its own fee | 354,369 | 15,000 | 369,369 |
| `claims-wallet-position-zero-payout` | claims-rational-representation-v2-programtest | transaction | claims rational-representation-v2: a losing wallet coordinate pays zero | 223,244 | 15,000 | 238,244 |

## Recorded, not enforced

| id | campaign | scope | what | measured | why unenforced |
| --- | --- | --- | --- | ---: | --- |
| `dcltgmf1-stage-5-open-and-outer-joins` | tier1 | derived | found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF1) | -- | There is no measurement to budget. |
| `hot-canonical-bundle-phase-subtotals` | unassigned | derived | canonical Direct Profile14 Hot bundle | -- | There is no green number to pin. |

## Provenance

- `dcltgmf1-whole` -- THE transaction this lane exists for. Five stages in one rollback domain: Custody LockHoardAndCloseSource, Core Found-and-permit, Custody RealizeAndClose, Claims FoundingV5, Core Open LAST. Measured 1,184,132 at cd05331 (84.6% of the ceiling) and 1,278,747 at d9f79bb (91.3%) in ONE EVENING from other lanes' changes to Core, Claims and Trading, which is the growth W1f named as urgent and unowned. The pin is the higher of the two 2026-08-27 draws; the lower draw at 3b0c588 was 1,220,253. Remaining ceiling headroom from the pin is 121,253 CU, 8.7%, AND SHRINKING. The budget is 51,253 CU below the ceiling: when this transaction's measured value rises past 1,330,000 the budget can no longer be written at this tolerance and the evaluator refuses the file. That refusal is the point. This number's job is not to bless 1,278,747.
- `dcltgmf1-stage-1-custody-lock` -- The first inner stage's own `consumed` line from the chain's finalized logs, including its Registry reauthentication and both Token-2022 calls. A stage budget localises a regression to a program when the whole-transaction number is too noisy to attribute: this stage grew 105,722 -> 144,840 between cd05331 and d9f79bb, which is where most of the +94,615 on the whole transaction came from, and it is the same window in which Custody gained AbortSourceAndClose, an exhaustive RentCredit-writability match and a distinct Expiry refusal.
- `dcltgmf1-stage-2-core-found-and-permit` -- The Core Found-and-permit stage's own `consumed` line, including its Registry reauthentication and four System calls. This is the single most expensive stage of the founding and the one whose ELF-hashing cost c61376d removed; it is worth its own row so a return of full-ELF authentication is attributed to Core rather than to `the founding got bigger`.
- `dcltgmf1-stage-3-custody-realize` -- The Realize stage's own `consumed` line, including its Registry reauthentication. It realizes the 808-byte projection in place into a 288-byte normal CustodyReplayV1.
- `dcltgmf1-stage-4-claims-foundingv5` -- The Claims stage's own `consumed` line, including four Registry reauthentications and six System calls. It allocates the liability-basis aggregate, the founder Position and the admission record at runtime-derived widths.
- `dcltgmf1-stage-5-open-and-outer-joins` -- Derived, not measured; see unenforced_reason. Recorded so a reader does not conclude the Open stage was forgotten.
- `dcltgmf1-hostile-rollback` -- The founding outer's cross-request join, refusing a well-formed ClaimsFoundingRequestV5 that differs in exactly one coordinate. It refuses inside Trading before its first CPI, so this number is a floor on how cheaply the outer can detect a substituted readonly record. A LARGE increase here means the join moved later; that is a semantic change wearing a CU costume.
- `dcltpcb1-whole` -- The four-stage projected-Custody prestate ladder: Custody Initialize (with Core ProjectFound), OpenHoard (with Token-2022 InitializeAccount3), OpenSourceCompartment, and three FundingState stagings. Its tolerance is set from the 79,500 CU in-run band, not from the 51,005 cross-run one, because the in-run figure is the stronger measurement: the same campaign stages this ladder twice on one binary and the two differ by exactly 53 bump-search iterations.
- `dcltpcb1-stage-1-custody-initialize` -- The first inner stage's own `consumed` line. It is the ladder's dominant cost and the one that carries Core's authenticated projection.
- `dcltpcb1-stage-2-custody-openhoard` -- The second inner stage's own `consumed` line.
- `dcltpcb1-stage-3-custody-opensourcecompartment` -- The third inner stage's own `consumed` line. This is the stage that had never executed at all before run 4 — it died out-of-memory under the heap wall — so a budget on it is a regression floor on a route with barely any history.
- `dcltpcb1-second-prestate-whole` -- The identical four-stage ladder at a second generation, staged to be abandoned by DCLTPCA1. Budgeted separately from the first because the two are the campaign's own control on run-to-run noise: they run the same code on the same binary, and their difference IS the band.
- `dcltpcb1-reordered-tail-refusal` -- The hostile case W1e refused to count until run 5 supplied a discriminator. It matters that it stays BELOW the honest transaction it shadows: when the two were within 300 CU of each other the refusal was indistinguishable from an out-of-memory death at the same point. This budget does not check that relation — it is a ceiling on one number — but a red row here is the first sign that the two have converged again.
- `dcltpcb1-non-terminal-refusal` -- Refuses inside `decode_projected_request` before any CPI. Measured identically (22,176) in both 2026-08-27 campaigns, so its band is zero at n=2 and it gets the floor tolerance. This is one of the entries that DOES have 30,000-scale teeth: an early refusal that starts costing 37,000 CU is no longer early.
- `found31-whole` -- Canonical Core Found31, the 31-account frame routed over a finalized ALT. This is the transaction that ALREADY died at the ceiling once: before c61376d it hashed Core's 1.0 MB ELF twice and exhausted 1,400,000 CU outright. Measured identically (237,041) in both 2026-08-27 campaigns, so it gets the floor tolerance and real teeth. A return of full-ELF authentication anywhere on this path is a six-figure jump and this row would be the first thing to say so.
- `found31-substituted-market-rollback` -- The attacker-chosen-routing case, whose whole multi-instruction transaction must roll back to a fee-only debit. Measured identically in both 2026-08-27 campaigns; floor tolerance.
- `core-infrastructure-profile-init` -- The one transaction Core must land under the plan-pinned ephemeral authority before it may revoke and be recognised as an immutable release. Measured identically in both 2026-08-27 campaigns; floor tolerance.
- `activation-role-core` -- Per-role activation of the immutable release set. Before the immutability-argument fast path this whole family hashed whole ELFs on chain — five-role activation with real artifacts exceeded 1,400,000 outright — so each role gets its own row and the worst role is the one that decides whether activation is reachable at all.
- `activation-role-claims` -- Per-role activation; see activation-role-core.
- `activation-role-trading` -- Per-role activation, and the WORST role: at 721,945 it is 51.6% of the ceiling on its own. Trading's ELF is the one other lanes grow most often, so this is the activation row most likely to move.
- `activation-role-resolution` -- Per-role activation. Re-pinned 2026-08-27 after 87e4590 grew the Resolution artifact 18,944 B (the funded failure walk, load-bearing demo function) -> activation +16,672 CU at ~1.13 CU/byte. Accepted by the orchestrator; activation headroom remains ~75%. Band noise is 1,500-multiples; see tolerance_rule.measured_bands.
- `activation-role-custody` -- Per-role activation; see activation-role-core.
- `activation-refuses-pre-revocation-core` -- The hostile counterpart of activation: a Core whose Loader-v3 upgrade authority has not been revoked is refused. Budgeted because a refusal that costs as much as a success is the shape a CU regression takes on this path.
- `dcltpca1-unwind` -- AbortSourceAndClose: the terminal that stopped a funded source compartment from stranding real collateral permanently. Newly reachable, so its budget is a floor on a route with one campaign of history.
- `dcltpca1-pre-expiry-refusal` -- The half that matters: while the founding is still satisfiable the authority over funded principal may not be destroyed. Refuses with custody/CustodySbfError::Expiry.
- `series-consume-founds-with-permit` -- The ProgramTest FAST LANE for the Core founding path, and the reason this campaign is in the budgets file at all: it drives Core's `found` plus the one-shot permit — the same Core code that costs stage 2 of DCLTGMF1 — with no validator and no port, in under a minute. A Core founding regression surfaces here BEFORE the six-minute tier-1 campaign. Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. The previous pin was 744,795, the highest of six UNSEEDED runs with a 24,000 band; the drop is not a saving, it is the bump-search iterations leaving the measurement.
- `series-consume-founds-with-permit-replay-campaign` -- The same founding driven by the fixture that then replays its ticket. Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. It now measures EXACTLY the same as series-consume-founds-with-permit, which is the honest answer: it is the same transaction against the same prestate, and the 14,847 CU that used to separate the two pins was entirely bump-search noise. Previous pin 737,295, highest of six unseeded runs, band 19,500.
- `series-consume-late-hoard-refusal` -- The rollback case: a refusal raised AFTER the child committed, which must take Found and all replay state with it. Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. Previous pin 692,942, highest of six unseeded runs, band 15,000.
- `series-consume-replayed-ticket-refusal` -- Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. Previous pin 370,694, highest of six unseeded runs, band 4,500.
- `series-consume-substituted-programdata-refusal` -- Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. This row is why the seed exists. Its previous pin was the highest of SEVEN runs, range 183,223-189,223, band 6,000. It looked like the quietest row in the fast lane at band 1,500 over the first six runs, and the seventh drew 189,223. That seventh run is NOISE and not a regression, and the attribution is exact rather than argued: none of the five programs this campaign builds (registry, rent, custody, core, the series-consume caller) changed at all between the first run and the seventh, so the ELFs are identical bytes and 4,500 is three `find_program_address` iterations. It is the clearest single demonstration in this file that six runs is not enough to bound a band.
- `hot-canonical-bundle-phase-subtotals` -- Recorded from ADR 0005's own measured table rather than re-measured, per the close-out doctrine's `no more re-measuring into tables`. It becomes an enforced entry the day W2i's heap gate goes green, and the entry to write then is the whole-bundle transaction CU plus the phase subtotals from a `--features hot-cu-profile` run.
- `claims-sparse-admit-transfer-close` -- The heaviest transaction in either family campaign and the one the composed Claims chain exists for: Admit -> SparseNativeTransfer -> Close, three Claims stages inside one rollback domain, driven by a real test caller through CPI. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [561881, 561881], band 0. Band 0, so the tolerance is the 15,000 floor. NOT MOVED at 2026-09-02, and it was red by +45,580 for four days. The cause was not this route: sparse_native_transfer_v1 authenticates three roles out of ONE immutable activation cache and paid a full hostile decode of it -- the five-role projection and all ten aliasing pairs -- for each, and it CPIs Custody, which decoded the same account three more times. 5767be46 and a216bbfc removed the repetition; the draw at 0f69918c is 511,538 against this 576,881, so the pin was right and nothing here moved. The 50,343 gap to the pinned 561,881 is recovered headroom, not drift, and it is recorded here rather than ratcheted away because this campaign's draw rides the same 1,500 CU search-depth grid the claims-wallet-position-payout row documents.
- `claims-sparse-stage-1-admit` -- Stage budgets are where the 15,000-scale teeth are: a regression localised to one Claims stage is red here even when the whole-transaction row is not. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [207719, 207719], band 0. Band 0, so the tolerance is the 15,000 floor.
- `claims-sparse-stage-2-sparse-native-transfer` -- The middle stage, and the only one whose cost scales with the transfer's own sparse extent rather than with the admission. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [178046, 178046], band 0. Band 0, so the tolerance is the 15,000 floor.
- `claims-sparse-stage-3-close` -- The terminal stage. Its cost is the one that must stay small for a Position to be closable in the same transaction that empties it. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [117315, 117315], band 0. Band 0, so the tolerance is the 15,000 floor.
- `claims-position-admit` -- The protocol Position lifecycle's single-stage admission, which is the Claims code DCLTGMF1's stage 4 (Claims FoundingV5) shares. A Claims regression shows up here in seconds instead of after a six-minute validator campaign. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [236191, 236191], band 0. Band 0, so the tolerance is the 15,000 floor. NOT MOVED at 2026-09-02: red by the same +45,580 class and green at 222,293 against this 251,191 once 5767be46 and a216bbfc removed the repeated activation-cache decode from Claims, Custody and Core. Its Custody leg is where its share of that was.
- `claims-sparse-substituted-admission-receipt-refusal` -- A hostile case, budgeted because a refusal that gets more expensive is a refusal that can stop fitting: the cost of REFUSING must stay far from the ceiling or an attacker picks the transaction that cannot complete either way. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [362405, 362405], band 0. Band 0, so the tolerance is the 15,000 floor.
- `custody-legacy-open-vault` -- Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [145500, 145500], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 130494, and the delta is +15006 CU = +10 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +15006 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one.
- `custody-token-2022-open-vault` -- Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [145567, 145567], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 141061, and the delta is +4506 CU = +3 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +4506 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one.
- `custody-legacy-delegated-external-transfer` -- Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [148297, 148297], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 134791, and the delta is +13506 CU = +9 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +13506 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one.
- `custody-token-2022-delegated-external-transfer` -- Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [139746, 139746], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 142740, and the delta is -2994 CU = -2 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved -2994 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one.
- `custody-legacy-close-vault` -- Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [144553, 144553], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 138547, and the delta is +6006 CU = +4 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +6006 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one.
- `custody-token-2022-close-vault` -- Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [158020, 158020], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 137014, and the delta is +21006 CU = +14 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +21006 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one.
- `structured-cross-schema-exposure-as-descriptor` -- Cross-family/cross-schema substitution, direction one: the finalized CompositionExposureBundleV3 record offered in the immutable descriptor's account slot. The cost is the record-authentication prologue -- release set, Product/representation admission, both record digests -- which is where a substituted record is caught, so this row is the price of the join rather than of any transition. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [153291, 153291], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-cross-schema-descriptor-as-exposure` -- The SAME substitution the other way, because a family wire that refuses one direction and admits the other is not joined. One compute unit cheaper than its twin, which is what a symmetric refusal reached at the same depth looks like. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [153290, 153290], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-recipe-substitution-permuted-coefficients` -- A same-width RepresentationDescriptorV2 whose coefficients are the canonical ones PERMUTED -- a valid record naming a recipe the composition does not state. More expensive than the cross-schema pair because the descriptor decodes cleanly and the refusal comes from the request/descriptor identity join rather than from the record admission. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [197124, 197124], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-receipt-backed-by-receipt` -- THE ACCUSATION MOVED, and with it the price. This row used to read 11,302 -- the cheapest refusal in the island by an order of magnitude -- because the hostile substituted the receipt Mint into the ASSET ROW, where `RepresentationRequestV2::validate` refused it before any caller could derive its Trading authority, so it died in the caller with no CPI issued. Physical ABI v3 derives the three per-coordinate keys instead of carrying them, so no row byte is left to corrupt; the substitution is made in the ACCOUNT METAS now and is refused on chain by `ClaimsSbfError::ReceiptAlias` (0x500F) in `authenticate_asset_identities`. A refusal that reaches the Claims adapter costs what reaching the Claims adapter costs: release authentication, the Product join, the descriptor and exposure decodes and the replay all run first. So a twentyfold rise is the CORRECT reading of a refusal that moved from the caller into the chain, and the regression to watch for is the opposite one -- a collapse back toward the caller would mean the frame check stopped being reached. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [230625, 230625], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-one-atom-skew-coordinate-0` -- Coordinate 0's backing skewed by exactly ONE shard atom. The full projection is built before prepare() compares the asset row's coefficient against the descriptor's, so a skew costs nearly a whole successful issue -- which is the point: the campaign basis [2, 3, 5] over 7 is pairwise coprime and coprime to the denominator so that a one-atom skew cannot be presented as a legitimate quantity at another coordinate, and the chain has to walk the whole frame to say so. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [264338, 264338], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-one-atom-skew-coordinate-1` -- The same one-atom skew at coordinate 1. The 764-unit spread from coordinate 0 is the extra projection row walked before the mismatch is reached, not noise: both runs are byte-identical. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [269296, 269296], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-one-atom-skew-coordinate-2` -- The same one-atom skew at coordinate 2, the last coordinate and therefore the most expensive of the three. Three separate transactions, so the coprime basis is exercised per coordinate rather than once. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [265254, 265254], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-under-configured-receipt-mint-issue` -- Decision 0011 s3b's under-configured founding: the receipt Mint carries the representation authority as its Mint authority and not as its permissioned-burn authority. parse_behavior_mint refuses it (ClaimsSbfError::Token, 0x5009) with ZERO Token-2022 CPIs issued, so this row is the cost of the behavior-profile read and everything before it. s3b predicted the failure would land at BurnReceipt with the descriptor already committed; it lands at the first issue instead, and this is what that costs. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [229834, 229834], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `structured-under-configured-receipt-mint-unwrap` -- The consequence of the row above rather than a second finding: the issue committed nothing, so no representation replay exists to carry revision one and the unwrap is refused on Accounts (0x5001). BurnReceipt is never the thing that fails, because there is never anything to unwrap. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [216007, 216007], band 0. Band 0, so the tolerance is this file's 15,000 floor.
- `claims-wallet-position-payout` -- The redemption's step two: a plain wallet-held LBV2 Position paid out of a resolved Market's Hoard through claims/terminal_settlement_v3 under the widened execution role Claims (decision 0008 s8). The heaviest of the three wallet-payout shapes, because it is the only one that both evaluates the Product basis AND crosses the Custody boundary. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [354370, 354370], band 0, so the tolerance is the 15,000 floor. NOT MOVED, and it briefly was. This row reached 475,665 by 2026-08-31 and a first pass raised it to 390,251 because 12,504 of the excess was an unlocated growth in the Custody CPI. It was not a conjunct: Custody decoded the same immutable 1,288-byte activation cache THREE times per invocation and Claims six, and a216bbfc and 5767be46 removed the repetition rather than paying for it. Draw at a216bbfc, on real ELFs: 359,529, with the Custody CPI at 112,625 against 121,843 at the 2026-08-27 pin commit 082f942f. So the 08-27 numbers were never wrong and the raise is reverted; 9,841 CU of margin, 25.7% of the 1,400,000 ceiling, 1,040,471 CU of headroom. READ THIS BEFORE RE-PINNING. The draw is exact per SOURCE REVISION and it is not a property of this route alone: release_set_id hashes the five deployed ELF digests, so every PDA seeded by it redraws its search depth when ANY lane changes ANY of the five programs, at 1,500 CU per rejected candidate. Measured on this transaction: +16,500 on all three rows between 3fa1a432 and 5767be46 with no source change to the route, -9,000 more between 5767be46 and a216bbfc, and a 245,491/292,005/251,506/266,506/256,006 spread across the 2026-08-28..08-31 window. Every difference is a multiple of 1,500. So band 0 from two runs at one revision measures re-runs of one ELF set and cannot see this at all, and NO tolerance that leaves this row any teeth can absorb it. The fix is the attempt subtraction the Direct margin gate already runs -- see programs/dclutch-trading-sbf/program-test/tests/direct_hot_top_level_margin_gate.rs, which censuses every find_program_address the route reaches, subtracts 1,500 * (256 - bump) per site, and proves the census complete by the residual being constant. Both gates should share one author for that arithmetic and tools/gauntlet/program-test-evidence is the crate they both already dev-depend on. Until then this row is a draw at a named commit, which is a gate defect with a named owner and not a reason to widen the tolerance.
- `claims-wallet-position-payout-owner-paid` -- The same payout in the browser's shape: one wallet as both fee payer and authority, so coordinate 0 arrives writable (decision 0008 s8.4). Draws [354369, 354369], band 0, tolerance at the floor. NOT MOVED, for the row above's reasons; read it, including its re-pinning caution. Still pinned separately and still ONE unit below it -- draw at a216bbfc 359,528 against 359,529 -- which is the entire measured cost of the single-signer message. After a 116,136 CU recovery across three programs it is still one unit, which is the strongest available statement that the relaxed privilege never started doing work.
- `claims-wallet-position-zero-payout` -- terminal_settlement_v3's zero-payout branch, reached from a wallet holding a claim at a LOSING coordinate. Draws [223244, 223244], band 0, tolerance at the floor. 131,126 CU cheaper than the paying shape, and that gap IS the Custody leg: authenticate_zero_custody_accounts re-derives the whole Custody frame from the persisted namespace without spending it, so the difference is one CPI and its token reads rather than a shortcut. NOT MOVED either, and this row is the one that shows what the other two cost: it reached 317,096 on 2026-08-31 and its draw at a216bbfc is 228,698, because 5767be46 removed two repetitions from Claims' own frame -- the activation cache decoded twice per single-role authentication, three roles, and the plan and 36-account frame parsed twice per parent-authenticated route. It CPIs neither Custody nor Core, so none of a216bbfc's recovery reaches it and none is claimed. 9,546 CU of margin, and the paying gap is now 130,831 -- back within 300 CU of the 131,126 this row was pinned against.
