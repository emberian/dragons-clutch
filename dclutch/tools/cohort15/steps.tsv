# The cohort-15 runbook, as rows a checker can read.
#
# id	stage	command	verifier	cost_sol	blocks
# THIS IS A DELTA, not a second copy of cohort-14. Cohort-14's nineteen rows
# still describe the cohort; these four are what cohort-15 carries that
# cohort-14 could not, and each one names where it inserts. `cost_sol` is
# priced from cohort-14's ledger; `-` moves no lamports.
# Tabs separate the fields. `../cohort14/check-steps.py .` gates this file.
00	redeploy	the whole cohort-14 ladder from a commit carrying the caller-authority seed change, replacing its step 01	CANDIDATE_EXIT=0, sbf_build_diagnostics_total=0, seven ELFs reproduced byte-identically from a second detached worktree, and every live image dumped back and compared before the next deploy starts	-42.26	01
01	record-core-digest	put the deployed Core's ELF sha256 into RECORDED_PRODUCT_GRAPH_CORE_ELF_SHA256_V1 and commit it BEFORE any founding	core_bump_projection classifies the deployed digest as RECORDED rather than as naming a Core in neither list, and the first General founding does not refuse fail-closed with that message	0.00	02
02	refound-general	devnet-general-market against a policy file with no external_widths block, then the ordinary founding campaign	the published OpenBatch AccountProfile's RentCredit coordinate reads Exact(128) when recovered off chain by devnet-general-session's two-encoding offset probe, and 128 equals the market's own lifecycle RentCredit account width read from the chain	-0.34	03
03	openbatch	OpenBatch N=2 against the re-founded, activated General root	devnet-general-session's frame report names no unsatisfiable conjunct at any of the 55 top-level coordinates, the transaction commits, and the Batch account appears at the address the request's own seeds derive carrying a DCLTGBT01 body whose opening slot window brackets the executing slot	-0.02	-
