# What CI does with each root-workspace package clippy still refuses.
#
# columns: status	package	lints	sites	owner	note
#
# `cargo clippy --workspace --all-targets -- -D warnings` is printed at
# README.md:183 and its deny table lives at Cargo.toml:119, and until
# `tools/ci/run.sh clippy` existed NO TIER RAN IT -- the table was enforced by a
# human typing that line. 1bdf5572f found a red that had survived a day, and
# named this as the debt it was evidence of.
#
# THE FIRST FULL CENSUS, at the commit that added this file, found reds in 44 of
# 105 workspace members, and 69 members that clippy NEVER REACHED because a
# package they depend on was red -- `--keep-going` builds what it can and stops
# at a red lib, so a red kernel hides everything above it. That is why this file
# is a debt list and not a quarantine of individual lints: the unit that matters
# is the PACKAGE, because a red package is what blinds the tier.
#
# STATUS:
#   debt        is RED today and must STAY red. A `debt` package that goes GREEN
#               fails this tier BY NAME -- the fix landed and the row is now
#               stale coverage. Delete the row in the commit that fixed it.
#               Every other package must be clean; a red one with no row here
#               fails the tier, which is the ratchet.
#
# THE `sites` NUMBER IS NOT A GATE and is deliberately not compared. It is the
# size of the debt when the row was written, so a reader can see whether a row
# is one line or fifty without running anything. The gate is the package's
# red/green, which cannot drift.
#
# `owner` is the LAST COMMIT to touch the file, and its `Lane:` trailer where it
# has one. Commits before 2026-09-02 carry no trailer (`tools/lane.sh` began
# writing it that day), so an older row names a commit and not a lane. It is a
# starting point for whoever picks the row up, not an accusation.
#
# WHY THESE WERE NOT FIXED IN THE SAME COMMIT, in one sentence each, in the note
# column. Three shapes recur: a live lane holds the file (the CI lane does not
# edit another lane's working file), the lint is a design question whose answer
# is not a lint fix, or the finding is a REAL DEFECT that belongs to whoever
# knows the code -- `dead_code` on a test function is a test that does not run,
# and no lint lane should decide whether to delete it or wire it up.
status	package	lints	sites	owner	note
debt	dclutch-chain-bundle-builder	unused_imports	1	efca69663	an unused `ProgramV3 as EffectBaseV3` in the Trading bundle-builder, whose program-test lane is live
debt	dclutch-claims-sbf	cast_possible_truncation,indexing_slicing	5	9438c8a1b	two sites are in rational_representation_v2.rs, which a live lane holds uncommitted; the const-block rewrite that fixed the other twelve programs was reverted there for that reason
debt	dclutch-custody-sbf	dead_code,indexing_slicing,large_enum_variant,manual_range_patterns	8	45b48a43e	the length-collision const block is a FOUR-deep nested index loop, not the one-deep `::ALL` walk the other twelve programs took; and `has_duplicate_keys` is dead -- a helper nothing calls, which is a finding for the dealer-reservation owner
debt	dclutch-trading	type_complexity	1	dcfb7debb	the DEALER lane committed this file 1h50m before the census; its test case table wants a type alias, which is DEALER's line to write
debt	dclutch-fractional-claim-operator	bool_comparison,cast_possible_truncation,indexing_slicing,large_enum_variant	50	cf47560ba	44 of the 50 are fixed-offset writes into `&mut [u8]` parameters in atomic_v3.rs -- the same shape direct-codec's settlement headers had, and the same fix (give the buffer its width back) applies, but this one is 44 sites across a whole encoder and belongs to whoever owns the atomic route
debt	dclutch-resolution-proof-sbf	collapsible_if,indexing_slicing,needless_borrow,too_many_arguments	10	f47c25fec	first measured here: relay-contract was its only blocker and clearing that revealed it. Two `too_many_arguments` in the relay transport and the pre-market funding route are signature questions for the Resolution owner; the rest are ordinary
debt	dclutch-trading-sbf	assertions_on_constants,cast_possible_truncation,collapsible_if,dead_code,identity_op,indexing_slicing,items_after_test_module,large_enum_variant,needless_borrow,needless_range_loop,panic,single_match,too_many_arguments,type_complexity,unused_imports	318	a0d556b9e / DEALER	THE LARGEST ROW IN THE TREE and the one that was most thoroughly hidden: 318 sites over 15 lints, invisible until direct-codec went green. 64 in series/release_v5.rs, 47 in hot_v3.rs which the DEALER lane is editing right now. Nine of the ten remaining never-reached packages are behind this row and dealer-codec
