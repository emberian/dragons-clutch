[package]
name = "dclutch-local-successor-bootstrap"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
rust-version = "1.89"
publish = false

# This localhost-only host utility is a member of the one workspace. It used to
# resolve independently, which is how it spent a day not compiling at HEAD:
# nothing built its workspace between program commits.
[dependencies]
base64 = "=0.22.1"
bincode = "=1.3.3"
# The published AccountProfile the General route's runtime-suffix privileges
# are READ OFF, rather than typed into the route producer by hand.
dclutch-vm = { path = "../../../../crates/dclutch-vm" }
dclutch-market = { path = "../../../../crates/dclutch-market" }
dclutch-core-contract = { path = "../../../../crates/dclutch-core-contract" }
dclutch-claims-sbf = { path = "../../../../programs/dclutch-claims-sbf", features = ["no-entrypoint"] }
dclutch-claims = { path = "../../../../crates/dclutch-claims" }
dclutch-custody = { path = "../../../../crates/dclutch-custody" }
dclutch-trading = { path = "../../../../crates/dclutch-trading", features = ["svm"] }
# The Direct intent ticket author, which used to be `pub(crate)` in THIS crate
# and is now shared with the released `dclutch` CLI. `direct_ticket.rs` is the
# adapter that keeps this binary's subcommand and error type over it.
dclutch-direct-ticket = { path = "../../../../crates/dclutch-direct-ticket" }
# The wallet-terminal payout derivation this binary used to carry inline. It
# moved to a workspace crate so a browser can compile the same code; this
# binary keeps the shell and consumes it.
# Stage ONE of the same derivation: the three pure phases that produce the
# payout input stage two consumes. It moved out for the same reason stage two
# did -- so a browser can compile it instead of reimplementing it -- and this
# binary keeps the shell: arguments, the two artifact files, RPC, and the
# cluster-origin policy.

dclutch-market-retirement-v1-operator = { path = "../../../../crates/dclutch-market-retirement-v1-operator" }
dclutch-operator = { path = "../../../../crates/dclutch-operator" }
dclutch-product = { path = "../../../../crates/dclutch-product" }
dclutch-product-runtime-v2-operator = { path = "../../../../crates/dclutch-product-runtime-v2-operator" }
dclutch-source = { path = "../../../../crates/dclutch-source", features = ["synthetic-local-fixture"] }
dclutch-registry = { path = "../../../../crates/dclutch-registry", features = ["svm"] }
dclutch-release-tool = { path = "../../../../crates/dclutch-release-tool" }
dclutch-resolution-core-v3-operator = { path = "../../../../crates/dclutch-resolution-core-v3-operator" }
dclutch-trading-sbf = { path = "../../../../programs/dclutch-trading-sbf", default-features = false, features = ["no-entrypoint", "series-family"] }
dclutch-versioned-message-operator = { path = "../../../../crates/dclutch-versioned-message-operator" }
reqwest = { version = "=0.12.28", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.10.9"
solana-hash = "=4.6.0"
solana-address-lookup-table-interface = { version = "=3.2.0", features = ["bincode", "bytemuck"] }
solana-compute-budget-interface = "=3.1.0"
solana-program = { version = "=3.0.0", default-features = false }
solana-loader-v3-interface = { version = "=6.1.0", features = ["bincode"] }
solana-sdk = "=3.0.0"
solana-sdk-ids = "=3.1.0"
solana-system-interface = { version = "=2.0.0", features = ["bincode"] }

[lints.rust]
unsafe_code = "forbid"

[dev-dependencies]
