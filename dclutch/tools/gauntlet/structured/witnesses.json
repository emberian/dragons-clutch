{
  "campaign": "structured-v2-programtest",
  "witnesses": [
    {
      "id": "structured-v2-fits-the-compute-maximum",
      "kind": "evidence-jq",
      "query": "[.transactions[].compute_units_consumed | select(. != null)] | max | (. <= 1400000) | tostring",
      "expect": "true",
      "provenance": "Solana's per-transaction MAX_COMPUTE_UNIT_LIMIT is 1,400,000 compute units. It is the chain's limit, not ours. The campaign sets ProgramTest's ceiling to exactly that value and never raises it."
    },
    {
      "id": "structured-v2-measured-every-transaction",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.wire_bytes == null)] | length | tostring",
      "expect": "0",
      "provenance": "A `wire_bytes` of null says the campaign did not measure, which is honest but is not a claim that the frame fits. No transaction in this island is unmeasured."
    },
    {
      "id": "the-full-width-structured-frame-is-inside-the-packet-maximum",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | test(\"skew|receipt backed|cross-schema|recipe substitution|only the Mint role\")) | .wire_bytes] | max | tostring",
      "expect": "1005",
      "provenance": "THE MEASUREMENT THIS ISLAND EXISTS TO RECORD, AND IT REVERSED. Every Structured hostile rides the FULL-WIDTH frame, because RepresentationRequestV2::validate refuses IssueStructured/UnwrapStructured unless asset_count == outcome_count. At K = 3 that frame measured 1,397 bytes as a v0 message against a real activated Address Lookup Table, against Solana's 1,232-byte PACKET_DATA_BYTES -- over by 165 -- and this island was founded to say so. Physical ABI v3 closed it: the three re-derived per-coordinate keys left the asset row and the header became action-conditional, which is -392 request bytes at K = 3, and the same frame is now 1,005 bytes with 227 to spare. The witness stays an EQUALITY rather than becoming '<= 1232' for the reason it was an equality before: a claim that only says the route works cannot report the day it stops. Re-pinned 2026-09-02 by STRUCTURED from a green filtered structured campaign, two seeded runs, band 0 on every transaction in it. WHICH FRAME THIS IS, added 2026-09-03 by STRUCTURED because the number was being read as an answer to a question it does not answer. 1,005 bytes is the CLAIMS-DIRECT frame: the test caller's wrapper around the Claims child, which is what every hostile in this campaign rides and what no wallet sends. The route a wallet sends is common Hot, which carries the Trading envelope and its own account frame on top of the same child, and it measures 1,197 bytes at K = 3 with 35 to spare -- so the 227 bytes of headroom here are not headroom a Product width can spend. The ceiling each frame implies differs with it: this frame admits six coordinates, common Hot admits three, and a fourth coordinate is 1,269 bytes against 1,232. Both numbers are now derived and asserted in programs/dclutch-claims-sbf/tests/rational_representation_v2_program_test.rs, in the_full_width_structured_frame_now_fits_a_packet_at_k_three and in current_common_hot_executes_issue_and_selected_denominate_through_real_elves respectively."
    },
    {
      "id": "every-structured-v2-frame-fits-the-packet-maximum",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.wire_bytes > 1232)] | length | tostring",
      "expect": "0",
      "provenance": "The general form of the row above, with no label exclusions at all: NOTHING this island submits is over Solana's 1,232-byte PACKET_DATA_BYTES. It could not have been written before 2026-09-02 -- the full-width frame was 165 bytes over and the exclusion lists were what kept the other packet witnesses honest about it. Written as a count rather than a maximum so that a new transaction added to this campaign is covered the day it is added, without anyone remembering to extend a label pattern."
    },
    {
      "id": "every-structured-v2-refusal-reached-a-programs-own-taxonomy",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.error != null) | select([.logs[] | select(contains(\"custom program error: 0x\"))] | length == 0)] | length | tostring",
      "expect": "0",
      "provenance": "The Solana runtime reports a privilege, frame or account refusal it decides ITSELF as a bare InstructionError with no `custom program error:` log line; that line appears only when a program returned ProgramError::Custom. Every refusal this island claims is a program decision -- including receipt-backed-by-receipt, which is refused by the test-only wrapper because every caller must decode the request to derive its Trading authority, and which is bound as an unnamed_refusal for exactly that reason."
    },
    {
      "id": "every-structured-v2-hostile-was-refused",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | test(\"skew|receipt backed|cross-schema|recipe substitution|only the Mint role\")) | select(.error == null)] | length | tostring",
      "expect": "0",
      "provenance": "Eight Structured-family hostiles, and not one of them may commit: cross-schema substitution in BOTH directions (a family wire that refuses one direction and admits the other is not joined), a same-width descriptor whose recipe is the canonical one permuted, a receipt backed by itself, a one-atom K_i skew at each of the three coordinates, and the two actions against a receipt Mint configured in only one of its two required Token-2022 roles. The campaign additionally asserts a byte-identical resource snapshot after each; that assertion cannot be made from evidence, so it lives in the test and this witness covers the part the chain can say."
    },
    {
      "id": "the-under-configured-receipt-mint-was-refused-before-any-token-cpi",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | test(\"issue against a receipt Mint with only the Mint role\")) | .logs[] | select(startswith(\"Program TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb invoke\"))] | length | tostring",
      "expect": "0",
      "provenance": "Decision 0011 s3b ruled that a founding configuring only the Mint authority would let IssueStructured COMMIT and then fail at BurnReceipt 'at the Token program with the descriptor already committed'. This is the measurement that corrects it: parse_behavior_mint reads the receipt Mint's Token-2022 behavior profile on every action and requires the permissioned-burn authority present and pinned to the representation authority, so the FIRST issue refuses ClaimsSbfError::Token (0x5009) with ZERO Token-2022 CPIs ever issued. Zero invoke lines is the whole claim: no receipt is ever minted against a Mint that could not burn it. The two-role cost stands; its consequence is a founding that can never issue, not a representation that can never be unwound."
    },
    {
      "id": "the-structured-island-is-inside-its-cu-budgets",
      "kind": "cu-budget",
      "campaign": "structured-v2-programtest",
      "provenance": "The budgets and their tolerances live in tools/gauntlet/CU_BUDGETS.json with their measurement dates and observed noise bands; this witness carries no number of its own. Every row was pinned from TWO seeded runs of this filtered campaign at one revision on one set of ELFs, and every row was band 0 -- byte-identical across both -- so each tolerance is the file's 15,000 floor rather than a measured band. The fixture seeds its keypairs (Keypair::new_from_array) and starts no validator, so the find_program_address bump-search noise that makes tier 1 non-deterministic does not reach here."
    }
  ]
}
