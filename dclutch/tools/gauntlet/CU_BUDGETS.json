{
  "schema": "dclutch-cu-budgets-v1",
  "owner": "the CU-BUDGET lane. ONE file, ONE owner. A tier does not carry its own copy of a number that lives here; it carries a `cu-budget` witness naming its campaign, and the shared evaluator tools/gauntlet/tier1/check-witnesses.sh reads this file.",
  "note": "Checked-in per-transaction compute budgets for the golden transactions. A budget is a CEILING a campaign must stay under, evaluated against that campaign's own finalized evidence. Over budget is a red row naming the transaction and the delta. Read CU_BUDGETS.md before changing a number in here; in particular, read what it says about noise, because the numbers in this file are NOT deterministic and the tolerances are the whole reason the file is usable.",
  "ceiling": {
    "compute_units": 1400000,
    "provenance": "Solana's per-transaction MAX_COMPUTE_UNIT_LIMIT. It is the chain's limit, not ours: a transaction that asks for more is refused at request time, and one that consumes it is refused at the ceiling with no partial result. Found31 died exactly this way before c61376d. Every `transaction`-scope budget in this file must be at or below it; a budget above the ceiling is refused by the evaluator, which is how this file says out loud that a transaction has stopped fitting."
  },
  "tolerance_rule": {
    "formula": "tolerance = roundup(observed_band, 10000) + 10000, with a floor of 15000; budget = measured + tolerance",
    "measured_at": "the `measured` value of every entry is the HIGHEST draw this lane observed, not a single run. Pinning the highest draw is what keeps ordinary noise from producing a red row.",
    "noise": "BOTH CAMPAIGNS ARE SEEDED NOW, and the residual is measured rather than assumed. The original cause was exact: every campaign drew fresh signing keypairs per run (Keypair::new() in the successor bootstrap; Keypair::new() and Pubkey::new_unique() in the ProgramTest fast lane), which changes how many iterations find_program_address needs for an off-curve bump, and each iteration is one sol_create_program_address syscall at 1,500 CU. Every delta measured between runs was an exact multiple of 1,500 (a handful of +-4 residuals aside), which is what made the diagnosis a measurement rather than a story. tier 4 is now band 0 on every row, proven over four runs of one compiled binary. tier 1 is NOT: 82 of its 101 transactions are byte-identical across two seeded runs at one revision and 19 are not, because A KEYPAIR SEED ONLY SEEDS KEYPAIRS -- the founding ladders carry expiry slots derived as finalized_slot + 500,000 and lookup tables derived from [authority, recent_slot], and those move with when the campaign happens to run. See CU_BUDGETS.md.",
    "measured_bands": "SEEDED, tier 1, two campaigns at 5465341 on 2026-08-27 with one seed digest and one set of ELFs: 14 of the 24 enforced rows are band 0, including every per-role activation, Found31 and its rollback case, the infrastructure profile init, the non-terminal DCLTPCB1 refusal, the second projected-Custody prestate and both DCLTPCA1 cases. The other 10 are the two founding ladders and their stages: dcltgmf1-whole 9,004; its stages 1,500 / 2 / 2 / 1,500; dcltpcb1-whole 24,000; its stages 6,000 / 4,500 / 1,500; the reordered-tail refusal 24,000. UNSEEDED, which is what this file's pins were written against and what a real founder still pays: DCLTGMF1 58,494, DCLTPCB1 51,005 across campaigns and 79,500 within ONE campaign, the reordered-tail refusal 55,503, per-role activation 1,500-9,000. tier 4 seeded: 0 on every row over four runs; unseeded it spanned 24,000 on the founding case and 6,000 on the substituted-ProgramData refusal, which six runs had not bounded.",
    "what_this_buys_and_what_it_does_not": "A tolerance that exceeds the band cannot also catch a regression smaller than the band. On tier 1's founding transactions the band is 58,494-79,500, so a +30,000 regression to DCLTGMF1 is NOT reliably caught by its whole-transaction budget and this file will not pretend otherwise. What IS caught: any structural regression larger than the tolerance, on any draw; and — the thing W1f actually asked for — the moment the transaction gets close enough to the 1,400,000 ceiling that the budget can no longer be written down. The teeth at the 30,000 scale live on the entries whose measured band is zero or near it (Found31, the Found31 rollback case, the infrastructure profile init, the DCLTPCB1 non-terminal refusal) and on the tier-4 fast lane, which is also the one that runs pre-campaign.",
    "how_to_shrink_it": "TAKEN, 2026-08-27. dclutch-local-successor-bootstrap run takes --keypair-seed <64 hex> behind a loopback-only refusal, and programs/dclutch-core-sbf/tests/found_program_test.rs and programs/dclutch-custody-sbf/tests/program_test.rs derive their fixture keys from documented preimages. What remains is NOT a keypair: the tier-1 founding ladders hash slot- and clock-derived material into addresses, so shrinking those rows further means making the campaign's expiry and lookup-table inputs a function of the seed rather than of the wall clock. That is a change to what the campaign SUBMITS, not to how it draws keys, and it is not obviously worth it: the world is still as noisy as it ever was and a founding is exactly where a real founder pays that noise too."
  },
  "budgets": [
    {
      "id": "dcltgmf3-whole",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF3)",
      "measured": 829068,
      "tolerance": 70000,
      "budget": 899068,
      "enforced": true,
      "provenance": "The founding is `DCLTGMF3` now, not `DCLTGMF1`: it was split into `DCLTCFQ1` + `DCLTCF1A` + `DCLTPCB2` + `DCLTGMF3` and the old rows named transactions the campaign stopped submitting. THE transaction this lane exists for, and the first time it has ever succeeded. Five stages in one rollback domain: Custody LockHoardAndCloseSource, Core Found-and-permit, Custody RealizeAndClose, Claims FoundingV5, Core Open LAST. The predecessor `dcltgmf1-whole` was pinned at 1,278,747 with 121,253 CU of ceiling headroom and the note said it was SHRINKING; at 829,068 the headroom is 570,932 (40.8%). That is not a regression repaired -- it is a different transaction, because the controller funding and its checkpoint moved out into DCLTCFQ1. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltgmf3-stage-1-custody-lock",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF3)",
      "stage": {
        "index": 1,
        "name": "Custody LockHoardAndCloseSource",
        "program_role": "custody"
      },
      "measured": 74034,
      "tolerance": 40000,
      "budget": 114034,
      "enforced": true,
      "provenance": "The first inner stage's own `consumed` line: Custody LockHoardAndCloseSource, including both Token-2022 calls. Its predecessor drew 144,840 with the Registry reauthentication inside it; DCLTGMF3 no longer invokes the Registry at all. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltgmf3-stage-2-core-found-and-permit",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF3)",
      "stage": {
        "index": 2,
        "name": "Core Found and permit",
        "program_role": "core"
      },
      "measured": 299927,
      "tolerance": 30000,
      "budget": 329927,
      "enforced": true,
      "provenance": "Core's Found-and-permit: the Market creation plan, the Product-graph walk, and the one-shot permit whose intent this lane spent the day convicting. The largest of the five. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltgmf3-stage-3-custody-realize",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF3)",
      "stage": {
        "index": 3,
        "name": "Custody RealizeAndClose",
        "program_role": "custody"
      },
      "measured": 45921,
      "tolerance": 20000,
      "budget": 65921,
      "enforced": true,
      "provenance": "Custody RealizeAndClose: the projection is realized against the candidate CoreState digest and the source compartment is closed. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltgmf3-stage-4-claims-foundingv5",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF3)",
      "stage": {
        "index": 4,
        "name": "Claims FoundingV5",
        "program_role": "claims"
      },
      "measured": 177589,
      "tolerance": 20000,
      "budget": 197589,
      "enforced": true,
      "provenance": "Claims FoundingV5: the twenty-nine joins, the four-role activation loop, and the aggregate, Position and admission it allocates. This is the stage that refused 0x518D PermitBody on every run before 93a2793bd, at about 63,000 CU.\n\nRE-PINNED 2026-09-04 from 155,086 to the highest of THREE observed draws, which is what CU_BUDGETS.md asks for and what the 155,086 row could not do: it was one draw, taken on the first tier-1 run that ever completed, and it said so. The two new draws are a matched pair taken the same evening on one laptop against BYTE-IDENTICAL ELFs (claims f6ab44acb90423440af02d312466305b3a4dea3afa793999d06f1ae1c3850d70 in both): 161,089 at d24c191c2 with a 199-transaction campaign, and 177,589 at c42da8fef with a 207-transaction one, the eight extra being the Registry reauthentication and record-abort lanes.\n\nIT IS NOT A REGRESSION, and the control says so in a way that admits no second reading. Every one of the five DCLTGMF3 stage deltas between those two runs is a near-exact multiple of the 1,500 CU a `find_program_address` bump-search iteration costs -- stage 1 +4,498, stage 2 +4,498, stage 3 MINUS 4,502, stage 4 +16,500, stage 5 +3,000 -- and one of them is NEGATIVE. A compute regression cannot make a stage cheaper. What moved is the campaign's own documented noise cause, already named in the row this replaces: expiry slots and lookup-table slots are wall-clock derived under a keypair seed, a longer campaign reaches the founding at different slots, and the PDAs those slots seed draw different bumps.\n\nThe tolerance is UNCHANGED at 20,000. This is a re-pin of `measured` to the top of the observed band, not a widening: widening the tolerance to absorb a draw the band already covers is how a ratchet stops ratcheting. The four sibling dcltgmf3 stage rows and the whole-transaction row are still single draws from 93a2793bd and are the same shape; they are left alone because they are green and because re-pinning a green row upward can only cost sensitivity."
    },
    {
      "id": "dcltgmf3-stage-5-core-open",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "found the Market atomically: Lock, Found, Realize, Claims, Open (DCLTGMF3)",
      "stage": {
        "index": 5,
        "name": "Core Open",
        "program_role": "core"
      },
      "measured": 56265,
      "tolerance": 20000,
      "budget": 76265,
      "enforced": true,
      "provenance": "Core Open, LAST, so that any late refusal rolls the whole chain back. Its predecessor row was `dcltgmf1-stage-5-open-and-outer-joins`, scope `derived`, measured 0 and NOT enforced, because no run had ever reached it. It is a depth-2 stage on the chain's own accounting now, so it is an ordinary enforced stage budget. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltgmf3-hostile-rollback",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "DCLTGMF3 refuses a substituted Claims request and rolls the whole founding back",
      "measured": 30660,
      "tolerance": 20000,
      "budget": 50660,
      "enforced": true,
      "provenance": "HOSTILE. The outer's cross-request join refuses a substituted Claims record before its first CPI, and the refusal has to take Lock, Found, Realize and Claims with it. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltcfq1-whole",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "prepare exact controller funding ledgers and checkpoint (DCLTCFQ1)",
      "measured": 496732,
      "tolerance": 90000,
      "budget": 586732,
      "enforced": true,
      "provenance": "The controller-funding half of the old `DCLTPCB1`, now its own transaction: both FundingLedgerV2 accounts and the checkpoint DCLTCF1A and DCLTPCB2 read. Replaces `dcltpcb1-whole`. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltcfq1-stage-1-resolution-pre-market-funding",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "prepare exact controller funding ledgers and checkpoint (DCLTCFQ1)",
      "stage": {
        "index": 1,
        "name": "Resolution PreMarketFunding V2",
        "program_role": "resolution"
      },
      "measured": 313180,
      "tolerance": 30000,
      "budget": 343180,
      "enforced": true,
      "provenance": "The Resolution CPI that creates the 0b0111 controller ledger and projects Core's Found to authenticate the Market it is funding: the only depth-2 stage of this transaction, and 63% of it. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltpcb2-whole",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "stage projected custody against prepared controller funding (DCLTPCB2)",
      "measured": 619359,
      "tolerance": 90000,
      "budget": 709359,
      "enforced": true,
      "provenance": "Projected Custody staged against the ledgers DCLTCFQ1 prepared. Replaces `dcltpcb1-second-prestate-whole`, whose transaction the campaign no longer submits. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltpcb2-stage-1-custody-initialize",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "stage projected custody against prepared controller funding (DCLTPCB2)",
      "stage": {
        "index": 1,
        "name": "Custody Initialize, including the Core projection CPI",
        "program_role": "custody"
      },
      "measured": 253115,
      "tolerance": 30000,
      "budget": 283115,
      "enforced": true,
      "provenance": "Custody Initialize, including Core's ProjectFound37 projection CPI. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltpcb2-stage-2-custody-openhoard",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "stage projected custody against prepared controller funding (DCLTPCB2)",
      "stage": {
        "index": 2,
        "name": "Custody OpenHoard",
        "program_role": "custody"
      },
      "measured": 56015,
      "tolerance": 20000,
      "budget": 76015,
      "enforced": true,
      "provenance": "Custody OpenHoard, including its Token-2022 account creation. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltpcb2-stage-3-custody-opensourcecompartment",
      "campaign": "tier1",
      "scope": "stage",
      "transaction": "stage projected custody against prepared controller funding (DCLTPCB2)",
      "stage": {
        "index": 3,
        "name": "Custody OpenSourceCompartment",
        "program_role": "custody"
      },
      "measured": 79976,
      "tolerance": 50000,
      "budget": 129976,
      "enforced": true,
      "provenance": "Custody OpenSourceCompartment: the source vault and its replay authority. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltpcb2-reordered-tail-refusal",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "DCLTPCB2 refuses a reordered FundingLedgerV2 tail and rolls the transaction back",
      "measured": 38746,
      "tolerance": 70000,
      "budget": 108746,
      "enforced": true,
      "provenance": "HOSTILE. Swapping the two FundingLedgerV2 accounts breaks the canonical controller partition and the artifact's ordered `funding_list_id` join. It draws 38,746 now against its predecessor's 781,102, because the ledgers it refuses are PREPARED by an earlier transaction rather than created by this one -- the refusal is reached before the work is done. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltpcb2-non-terminal-refusal",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "DCLTPCB2 refuses a non-terminal projected-Custody request",
      "measured": 24706,
      "tolerance": 15000,
      "budget": 39706,
      "enforced": true,
      "provenance": "HOSTILE. A well-formed but NON-TERMINAL prestate request refuses in `decode_projected_request`, before any CPI. The label moved from DCLTPCB1 to DCLTPCB2; the draw is within 2,530 of its predecessor's 22,176. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "dcltcf1a-pre-expiry-cleanup-refusal",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "DCLTCF1A refuses to close the first controller ledger before checkpoint expiry",
      "measured": 8453,
      "tolerance": 15000,
      "budget": 23453,
      "enforced": true,
      "provenance": "HOSTILE, and NEW: this transaction did not exist when the founding was one ladder. While the checkpoint has not expired the founding is still satisfiable and the prepared ledgers may not be destroyed. 8,453 CU, before any CPI. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "found37-whole",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "create canonical Found37 Market",
      "measured": 196380,
      "tolerance": 15000,
      "budget": 211380,
      "enforced": true,
      "provenance": "Canonical Core Found37 over a finalized address lookup table. Replaces `found31-whole`, whose label the campaign stopped submitting; 196,380 against its 237,041. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "found37-substituted-market-rollback",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "Found37 refuses a substituted Market coordinate and rolls the transaction back",
      "measured": 164293,
      "tolerance": 15000,
      "budget": 179293,
      "enforced": true,
      "provenance": "HOSTILE. A substituted Market coordinate under attacker-chosen lookup-table routing, refused by the rent-credit-to-Market PDA binding. Replaces `found31-substituted-market-rollback`. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "core-infrastructure-profile-init",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "initialize Core infrastructure profile",
      "measured": 243321,
      "tolerance": 15000,
      "budget": 258321,
      "enforced": true,
      "provenance": "The sole Core-owned infrastructure profile. Re-pinned from 229,835: the observed draw is 243,321, which was 1,514 CU UNDER its old budget -- a row that was about to go red for growth nobody had attributed. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "activation-role-core",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "activate immutable release-set role: Core",
      "measured": 641772,
      "tolerance": 20000,
      "budget": 661772,
      "enforced": true,
      "provenance": "On-chain release admission hashes the whole ProgramData ELF at about one compute unit per two bytes, so this row is a measurement of the ARTIFACT and moves whenever the ELF does. Every one of the five was OVER its 2026-08-27 pin on this run -- Trading by 458,466 -- which is the ELFs having grown, not a regression in activation. Trading's 1,200,411 leaves 199,589 CU (14.3%) to the 1,400,000 ceiling and is the row to watch in this file. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "activation-role-claims",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "activate immutable release-set role: Claims",
      "measured": 729162,
      "tolerance": 20000,
      "budget": 749162,
      "enforced": true,
      "provenance": "On-chain release admission hashes the whole ProgramData ELF at about one compute unit per two bytes, so this row is a measurement of the ARTIFACT and moves whenever the ELF does. Every one of the five was OVER its 2026-08-27 pin on this run -- Trading by 458,466 -- which is the ELFs having grown, not a regression in activation. Trading's 1,200,411 leaves 199,589 CU (14.3%) to the 1,400,000 ceiling and is the row to watch in this file. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "activation-role-trading",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "activate immutable release-set role: Trading",
      "measured": 1200411,
      "tolerance": 20000,
      "budget": 1220411,
      "enforced": true,
      "provenance": "On-chain release admission hashes the whole ProgramData ELF at about one compute unit per two bytes, so this row is a measurement of the ARTIFACT and moves whenever the ELF does. Every one of the five was OVER its 2026-08-27 pin on this run -- Trading by 458,466 -- which is the ELFs having grown, not a regression in activation. Trading's 1,200,411 leaves 199,589 CU (14.3%) to the 1,400,000 ceiling and is the row to watch in this file. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "activation-role-resolution",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "activate immutable release-set role: Resolution",
      "measured": 446563,
      "tolerance": 20000,
      "budget": 466563,
      "enforced": true,
      "provenance": "On-chain release admission hashes the whole ProgramData ELF at about one compute unit per two bytes, so this row is a measurement of the ARTIFACT and moves whenever the ELF does. Every one of the five was OVER its 2026-08-27 pin on this run -- Trading by 458,466 -- which is the ELFs having grown, not a regression in activation. Trading's 1,200,411 leaves 199,589 CU (14.3%) to the 1,400,000 ceiling and is the row to watch in this file. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "activation-role-custody",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "activate immutable release-set role: Custody",
      "measured": 328016,
      "tolerance": 20000,
      "budget": 348016,
      "enforced": true,
      "provenance": "On-chain release admission hashes the whole ProgramData ELF at about one compute unit per two bytes, so this row is a measurement of the ARTIFACT and moves whenever the ELF does. Every one of the five was OVER its 2026-08-27 pin on this run -- Trading by 458,466 -- which is the ELFs having grown, not a regression in activation. Trading's 1,200,411 leaves 199,589 CU (14.3%) to the 1,400,000 ceiling and is the row to watch in this file. one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "activation-refuses-pre-revocation-core",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "immutable release activation refuses pre-revocation Core",
      "measured": 630713,
      "tolerance": 20000,
      "budget": 650713,
      "enforced": true,
      "provenance": "HOSTILE. Core still carries an upgrade authority, so activation must refuse -- after hashing the ELF, which is why this row tracks Core's activation row almost exactly (641,772 against 630,713). one COMPLETING tier-1 run at 93a2793bd on 2026-09-03, the first this campaign has ever produced: the founding refused on its last transaction from 2026-08-27 until that commit. So `measured` here is ONE draw, not the highest of several, and the tolerance is the one the row it replaces carried -- the noise CAUSE is unchanged (expiry slots and lookup-table slots are still wall-clock derived under a keypair seed), so the recorded band still applies, and nothing about this run licenses tightening it."
    },
    {
      "id": "core-funding-create",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "core-funding-create-v1",
      "measured": 303613,
      "tolerance": 30000,
      "budget": 333613,
      "enforced": true,
      "provenance": "CreateFund, the first of the post-founding readiness ladder. ENFORCED 2026-09-04. This row said in its own words that it was 'enforced by nothing until a second completing tier-1 run exists' and that 'that second run is the whole of what this row is waiting for'. There are now three. three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. The three draws are 287,115, 293,113 and 287,115, all BELOW the 303,613 recorded from the single completing run at 93a2793bd, so `measured` stays at 303,613 -- the highest of four draws, which is this file's rule -- and nothing is re-pinned downward onto a lucky run. The band over all four is 16,498, and it is bump-search noise rather than a change in what executed: it is within two CU of a whole number of the 1,500 a `sol_create_program_address` iteration costs. tolerance = roundup(16,498, 10,000) + 10,000 = 30,000, which is what the rule at the top of this file returns and not a number chosen to make the row green."
    },
    {
      "id": "resolution-funding-activate",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "resolution-funding-activate-v1",
      "measured": 278311,
      "tolerance": 30000,
      "budget": 308311,
      "enforced": true,
      "provenance": "The Resolution activation in the readiness ladder. ENFORCED 2026-09-04. This row said in its own words that it was 'enforced by nothing until a second completing tier-1 run exists' and that 'that second run is the whole of what this row is waiting for'. There are now three. three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. The three draws are 267,813, 269,311 and 267,813, all BELOW the 278,311 recorded from the single completing run at 93a2793bd, so `measured` stays at 278,311 -- the highest of four draws, which is this file's rule -- and nothing is re-pinned downward onto a lucky run. The band over all four is 10,498, and it is bump-search noise rather than a change in what executed: it is within two CU of a whole number of the 1,500 a `sol_create_program_address` iteration costs. tolerance = roundup(10,498, 10,000) + 10,000 = 30,000, which is what the rule at the top of this file returns and not a number chosen to make the row green."
    },
    {
      "id": "core-funding-accept",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "core-funding-accept-v1",
      "measured": 192371,
      "tolerance": 30000,
      "budget": 222371,
      "enforced": true,
      "provenance": "VerifyFundReady, the last of the readiness ladder. ENFORCED 2026-09-04. This row said in its own words that it was 'enforced by nothing until a second completing tier-1 run exists' and that 'that second run is the whole of what this row is waiting for'. There are now three. three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. The three draws are 174,372, 180,371 and 174,372, all BELOW the 192,371 recorded from the single completing run at 93a2793bd, so `measured` stays at 192,371 -- the highest of four draws, which is this file's rule -- and nothing is re-pinned downward onto a lucky run. The band over all four is 17,999, and it is bump-search noise rather than a change in what executed: it is within two CU of a whole number of the 1,500 a `sol_create_program_address` iteration costs. tolerance = roundup(17,999, 10,000) + 10,000 = 30,000, which is what the rule at the top of this file returns and not a number chosen to make the row green."
    },
    {
      "id": "reauthenticate-role-core",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "reauthenticate the activated role: Core",
      "measured": 11337,
      "tolerance": 15000,
      "budget": 26337,
      "enforced": true,
      "provenance": "`registry/process_reauthenticate#Reauthenticate` for Core, one of the five transactions SEVEN added at `c42da8fef` -- the route every role adapter's rule is written in and none of them can call, because a child entered under a Registry continuation cannot CPI back. Three draws, 11337, 11337, 11337: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. THE SPREAD ACROSS ROLES IS ONE CU AND IT IS STRUCTURAL, not noise: Core, Claims and Trading draw 11,337 and Resolution and Custody 11,336, the same split on every run. It is the role discriminant's own encoding, not a measurement error, and it is why these are five rows and not one. WHAT THIS ROW IS NOT: a measurement of the artifact. Reauthentication does not hash the role ELF -- the witness `reauthentication-does-not-rehash-the-role-elf` proves it against activation's own numbers out of this same evidence, at a factor of twenty-eight -- so unlike the five `activation-role-*` rows above, this number does not move when the ELF does. A change here is a change in the Registry's code."
    },
    {
      "id": "reauthenticate-role-claims",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "reauthenticate the activated role: Claims",
      "measured": 11337,
      "tolerance": 15000,
      "budget": 26337,
      "enforced": true,
      "provenance": "`registry/process_reauthenticate#Reauthenticate` for Claims, one of the five transactions SEVEN added at `c42da8fef` -- the route every role adapter's rule is written in and none of them can call, because a child entered under a Registry continuation cannot CPI back. Three draws, 11337, 11337, 11337: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. THE SPREAD ACROSS ROLES IS ONE CU AND IT IS STRUCTURAL, not noise: Core, Claims and Trading draw 11,337 and Resolution and Custody 11,336, the same split on every run. It is the role discriminant's own encoding, not a measurement error, and it is why these are five rows and not one. WHAT THIS ROW IS NOT: a measurement of the artifact. Reauthentication does not hash the role ELF -- the witness `reauthentication-does-not-rehash-the-role-elf` proves it against activation's own numbers out of this same evidence, at a factor of twenty-eight -- so unlike the five `activation-role-*` rows above, this number does not move when the ELF does. A change here is a change in the Registry's code."
    },
    {
      "id": "reauthenticate-role-trading",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "reauthenticate the activated role: Trading",
      "measured": 11337,
      "tolerance": 15000,
      "budget": 26337,
      "enforced": true,
      "provenance": "`registry/process_reauthenticate#Reauthenticate` for Trading, one of the five transactions SEVEN added at `c42da8fef` -- the route every role adapter's rule is written in and none of them can call, because a child entered under a Registry continuation cannot CPI back. Three draws, 11337, 11337, 11337: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. THE SPREAD ACROSS ROLES IS ONE CU AND IT IS STRUCTURAL, not noise: Core, Claims and Trading draw 11,337 and Resolution and Custody 11,336, the same split on every run. It is the role discriminant's own encoding, not a measurement error, and it is why these are five rows and not one. WHAT THIS ROW IS NOT: a measurement of the artifact. Reauthentication does not hash the role ELF -- the witness `reauthentication-does-not-rehash-the-role-elf` proves it against activation's own numbers out of this same evidence, at a factor of twenty-eight -- so unlike the five `activation-role-*` rows above, this number does not move when the ELF does. A change here is a change in the Registry's code."
    },
    {
      "id": "reauthenticate-role-resolution",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "reauthenticate the activated role: Resolution",
      "measured": 11336,
      "tolerance": 15000,
      "budget": 26336,
      "enforced": true,
      "provenance": "`registry/process_reauthenticate#Reauthenticate` for Resolution, one of the five transactions SEVEN added at `c42da8fef` -- the route every role adapter's rule is written in and none of them can call, because a child entered under a Registry continuation cannot CPI back. Three draws, 11336, 11336, 11336: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. THE SPREAD ACROSS ROLES IS ONE CU AND IT IS STRUCTURAL, not noise: Core, Claims and Trading draw 11,337 and Resolution and Custody 11,336, the same split on every run. It is the role discriminant's own encoding, not a measurement error, and it is why these are five rows and not one. WHAT THIS ROW IS NOT: a measurement of the artifact. Reauthentication does not hash the role ELF -- the witness `reauthentication-does-not-rehash-the-role-elf` proves it against activation's own numbers out of this same evidence, at a factor of twenty-eight -- so unlike the five `activation-role-*` rows above, this number does not move when the ELF does. A change here is a change in the Registry's code."
    },
    {
      "id": "reauthenticate-role-custody",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "reauthenticate the activated role: Custody",
      "measured": 11336,
      "tolerance": 15000,
      "budget": 26336,
      "enforced": true,
      "provenance": "`registry/process_reauthenticate#Reauthenticate` for Custody, one of the five transactions SEVEN added at `c42da8fef` -- the route every role adapter's rule is written in and none of them can call, because a child entered under a Registry continuation cannot CPI back. Three draws, 11336, 11336, 11336: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. THE SPREAD ACROSS ROLES IS ONE CU AND IT IS STRUCTURAL, not noise: Core, Claims and Trading draw 11,337 and Resolution and Custody 11,336, the same split on every run. It is the role discriminant's own encoding, not a measurement error, and it is why these are five rows and not one. WHAT THIS ROW IS NOT: a measurement of the artifact. Reauthentication does not hash the role ELF -- the witness `reauthentication-does-not-rehash-the-role-elf` proves it against activation's own numbers out of this same evidence, at a factor of twenty-eight -- so unlike the five `activation-role-*` rows above, this number does not move when the ELF does. A change here is a change in the Registry's code."
    },
    {
      "id": "abandoned-record-begin",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "publish record: Begin JCuya8hCp3Xxftcr8ts2USpA5d3isiw6eDP7SFSde7zt",
      "measured": 18191,
      "tolerance": 15000,
      "budget": 33191,
      "enforced": true,
      "provenance": "The Begin of the record that exists to be abandoned -- SEVEN's `c42da8fef` publishes it, never appends to it and never finalizes it, so that `registry/process_abort#4` has something to reclaim. Three draws, 18,191, 18,191, 18,191: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. THE LABEL CARRIES AN ADDRESS AND THAT IS DELIBERATE. `publish record: Begin *` is a BINDING pattern covering 41 publications in this campaign; a budget must name exactly one transaction or the evaluator returns AMBIGUOUS, so this row names the record's own derived address. That address is a PDA over the Registry's gauntlet-local program id and the digest of a body that ends in the seeded payer's pubkey, so it is stable across runs -- verified identical on all three -- and it MOVES if the keypair-seed preimage or the program-id domain moves. That is the right failure direction: the row goes MISSING, which is red, rather than silently matching a different Begin. It is also the only Begin in the campaign with no Append and no Finalize, which is how it was identified rather than guessed."
    },
    {
      "id": "abort-substituted-refund-refusal",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "Abort refuses a substituted sponsor refund wallet",
      "measured": 7258,
      "tolerance": 15000,
      "budget": 22258,
      "enforced": true,
      "provenance": "HOSTILE, and the boundary `registry/process_abort#4` is judged on: the staging cursor is the sole author of the sponsor refund identity, and a frame naming a stranger's wallet must die on that comparison before a lamport moves. It rides in FRONT of the reclaim on purpose, so the record has to survive being refused for. Three draws, 7,258, 7,258, 7,258: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. A REFUSAL IS BUDGETED ON PURPOSE -- a refusal that gets more expensive is a refusal that can stop fitting. SEVEN measured 7,257 at `c42da8fef` against a 207-transaction campaign; this tier submits 209 and draws 7,258. One CU, on a route that runs no bump search, so it is a length that moved by a byte and not an iteration."
    },
    {
      "id": "abort-reclaims-abandoned-record",
      "campaign": "tier1",
      "scope": "transaction",
      "transaction": "reclaim an abandoned record's rent and staging bounty (Abort)",
      "measured": 8503,
      "tolerance": 15000,
      "budget": 23503,
      "enforced": true,
      "provenance": "`registry/process_abort#4`, DCLTRIX1 action 4, and the campaign's only exercise of the record family's liveness guarantee: an abandoned record set can always be reclaimed and never strands its rent or its prepaid bounty. Until `c42da8fef` this route was unreachable from outside the program BY CONSTRUCTION -- `AbortRecordV1` had no host builder anywhere -- so it had never executed rather than merely never been scheduled. Three draws, 8,503, 8,503, 8,503: three tier-1 runs on ONE ELF set at 9ae8fd53be60, one seed, the campaign stage forced with `--from campaign` so nothing else was rebuilt between them: 03:44:58Z at load average 6.98, 04:05:06Z at 9.17, and 04:26:51Z at 16.50 -- a factor of 2.4 in machine load across the three, which is the variable that moves this campaign's slots and therefore its bump search. 209 transactions each, exit 0, 24 witnesses checked and 0 failed on all three. BAND 0 ACROSS ALL THREE, byte-identical, so the tolerance is this file's 15,000 floor: roundup(0, 10,000) + 10,000 = 10,000, lifted to the floor. The draws being identical under a 2.4x spread in load is not luck -- this route runs no `find_program_address`, so it has no bump search to draw, which is the one noise source this campaign has left after the keypair seed. It matches SEVEN's 8,503 at `c42da8fef` exactly, on a campaign two transactions longer, which is what a route with no bump search and no ELF hash looks like."
    },
    {
      "id": "series-consume-founds-with-permit",
      "campaign": "tier4-series-occurrence-programtest",
      "scope": "transaction",
      "transaction": "Series occurrence Consume founds its Market with a Core permit",
      "measured": 722142,
      "tolerance": 15000,
      "budget": 737142,
      "enforced": true,
      "provenance": "The ProgramTest FAST LANE for the Core founding path, and the reason this campaign is in the budgets file at all: it drives Core's `found` plus the one-shot permit — the same Core code that costs stage 2 of DCLTGMF1 — with no validator and no port, in under a minute. A Core founding regression surfaces here BEFORE the six-minute tier-1 campaign. Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. The previous pin was 744,795, the highest of six UNSEEDED runs with a 24,000 band; the drop is not a saving, it is the bump-search iterations leaving the measurement."
    },
    {
      "id": "series-consume-founds-with-permit-replay-campaign",
      "campaign": "tier4-series-occurrence-programtest",
      "scope": "transaction",
      "transaction": "Series occurrence Consume founds its Market with a Core permit (replay campaign)",
      "measured": 722142,
      "tolerance": 15000,
      "budget": 737142,
      "enforced": true,
      "provenance": "The same founding driven by the fixture that then replays its ticket. Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. It now measures EXACTLY the same as series-consume-founds-with-permit, which is the honest answer: it is the same transaction against the same prestate, and the 14,847 CU that used to separate the two pins was entirely bump-search noise. Previous pin 737,295, highest of six unseeded runs, band 19,500."
    },
    {
      "id": "series-consume-late-hoard-refusal",
      "campaign": "tier4-series-occurrence-programtest",
      "scope": "transaction",
      "transaction": "Series Consume refuses a late Hoard postcondition",
      "measured": 682391,
      "tolerance": 15000,
      "budget": 697391,
      "enforced": true,
      "provenance": "The rollback case: a refusal raised AFTER the child committed, which must take Found and all replay state with it. Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. Previous pin 692,942, highest of six unseeded runs, band 15,000."
    },
    {
      "id": "series-consume-replayed-ticket-refusal",
      "campaign": "tier4-series-occurrence-programtest",
      "scope": "transaction",
      "transaction": "Series Consume refuses a replayed ticket",
      "measured": 366167,
      "tolerance": 15000,
      "budget": 381167,
      "enforced": true,
      "provenance": "Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. Previous pin 370,694, highest of six unseeded runs, band 4,500."
    },
    {
      "id": "series-consume-substituted-programdata-refusal",
      "campaign": "tier4-series-occurrence-programtest",
      "scope": "transaction",
      "transaction": "Series Consume refuses a substituted Claims ProgramData",
      "measured": 183196,
      "tolerance": 15000,
      "budget": 198196,
      "enforced": true,
      "provenance": "Re-pinned by the SEED lane at bbfc53a, on a SEEDED fixture: programs/dclutch-core-sbf/tests/found_program_test.rs now derives its payer and its vault addresses from a fixed checked-in seed instead of Keypair::new() and Pubkey::new_unique(). FOUR runs of one compiled test binary against byte-identical ELFs (core d272bc7d669f0983, custody fe7ce5f80f4a08c8, registry 0033c6b55e8277dc, rent 3486a8197af49231, series-consume-caller ce1160b7035c4295) produced the SAME number every time, the fourth under --test-threads 1. Band 0, so tolerance is the 15,000 floor. The single-threaded run is the load-bearing one: Pubkey::new_unique() reads a process-global counter, so before the seed the value a test drew depended on how four concurrent tests interleaved, and a run that changes the thread count would have moved it. This row is why the seed exists. Its previous pin was the highest of SEVEN runs, range 183,223-189,223, band 6,000. It looked like the quietest row in the fast lane at band 1,500 over the first six runs, and the seventh drew 189,223. That seventh run is NOISE and not a regression, and the attribution is exact rather than argued: none of the five programs this campaign builds (registry, rent, custody, core, the series-consume caller) changed at all between the first run and the seventh, so the ELFs are identical bytes and 4,500 is three `find_program_address` iterations. It is the clearest single demonstration in this file that six runs is not enough to bound a band."
    },
    {
      "id": "hot-canonical-bundle-phase-subtotals",
      "campaign": "unassigned",
      "scope": "derived",
      "transaction": "canonical Direct Profile14 Hot bundle",
      "measured": 0,
      "tolerance": 0,
      "budget": 0,
      "enforced": false,
      "unenforced_reason": "There is no green number to pin. The canonical Hot bundle does not pass at HEAD: the tail is over the 32,768-byte heap at phase 7 (WAVE.md, W2i lane), so `real_registry_executes_profile14_direct_hot_under_protocol_limit` in programs/dclutch-trading-sbf/program-test/tests/registry_hot_continuation.rs is not a green transaction whose CU could be budgeted. Its phase subtotals additionally require `--features hot-cu-profile`, which switches the ten `hot_cu_checkpoint!` sites from no-ops to log lines, so an ordinary run emits no phase data at all. Both files are W2i's live surface and this lane is read-only toward them.",
      "recorded_measurements": {
        "source": "docs/decisions/0005-per-market-authentication-cache.md, converged at ca5e5f1, canonical Direct Profile14 bundle, COMPUTE_LIMIT 1,400,000, real 32,768-byte heap, at the same `TradingSbfError::Content` lifecycle-preplan refusal before and after -- ADR 0005 was written before the 0007 band renumber and records it as `Custom(3)`; the same refusal is 0x4003 now",
        "artifacts_phase_before": 645836,
        "artifacts_phase_after": 56693,
        "cumulative_to_runtime_observations_before": 850425,
        "cumulative_to_runtime_observations_after": 253391,
        "cumulative_to_mid_preplan_refusal_before": 1220769,
        "cumulative_to_mid_preplan_refusal_after": 568486
      },
      "provenance": "Recorded from ADR 0005's own measured table rather than re-measured, per the close-out doctrine's `no more re-measuring into tables`. It becomes an enforced entry the day W2i's heap gate goes green, and the entry to write then is the whole-bundle transaction CU plus the phase subtotals from a `--features hot-cu-profile` run."
    },
    {
      "id": "claims-sparse-admit-transfer-close",
      "campaign": "claims-family-programtest",
      "scope": "transaction",
      "transaction": "claims sparse: admit, transfer and close",
      "measured": 561881,
      "tolerance": 15000,
      "budget": 576881,
      "enforced": true,
      "provenance": "The heaviest transaction in either family campaign and the one the composed Claims chain exists for: Admit -> SparseNativeTransfer -> Close, three Claims stages inside one rollback domain, driven by a real test caller through CPI. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [561881, 561881], band 0. Band 0, so the tolerance is the 15,000 floor. NOT MOVED at 2026-09-02, and it was red by +45,580 for four days. The cause was not this route: sparse_native_transfer_v1 authenticates three roles out of ONE immutable activation cache and paid a full hostile decode of it -- the five-role projection and all ten aliasing pairs -- for each, and it CPIs Custody, which decoded the same account three more times. 5767be46 and a216bbfc removed the repetition; the draw at 0f69918c is 511,538 against this 576,881, so the pin was right and nothing here moved. The 50,343 gap to the pinned 561,881 is recovered headroom, not drift, and it is recorded here rather than ratcheted away because this campaign's draw rides the same 1,500 CU search-depth grid the claims-wallet-position-payout row documents."
    },
    {
      "id": "claims-sparse-stage-1-admit",
      "campaign": "claims-family-programtest",
      "scope": "stage",
      "transaction": "claims sparse: admit, transfer and close",
      "measured": 207719,
      "tolerance": 15000,
      "budget": 222719,
      "enforced": true,
      "provenance": "Stage budgets are where the 15,000-scale teeth are: a regression localised to one Claims stage is red here even when the whole-transaction row is not. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [207719, 207719], band 0. Band 0, so the tolerance is the 15,000 floor.",
      "stage": {
        "index": 1,
        "name": "Claims Admit",
        "program_role": "claims"
      }
    },
    {
      "id": "claims-sparse-stage-2-sparse-native-transfer",
      "campaign": "claims-family-programtest",
      "scope": "stage",
      "transaction": "claims sparse: admit, transfer and close",
      "measured": 178046,
      "tolerance": 15000,
      "budget": 193046,
      "enforced": true,
      "provenance": "The middle stage, and the only one whose cost scales with the transfer's own sparse extent rather than with the admission. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [178046, 178046], band 0. Band 0, so the tolerance is the 15,000 floor.",
      "stage": {
        "index": 2,
        "name": "Claims SparseNativeTransfer",
        "program_role": "claims"
      }
    },
    {
      "id": "claims-sparse-stage-3-close",
      "campaign": "claims-family-programtest",
      "scope": "stage",
      "transaction": "claims sparse: admit, transfer and close",
      "measured": 117315,
      "tolerance": 15000,
      "budget": 132315,
      "enforced": true,
      "provenance": "The terminal stage. Its cost is the one that must stay small for a Position to be closable in the same transaction that empties it. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [117315, 117315], band 0. Band 0, so the tolerance is the 15,000 floor.",
      "stage": {
        "index": 3,
        "name": "Claims Close",
        "program_role": "claims"
      }
    },
    {
      "id": "claims-position-admit",
      "campaign": "claims-family-programtest",
      "scope": "transaction",
      "transaction": "claims position: admit",
      "measured": 236191,
      "tolerance": 15000,
      "budget": 251191,
      "enforced": true,
      "provenance": "The protocol Position lifecycle's single-stage admission, which is the Claims code DCLTGMF1's stage 4 (Claims FoundingV5) shares. A Claims regression shows up here in seconds instead of after a six-minute validator campaign. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [236191, 236191], band 0. Band 0, so the tolerance is the 15,000 floor. NOT MOVED at 2026-09-02: red by the same +45,580 class and green at 222,293 against this 251,191 once 5767be46 and a216bbfc removed the repeated activation-cache decode from Claims, Custody and Core. Its Custody leg is where its share of that was."
    },
    {
      "id": "claims-sparse-substituted-admission-receipt-refusal",
      "campaign": "claims-family-programtest",
      "scope": "transaction",
      "transaction": "claims sparse: transfer under a substituted admission receipt",
      "measured": 362405,
      "tolerance": 15000,
      "budget": 377405,
      "enforced": true,
      "provenance": "A hostile case, budgeted because a refusal that gets more expensive is a refusal that can stop fitting: the cost of REFUSING must stay far from the ceiling or an attacker picks the transaction that cannot complete either way. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [362405, 362405], band 0. Band 0, so the tolerance is the 15,000 floor."
    },
    {
      "id": "custody-legacy-open-vault",
      "campaign": "custody-family-programtest",
      "scope": "transaction",
      "transaction": "custody legacy: open vault",
      "measured": 145500,
      "tolerance": 15000,
      "budget": 160500,
      "enforced": true,
      "provenance": "Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [145500, 145500], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 130494, and the delta is +15006 CU = +10 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +15006 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one."
    },
    {
      "id": "custody-token-2022-open-vault",
      "campaign": "custody-family-programtest",
      "scope": "transaction",
      "transaction": "custody token-2022: open vault",
      "measured": 145567,
      "tolerance": 15000,
      "budget": 160567,
      "enforced": true,
      "provenance": "Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [145567, 145567], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 141061, and the delta is +4506 CU = +3 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +4506 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one."
    },
    {
      "id": "custody-legacy-delegated-external-transfer",
      "campaign": "custody-family-programtest",
      "scope": "transaction",
      "transaction": "custody legacy: delegated external transfer",
      "measured": 148297,
      "tolerance": 15000,
      "budget": 163297,
      "enforced": true,
      "provenance": "Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [148297, 148297], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 134791, and the delta is +13506 CU = +9 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +13506 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one."
    },
    {
      "id": "custody-token-2022-delegated-external-transfer",
      "campaign": "custody-family-programtest",
      "scope": "transaction",
      "transaction": "custody token-2022: delegated external transfer",
      "measured": 139746,
      "tolerance": 15000,
      "budget": 154746,
      "enforced": true,
      "provenance": "Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [139746, 139746], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 142740, and the delta is -2994 CU = -2 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved -2994 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one."
    },
    {
      "id": "custody-legacy-close-vault",
      "campaign": "custody-family-programtest",
      "scope": "transaction",
      "transaction": "custody legacy: close vault",
      "measured": 144553,
      "tolerance": 15000,
      "budget": 159553,
      "enforced": true,
      "provenance": "Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [144553, 144553], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 138547, and the delta is +6006 CU = +4 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +6006 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one."
    },
    {
      "id": "custody-token-2022-close-vault",
      "campaign": "custody-family-programtest",
      "scope": "transaction",
      "transaction": "custody token-2022: close vault",
      "measured": 158020,
      "tolerance": 15000,
      "budget": 173020,
      "enforced": true,
      "provenance": "Closing/opening/transferring under the role-seeded Custody replay PDA (decision 0008 section 7). RE-PINNED 2026-08-27 by the CUSTROLE lane from TWO runs of this campaign at one revision: draws [158020, 158020], band 0, so the tolerance is the 15,000 floor. The move is measured, not assumed: the same test at 3918cb8 (the revision before the role entered the replay seeds, built from a worktree so only custody-sbf and dclutch-custody-contract differ) drew 137014, and the delta is +21006 CU = +14 x 1,500 + 6. 1,500 CU is one find_program_address iteration and 6 CU is the extra sha256 for one more seed byte, so the ENTIRE move is the bump search re-rolling on a seed tuple that gained a component. It is not a code-path cost and it has no direction: the six rows moved +21006 here and one of them moved DOWN. NOTE for the CU-BUDGET lane: the pins this replaces were ALREADY stale at 3918cb8 -- the baseline draws above differ from them by -12,521 to +11,480, which is drift from lanes between c55b606 and 3918cb8, not from this one."
    },
    {
      "id": "structured-cross-schema-exposure-as-descriptor",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: cross-schema substitution: exposure as descriptor",
      "measured": 153291,
      "tolerance": 15000,
      "budget": 168291,
      "enforced": true,
      "provenance": "Cross-family/cross-schema substitution, direction one: the finalized CompositionExposureBundleV3 record offered in the immutable descriptor's account slot. The cost is the record-authentication prologue -- release set, Product/representation admission, both record digests -- which is where a substituted record is caught, so this row is the price of the join rather than of any transition. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [153291, 153291], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-cross-schema-descriptor-as-exposure",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: cross-schema substitution: descriptor as exposure",
      "measured": 153290,
      "tolerance": 15000,
      "budget": 168290,
      "enforced": true,
      "provenance": "The SAME substitution the other way, because a family wire that refuses one direction and admits the other is not joined. One compute unit cheaper than its twin, which is what a symmetric refusal reached at the same depth looks like. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [153290, 153290], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-recipe-substitution-permuted-coefficients",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: recipe substitution: permuted coefficients",
      "measured": 197124,
      "tolerance": 15000,
      "budget": 212124,
      "enforced": true,
      "provenance": "A same-width RepresentationDescriptorV2 whose coefficients are the canonical ones PERMUTED -- a valid record naming a recipe the composition does not state. More expensive than the cross-schema pair because the descriptor decodes cleanly and the refusal comes from the request/descriptor identity join rather than from the record admission. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [197124, 197124], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-receipt-backed-by-receipt",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: receipt backed by receipt",
      "measured": 230625,
      "tolerance": 15000,
      "budget": 245625,
      "enforced": true,
      "provenance": "THE ACCUSATION MOVED, and with it the price. This row used to read 11,302 -- the cheapest refusal in the island by an order of magnitude -- because the hostile substituted the receipt Mint into the ASSET ROW, where `RepresentationRequestV2::validate` refused it before any caller could derive its Trading authority, so it died in the caller with no CPI issued. Physical ABI v3 derives the three per-coordinate keys instead of carrying them, so no row byte is left to corrupt; the substitution is made in the ACCOUNT METAS now and is refused on chain by `ClaimsSbfError::ReceiptAlias` (0x500F) in `authenticate_asset_identities`. A refusal that reaches the Claims adapter costs what reaching the Claims adapter costs: release authentication, the Product join, the descriptor and exposure decodes and the replay all run first. So a twentyfold rise is the CORRECT reading of a refusal that moved from the caller into the chain, and the regression to watch for is the opposite one -- a collapse back toward the caller would mean the frame check stopped being reached. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [230625, 230625], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-one-atom-skew-coordinate-0",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: one-atom K_i skew at coordinate 0",
      "measured": 264338,
      "tolerance": 15000,
      "budget": 279338,
      "enforced": true,
      "provenance": "Coordinate 0's backing skewed by exactly ONE shard atom. The full projection is built before prepare() compares the asset row's coefficient against the descriptor's, so a skew costs nearly a whole successful issue -- which is the point: the campaign basis [2, 3, 5] over 7 is pairwise coprime and coprime to the denominator so that a one-atom skew cannot be presented as a legitimate quantity at another coordinate, and the chain has to walk the whole frame to say so. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [264338, 264338], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-one-atom-skew-coordinate-1",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: one-atom K_i skew at coordinate 1",
      "measured": 269296,
      "tolerance": 15000,
      "budget": 284296,
      "enforced": true,
      "provenance": "The same one-atom skew at coordinate 1. The 764-unit spread from coordinate 0 is the extra projection row walked before the mismatch is reached, not noise: both runs are byte-identical. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [269296, 269296], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-one-atom-skew-coordinate-2",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: one-atom K_i skew at coordinate 2",
      "measured": 265254,
      "tolerance": 15000,
      "budget": 280254,
      "enforced": true,
      "provenance": "The same one-atom skew at coordinate 2, the last coordinate and therefore the most expensive of the three. Three separate transactions, so the coprime basis is exercised per coordinate rather than once. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [265254, 265254], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-under-configured-receipt-mint-issue",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: issue against a receipt Mint with only the Mint role",
      "measured": 229834,
      "tolerance": 15000,
      "budget": 244834,
      "enforced": true,
      "provenance": "Decision 0011 s3b's under-configured founding: the receipt Mint carries the representation authority as its Mint authority and not as its permissioned-burn authority. parse_behavior_mint refuses it (ClaimsSbfError::Token, 0x5009) with ZERO Token-2022 CPIs issued, so this row is the cost of the behavior-profile read and everything before it. s3b predicted the failure would land at BurnReceipt with the descriptor already committed; it lands at the first issue instead, and this is what that costs. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [229834, 229834], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "structured-under-configured-receipt-mint-unwrap",
      "campaign": "structured-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: unwrap against a receipt Mint with only the Mint role",
      "measured": 216007,
      "tolerance": 15000,
      "budget": 231007,
      "enforced": true,
      "provenance": "The consequence of the row above rather than a second finding: the issue committed nothing, so no representation replay exists to carry revision one and the unwrap is refused on Accounts (0x5001). BurnReceipt is never the thing that fails, because there is never anything to unwrap. Re-pinned 2026-09-02 by STRUCTURED from TWO seeded runs of the filtered structured campaign at one revision on one set of ELFs: draws [216007, 216007], band 0. Band 0, so the tolerance is this file's 15,000 floor."
    },
    {
      "id": "claims-wallet-position-payout",
      "campaign": "claims-rational-representation-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: a wallet-held Position is paid",
      "measured": 354370,
      "tolerance": 15000,
      "budget": 369370,
      "enforced": true,
      "provenance": "The redemption's step two: a plain wallet-held LBV2 Position paid out of a resolved Market's Hoard through claims/terminal_settlement_v3 under the widened execution role Claims (decision 0008 s8). The heaviest of the three wallet-payout shapes, because it is the only one that both evaluates the Product basis AND crosses the Custody boundary. Pinned 2026-08-27 from TWO runs of this campaign at one revision: draws [354370, 354370], band 0, so the tolerance is the 15,000 floor. NOT MOVED, and it briefly was. This row reached 475,665 by 2026-08-31 and a first pass raised it to 390,251 because 12,504 of the excess was an unlocated growth in the Custody CPI. It was not a conjunct: Custody decoded the same immutable 1,288-byte activation cache THREE times per invocation and Claims six, and a216bbfc and 5767be46 removed the repetition rather than paying for it. Draw at a216bbfc, on real ELFs: 359,529, with the Custody CPI at 112,625 against 121,843 at the 2026-08-27 pin commit 082f942f. So the 08-27 numbers were never wrong and the raise is reverted; 9,841 CU of margin, 25.7% of the 1,400,000 ceiling, 1,040,471 CU of headroom. READ THIS BEFORE RE-PINNING. The draw is exact per SOURCE REVISION and it is not a property of this route alone: release_set_id hashes the five deployed ELF digests, so every PDA seeded by it redraws its search depth when ANY lane changes ANY of the five programs, at 1,500 CU per rejected candidate. Measured on this transaction: +16,500 on all three rows between 3fa1a432 and 5767be46 with no source change to the route, -9,000 more between 5767be46 and a216bbfc, and a 245,491/292,005/251,506/266,506/256,006 spread across the 2026-08-28..08-31 window. Every difference is a multiple of 1,500. So band 0 from two runs at one revision measures re-runs of one ELF set and cannot see this at all, and NO tolerance that leaves this row any teeth can absorb it. The fix is the attempt subtraction the Direct margin gate already runs -- see programs/dclutch-trading-sbf/program-test/tests/direct_hot_top_level_margin_gate.rs, which censuses every find_program_address the route reaches, subtracts 1,500 * (256 - bump) per site, and proves the census complete by the residual being constant. Both gates should share one author for that arithmetic and tools/gauntlet/program-test-evidence is the crate they both already dev-depend on. Until then this row is a draw at a named commit, which is a gate defect with a named owner and not a reason to widen the tolerance."
    },
    {
      "id": "claims-wallet-position-payout-owner-paid",
      "campaign": "claims-rational-representation-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: the Position owner pays its own fee",
      "measured": 354369,
      "tolerance": 15000,
      "budget": 369369,
      "enforced": true,
      "provenance": "The same payout in the browser's shape: one wallet as both fee payer and authority, so coordinate 0 arrives writable (decision 0008 s8.4). Draws [354369, 354369], band 0, tolerance at the floor. NOT MOVED, for the row above's reasons; read it, including its re-pinning caution. Still pinned separately and still ONE unit below it -- draw at a216bbfc 359,528 against 359,529 -- which is the entire measured cost of the single-signer message. After a 116,136 CU recovery across three programs it is still one unit, which is the strongest available statement that the relaxed privilege never started doing work."
    },
    {
      "id": "claims-wallet-position-zero-payout",
      "campaign": "claims-rational-representation-v2-programtest",
      "scope": "transaction",
      "transaction": "claims rational-representation-v2: a losing wallet coordinate pays zero",
      "measured": 223244,
      "tolerance": 15000,
      "budget": 238244,
      "enforced": true,
      "provenance": "terminal_settlement_v3's zero-payout branch, reached from a wallet holding a claim at a LOSING coordinate. Draws [223244, 223244], band 0, tolerance at the floor. 131,126 CU cheaper than the paying shape, and that gap IS the Custody leg: authenticate_zero_custody_accounts re-derives the whole Custody frame from the persisted namespace without spending it, so the difference is one CPI and its token reads rather than a shortcut. NOT MOVED either, and this row is the one that shows what the other two cost: it reached 317,096 on 2026-08-31 and its draw at a216bbfc is 228,698, because 5767be46 removed two repetitions from Claims' own frame -- the activation cache decoded twice per single-role authentication, three roles, and the plan and 36-account frame parsed twice per parent-authenticated route. It CPIs neither Custody nor Core, so none of a216bbfc's recovery reaches it and none is claimed. 9,546 CU of margin, and the paying gap is now 130,831 -- back within 300 CU of the 131,126 this row was pinned against."
    },
    {
      "id": "general-hot-open-batch",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: OpenBatch on a founded market, N=2 (55 accounts, 4 invocations)",
      "measured": 669525,
      "tolerance": 15000,
      "budget": 684525,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. The campaign's first action and the one every other row is read against. 4 admitted invocations over a 55-account frame; the batch principal moves from the protocol payer."
    },
    {
      "id": "general-hot-close-batch",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: CloseBatch on the same founded market, N=2, at the batch's own collection close slot",
      "measured": 647858,
      "tolerance": 15000,
      "budget": 662858,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. The SECOND action on one market in one bank, executed at slot 17 -- the collection close slot the Batch itself names, warped to and then asserted through the executed Clock. Creates nothing and spends no credit."
    },
    {
      "id": "general-hot-close-batch-seal",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: the CloseBatch capability seal, produced by the family-neutral producer",
      "measured": 604075,
      "tolerance": 15000,
      "budget": 619075,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. CapabilitySealKeyV1's third seed is the action selector, so a market running two actions needs two seals; this is the real Trading ELF writing the second one, with 8 artifact records installed alongside it."
    },
    {
      "id": "general-hot-second-open-batch",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: a SECOND OpenBatch on the same founded market, sequence 1",
      "measured": 669525,
      "tolerance": 15000,
      "budget": 684525,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. Root revision 3 => 4, a different Batch identity at a different address, and the first batch left byte-for-byte. Reuses the first open's artifact records and its seal exactly: 0 records installed. 3,000 CU above the first open, which is the root's sequence coordinate being nonzero."
    },
    {
      "id": "general-hot-open-batch-n13",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: OpenBatch at N=13",
      "measured": 670185,
      "tolerance": 15000,
      "budget": 685185,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. The width ladder's middle rung. 5,160 CU above N=2 and the frame is 55 accounts at every width, because OpenBatch declares a zero per-outcome scalar stride and its bank does not grow with the Product."
    },
    {
      "id": "general-hot-open-batch-n258",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: OpenBatch at N=258",
      "measured": 695501,
      "tolerance": 15000,
      "budget": 710501,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. The accepted maximum Product width. 22,956 CU above N=2 over 256 more outcomes -- 89.7 CU per outcome, and none of it is bank width; the frame is the same 55 accounts and the same 151 scalars and 45 identities."
    },
    {
      "id": "general-hot-standalone-capability-seal",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: one standalone capability seal over 968 descriptor bytes",
      "measured": 619302,
      "tolerance": 15000,
      "budget": 634302,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. The seal producer alone, action selector 7. This is the one row whose test draws FRESH keypairs (Keypair::new()) and it still drew identically three times, which is consistent with a route that runs no bump search and is not a bound on one."
    },
    {
      "id": "general-hot-out-of-sequence-close",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot refusal: a CloseBatch resubmitted against its own poststate (0x4002 Root)",
      "measured": 40730,
      "tolerance": 15000,
      "budget": 55730,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. The out-of-sequence hostile, and it refuses as Root rather than Transition: HotExecutionEnvelopeV3 carries the root prestate digest the bundle was built against and Trading compares it to the account it was handed before any artifact runs. 40,730 CU is what a General action costs when the market's own state has moved under it."
    },
    {
      "id": "general-hot-foreign-entry",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot refusal: a market founded on a foreign manifest entry (0x4015 DescriptorManifestEntry)",
      "measured": 118766,
      "tolerance": 15000,
      "budget": 133766,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Measured through tools/gauntlet/general-hot/run-general-hot.sh --at. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). EVERY ROW DREW IDENTICALLY ON ALL THREE, band 0, so the tolerance is the rule's floor of 15,000. The campaign's cause for that is structural rather than lucky: its keypairs are fixed byte arrays (Keypair::new_from_array) and its slots come from warp_to_slot rather than wall clock, so the bump-search draw that is tier 1's remaining noise source has nothing to vary. THE COHORT-15 WALL, reproduced. That deployment's OpenBatch refused this code after 128,724 CU on devnet and had never been reproducible in a harness, because the harness founded its entry from the action it was about to run. Here the entry is a parameter and everything else is byte-for-byte the campaign that commits."
    },
    {
      "id": "general-hot-submit-candidate-assembled",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: SubmitCandidate assembled and executed against the closed batch, N=2 -- every conjunct the program owns accepts and the RUNTIME refuses",
      "measured": 678245,
      "tolerance": 15000,
      "budget": 693245,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). Band 0 on every draw, so the tolerance is the rule's floor of 15,000. THIS IS EXECUTED PROGRAM WORK AND NOT A COMMITTED TRANSACTION, and the row's name says so. At 44c0ccf19 SubmitCandidate could not be ASSEMBLED at all -- the table carried a builder refusal where a number belongs -- because its projector required identity::CANDIDATE and identity::PRIMARY_BENEFICIARY and nothing wrote either. 1999e4e07 gave the candidate register a profile operation (it is the register the state address is seeded on), gave the beneficiary decision 0021's Payer declaration, gave the Market and the config identity derived operations that ten of the fifteen actions had lacked, and repaired a Product join that compared a product_id against hash(product account). The action now assembles, seals, invokes the real accelerator four times, folds, preplans and commits -- and the RUNTIME then refuses ExternalAccountLamportSpend, because the Effect funds the work escrow with a LOCAL transfer_lamports out of a System-owned payer and a program may not decrease the lamports of an account it does not own. The lifecycle's rent debit of the same payer is legal for exactly the reason this is not: it goes through a System CPI. 678,245 is what Trading consumed before returning; the transaction commits nothing and the bank is unmoved."
    },
    {
      "id": "general-hot-submit-candidate-seal",
      "campaign": "general-hot",
      "scope": "transaction",
      "transaction": "General Hot: the SubmitCandidate capability seal, produced by the real Trading ELF (CapabilitySealKeyV1's third seed is the action selector)",
      "measured": 700954,
      "tolerance": 15000,
      "budget": 715954,
      "enforced": false,
      "unenforced_reason": "The General Hot campaign has no `cu-budget` witness and is not a SUITE_RUNNERS row. tools/ci/run.sh records the reason: it builds SIX SBF links from an archive of a named commit and publishes a CU table, which is a campaign tier's shape and minutes of work, not a program-test suite -- and what it waits on is a census binding. check-witnesses.sh evaluates a budget only for a campaign some witness names, so an ENFORCED entry here would never be asserted and would overstate coverage exactly the way a MISSING row does. These are RECORDED measurements; they become enforced the day this campaign gets its binding, and the tolerance below is already the one the rule gives a band-0 row.",
      "provenance": "THREE DRAWS AND A FOURTH FROM ANOTHER ELF SET. The runner's own campaign at 1999e4e074d78d4e17b7b4bfda1cf198fb101e1e plus a re-run of the same test binary against the same $SBF_OUT_DIR, so nothing between them was rebuilt; and before either, the lane's working tree drew every row on a SEPARATELY BUILT set of the same six links and agreed to the unit. Two of the three draws therefore share no build at all, which is a stronger repeatability witness than three re-runs of one binary and is why it is said here. Six links, zero SBF stack-frame-overwrite diagnostics: registry 83c9b0e8, trading 654aeb59, core 64702fcf, claims 38633f4d, custody 176f8007, general-accelerator 4e5b0760 (316,872 bytes, up 7,520 on the fifty-seven clause strings the SubmitCandidate conjunct split into). Band 0 on every draw, so the tolerance is the rule's floor of 15,000. SubmitCandidate is the campaign's third distinct action and so its third seal; the campaign asserts the key differs from CloseBatch's before producing it. It is the most expensive row in this table, which is a property of the descriptor closure the producer hashes rather than of the action it seals."
    }
  ]
}
