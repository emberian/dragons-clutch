{
  "campaign": "claims-rational-representation-v2-programtest",
  "note": "Real-ELF ProgramTest for the canonical Rational Representation V2 waist -- open-market structured issuance/unwrap/denominate/reconstitute plus terminal redemption (blocked.json: 'A real-ELF ProgramTest for this route already exists and passes; it simply does not emit census evidence yet'). Real Claims, Custody, Registry and Core ELFs plus the genuine, canonically-built Token-2022 v11 runtime, with the Product graph and LiabilityBasisV2 aggregate INSTALLED directly. Campaign: programs/dclutch-claims-sbf/tests/rational_representation_v2_program_test.rs. Every submitted transaction goes through a test-only wrapper that CPIs into Claims, so a refusal Claims itself raises propagates the same numeric code to the wrapper's own top-level failure log -- a failed CPI aborts the whole transaction before the wrapper's Rust code regains control, so that repetition is the runtime's own account of the failure depth, not a second refusal.",
  "bindings": [
    {
      "label": "claims rational-representation-v2: open actions: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: open actions: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: terminal: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: terminal: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: losing terminal: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: losing terminal: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: issue against an obsolete structured ATA",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. IssueStructured names a structured-custody token account that is not the canonical associated token account. Refuses Identity (0x5002); the campaign asserts the whole resource snapshot is byte-identical to its prestate."
    },
    {
      "label": "claims rational-representation-v2: IssueStructured commits",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "Structured issuance: mints the receipt and both shard mints through the real, canonically-built Token-2022 v11 runtime."
    },
    {
      "label": "claims rational-representation-v2: UnwrapStructured commits",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "Unwraps the structured receipt back to its constituent shards, unchanged in aggregate."
    },
    {
      "label": "claims rational-representation-v2: Denominate commits",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "Converts raw shards into the LiabilityBasisV2-denominated Position at the winning outcome."
    },
    {
      "label": "claims rational-representation-v2: Reconstitute commits",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The exact complement of Denominate: converts the LiabilityBasisV2 Position back into raw shards."
    },
    {
      "label": "claims rational-representation-v2: terminal against a same-width descriptor",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. The finalized descriptor raw/staging pair is substituted for another, same-width descriptor. Refuses Identity (0x5002)."
    },
    {
      "label": "claims rational-representation-v2: terminal against a same-width graph",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. The finalized Product graph raw/staging pair is substituted for another, same-width graph. Refuses Identity (0x5002)."
    },
    {
      "label": "claims rational-representation-v2: caller refuses after a complete terminal redemption",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "unnamed_refusal": {
        "code": 1064963,
        "reason": "RationalV2CallerError::DeliberateLateFailure (0x104003), raised by the test-only wrapper at programs/dclutch-claims-sbf/test-programs/rational-v2-caller AFTER Claims, Custody and Token-2022 all logged success, to prove the terminal redemption rolls back whole. Band 0x104 is that wrapper's alone and can never appear on a real cluster, so the code no longer collides with any first-party refusal; it is still uncredited because a test caller has no census taxonomy to credit."
      },
      "note": "The chain's own logs show `Program <claims> success`, real Custody and real Token-2022 invocations, all inside a transaction that did not commit."
    },
    {
      "label": "claims rational-representation-v2: winning terminal redemption commits",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The canonical winning terminal: real Custody pays the winning shard's collateral through real Token-2022, and the receipt Mint's supply is conserved."
    },
    {
      "label": "claims rational-representation-v2: losing terminal redemption commits",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The losing-outcome terminal: raw shards are burned through a real permissioned Token-2022 burn with no Custody payout, and the burned/held remainder is conserved exactly."
    },
    {
      "label": "claims rational-representation-v2: cross-role replay: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: cross-role replay: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: create the Claims-role Custody replay",
      "routes": [
        "claims/process_instruction",
        "claims/custody_replay_v1::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The Claims-role Custody replay this Market's redemption needs, CREATED rather than planted. Claims forwards a Custody InitializeReplay under its own release-pinned caller authority; Custody creates the account at the role-seeded replay PDA (decision 0008 section 7). Every terminal case in this campaign now opens with this transaction, so the redemption below consumes the cursor the chain actually wrote -- revision 1, zero open Vaults -- instead of one the fixture invented. 711 legacy bytes against the 1,232 limit, asserted: creating this cursor must not require a published lookup table."
    },
    {
      "label": "claims rational-representation-v2: replay creation with the Core Market standing in for the Claims aggregate",
      "routes": [
        "claims/process_instruction",
        "claims/custody_replay_v1::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. The Core Market account is offered where the Claims aggregate belongs. Claims derives the aggregate PDA from the wire's Market and refuses Identity (0x5002); the campaign asserts neither the Claims-role replay nor the Trading-role one exists afterwards."
    },
    {
      "label": "claims rational-representation-v2: replay creation with the Trading-role replay address offered for a Claims-role cursor",
      "routes": [
        "claims/process_instruction",
        "claims/custody_replay_v1::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, and the role-seeded address spoof specifically. The Trading compartment's own replay address is offered as the place to write a Claims-role cursor. Claims derives the Claims-role address from the request it built and refuses Identity (0x5002); Custody would derive it too, one layer down."
    },
    {
      "label": "claims rational-representation-v2: a second replay creation",
      "routes": [
        "claims/process_instruction",
        "claims/custody_replay_v1::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "Creation is permissionless, so the second submitter has to lose HARMLESSLY. Claims requires the replay account to be vacant and System-owned before it forwards anything, so this refuses Identity (0x5002) before Custody is reached; the campaign asserts the live cursor is byte-identical and the first prepayer keeps the rent refund."
    },
    {
      "label": "claims rational-representation-v2: a Trading-role replay at the Claims-role address",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. With the role in the replay seeds a replay's bytes and its address agree by construction, so this state is forged directly into the account -- the only way it can exist. The winning terminal redemption refuses Identity (0x5002) and the campaign asserts the Hoard and the recipient are byte-identical. Custody would refuse it a layer down on ReplayBindingMismatch; the point is that Claims does not get there."
    },
    {
      "label": "claims rational-representation-v2: substituted custody namespace: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: substituted custody namespace: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: custody namespace substituted with the Market address",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE (decision 0008 section 4). The aggregate's persisted custody_context is overwritten with the Market's OWN ADDRESS -- the value FoundingV5 wrote before decision 0008, and the one the first open Market is still stuck with; the founding's own state is untouched, so only the Market's CLAIM about where its Hoard lives moves. Refuses Identity (0x5002) with the Hoard, the recipient and the Custody replay byte-identical and no successful Custody transfer."
    },
    {
      "label": "claims rational-representation-v2: custody namespace substituted with a second founding's context digest",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE (decision 0008 section 4). The aggregate's persisted custody_context is overwritten with another founding's projected-Hoard digest, a well-formed namespace belonging to a different Market; the founding's own state is untouched, so only the Market's CLAIM about where its Hoard lives moves. Refuses Identity (0x5002) with the Hoard, the recipient and the Custody replay byte-identical and no successful Custody transfer."
    },
    {
      "label": "claims rational-representation-v2: custody namespace substituted with the raw founding action context",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE (decision 0008 section 4). The aggregate's persisted custody_context is overwritten with the UNHASHED founding action context, one SHA-256 short of the real namespace and the coordinate the funding source compartment uses; the founding's own state is untouched, so only the Market's CLAIM about where its Hoard lives moves. Refuses Identity (0x5002) with the Hoard, the recipient and the Custody replay byte-identical and no successful Custody transfer."
    },
    {
      "label": "claims rational-representation-v2: family hostiles: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: family hostiles: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: single-role receipt Mint: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: single-role receipt Mint: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: cross-schema substitution: exposure as descriptor",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, cross-family/cross-schema, direction one. The finalized CompositionExposureBundleV3 record is offered in the immutable RepresentationDescriptorV2's account slot. Refuses Identity (0x5002) at 185,950 CU; the campaign asserts a byte-identical resource snapshot."
    },
    {
      "label": "claims rational-representation-v2: cross-schema substitution: descriptor as exposure",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, cross-family/cross-schema, direction two -- the SAME substitution the other way, because a family wire that refuses one direction and admits the other is not joined. The descriptor record is offered in the exposure record's slot. Refuses Identity (0x5002) at 185,949 CU."
    },
    {
      "label": "claims rational-representation-v2: recipe substitution: permuted coefficients",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. A same-width RepresentationDescriptorV2 whose coefficients are the canonical ones PERMUTED -- a valid record naming a recipe the composition does not state. Built by mutating the DERIVED preimage, so it is a near-miss of the real record rather than a hand-written seventh one. Refused twice for two reasons: host-side, require_coefficients_are_the_composition_root will not mint it (the join the live route lost when authenticate_exposure replaced authenticate_graph, decision 0011 s3d); on chain, Identity (0x5002) because the request names a descriptor_id that is the digest of other bytes."
    },
    {
      "label": "claims rational-representation-v2: receipt backed by receipt",
      "routes": [],
      "program": "",
      "outcome": "refused",
      "note": "HOSTILE, and the campaign records WHERE it dies rather than claiming Claims caught it. Coordinate zero's asset row and account frame name the RECEIPT Mint, so a receipt would be backed by itself. RepresentationRequestV2::decode refuses those bytes outright (AccountAlias), and EVERY caller must decode the request to derive its own Trading authority (CallerAuthoritySeedsV1 over the request's parent_context) -- so the request can never acquire a signer and never reaches the Claims CPI at all. The test-only wrapper refuses at its own Instruction code (0x104000) at 11,302 CU. The founding-time half is proved separately in the same test: StructuredTermsV2::bind_shard_terms refuses a receipt Mint aliasing a shard Mint with DuplicateIdentity.",
      "unnamed_refusal": {
        "code": 1064960,
        "reason": "RationalV2CallerError::Instruction, raised by the campaign's TEST-ONLY wrapper program, not by a first-party dClutch program. That is the honest depth: the wrapper decodes the request to derive its Trading caller authority exactly as a real caller must, so a request the grammar rejects can never acquire a signer and can never reach the Claims CPI. Recorded as unnamed because the wrapper's refusal band is test scaffolding and must not enter any program's taxonomy."
      }
    },
    {
      "label": "claims rational-representation-v2: one-atom K_i skew at coordinate 0",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Representation",
      "note": "HOSTILE. Coordinate 0's backing skewed by exactly ONE shard atom. The callee recomputes K_i = S * c_i as asset.coefficient * header.quantity (plan.rs:263) and the campaign basis [2, 3, 5] over 7 is pairwise coprime and coprime to the denominator precisely so a one-atom skew at one coordinate cannot be presented as a legitimate quantity at another. Refuses Representation (0x5008) at 313,356 CU."
    },
    {
      "label": "claims rational-representation-v2: one-atom K_i skew at coordinate 1",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Representation",
      "note": "HOSTILE. The same one-atom skew at coordinate 1. Refuses Representation (0x5008) at 314,120 CU."
    },
    {
      "label": "claims rational-representation-v2: one-atom K_i skew at coordinate 2",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Representation",
      "note": "HOSTILE. The same one-atom skew at coordinate 2. Refuses Representation (0x5008) at 323,884 CU. All three coordinates are separate transactions, so the coprime basis is exercised per coordinate rather than once."
    },
    {
      "label": "claims rational-representation-v2: the winning terminal redemption replayed",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, double redeem. The identical winning redemption resubmitted at a LATER SLOT, so it carries a different blockhash and a different signature and the runtime's duplicate-signature dedup is not what stops it. Claims itself refuses Identity (0x5002) at 181,418 CU, having already consumed the representation replay's revision and the Custody replay's cursor; the settled state stays byte-identical."
    },
    {
      "label": "claims rational-representation-v2: issue against a receipt Mint with only the Mint role",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Token",
      "note": "HOSTILE, and it CORRECTS decision 0011 s3b. The receipt Mint carries the representation authority as its Mint authority and NOT as its permissioned-burn authority: s3b's under-configured founding, which the ruling predicted would let Issue commit and then fail at BurnReceipt 'at the Token program with the descriptor already committed'. Measured: parse_behavior_mint reads the receipt Mint's Token-2022 behavior profile on every action and requires the permissioned-burn authority present and pinned, so the FIRST IssueStructured refuses Token (0x5009) at 255,972 CU with ZERO Token-2022 CPIs issued. The two-role cost is real; its consequence is a founding that can never issue, not a representation that can never be unwound."
    },
    {
      "label": "claims rational-representation-v2: unwrap against a receipt Mint with only the Mint role",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Accounts",
      "note": "The consequence of the row above, recorded for what it is rather than for what it looks like: the Issue committed nothing, so no representation replay exists to carry revision one and the unwrap is refused on Accounts (0x5001) at 251,184 CU. BurnReceipt is never the thing that fails, because there is never anything to unwrap."
    },
    {
      "label": "claims rational-representation-v2: a wallet-held Position is paid",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "STEP TWO OF THE REDEMPTION, and the first execution this route has ever had. A PLAIN WALLET signs for its own Position under the widened execution role Claims (decision 0008 s8): coordinate 0 carries the owner's signature instead of a caller-program PDA, and because a program-derived address has no key, that signature IS the proof the Position is wallet-held. 354,370 CU. Hoard 9 -> 7 and recipient 5 -> 7 for the two claims the wallet held at the winning coordinate; the Position's winning balance is burned to zero, the aggregate's supply falls by the same atoms, no other coordinate moves, both revisions advance by one and the Claims-role cursor advances from the value InitializeReplay wrote. The representation layer -- shard Mints, capability Positions -- is byte-identical."
    },
    {
      "label": "claims rational-representation-v2: the Position owner pays its own fee",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "THE BROWSER'S SHAPE. One wallet pays the fee AND authorizes, so coordinate 0 arrives WRITABLE: an account that is both a fee payer and a readonly signer compiles to a single writable-signer entry, and there is no message in which one wallet can present itself readonly. Decision 0008 s8.4 relaxes the frame spec's readonly pin for role Claims along that one axis only. 354,369 CU -- one unit under the two-signer shape, which is the whole cost of the difference."
    },
    {
      "label": "claims rational-representation-v2: a losing wallet coordinate pays zero",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "terminal_settlement_v3's ZERO-PAYOUT BRANCH, which nothing in the tree had ever executed. 223,244 CU. The wallet's claim at a losing coordinate is burned and the aggregate's supply falls -- a worthless claim is still a claim, and retiring it is a real transition -- but Custody is never invoked (no 'Program <custody> success' in the logs), the Hoard and the recipient are byte-identical afterwards, and the Claims-role cursor does not move. authenticate_zero_custody_accounts is what re-derives the whole Custody frame from the persisted namespace without spending it."
    },
    {
      "label": "claims rational-representation-v2: the first half of a wallet payout",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "A PARTIAL redemption: one of the wallet's two winning claims, 354,370 CU. Exists to set up the stale-cursor row below, and to record that quantity is a caller coordinate the evaluator bounds against the Position's own balance rather than a whole-position sweep."
    },
    {
      "label": "claims rational-representation-v2: a wallet payout on a stale Custody cursor",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, the double-spend shape. The second payout names the revisions the first left behind, so the aggregate and the Position join cleanly, and carries the STALE Custody cursor. Refused Identity (0x5002) at 226,195 CU, and the honest depth is that CLAIMS refuses it, not Custody: authenticate_custody_accounts reads the replay's own next_revision and compares it to the request before any CPI is issued, so the transfer is never attempted. Hoard, recipient, Position, aggregate and replay all byte-identical."
    },
    {
      "label": "claims rational-representation-v2: a wallet payout against an unresolved Market",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. The Core Market is walked back to Phase::Open with its terminal receipt removed -- through the bank, so the alteration actually reaches the chain -- and the same request that just committed is refused Identity (0x5002) at 15,670 CU. The Core state's address is derived from an identity that does not move, so this is a PHASE refusal and not an address one."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with the owner named but not signing",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/SignedDeltaSbfErrorV3::Accounts",
      "note": "HOSTILE. The owner is named in the request and present in the frame WITHOUT its signature. The SignedDeltaV3 frame spec's SIGNER pin at coordinate 0 refuses first, 0x5201 at 103,634 CU, before any economics is read. Naming an owner is not authorizing anything."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with a signer who is not the Position owner",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Accounts",
      "note": "HOSTILE. A valid signature from an identity that is not request.owner. Coordinate 0 must BE the owner under role Claims, so this refuses Accounts (0x5001) at 8,212 CU -- the cheapest refusal in the set, before the aggregate is even decoded."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with a stranger claiming to own this Position",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Economic",
      "note": "HOSTILE, the same substitution pushed one step further: the stranger signs AND is named as the owner, so the authority check passes. The evaluator's Position join then refuses Economic (0x5005) at 59,869 CU because the offered account's header says the actor owns it. Behind that stands signed_delta_v3::build_candidates, which would refuse the same frame again on the canonical PDA under (aggregate, stranger) -- two independent derivations of one fact, and the first one is enough."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with another party's Position at this Market",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Economic",
      "note": "HOSTILE, the same join from the other direction: a Claims capability Position at this same Market, canonical for ITS owner, offered with the wallet's signature. Economic (0x5005) at 59,870 CU. A wallet cannot redeem the representation layer's collateral by pointing at it."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with role Claims with a foreign caller program",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/SignedDeltaSbfErrorV3::Release",
      "note": "HOSTILE. Role Claims means THIS program is the executor and there is no external caller, so coordinates 14/15 are pinned to the Claims program and its ProgramData. A frame naming the test caller there leaves an unauthenticated executable in a role slot; refused Release (0x5202) at 105,911 CU."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with role Trading with an owner signature at the authority coordinate",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/SignedDeltaSbfErrorV3::Release",
      "note": "HOSTILE, the crossing in the other direction and the sharpest one in the set: an EXTERNAL role offering a wallet signature where its own release-pinned caller PDA belongs. Refused Release (0x5202) at 223,065 CU. The owner-signature proof is admissible under role Claims and nowhere else; the two authority kinds do not substitute for one another."
    },
    {
      "label": "claims rational-representation-v2: wallet payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked. Terminal settlement's 35-account frame plus its 640-byte request measures 1,869 bytes as a legacy transaction against a 1,232-byte limit, so this route CANNOT ride an ordinary packet -- unlike the replay creation that precedes it, which deliberately can. Any redemption builder, the browser's included, publishes a table."
    },
    {
      "label": "claims rational-representation-v2: wallet payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: owner-paid wallet payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked. Terminal settlement's 35-account frame plus its 640-byte request measures 1,869 bytes as a legacy transaction against a 1,232-byte limit, so this route CANNOT ride an ordinary packet -- unlike the replay creation that precedes it, which deliberately can. Any redemption builder, the browser's included, publishes a table."
    },
    {
      "label": "claims rational-representation-v2: owner-paid wallet payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: losing wallet coordinate: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked. Terminal settlement's 35-account frame plus its 640-byte request measures 1,869 bytes as a legacy transaction against a 1,232-byte limit, so this route CANNOT ride an ordinary packet -- unlike the replay creation that precedes it, which deliberately can. Any redemption builder, the browser's included, publishes a table."
    },
    {
      "label": "claims rational-representation-v2: losing wallet coordinate: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: replayed wallet payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked. Terminal settlement's 35-account frame plus its 640-byte request measures 1,869 bytes as a legacy transaction against a 1,232-byte limit, so this route CANNOT ride an ordinary packet -- unlike the replay creation that precedes it, which deliberately can. Any redemption builder, the browser's included, publishes a table."
    },
    {
      "label": "claims rational-representation-v2: replayed wallet payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: wallet payout hostiles: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked. Terminal settlement's 35-account frame plus its 640-byte request measures 1,869 bytes as a legacy transaction against a 1,232-byte limit, so this route CANNOT ride an ordinary packet -- unlike the replay creation that precedes it, which deliberately can. Any redemption builder, the browser's included, publishes a table."
    },
    {
      "label": "claims rational-representation-v2: wallet payout hostiles: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: unresolved wallet payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked. Terminal settlement's 35-account frame plus its 640-byte request measures 1,869 bytes as a legacy transaction against a 1,232-byte limit, so this route CANNOT ride an ordinary packet -- unlike the replay creation that precedes it, which deliberately can. Any redemption builder, the browser's included, publishes a table."
    },
    {
      "label": "claims rational-representation-v2: unresolved wallet payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: a wallet payout against a cross-market Position",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Economic",
      "note": "HOSTILE. The wallet's own Position is forged in place -- through the bank, so the alteration reaches the chain -- to carry a different market_account, and everything else about the request is the one that just committed: same signer, same canonical PDA, same aggregate. What is left is exactly the cross-Market join. Economic (0x5005): the evaluator's validate_joins fires first, and signed_delta_v3::build_candidates would refuse the same frame again. A Position is scoped to the book it was admitted into, and a resolved Market pays only its own."
    },
    {
      "label": "claims rational-representation-v2: cross-market wallet payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: cross-market wallet payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: curved wallet payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "claims rational-representation-v2: curved wallet payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "claims rational-representation-v2: failure-terms exit: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "claims rational-representation-v2: failure-terms exit: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "claims rational-representation-v2: replay close: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "claims rational-representation-v2: replay close: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "claims rational-representation-v2: Resolution certificate hostiles: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "claims rational-representation-v2: Resolution certificate hostiles: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "claims rational-representation-v2: retiring payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "claims rational-representation-v2: retiring payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "claims rational-representation-v2: retiring representation redemption: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "claims rational-representation-v2: retiring representation redemption: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "a provider success may not occupy the pre-disclosed failure cell: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "a provider success may not occupy the pre-disclosed failure cell: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "failure terms may not be claimed for an ordinary coordinate: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "failure terms may not be claimed for an ordinary coordinate: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "Trading common-Hot Rational IssueStructured: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "Trading common-Hot Rational IssueStructured: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "Trading common-Hot Rational Denominate: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: an Address Lookup Table a control needs to fit its frame. Bound PER LABEL FAMILY, which is this file's existing convention and not a broad wildcard -- a `campaign: *: create lookup table` row collides with every family row already here and the census refuses an ambiguous binding as loudly as a missing one. Claims no route because it drives none."
    },
    {
      "label": "Trading common-Hot Rational Denominate: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the create row for this label family."
    },
    {
      "label": "claims rational-representation-v2: degree-two cumulative-floor payout",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The wallet payout against a degree-two cumulative-floor basis rather than the categorical one: the same route, the same signature shape, a payoff the Product runtime evaluates by integrating the basis instead of reading a coefficient. 603,469 CU."
    },
    {
      "label": "claims rational-representation-v2: a wallet exits at failure terms",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The pre-disclosed failure cell paid to a wallet-held Position. Real Custody moves the collateral through real Token-2022 at 471,243 CU; the failure coordinate is an ordinary payout coordinate to this route, which is what makes the two hostiles below meaningful."
    },
    {
      "label": "claims rational-representation-v2: a holder is paid after a stranger began retiring",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "THE POINT OF THE PERMISSIONLESS-RETIREMENT ATTACK: the same payout, to the atom, after an arbitrary actor drove Core into `Retiring`. A holder's redemption right does not depend on nobody having called `begin_retiring`."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with a substituted certificate identity in the request",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. The request names a terminal receipt digest the Core Market does not carry. Refused at the Resolution join before any collateral moves, 13,733 CU -- the cheapest of the certificate hostiles because the substitution is visible in the request itself."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with a substituted certificate account",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. The request stays honest and the CERTIFICATE ACCOUNT is swapped for the Rent sysvar. Keeping the request truthful does not permit an account substitution at the Resolution seam."
    },
    {
      "label": "claims rational-representation-v2: wallet payout with a substituted Resolution ProgramData",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Release",
      "note": "HOSTILE, and it refuses `Release` rather than `Identity` because the defect is a DEPLOYMENT, not an identity: the certificate's owner alone is not authority, and the Resolution ProgramData offered here is the Core program's. The distinct code is the whole reason this case is separable from the three above."
    },
    {
      "label": "claims rational-representation-v2: certificate with another owner",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, at the certificate ACCOUNT rather than at the request: a byte-identical certificate body owned by the Core program instead of Resolution."
    },
    {
      "label": "claims rational-representation-v2: certificate with less than rent exemption",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. One lamport below the rent-exempt minimum for its own width -- an account the runtime may reap, so its bytes are not a durable commitment."
    },
    {
      "label": "claims rational-representation-v2: certificate with a truncated width",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. The canonical certificate with its last byte removed."
    },
    {
      "label": "claims rational-representation-v2: certificate with another Market in its canonical body",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, and the sharpest of the four: a WELL-FORMED certificate, correctly owned and rent-exempt, that resolves another Market. Structural validity is not the join."
    },
    {
      "label": "a provider success may not occupy the pre-disclosed failure cell",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE. A provider outcome offered at the coordinate the market pre-disclosed as its failure cell. The failure cell is reserved by the disclosure, not by whatever the resolution later says."
    },
    {
      "label": "failure terms may not be claimed for an ordinary coordinate",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE, the other direction: failure terms claimed at a coordinate the disclosure did not name. Both directions of the same join, and both refuse before Custody is reached."
    },
    {
      "label": "claims rational-representation-v2: the actor reclaims its replay rent",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process_replay_close"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The actor closes its own representation replay cursor and is credited EXACTLY the stranded rent -- no split, no reward, no residue. 3,107 CU, and the only act in this family that moves lamports and touches no Claims economics, which is why it has its own refusal sub-band."
    },
    {
      "label": "claims rational-representation-v2: replay close naming another actor",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process_replay_close"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/RationalReplayCloseSbfErrorV1::Authority",
      "note": "HOSTILE. The cursor's own actor did not sign. Refused `Authority` (0x5681) at 1,099 CU, before the cursor is read."
    },
    {
      "label": "claims rational-representation-v2: a stranger reaches for the actor's rent",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process_replay_close"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/RationalReplayCloseSbfErrorV1::Identity",
      "note": "HOSTILE. A stranger signs and names itself as the beneficiary; the cursor address then does not derive from the coordinates the request names. Refused `Identity` (0x5682), and no refusal moves a lamport."
    },
    {
      "label": "claims rational-representation-v2: replay close at another descriptor",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process_replay_close"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/RationalReplayCloseSbfErrorV1::Identity",
      "note": "HOSTILE. The right actor, the wrong descriptor: the cursor named does not derive under the descriptor the request carries. Refused `Identity` (0x5682), which is the same code as the stranger case for the same reason -- the address is the join."
    },
    {
      "label": "claims rational-representation-v2: a stranger begins retiring",
      "routes": [
        "core/process_instruction",
        "core/begin_retiring::process#BeginRetiring"
      ],
      "program": "core",
      "outcome": "executed",
      "note": "PERMISSIONLESS BY DESIGN, executed against the real Core ELF at 83,130 CU by an actor holding no role in this market. It is bound as EXECUTED because that is the attack: the transition lands, and the two payout rows above are the proof that landing it ends nobody's redemption. Two transactions carry this label -- one per attack scenario -- and the binding covers both."
    },
    {
      "label": "claims rational-representation-v2: IssueStructured before a replay close",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "A full-width structured issuance planted so the replay-close route has a cursor to close. 1,397 wire bytes, over the packet maximum, which is the campaign's own K = 3 measurement rather than a fault of this row."
    },
    {
      "label": "claims rational-representation-v2: terminal against a substituted Resolution certificate",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/ClaimsSbfError::Identity",
      "note": "HOSTILE at the terminal redemption rather than at the wallet payout: the certificate account is substituted under an otherwise honest terminal request. Byte-exact rollback of the whole settled state."
    },
    {
      "label": "claims rational-representation-v2: representation redemption after a stranger retired",
      "routes": [
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The representation-layer half of the permissionless-retirement attack: a terminal redemption commits at 650,115 CU with Core in `Retiring`, real Custody paying through real Token-2022."
    },
    {
      "label": "Trading common-Hot Rational IssueStructured commits",
      "routes": [
        "trading/process_instruction",
        "trading/hot_v3::process_hot_execution_v3",
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "trading",
      "outcome": "executed",
      "note": "THE COMMON-HOT ROUTE, and the first campaign row whose top-level program is Trading rather than Claims or the test caller. Trading authenticates the release, specializes the Claims child from the family request, and invokes it through the release-selected caller PDA: 855,481 CU, 1,589 wire bytes on a live Address Lookup Table. This campaign's program map did not carry Trading at all until 2026-09-02, so every row in this group was unbindable and the census counted each as unbound coverage."
    },
    {
      "label": "Trading common-Hot Rational IssueStructured substituted request digest refuses",
      "routes": [
        "trading/process_instruction",
        "trading/hot_v3::process_hot_execution_v3"
      ],
      "program": "trading",
      "outcome": "refused",
      "refusal": "trading/TradingSbfError::Release",
      "note": "HOSTILE, one flipped byte in the family request. It refuses at the BINDING, not at a content check: the child caller authority is a PDA over the exact projected child request, whose parent context is the family request's SHA-256, so a moved family byte moves the derived address and Trading cannot present a frame for the request it actually carries. 393,359 CU, and Claims is never reached."
    },
    {
      "label": "Trading common-Hot Rational Denominate commits",
      "routes": [
        "trading/process_instruction",
        "trading/hot_v3::process_hot_execution_v3",
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "trading",
      "outcome": "executed",
      "note": "The selected-outcome action on the common-Hot route: 774,025 CU, 1,253 wire bytes. Over the 1,232-byte packet maximum by 21 even with the table applied -- the Claims-direct Denominate is 1,061, and the difference is the Trading envelope and the outer account frame this route carries on top of it."
    },
    {
      "label": "Trading common-Hot Rational RedeemTerminal commits",
      "routes": [
        "trading/process_instruction",
        "trading/hot_v3::process_hot_execution_v3",
        "claims/process_instruction",
        "claims/rational_representation_v2::process"
      ],
      "program": "trading",
      "outcome": "executed",
      "note": "THE TERMINAL REDEMPTION ON THE HOT ROUTE, executing for the first time on 2026-09-02. Trading -> Claims -> Custody -> Token-2022, 1,119,672 CU, 1,277 wire bytes. It was unreachable until the terminal AccountProfile stopped being one account narrower than the frame the request contract specifies: the Resolution program sits at the fifth slot of the fourteen-account terminal suffix, and its absence put the Custody replay, the Hoard and the recipient one index low."
    },
    {
      "label": "Trading common-Hot Rational RedeemTerminal: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "Trading common-Hot Rational RedeemTerminal: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: ATA-shaped wallet payout: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup, not a protocol route: the Address Lookup Table the 35-account terminal frame needs. Bound per label family, this file's convention."
    },
    {
      "label": "claims rational-representation-v2: ATA-shaped wallet payout: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: a 170-byte associated token account reaches Custody",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "custody",
      "outcome": "refused",
      "refusal": "custody/CustodySbfError::TokenState",
      "note": "THE WALL, on a Realm founded under the Token-2022 ZERO-EXTENSION adapter release. The destination is the account the Associated Token Account program actually produces -- 165 base bytes, the account-type discriminant, and one empty ImmutableOwner TLV entry: exactly 170. Claims admits it (rational_terminal_v3 parses with parse_base_or_immutable_owner) and the payout reaches CUSTODY, which selects ExactTransferProfileV1 by matching the Realm's stored collateral_adapter_release_id and gets the exact-base-widths profile. Refused TokenState (0x6006) at 363,236 CU, 1,050 wire bytes, with the Hoard, the destination balance, the Position and the Custody cursor all asserted unmoved. This is not a defect a commit repairs: the policy byte is inside the released preimage, so a market founded here can never pay a wallet's own ATA."
    },
    {
      "label": "claims rational-representation-v2: ATA-shaped wallet payout on the third release: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Campaign setup; see the ATA-shaped family's create row."
    },
    {
      "label": "claims rational-representation-v2: ATA-shaped wallet payout on the third release: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims rational-representation-v2: a 170-byte associated token account is paid",
      "routes": [
        "claims/process_instruction",
        "claims/terminal_settlement_v3::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "THE MATCHED PAIR of the row above, and the ONE difference between them is the 32 bytes the Realm record stores as collateral_adapter_release_id: 430369ce... instead of 228c14f9..., the third CollateralAdapterReleaseV1 whose ExtensionStoragePolicy is BaseWidthsOrImmutableOwnerAccounts. Same real Claims, Custody, Core, Registry, Resolution and Token-2022 ELFs, same frame, same host builder, same planted destination bytes. 369,366 CU, 1,050 wire bytes -- 6,130 CU more than the refusal, which is what reaching the CPI and the poststate join costs. The Hoard drains to the wallet's own associated token account, the account is still 170 bytes afterwards with its ImmutableOwner suffix byte-identical, the winning coordinate is burned and the aggregate falls by the same atoms. This is the row cohort-14 founds for."
    }
  ]
}
