{
  "campaign": "claims-affine-batch-programtest",
  "note": "Real-ELF ProgramTest for the canonical affine LBV2 Claims waist (blocked.json: 'A real-ELF ProgramTest for this route already exists and passes; it simply does not emit census evidence yet'). Real Claims, Registry and Core ELFs with the Product Runtime V3 graph and LiabilityBasisV2 aggregate INSTALLED directly, so the route is reachable with no Hot execution and no open Market. Campaign: programs/dclutch-claims-sbf/program-test/affine-batch (tests/affine_batch_v2.rs).",
  "bindings": [
    {
      "label": "claims affine-batch: create lookup table",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table creation. No dClutch program is invoked."
    },
    {
      "label": "claims affine-batch: extend lookup table *",
      "routes": [],
      "program": "",
      "outcome": "executed",
      "note": "Address Lookup Table extension. No dClutch program is invoked."
    },
    {
      "label": "claims affine-batch: admit under a substituted Position alias",
      "routes": [
        "claims/process_instruction",
        "claims/affine_batch_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/AffineBatchSbfErrorV2::Accounts",
      "note": "HOSTILE. Position outcome 21's account is substituted for an unrelated Position, aliasing a coordinate. Refuses Accounts (0x5161); the campaign asserts the Claims aggregate and both Positions are byte-identical to their prestate."
    },
    {
      "label": "claims affine-batch: admit against a substituted Product record",
      "routes": [
        "claims/process_instruction",
        "claims/affine_batch_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/AffineBatchSbfErrorV2::ProductBasis",
      "note": "HOSTILE. The finalized Product raw/staging pair is substituted for a differently-shaped Product. Refuses ProductBasis (0x5163)."
    },
    {
      "label": "claims affine-batch: admit against a substituted linked basis",
      "routes": [
        "claims/process_instruction",
        "claims/affine_batch_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/AffineBatchSbfErrorV2::ProductBasis",
      "note": "HOSTILE. The finalized linked-basis raw/staging pair is substituted for another Product's basis at the same semantic identity. Refuses ProductBasis (0x5163)."
    },
    {
      "label": "claims affine-batch: caller refuses after a complete batch",
      "routes": [
        "claims/process_instruction",
        "claims/affine_batch_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "unnamed_refusal": {
        "code": 1048579,
        "reason": "AffineBatchTestCallerError::DeliberateLateFailure (0x100003), raised by the test-only caller at programs/dclutch-claims-sbf/test-programs/affine-batch-caller AFTER Claims logged success, to prove the affine batch's allocation and mutation roll back together. Band 0x100 is that caller's alone and can never appear on a real cluster, so the code no longer collides with any first-party refusal; it is still uncredited because a test caller has no census taxonomy to credit."
      },
      "note": "The chain's own logs show `Program <claims> success` inside a transaction that did not commit. The campaign asserts the Claims aggregate and both Positions are byte-identical to their prestate."
    },
    {
      "label": "claims affine-batch: canonical batch commits",
      "routes": [
        "claims/process_instruction",
        "claims/affine_batch_v2::process"
      ],
      "program": "claims",
      "outcome": "executed",
      "note": "The canonical runtime-width-exact affine batch: two Position mutations and the aggregate update commit atomically in one instruction."
    },
    {
      "label": "claims affine-batch: stale batch refuses",
      "routes": [
        "claims/process_instruction",
        "claims/affine_batch_v2::process"
      ],
      "program": "claims",
      "outcome": "refused",
      "refusal": "claims/AffineBatchSbfErrorV2::ClaimsState",
      "note": "HOSTILE replay. The same request is resubmitted after the canonical batch already advanced the aggregate and Position revisions. Refuses ClaimsState (0x5164)."
    }
  ]
}
