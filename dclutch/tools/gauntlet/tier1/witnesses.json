{
  "campaign": "tier1",
  "note": "Asserted witnesses for the tier-1 campaign. Each states a value whose provenance is INDEPENDENT of the code under test: a Solana runtime limit, an SPL program identity, an Agave serialisation fact, or a cross-check between the host's prediction and the chain's poststate. `provenance` is required and its absence is a hard refusal \u2014 a number with no provenance is a mirror wearing a hat.",
  "witnesses": [
    {
      "id": "every-transaction-fits-the-compute-maximum",
      "kind": "evidence-jq",
      "query": "[.transactions[].compute_units_consumed | select(. != null)] | max | (. <= 1400000) | tostring",
      "expect": "true",
      "provenance": "Solana's per-transaction MAX_COMPUTE_UNIT_LIMIT is 1,400,000 compute units. This is the chain's limit, not ours, and there is no headroom to buy: the campaign already requests the maximum. A run that raised it would be a measurement, and a measurement never satisfies a gate."
    },
    {
      "id": "activation-is-one-role-per-transaction",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | startswith(\"activate immutable release-set role:\")) | select(.error == null)] | length | tostring",
      "expect": "5",
      "provenance": "On-chain release admission hashes whole ProgramData ELFs at about one compute unit per two bytes. The five real role artifacts predict Core 502,400 + Claims 536,900 + Trading 642,300 + Resolution 231,800 + Custody 165,200 = ~2,078,600 CU before activation's other ~136,000 \u2014 measured against the 1,400,000 maximum on solana-test-validator 4.0.2, 2026-08-26 (docs/evidence/GENERIC_FOUNDING_REACHABILITY_2026_08_26.md). A single five-role activation transaction therefore cannot execute on any cluster, and admission must be one role per transaction. Five is the ExecutionRoleV1 cardinality: Core, Claims, Trading, Resolution, Custody."
    },
    {
      "id": "core-programdata-poststate-is-the-predicted-revoke-poststate",
      "kind": "evidence-jq",
      "query": ".accounts.core_programdata.data_sha256",
      "expect_from": ".core_bootstrap.post_revoke_programdata_sha256",
      "provenance": "Agave's Loader v3 owns upgrade-authority presence with the tag at ProgramData byte 12. Its real Some -> None SetAuthority serialisation overwrites the 13-byte serialized None state but does NOT clear the former 32-byte authority region, so bytes 13..45 remain as inactive residue and the ELF still begins at byte 45. The bootstrap plan constructs the exact expected poststate offline from the ELF; the chain must produce exactly those bytes. Two independent derivations of the same 32-byte digest \u2014 this is a cross-check, not a read-back."
    },
    {
      "id": "core-revoke-actually-changed-the-chain-bytes",
      "kind": "plan-jq",
      "query": "(.core_bootstrap.genesis_programdata_sha256 != .core_bootstrap.post_revoke_programdata_sha256) | tostring",
      "expect": "true",
      "provenance": "The genesis ProgramData carries an authority (tag byte 12 = 1, 32 authority bytes) and the post-revoke one does not (tag byte 12 = 0). They cannot be equal. Taken together with the witness above \u2014 which pins the chain's observed poststate to the predicted post-revoke digest \u2014 this establishes that the revocation transaction changed the account and that release recognition is genuinely gated behind it."
    },
    {
      "id": "infrastructure-profile-body-is-the-predicted-body",
      "kind": "evidence-jq",
      "query": ".accounts.infrastructure_profile.data_sha256",
      "expect_from": ".infrastructure_profile.body_sha256",
      "provenance": "The bootstrap plan computes the exact expected ProtocolInfrastructureProfileV1 body offline, before the validator starts, and the profile PDA is created by a submitted Core transaction \u2014 never genesis-injected. Equality is a cross-check between a host-side prediction and a chain poststate."
    },
    {
      "id": "infrastructure-profile-width-is-the-predicted-width",
      "kind": "evidence-jq",
      "query": ".accounts.infrastructure_profile.data_len | tostring",
      "expect_from": "(.infrastructure_profile.body_hex | length / 2 | floor | tostring)",
      "provenance": "Same cross-check at the layout level: the account the chain created must be exactly as wide as the body the host predicted, with no slack. A width mismatch would mean the on-chain allocation and the host's layout have diverged, which is the class of defect that hides behind a passing digest comparison over a truncated read."
    },
    {
      "id": "release-activation-cache-is-registry-owned",
      "kind": "evidence-jq",
      "query": ".accounts.release_activation.owner",
      "expect_from": ".registry.program_id",
      "provenance": "The activation cache is a Registry PDA and must be owned by the Registry program that created it. Loopscale (2025-04-26, 5,726,724.97 USDC + 1,211.4 SOL) is the empirical case for checking this class of thing: root cause 'missing program identity verification', fix 'enforce strict validation of program IDs'. Trusting an account's shape without binding its owning program is the whole failure mode (docs/research/CHAIN_STATE_SOURCES_2026_08.md)."
    },
    {
      "id": "found31-was-actually-submitted-and-succeeded",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label == \"create canonical Found31 Market\") | .error] | tostring",
      "expect": "[null]",
      "provenance": "This is the witness the whole gauntlet exists for. With its keys inline the canonical 31-account Found frame serialises to 1,242 raw bytes against Solana's 1,232-byte legacy packet maximum (PACKET_DATA_SIZE) \u2014 it misses by ten. That defect survived every fixture test in the tree because the transaction was constructed and inspected but never submitted. Exactly one entry, and its error null, means one Found31 was really put on the wire and the validator really executed it."
    },
    {
      "id": "found31-market-account-is-core-owned",
      "kind": "evidence-jq",
      "query": ".accounts.market.owner",
      "expect_from": ".core.program_id",
      "provenance": "A Market is Core-owned state. Checking the owner rather than the data shape is the Loopscale lesson again: an attacker-supplied account with the right bytes and the wrong owner is the attack, not the anomaly."
    },
    {
      "id": "lifecycle-rent-credit-is-rent-program-owned",
      "kind": "evidence-jq",
      "query": ".accounts.lifecycle_rent_credit.owner",
      "expect_from": ".rent_credit.program_id",
      "provenance": "The Market-scoped LifecycleRentCreditV2 is a PDA of the RentCredit program and must be owned by it."
    },
    {
      "id": "no-private-key-was-persisted",
      "kind": "evidence-jq",
      "query": ".private_key_persisted | tostring",
      "expect": "false",
      "provenance": "AGENTS.md: default work is offline; never read wallet or private-key material. The runner generates every signing keypair in process memory and gives `prepare` only the Core authority public key. This witness is here so that a change which starts persisting key material fails a gate rather than passing quietly."
    },
    {
      "id": "collateral-mint-is-a-real-token-2022-mint",
      "kind": "evidence-jq",
      "query": ".accounts.collateral_mint.owner",
      "expect": "TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb",
      "provenance": "The SPL Token-2022 program address, stated here independently of the tree and confirmed to agree with crates/dclutch-token-svm's TOKEN_2022_PROGRAM_ID_BASE58. The collateral Mint is created by a real submitted Token-2022 InitializeMint2, not injected as a fixture: 'real Agave account shapes' means the mint the protocol reads is one Token-2022 actually made."
    },
    {
      "id": "collateral-wallet-is-a-real-token-2022-account",
      "kind": "evidence-jq",
      "query": ".accounts.collateral_wallet.owner",
      "expect": "TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb",
      "provenance": "Same program address. The raw-atom wallet is a real Token-2022 account created and funded by submitted transactions."
    },
    {
      "id": "the-campaign-really-reached-found",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.error == null)] | length | (. >= 30) | tostring",
      "expect": "true",
      "provenance": "The W1 campaign that first reached Found31 ran 46 finalized transactions in 747 seconds on solana-test-validator 4.0.2 (tools/local-validator/bootstrap/successor/README.md, 2026-08-26). A run that reached Found with far fewer succeeding transactions would mean stages were skipped rather than executed. 30 is a deliberately loose floor: it fails a truncated run without pinning a count that legitimate campaign changes would move."
    },
    {
      "id": "the-founded-market-is-core-owned",
      "kind": "evidence-jq",
      "query": ".accounts.founding_market.owner",
      "expect_from": ".core.program_id",
      "provenance": "The Market DCLTGMF1 creates must be owned by the Core program the bootstrap plan deployed. The plan's program id and the chain's account owner are two independent derivations: the plan names the address it built genesis for, the chain reports who owns the account the founding created. Neither reads the other."
    },
    {
      "id": "the-claims-founding-allocated-three-claims-owned-accounts",
      "kind": "evidence-jq",
      "query": "[.accounts.claims_aggregate.owner, .accounts.founder_position.owner, .accounts.claims_admission.owner] | unique | if length == 1 then .[0] else \"MIXED OWNERS\" end",
      "expect_from": ".claims.program_id",
      "provenance": "Claims FoundingV5 allocates the liability aggregate, the founder Position, and the admission record with System `allocate` + `assign` under its own program id, and refuses unless each is System-owned, empty, and rent-funded beforehand. All three must therefore end up owned by exactly the Claims program the plan deployed, and by no other. A single differing owner collapses this witness to MIXED OWNERS rather than passing on the first entry."
    },
    {
      "id": "the-hoard-is-a-real-token-2022-account",
      "kind": "evidence-jq",
      "query": ".accounts.founding_hoard_vault_open.owner",
      "expect": "TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb",
      "provenance": "The canonical SPL Token-2022 program address, an external identity dClutch does not define. The Hoard is a real Token-2022 account holding real collateral, not a dClutch-owned imitation of one; the runner separately requires its balance to equal the founding principal exactly."
    },
    {
      "id": "the-atomic-founding-was-actually-submitted-and-succeeded",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | startswith(\"found the Market atomically\")) | .error] | tostring",
      "expect": "[null]",
      "provenance": "Exactly one such transaction, and the chain reports no error on it. This is the validator's own account of the five-stage rollback domain, not the runner's: a campaign that never submitted it, or submitted it twice, or had it refuse, fails this witness rather than reporting an Open Market it inferred."
    },
    {
      "id": "the-founding-outer-hostile-case-really-refused",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | startswith(\"DCLTGMF1 refuses\")) | .error != null] | tostring",
      "expect": "[true]",
      "provenance": "A substituted Claims request naming a different founder must not found anything. The chain must report an error on it. A hostile case that quietly succeeded, or that was never submitted, fails here; the runner separately requires the whole multi-instruction transaction to roll back to a fee-only debit and all five program-allocated accounts to remain vacant."
    },
    {
      "id": "the-projection-was-realized-into-a-narrower-normal-replay",
      "kind": "evidence-jq",
      "query": "(.accounts.founding_projected_custody_replay.data_len > .accounts.founding_normal_custody_replay.data_len) | tostring",
      "expect": "true",
      "provenance": "Two snapshots of the SAME account address, taken either side of the founding. The projected-Custody state and the normal CustodyReplayV1 are different widths, and Custody's RealizeAndClose rewrites the projection in place and resizes it rather than closing one account and opening another. A run where the replay never realized shows two equal widths. The key is lane-prefixed because the campaign now stages two prestates at two generations, and evidence that silently compared one lane's account against the other's would not be evidence."
    },
    {
      "id": "the-expired-source-abort-was-submitted-and-succeeded",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | startswith(\"unwind an expired founding\")) | .error] | tostring",
      "expect": "[null]",
      "provenance": "Exactly one such transaction, and the chain reports no error on it. This is the only route out of a funded projection whose founding expired; before it existed that collateral could not be moved by anything. A campaign that never submitted it fails here rather than reporting a safety route it did not exercise."
    },
    {
      "id": "the-pre-expiry-source-abort-really-refused",
      "kind": "evidence-jq",
      "query": "[.transactions[] | select(.label | startswith(\"DCLTPCA1 refuses\")) | .error != null] | tostring",
      "expect": "[true]",
      "provenance": "While a founding is still satisfiable, the authority over funded principal may not be destroyed. The chain must report an error on the pre-expiry attempt. An unwind route that let anyone empty a live founding would be a worse defect than the stranding it was written to fix, so this is the boundary the abort is judged on, not the happy path."
    },
    {
      "id": "the-abort-returned-the-principal-to-a-real-token-2022-account",
      "kind": "evidence-jq",
      "query": ".accounts.abort_refunded_principal_wallet.owner",
      "expect": "TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb",
      "provenance": "The canonical SPL Token-2022 program address, an external identity dClutch does not define. The refunded principal lands in a real token account owned by the party that supplied it; the runner separately requires the balance to have risen by exactly the principal and all four staged accounts to be closed."
    }
  ]
}
