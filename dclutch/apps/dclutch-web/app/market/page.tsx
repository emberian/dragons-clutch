import MarketAddressWorkspace from '@/components/MarketAddressWorkspace';

/** Static-host-safe Market detail entry point: `/market?address=<pubkey>`. */
export default function MarketAddressPage() {
  return <MarketAddressWorkspace />;
}
