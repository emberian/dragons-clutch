/**
 * The SDK owns principal-cap semantics. Keep this stable app import while the
 * worker consumes that one implementation instead of a second browser mirror.
 */
export * from '../../../../packages/dclutch-sdk/lib/founding/principalCapacity';
