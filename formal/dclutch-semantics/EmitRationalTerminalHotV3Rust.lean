import DClutchSemantics.RationalTerminalHotV3Abi
import DClutchSemantics.Codec

open DClutch
open DClutch.RationalTerminalHotV3Abi

def rustByte (byte : UInt8) : String := s!"0x{Codec.byteHex byte}"

def main : IO Unit := do
  IO.println "// @generated by formal/dclutch-semantics/EmitRationalTerminalHotV3Rust.lean; do not edit."
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_VERSION_V3: u16 = {version};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_REQUEST_BYTES_V3: usize = {requestBytes};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_FIXED_ASSET_COUNT_V3: u32 = {fixedAssetCount};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_MAGIC_V3: [u8; {familyMagic.length}] ="
  IO.println s!"    [{String.intercalate ", " (familyMagic.map rustByte)}];"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_MAGIC_OFFSET_V3: usize = {childMagicOffset};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_VERSION_OFFSET_V3: usize = {childVersionOffset};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ACTION_OFFSET_V3: usize = {childActionOffset};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_CALLER_ROLE_OFFSET_V3: usize = {childCallerRoleOffset};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_PARENT_CONTEXT_OFFSET_V3: usize = {parentContextOffset};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_RESERVED_HEADER_OFFSET_V3: usize = {requestOffset .reservedHeader};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_RELEASE_SET_OFFSET_V3: usize = {requestOffset .releaseSet};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_MARKET_OFFSET_V3: usize = {requestOffset .market};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_GRAPH_ID_OFFSET_V3: usize = {requestOffset .graphId};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_DESCRIPTOR_ID_OFFSET_V3: usize = {requestOffset .descriptorId};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ACTOR_OFFSET_V3: usize = {requestOffset .actor};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_RECEIPT_MINT_OFFSET_V3: usize = {requestOffset .receiptMint};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_RECEIPT_ACCOUNT_OFFSET_V3: usize = {requestOffset .receiptAccount};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_REPRESENTATION_AUTHORITY_OFFSET_V3: usize = {requestOffset .representationAuthority};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_TOKEN_PROGRAM_OFFSET_V3: usize = {requestOffset .tokenProgram};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_REALM_OFFSET_V3: usize = {requestOffset .realm};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_COLLATERAL_RECIPIENT_OFFSET_V3: usize = {requestOffset .collateralRecipient};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_EXPECTED_REPRESENTATION_REVISION_OFFSET_V3: usize = {requestOffset .expectedRepresentationRevision};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_EXPECTED_CLAIMS_MARKET_REVISION_OFFSET_V3: usize = {requestOffset .expectedClaimsMarketRevision};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_EXPECTED_ACTOR_POSITION_REVISION_OFFSET_V3: usize = {requestOffset .expectedActorPositionRevision};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_EXPECTED_CUSTODY_POSITION_REVISION_OFFSET_V3: usize = {requestOffset .expectedCustodyPositionRevision};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_EXPECTED_CUSTODY_REPLAY_REVISION_OFFSET_V3: usize = {requestOffset .expectedCustodyReplayRevision};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_GENERATION_OFFSET_V3: usize = {requestOffset .generation};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_QUANTITY_OFFSET_V3: usize = {requestOffset .quantity};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_DENOMINATOR_OFFSET_V3: usize = {requestOffset .denominator};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_EXPECTED_RECEIPT_SUPPLY_OFFSET_V3: usize = {requestOffset .expectedReceiptSupply};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_OUTCOME_COUNT_OFFSET_V3: usize = {requestOffset .outcomeCount};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_SELECTED_OUTCOME_OFFSET_V3: usize = {requestOffset .selectedOutcome};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_COUNT_OFFSET_V3: usize = {requestOffset .assetCount};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_RESERVED_TAIL_OFFSET_V3: usize = {requestOffset .reservedTail};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_OFFSET_V3: usize = {assetStartOffset};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_SHARD_MINT_OFFSET_V3: usize = {absoluteAssetOffset .shardMint};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_ACTOR_SHARD_ACCOUNT_OFFSET_V3: usize = {absoluteAssetOffset .actorShardAccount};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_STRUCTURED_CUSTODY_OFFSET_V3: usize = {absoluteAssetOffset .structuredCustodyAccount};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_CLAIMS_CUSTODY_OWNER_OFFSET_V3: usize = {absoluteAssetOffset .claimsCustodyOwner};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_COEFFICIENT_OFFSET_V3: usize = {absoluteAssetOffset .coefficient};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_EXPECTED_SHARD_SUPPLY_OFFSET_V3: usize = {absoluteAssetOffset .expectedShardSupply};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_EXPECTED_ACTOR_SHARDS_OFFSET_V3: usize = {absoluteAssetOffset .expectedActorShards};"
  IO.println s!"pub const RATIONAL_TERMINAL_HOT_ASSET_EXPECTED_STRUCTURED_SHARDS_OFFSET_V3: usize = {absoluteAssetOffset .expectedStructuredShards};"
