[package]
name = "dragon-clutch-toolchain-probe-host"
version = "0.0.0"
edition = "2021"
publish = false
license = "AGPL-3.0-or-later"

[dependencies]
dragon-clutch-toolchain-probe = { path = "../no_std_core" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
