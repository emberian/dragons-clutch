[package]
name = "clutch-resolution-work-v1"
version = "0.1.0"
edition = "2021"
publish = false
license = "AGPL-3.0-or-later"
description = "Bounded resumable occupation-resolution work model for Dragon's Clutch"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
clutch-bspline = { path = "../../crates/clutch-bspline" }
clutch-bspline-accumulator = { path = "../../crates/clutch-bspline-accumulator" }
sha2 = { version = "=0.10.9", default-features = false }
