# Optional Lean shadow seam

Lean is not a second production implementation or current V1 release blocker.
This directory preserves a clean place to reproduce a small finite relation,
check canonical semantic vectors, or independently restate selected property IDs
after the E0 schema is frozen.

If the experiment duplicates Rocq without meaningful independent evidence, keep
the language-neutral seam and stop the implementation.
