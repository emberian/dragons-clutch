# Adopted decisions — 2026-08-20

Status: **DECISION RECORD.** Ember reviewed the DECISION_PACKET and delegated
("the calls you made are wise... I'd encourage you to choose the 'weakest'
choice — the one most general, with the least constraining over resulting
dynamics. there are other factors to consider too that mean weakness isn't
always desirable."). Each adoption below names the report that analyzed it;
the reports carry the counterarguments, which remain part of the record.

## Adopted

1. **GENERAL_CLEARING_POLICY_V1 and CANDIDATE_WINDOW_SLOTS = 1,000 are
   FROZEN as pinned** (REPORT_general-clearing-policy-freeze). The pins are
   the enabling choices (AssignCanonical keeps remainder books clearable;
   Reject was the constraining alternative); a future profile is a sibling
   const with a new digest, so the freeze forecloses no dynamics. The
   in-source doc-comment status updates ride the next reseal-bearing wave
   rather than opening a drift window for a comment.
2. **ADR-0005: Lean is the proof substrate of record**
   (REPORT_adr-0003-supersession; adopted text at
   docs/adr/0005-lean-proof-substrate-of-record.md). Verus retained for
   checked-executable-body results; Rocq role retired, directory kept
   historical; native_decide ban codified.
3. **E1 — the R2 model close is ratified** (REPORT_r2-cutover). Research-only.
4. **Realm admission allowlist FROZEN as built**: Token-2022 base mints,
   extension ceiling zero, ImmutableOwner required on the Hoard; unknown
   discriminants fail closed (REPORT_realm-admission-and-token2022). The
   deliberate strong choice: fail-closed admission preserves future options;
   admission-then-exploitation would foreclose them. The DREGG dogfood mint
   has no executable V1 profile and this record says so plainly.
5. **Deployments use the sealed opt-3 identity only; opt-z is refused**
   until re-greened and gate-campaigned at its own identity
   (REPORT_deploy-economics-and-upgrade-posture). Devnet beta authority
   ratified as recorded in the deploy job.
6. **Plane-L: all five ResolutionWork charges are permanently zero as
   frozen policy** (REPORT_revenue-policy-v1 B4c). No vault is built. A V2
   cost schedule may reintroduce charges for new Works — the weak form.
7. **R4 terminal ratification**: incinerator sink, fractional Arm A, and
   the failure-payout decision are RATIFIED; **legacy-rows-permanent is
   ratified ONLY under the scope amendment** (legacy mints + prototype
   instances; the live general plane is explicitly NOT declared permanent);
   **the §8 reference-ownership variant is EXPLICITLY DEFERRED** until the
   provider-horizon evidence exists — the weakest of its options
   (REPORT_r4-terminal-ratification). The permanent-rent rows stand
   PERMANENT_TOMBSTONE for V1 with the shrink successor recorded.
8. **RevenuePolicy**: B4a custody requirements adopted, pubkey deferred to
   first fee-bearing Realm; B4b treasury Position adopted with the
   mid-epoch-close grief rider joining the hostile walk; B4d sequencing
   per B4c; B4e 60/0/40 + AllRestingMakers adopted as the V1 vector
   (constrains nothing until a fee-bearing Realm exists); B4f both terminal
   rows accepted under item 7 (REPORT_revenue-policy-v1).
9. **Fee base: the composite `kappa*G + kappa'*R` SHAPE is selected**;
   both rates remain undecided; every byte stays FeeBaseV1::None until the
   destination lands; the market-quality descope is ratified and the
   FEE_GEOMETRY promotion criteria are rewritten per ADR-0005
   (REPORT_fee-base-selection). Reversible until a rate freezes.
10. **Promotion**: the walk plane advances to rung W1 (CU/quote rows,
    no live flags) now that item 1 holds; the V3 syscall-era sealed
    measurement campaign is commissioned; the B/C closures are ratified as
    content with the process note recorded (REPORT_clearing-plane-promotion).

## Deferred with the tension named

- **Reference-deployment upgrade posture**: the report recommended
  immutable-at-first-deployment; ember's weakest-choice principle favors
  upgradeable-then-burn (burn is always available; un-burn never is).
  Deferred — mainnet is gated regardless, and the devnet posture is settled
  by item 5.

## Reserved to ember (unchanged by this record)

Filing submissions (Aug 24/26/27); the E2 freeze act (evidence-triggered,
post-cutover, decided against the assembled dossier); the E3 registry flip
(ember's explicit go at the 12-gate table, never pre-authorized); the
treasury pubkey; counsel/security/license engagements; mainnet; L0.
