[package]
name = "clutch-accumulator"
version = "0.1.0"
edition = "2021"
publish = false
description = "Pure no_std interval-summary monoid prototype for Dragon's Clutch"
license = "AGPL-3.0-or-later"

[lib]
path = "src/lib.rs"

[features]
default = []

