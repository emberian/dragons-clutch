[package]
name = "dclutch-refusal-registry"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Protocol-wide allocation of Solana custom program error codes"

[lib]
path = "src/lib.rs"

# Deliberately zero dependencies. This crate is linked into every on-chain
# program; it must add nothing to any ELF but constants, and it must be
# readable by host tooling that does not build the protocol workspace.
[dependencies]

[lints]
workspace = true
