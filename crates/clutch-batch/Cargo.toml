[package]
name = "clutch-batch"
version = "0.1.0"
edition = "2021"
publish = false
license = "AGPL-3.0-or-later"

[lib]
path = "src/lib.rs"

[features]
default = []
# The General SBF product reaches the resumable checkpoint codec through many
# handlers.  This internal code-generation selector lets that product share
# primitive codec bodies without changing the host/default or Direct builds.
profile-general-source-v2-point = []
